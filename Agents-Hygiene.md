# Agents-Hygiene.md — 仓库卫生通用规范

> 本文件面向在本仓库内工作的所有 Agent（以及人工协作者）。
> 目标是保证 git 历史干净、可审计、可回滚，避免把运行时数据与构建产物混入版本库。
> 内容为通用规范，不绑定任何具体项目模块；如有项目特定约定，以仓库内其他文档为准。

## 一、什么不该提交

以下类别默认**禁止**进入版本库（除非有明确、书面化的理由，并在 commit message 中说明）：

| 类别 | 典型示例 | 处理方式 |
|---|---|---|
| 编译/构建产物 | `bin/`、`obj/`、`pub/`、`.tmp-build/`、`*.dll`、`*.exe`、`*.pdb` | 不跟踪；由构建过程重新生成 |
| 包管理器产物 | `node_modules/`、NuGet 缓存、`*.nupkg` 之外的下载缓存 | 不跟踪；通过清单文件恢复 |
| 临时/草稿文件 | `.tmp-*`、`*.tmp`、`*_output.txt`、`*_result.txt`、编辑器备份 | 一律输出到 `temp/` 或系统临时目录 |
| 运行时数据 | 日志、数据库文件、会话缓存、`checkpoint.json`、进程残留 | 不跟踪；必要时用 ignore 规则兜底 |
| 密钥与凭据 | `.env`、`*.pem`、`*.key`、`*.pfx`、API Key、连接串（含明文口令） | **绝不提交**；用环境变量/密钥管理注入 |
| 大文件 | 单文件 > 5 MB 的二进制、模型权重、视频、镜像 | 改用对象存储/外部发布，仓库只放引用 |

## 二、临时输出纪律

- 需要产生临时文件的场景，**一律写入 `temp/` 目录**（仓库已对该目录配置 ignore）。
- 禁止在仓库根目录或源码目录散落 `*.tmp-*`、`msg.txt`、`commit_msg*.txt` 之类的草稿文件。
- 临时文件用完即清理；不清理由生成方负责在收尾时删除。
- 任何「临时输出物」不得通过 `git add .` 这类通配方式进入暂存区。

## 三、提交前自检清单

执行 `git add` / `git commit` 之前，逐项核对：

1. `git status` 是否只包含本次任务的预期文件？
2. 是否存在 `??` 未跟踪的临时文件、密钥文件、大文件？如有，确认它们已被 ignore 或删除。
3. `git diff --cached --stat` 是否出现不应出现的目录（如 `bin/`、`pub/`、`.tmp-build/`）？
4. 改动内容是否与 commit message 描述一致？（一个 commit 只做一件事）
5. 是否包含密钥/口令/Token？可用 `git grep -i -E "(api[_-]?key|secret|password|token=|sk-...)"` 快速筛查。
6. 大文件是否误入？可用 `git diff --cached --numstat` 检查单文件增量。

若以上任何一项异常，**先修正再提交**，不要带着垃圾提交。

## 四、大文件警觉

- 提交前估算：本次变更若新增 > 5 MB 的单个文件，先停下来确认必要性。
- 版本库不是文件存储。二进制、媒体、模型、数据快照应放外部存储，仓库保留指针或生成脚本。
- 已经误提交的大文件，即使后续删除，其对象仍保留在 git 历史中，会永久占用 `.git` 体积。
  此类问题的正确解法是改写历史（`git filter-repo` 等），属高成本操作，务必提前预防。

## 五、Agent 工作纪律

- **只改自己任务范围内的路径**：不触碰与当前任务无关的代码、配置、数据库。
- **可回滚优先**：优先使用可逆操作（如 `git rm --cached` 保留工作区文件），销毁性操作（物理删除文件、强制推送）需先评估并在报告中说明依据。
- **独立提交**：临时文件清理、文档、代码改动尽量分 commit 提交，便于追溯与回滚。
- **push 纪律**：只推送自己的 commit，不代推他方改动；推送前确认没有把他人未完成的工作夹带出去。
- **不留后门**：不为了「省事」把调试代码、feedback 代码、兼容性补丁长期留在主干；完成任务即收敛。

## 六、发现历史脏数据怎么办

如果发现历史中已混入构建产物/临时文件/大文件：

1. 若该文件**仍被跟踪**：用 `git rm --cached <path>` 取消跟踪（保留工作区），并在 `.gitignore` 补充规则防再犯。
2. 若文件**只存在于历史**（当前已不跟踪）：记录其影响（可用 `git count-objects -vH` 观察 `.git` 体积），评估是否值得改写历史，形成报告供人工裁决，不擅自强推。
3. 每次清理独立提交，message 写明清理对象与理由，便于审计与回滚。

---

*本规范随仓库实践持续修订；发现新类型的「脏数据」时，先在本文件补充规则，再执行清理。*
