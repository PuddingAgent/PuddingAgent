export interface AppSettings {
  title: string
  fixedHeader: boolean
  sidebarLogo: boolean
  tagsView: boolean
}

const settings: AppSettings = {
  title: 'Pudding Platform Admin',
  fixedHeader: true,
  sidebarLogo: true,
  tagsView: true,
}

export default settings
