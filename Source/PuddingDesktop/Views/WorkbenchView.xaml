<UserControl x:Class="PuddingDesktop.Views.WorkbenchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource CardFillBrush}">
        <Grid x:Name="WebViewHost" />

        <Grid x:Name="LoadingOverlay"
              Background="{DynamicResource LayerFillBrush}">
            <Border Width="420"
                    Padding="32"
                    VerticalAlignment="Center"
                    Style="{StaticResource CardStyle}"
                    Effect="{StaticResource CardShadowEffect}">
                <StackPanel>
                    <Border Width="54" Height="54"
                            HorizontalAlignment="Center"
                            Background="{DynamicResource AccentLightBrush}"
                            CornerRadius="16">
                        <TextBlock x:Name="LoadingStatusIcon"
                                   Text="P"
                                   Foreground="{DynamicResource AccentBrush}"
                                   FontFamily="{StaticResource DisplayFont}"
                                   FontSize="26"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>

                    <TextBlock Text="Pudding Workbench"
                               Margin="0,18,0,0"
                               HorizontalAlignment="Center"
                               FontFamily="{StaticResource DisplayFont}"
                               FontSize="20"
                               FontWeight="SemiBold" />
                    <TextBlock x:Name="LoadingText"
                               Text="等待 Core 就绪…"
                               Margin="0,7,0,0"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="13"
                               TextAlignment="Center"
                               TextWrapping="Wrap" />
                    <ProgressBar x:Name="LoadingProgress"
                                 Width="220" Height="3"
                                 Margin="0,20,0,0"
                                 IsIndeterminate="True"
                                 Foreground="{DynamicResource AccentBrush}"
                                 Background="{DynamicResource SubtleFillBrush}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
