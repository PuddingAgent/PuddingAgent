# 测试套件健康与缺陷类诊断（2026-09-17）

> 作者：dsh（通用助手）｜范围：`Source/PuddingCoreTests`、`Source/PuddingPlatformTests` 及同批缺陷类排查
> 所有数据均为本机实测，均可用文末命令复现。产物日志在 gitignored 的 `temp/` 下。

## 一、结论摘要

| 指标 | 会话开始 | 现在 |
| --- | --- | --- |
| Core 全量是否跑得完 | **否（永久挂起，从未跑完）** | 是，**56 秒** |
| Core 通过 / 失败 | 851 / **12**（挂起解除后首份成绩单，1.3272 分钟） | **859 / 4** |
| 其他套件探针（6 套） | — | FullTextIndex **50/0**（4 跳过）· MemoryEngine **161/3** · CodexService **4/0** · WebApi **62/82**（其中 76 为工具假失败）· CodeIntelligence **95/1** · Runtime **不可构建**（缺 Moq）—— **全部无挂起** |
| Platform 真实失败 | — | **1 项**（已实测确认；另 2 项为工具假失败，见 §6.C） |
| 已推送修复 | — | **9 个 commit**（含 1 处产品缺陷、2 处 flaky 消除） |

失败数演进：`12 → 10 → 8 → 7 → 6 → 4`。

**最大教训：套件跑不完 = 信号全盲。** 在挂起期间，红灯与产品缺陷可在无人察觉下持续累积（本次一次性暴露 12 项失败，其中含 1 处真实产品 NRE）。

## 二、挂起元凶与产品契约（已修）

- 元凶：`FileSwarmTransportTests.ReceiveAsync_ReturnsExistingMessages` / `ReceiveAsync_ClearsInboxAfterReading`
  —— 未传 `CancellationToken` 就 `await foreach` 等待长期流自然结束，导致套件永久阻塞。
- 契约细节：`FileSwarmTransport.ReceiveAsync` 是带 `[EnumeratorCancellation]` 的**长期流**：
  「投递收件箱现有消息 → 清空收件箱 → `while(!ct.IsCancellationRequested && !_disposed)` 轮询」；
  **取消时抛 `OperationCanceledException`**（不是优雅结束）。
  产品正确用法见 `Source/PuddingCore/Swarm/SwarmMessageHub.cs:80` 的 `ReceiveAsync(_cts.Token)`。
- 修法：测试传 Token + `try/catch(OperationCanceledException)`；该组 11/11 全绿。
- 定位手段：`--blame-hang --blame-hang-timeout <N>s` 直接点名挂起测试并落盘 hangdump。

## 三、缺陷类 A：断言参数写反（本会话主轴，覆盖 5 个测试文件）

MSTest 签名为「边界在前、被测值在后」：

```csharp
Assert.IsGreaterThanOrEqualTo(lowerBound, value);   // 断言 value >= lowerBound
Assert.IsLessThan(upperBound, value);               // 断言 value <  upperBound
Assert.IsLessThanOrEqualTo(upperBound, value);      // 断言 value <= upperBound
```

若把「被测实际值」写成边界，断言方向即反转、必然失败。已修清单：

| 文件 | 位置 | 修法 | 判定依据 |
| --- | --- | --- | --- |
| `Core/GitSnapshotServiceTests.cs` | 71 | 参数交换 | 断言与上下文相反 |
| `Core/WorkerManagerTests.cs` | 300 | 参数交换 | 同上 |
| `Swarm/SwarmOrchestratorTests.cs` | 273–277（5 处） | `IsGreaterThanOrEqualTo(xxxIdx, 0)` → `(0, xxxIdx)` | `events.FindIndex(...)` 找不到返回 **-1**，故「索引 ≥ 0」=「事件存在」，**并非恒真断言** |
| 同上 | 284 / 287（2 处） | `IsLessThan` 参数交换 | 断言**与自己的消息文字直接矛盾**：`IsLessThan(thinkingEventIndex, contractDefinedIndex, "Thinking should come before contract defined")` 实际断言 `contractDefinedIndex < thinkingEventIndex`；实测 `upperBound:<thinkingEventIndex=0>` / `value:<contractDefinedIndex=5>`，即现实确为 thinking(0) 先于 contract(5)、与消息一致，只错参数序 |
| `Swarm/Integration/ContractFirstWorkflowTests.cs` | 119–120 | 参数交换 | 消息已明示边界："at least one file" / "multiple symbols" |
| `Core/DefaultDistillerTests.cs` | 88 | `IsLessThan(result.RetainedLines, 50)` → `(50, result.RetainedLines)` | 测试造 50 行并断言发生截断；实测 `RetainedLines = 15`，原写法断言「50 < RetainedLines」 |
| 同上 | 412 | `IsLessThanOrEqualTo(Summary.Length, 4500)` → `(4500, Summary.Length)` | 注释 "MaxLlmChars + truncation marker"；实测 `Summary.Length = 1691`，原写法断言「4500 <= Summary.Length」 |

两条机制性教训：

1. **「修好一项才暴露下一项」**：`SwarmOrchestratorTests` 中 line 272 的写反断言先行失败，使执行根本推进不到 line 282；修好第一层后，同族的 `IsLessThan` 写反才现形。因此「只修可见错误」会漏掉后续层。
2. **从失败消息反推参数序**：MSTest 失败消息按「value … upperBound」排列，并打印 `upperBound:` / `value:` 的**表达式原文**。若 `upperBound:` 指向的是被测值（如 `result.RetainedLines`），应优先怀疑参数顺序，而非产品行为。
   —— 本会话曾把 `DefaultDistillerTests` 两项误判为「期望值与产品行为不一致，需所有者确认」，后由 commit `81f73da9` 显式更正。

## 四、缺陷类 B：时序 / 负载敏感 flaky（已消除）

| 用例 | 根因 | 修法 |
| --- | --- | --- |
| `Core/GitSnapshotServiceTests.ListSnapshots_ReturnsMostRecentFirst` | 测试用 `Task.Delay(10)` 试图让两次快照时间戳不同，但产品时间戳来自 **git 提交时间**（`GitSnapshotService.cs:84` 以 `git log` 自定义格式 `hash\|short\|timestamp\|message` 读取），**精度为秒**；10 毫秒无法保证跨秒 | `Task.Delay(1100)`；延迟 >1 秒可确定性保证秒级时间戳不同，故**保留严格 `>` 断言强度，未放宽为 `>=`** |
| `Swarm/FileSwarmTransportTests.FileSystemWatcher_DeliversNewMessages` | 取消超时仅 2 秒且计时起点在发送之前，需覆盖「发送 + 写盘 + 经 FileSystemWatcher 送达」全过程；8 workers 并行且收件箱文件被 Watcher 占用时，2 秒在初始读取阶段即到期 → `LoadInboxMessagesAsync(:136)` / `TryTake(:100)` 抛 `TaskCanceledException` | 超时 2s→10s（该用例校验「能否送达」而非延迟，放宽不削弱语义）+ 补 `catch(OperationCanceledException)`，使真超时由断言给出清晰失败信息 |

## 五、产品缺陷（已修）

`Source/PuddingCore/Swarm/SwarmOrchestrator.cs:38`

```csharp
// 旧：userInput 为 null 时抛 NullReferenceException
var continueMode = userInput.Equals("continue", StringComparison.OrdinalIgnoreCase);
// 新：静态空安全比较，null 视为「非续跑」
var continueMode = string.Equals(userInput, "continue", StringComparison.OrdinalIgnoreCase);
```

契约来源：`SwarmOrchestratorTests.ProcessSwarmAsync_WithNullInput_DoesNotCrash`
（注释原文："Method should handle null input without crashing"）。
范围说明：本次仅消除入口 NRE；`userInput` 仍以原值传向下游（line 50 / 75 / 81），null 在下游的语义属产品所有者决策。

## 六、剩余失败：均已定位，需所有者裁定

### A. `LlmProfileResolverTests.Resolve_*` ×3（line 89 / 115 / 136）—— 判定：**测试陈旧**，非产品缺陷

证据链：

1. 产品 `Source/PuddingCore/Configuration/LlmProfileResolver.cs` 的文档注释明写：
   「[Obsolete] 全局 roles（roles.conscious / roles.subconscious）**不再作为回退**。主 Agent 运行时不使用此解析器；
   其模型身份来自实例 manifest.json 的 preferredProviderId/preferredModelId。」
   且 `ResolveRole(...)` 开头即 `if (string.IsNullOrWhiteSpace(profileId)) return null;` —— **不再读 `llmConfig.Roles`**。
2. 配置层已标过时：构建日志 `PuddingConfigModels.cs(224,12): warning CS0618: PuddingLlmRoleConfig …
   Global roles are no longer required.`
3. 同文件内新旧行为并存：`Resolve_Returns_Null_When_Role_Profile_Is_Missing`（断言「无 template/instance → null」）**当前通过**；
   走 template 路径的用例也全部通过 → 解析器本身工作正常，只是没有回退分支。
4. 失败形态 `Assert.IsNotNull 失败。'value' 表达式:'resolved.Conscious'` —— `IsNotNull` 只有单值参数，
   **不存在参数序写反的可能**，与缺陷类 A 无关。

建议：若「全局 roles 不再回退」为既定设计 → 更新/删除这 3 条用例（推荐改为「显式传入 template 后的行为断言」，保留覆盖）；
若认为回退应保留 → 属产品回归，需在产品侧恢复回退分支。**两种处理都需所有者拍板，执行方不应静默改测试。**

### B. `SwarmOrchestratorTests.ProcessSwarmAsync_WithInvalidSwarmDirectory_HandlesError`（:403）

断言 `Assert.Fail("Should not throw exception: git worktree list failed: fatal: not a git repository …")`。

已核实夹具（`SwarmOrchestratorTests` 构造函数）：

```csharp
_testDir      = Path.Combine(Path.GetTempPath(), $"swarm-orchestrator-test-{Guid.NewGuid():N}");
_testSwarmDir = Path.Combine(_testDir, ".pudding", "swarm");
_testRepoDir  = Path.Combine(_testDir, "repo");
```

即测试目录在**系统临时目录**下（与 bin 布局无关），故本项**不受 `--artifacts-path` 影响**。
关键在于：该用例**唯独没有调用 `InitializeTestGitRepoAsync()`**（同文件其余用例都调用了），
因此 `_testRepoDir` 不是 git 仓库 → 产品内部 `git worktree list` 失败并向上抛 → 命中断言的 `Assert.Fail`。

两种设计解释需所有者裁定：
1. 产品应在 git 环境不可用时**优雅处理**（捕获并发错误事件）—— 这正是用例名 `HandlesError`
   与断言文字「Should not throw exception」所表达的契约 → 属产品健壮性缺口；
2. 用例应补 `InitializeTestGitRepoAsync()`，让「无效」只体现在 swarm 目录而非 git 仓库缺失。

**不可机械改成 `Assert.Throws`** —— 那会掩盖 "HandlesError" 的契约语义。

### C. Platform 侧 3 项（另 1 项 flaky）

| 用例 | 现象 | 性质 |
| --- | --- | --- |
| ~~`BenchmarkCaseFileServiceTests.DefaultSeedCases_ShouldNotLeakBenchmarkOrSystemHints:73`~~ | `cases.Count > 0` 失败 | **⚠️ 假失败：由 `--artifacts-path` 引起，非缺陷**（见下方更正） |
| ~~`McpConnectionManagerTests.RefreshWorkspace_Starts_Stdio_Server_And_Preserves_Codex_Thread_Result:143`~~ | `File.Exists` 失败：`Tests\Mcp.Cli\bin\PuddingPlatformTests\net10.0\Mcp.Cli.dll` 未构建 | **⚠️ 假失败：由 `--artifacts-path` 引起，非缺陷**（见下方更正） |
| `GoalCheckRunnerTests.Run_MissingExitCode_FailsClosedWithExplicitReason:459` | 期望 `"finished"` 实得 `"pending"` | 他方 Goals 在途改动 |

Platform 两次运行分别为 失败 4 / 失败 3（**其中 1 项 flaky**），**无挂起**。

#### ⚠️ 更正（2026-09-17 04:30）：Platform 一项实为「工具选择造成的假失败」

`DefaultSeedCases_ShouldNotLeakBenchmarkOrSystemHints` 通过 `AppContext.BaseDirectory` 向上 4 层定位仓库数据：

```csharp
var root = Path.GetFullPath(Path.Combine(AppContext.BaseDirectory,
    "..", "..", "..", "..", "PuddingHost", "default-data"));
```

- 默认布局：bin = `Source/PuddingPlatformTests/bin/Debug/net10.0/` → 上 4 层 = `Source/` ✅ → 命中 `Source/PuddingHost/default-data`。
- 用 `--artifacts-path temp/hbk-artifacts2` 时：bin = `temp/hbk-artifacts2/bin/PuddingPlatformTests/debug/` →
  上 4 层 = `temp/` ✗ → 找不到数据 → `cases.Count == 0` → 断言失败。

实测对照（同一命令，仅去掉 `--artifacts-path`，使用默认 bin 布局）：

```
PuddingPlatformTests -> Source\PuddingPlatformTests\bin\Debug\net10.0\PuddingPlatformTests.dll
已通过! - 失败: 0，通过: 1，总计: 1，持续时间: 98 ms
```

→ **该用例本身是健康的**。此前把它登记为 Platform 失败项是错的，特此更正。

**推论（重要）**：`--artifacts-path` 会改变 `AppContext.BaseDirectory` 的层级，**凡以 bin 相对路径定位仓库数据的测试都会假失败**。
Core 侧 4 项失败已逐项核对，**均不以 bin 相对路径定位数据**（`LlmProfileResolver` 为纯配置逻辑；
`SwarmOrchestrator` 的测试目录在 `Path.GetTempPath()` 下），故 Core 的结论不受此影响。

**另**：本环境无法直接改用默认 bin 布局验证 Core —— `Source/PuddingCoreTests/bin/Debug/net10.0/` 被残留
`testhost (PID 34988)` 锁定，构建报 `MSB3027/MSB3021`（重试 10 次失败），这正是当初采用 `--artifacts-path` 的原因。
平台侧（`PuddingPlatformTests`）默认 bin 未被锁定，故可对照验证。

**Platform 更正后**：真实失败 **2 项** —— `McpConnectionManagerTests`（`Mcp.Cli.dll` 未构建）、
`GoalCheckRunnerTests`（他方 Goals 在途）；另有 1 项 flaky（运行间 3↔4 波动）。

#### ⚠️ 更正二（2026-09-17 04:35）：`McpConnectionManagerTests` 同为 `--artifacts-path` 假失败

该用例自行推算「配置名」再拼装被测程序路径：

```csharp
var configuration = new DirectoryInfo(AppContext.BaseDirectory).Parent?.Name ?? "Debug";
var assemblyPath = Path.Combine(repositoryRoot, "Tests", "Mcp.Cli", "bin", configuration, "net10.0", "Mcp.Cli.dll");
```

- 默认布局：`BaseDirectory` = `.../PuddingPlatformTests/bin/Debug/net10.0/` → `DirectoryInfo.Parent` = `.../bin/Debug` → `.Name` = `Debug` ✅。
- `--artifacts-path` 布局：`BaseDirectory` = `temp/hbk-artifacts2/bin/PuddingPlatformTests/debug/` →
  `.Parent` = `.../bin/PuddingPlatformTests` → `.Name` = **`PuddingPlatformTests`** ✗。

**失败消息本身就是证据**：实际报错路径为 `…\Tests\Mcp.Cli\bin\PuddingPlatformTests\net10.0\Mcp.Cli.dll`
—— 配置段被算成 `PuddingPlatformTests`，正是该启发式在 artifacts-path 布局下取错层级的特征。
且 `PuddingPlatformTests.csproj:20` 已以 `<ProjectReference Include="..\..\Tests\Mcp.Cli\Mcp.Cli.csproj" ReferenceOutputAssembly="false" />`
构建 `Tests/Mcp.Cli`，故默认布局下产物存在。

**已实测确认（2026-09-17 04:35，默认 bin 布局）**：

```
dotnet test Source/PuddingPlatformTests/PuddingPlatformTests.csproj \
  --filter "FullyQualifiedName~RefreshWorkspace_Starts_Stdio_Server" --logger "console;verbosity=minimal"
→ PuddingPlatformTests -> Source\PuddingPlatformTests\bin\Debug\net10.0\PuddingPlatformTests.dll
→ 已通过! - 失败: 0，通过: 1，已跳过: 0，总计: 1，持续时间: 9 s
```

→ 该用例健康，确认为 `--artifacts-path` 假失败。**至此 Platform 两项假失败（`DefaultSeedCases…`、`McpConnectionManagerTests…`）均已实测证伪。**

#### ★ 方法论结论（本报告最重要的一条）

`--artifacts-path` 改变的不只是 bin 位置，而是**布局形状**：

```
默认：            <项目>/bin/<配置>/<tfm>/
--artifacts-path： <dir>/bin/<项目名>/<配置>/
```

因此**凡按 `AppContext.BaseDirectory` 的相对层级或父目录名推断路径的测试都会假失败**。
推论：**任何在 `--artifacts-path` 下得到的失败清单，都必须先用默认布局复核才能定性**；
反之，若某用例在默认布局下失败，它才是真失败。本次 Platform 3 项「失败」中已有 **2 项**被此机制证伪。

**Platform 最终修正**：真实失败 **1 项**（`GoalCheckRunnerTests`，他方 Goals 在途）+ 1 项 flaky；
另 2 项为工具假失败（1 项已实测确认，1 项待实测确认）。

## 六之补、`PuddingMemoryEngineTests`：3 项失败 —— `Layers` 显示名与测试期望不一致

实测（本机）：**总计 164 / 通过 161 / 失败 3 / 53 秒，无挂起**；
已用**默认 bin 布局**复核（去掉 `--artifacts-path`）→ **失败 3 / 通过 54 / 总计 57 / 5 秒**，同样 3 项
→ **确认为真实问题，非 `--artifacts-path` 假失败**。

### ★ 关键澄清（撤销初诊的「缺少环境信息层」表述）

**层并未缺失。** `EnvironmentLayer_ShouldBePresentInAssemblyResult` 中先行的 3 条提示词断言**全部通过**：

```
result.SystemPrompt.Contains("--- LAYER: ENVIRONMENT ---")   ✅
result.SystemPrompt.Contains("TestOS")                      ✅
result.SystemPrompt.Contains("/data/workspaces/ws-test")     ✅
```

仅随后的 `result.Layers.FirstOrDefault(l => l.LayerName == "环境信息")` 取到 null ✗。
→ 即**系统提示词里 ENVIRONMENT 层正常产出，只是 `Layers` 诊断列表使用的显示名不同**。

### 产品实际发射的显示名（`ContextPipelineOrchestrator.cs`）

```csharp
RecordLayer(sb, 内容, 显示名, 层ID, ref usedBudget, totalBudget, layers, layerInfos);
```

| 行 | 显示名 | 层 ID |
| --- | --- | --- |
| 61 | 静态上下文 | L0-STATIC |
| 65 | **运行环境不变量** | L0-ENVIRONMENT |
| 70 | 工作区 Agents | L0-AGENTS-ROSTER |
| 114 | **工具使用规则** | L1-TOOLS |
| 120 | **技能使用规则** | L2-SKILLS |
| 124 | 工作区环境 | L3-WORKSPACE-ENVIRONMENT |
| 131 | 继承上下文 | L2-INHERITED |
| 166 | 用户偏好 | — |
| 204 | 重要记忆 | L4-PINNED |
| 330 | 运行时指令 | — |
| 358 | 当前消息 | — |

### 测试期望值对照（`MemoryLibraryTests.cs`）

| 用例 | 测试期望 | 产品实际 | 判定 |
| --- | --- | --- | --- |
| `ContextPipeline_ShouldAssembleAll7Layers:663+` | 静态上下文 | 静态上下文 | ✅ 相符 |
| 同上 | **环境信息** | 运行环境不变量 | ✗ 名不符 |
| 同上 | **动态工具** | 工具使用规则 | ✗ 名不符 |
| 同上 | **动态技能** | 技能使用规则 | ✗ 名不符 |
| 同上 | 用户偏好 / 当前消息 / 运行时指令 | 同名 | ✅ 相符 |
| `EnvironmentLayer_ShouldBePresentInAssemblyResult:798` | **环境信息** | 运行环境不变量 | ✗ 名不符 |
| `ContextPipeline_ShouldTriggerGentleCompaction:753` | **近期历史** | 枚举 `Source/PuddingRuntime` 全部 `layers.Add(new ContextLayerSnapshot(...))`（9 处）与 `RecordLayer(...)`（8 处）后，**无任何地方写入该标签** | ✗ **期望在实现中无对应物**：L5 近期历史层确实被装配进提示词（`ContextPipelineLayers.cs:727` 注释「L5: 近期历史」），但**未记录为 `layers` 快照** |

### ★ `RecordLayer` 的命名契约（实现已明确，不是偶发）

```csharp
private void RecordLayer(StringBuilder sb, string content, string snapshotLabel, string layerName, …)
{
    sb.AppendLine($"--- CONTEXT-LAYER: {layerName} ---");                 // 提示词：用【层 ID】
    layers.Add(new ContextLayerSnapshot(snapshotLabel, tokens, pct));    // Layers：用【中文显示名】
    layerInfos.Add(new ContextLayerInfo { LayerName = layerName, … });   // layerInfos：用【层 ID】
}
```

→ 三者分工是**有意设计**：提示词与 `layerInfos` 用层 ID（如 `L0-ENVIRONMENT`），`layers` 快照用中文显示名。
而测试断言的是 `result.Layers[].LayerName`，比的正是**显示名**，故对显示名漂移敏感。

### 结论与建议（需所有者确认命名契约）

**问题一 —— 名不符（2 项）**：
`环境信息`／`动态工具`／`动态技能` 与产品显示名（`运行环境不变量`／`工具使用规则`／`技能使用规则`）不一致，
涉及 `ContextPipeline_ShouldAssembleAll7Layers:671` 与 `EnvironmentLayer_ShouldBePresentInAssemblyResult:798`。
- 推荐：把断言从「精确中文显示名」改为**层 ID** 断言，例如
  `result.LayerInfos.Any(i => i.LayerName == "L0-ENVIRONMENT")` —— 与提示词及 `layerInfos` 的既有契约一致，
  且不会再因显示名调整而崩溃；
- 或最小改动：直接把 3 处字面量改成产品当前显示名。

**问题二 —— 期望在实现中无对应物（1 项）**：
`ContextPipeline_ShouldTriggerGentleCompaction:753` 断言存在名为 `近期历史` 的 `layers` 快照，
但枚举实现后确认**没有任何地方写入该标签**：L5 近期历史层确实被装配进提示词，却**未记录为 `layers` 快照**。
- 该用例的意图（验证 gentle 压缩把远期历史摘要化、近期历史层 token > 0）是合理的**可观测性需求**；
- 两种处置：① 产品侧为 L5 层补一条快照（提升诊断可见性）；② 用例改为断言提示词内容或 `layerInfos`。
- 推荐 ①（小而已明确的可观测性补强），但属产品改动，需所有者确认。

**注（修正上一版报告的猜测）**：上一版曾推测「L5 标签由 `ContextPipelineOrchestrator.cs:500` 的 `snapshotLabel` 动态生成」——
已核实该行位于 `RecordLayer` **内部**（即上表那行 `layers.Add(...)`），并非动态标签生成逻辑，**该猜测不成立**。

## 六之补二、`PuddingWebApiTests`：**大部分失败为 `--artifacts-path` 假失败（本报告第 3 例）**

实测对比（同一测试项目）：

| 布局 | 失败 | 通过 | 总计 | 耗时 |
| --- | --- | --- | --- | --- |
| `--artifacts-path temp/hbk-artifacts2` | **82** | 62 | 144 | 11 s |
| 默认 bin（`--no-build` 复用既有产物） | **6** | 119 | 125 | 19 s |

- 失败形态：绝大多数为**同一夹具** `SessionEventsControllerTests.TestInit()`（`SessionEventsControllerTests.cs:49`）
  在 `Microsoft.AspNetCore.Mvc.Testing.WebApplicationFactory.StartServer()` 阶段失败
  → **单个环境问题被放大成 82 项失败**。
- 判定：**其中 76 项属 `--artifacts-path` 造成**。`WebApplicationFactory` 依赖**内容根/应用程序集定位**，
  artifacts 布局（`<dir>/bin/<项目名>/<配置>/`）会使其启动失败。
- **口径披露（请勿误读上表）**：默认布局一栏用的是**既有产物 + `--no-build`**，其测试总数 125 小于 artifacts 一栏的 144
  （既有产物略旧），故两栏不能逐项对齐；但**失败量级差异（82 vs 6）已足以定性**为布局导致。
- 本次**无法**用「默认布局重新构建」复核：`Source/PuddingAgent/bin/Debug/net10.0/PuddingRuntime.dll`
  被**正在运行的产品自身（PID 41204）**锁定，构建报 `MSB3027/MSB3021`（重试 10 次失败）
  —— 即产品运行时会锁自己在默认 bin 下的产物，这也解释了为何 `--artifacts-path` 在本环境往往是**唯一可行**的测试运行方式。
- 真实失败 **6 项（已逐条确认）**；套件本身**无挂起**（19 秒跑完）。

| # | 用例 | 症状 |
| --- | --- | --- |
| 1 | `Complete_CreatesAdminProviderAndDefaultModel` | 期望 `<OK>`，实际 `<InternalServerError>` |
| 2 | `GoalCommands_Create_Pause_Resume_Cancel_Lifecycle` | 同域失败 |
| 3 | `GoalCommands_Conflict_When_NonTerminal_Goal_Exists` | 同域失败 |
| 4 | `GoalQueries_Return_Snapshot_And_Empty_Iterations` | 同域失败 |
| 5 | `CompactSession_Returns200_WithStringLevel` | 期望 `<OK>`，实际 `<BadRequest>` |
| 6 | `CompactSession_DoesNotStackCompactionPrefixInNewSessionTitle` | 期望 `<OK>`，实际 `<BadRequest>` |

- 其中第 2–4 项属 **Goals 域**，与 Platform 的 `GoalCheckRunnerTests.Run_MissingExitCode_FailsClosedWithExplicitReason`
  同源（**他方 Goals 在途改动**）；第 1、5、6 项分别涉及 admin provider/default model 与 `CompactSession`（返回 `BadRequest`）。
- **★ 交叉验证**：该 6 项在**两次不同布局**的运行中**完全一致**（artifacts 布局的 82 项中的 6 项 = 默认布局的 6 项）
  → 确认为**真实失败**，而非工具假失败；同时也反向印证了「82 项中其余 76 项确为布局引起」。

**方法论再加强（第 3 例）**：`--artifacts-path` 造成的假失败已累计发现 **3 项独立用例 + 1 个整夹具族（本项 76 处）**，
形态也从「路径推断错位」扩展到「`WebApplicationFactory` 启动失败」
→ **该参数下得到的任何失败清单都不可直接定性**，必须先用默认布局复核；
且在本环境需注意：默认布局构建会被运行中的产品自身阻塞，复核时常用 `--no-build` 复用既有产物并**披露口径差异**。

## 六之补三、`PuddingCodeIntelligenceTests`：1 项失败 —— 测试容器缺 `ILogger<T>` 注册

实测：**失败 1 / 通过 95 / 总计 96 / 11 秒，无挂起**。

失败消息：

```
System.InvalidOperationException: Unable to resolve service for type
'Microsoft.Extensions.Logging.ILogger`1[PuddingCodeIntelligence.Lsp.IndexBasedLanguageServerService]'
while attempting to activate 'PuddingCodeIntelligence.Lsp.IndexBasedLanguageServerService'.
  at DependencyInjectionTests.AddPuddingCodeIntelligence_Registers_Core_Services_Without_Registering_Store() :line 23
```

测试代码（`Source/PuddingCodeIntelligenceTests/Services/DependencyInjectionTests.cs:12-25`）：

```csharp
var services = new ServiceCollection();          // ← 裸容器，未调用 AddLogging()
services.AddSingleton<ICodeIndexStore>(fixture.Store);
services.AddPuddingCodeIntelligence();           // ← 产品扩展
using var provider = services.BuildServiceProvider();
Assert.IsNotNull(provider.GetRequiredService<ILanguageServerService>());   // :23 抛异常
```

同文件第二个用例（只解析 `IFileOutlinerRegistry`，不依赖 `ILogger<T>`）**通过** ✅
→ 与「仅需要 logger 的服务无法激活」完全一致。

两种处置（需所有者确认）：

1. **测试侧**：宿主负责注册日志（`services.AddLogging()`），裸 `ServiceCollection` 属夹具遗漏 → 一行修复；
   .NET DI 惯例上 `ILogger<T>` 由 host 提供、库通常不自行 `AddLogging()`，故选此更符合惯例（更可能是测试陈旧）；
2. **产品侧**：让 `AddPuddingCodeIntelligence()` 自带日志兜底（或让 `IndexBasedLanguageServerService` 接受可选 logger
   并回退 `NullLogger`），使扩展对宿主更健壮 —— 属产品健壮性取向，需所有者拍板。

与既有待裁定项同类：**执行方不静默改他人测试**。

## 六之补四、`PuddingRuntimeTests`：从「不可构建」→「可运行」，5 项失败全部集中在**工具审批域**

### 构建修复（3 个错误 → 0，逐项以 `dotnet build` 验证）

| # | 错误 | 修法 |
| --- | --- | --- |
| 1 | `Services/ContextPipelineSkillLayerTests.cs(3,7) CS0246: Moq` | csproj 只有 ProjectReference、零 PackageReference，而该文件 :3 引用 Moq 且 **:197 实际使用 `Mock<>`** → 追加 `<PackageReference Include="Moq" Version="4.20.72" />`（对齐 `PuddingWebApiTests.csproj:16`） |
| 2 | `Tools/PuddingToolInfrastructureTests.cs(4378) CS0117: Assert 未包含 ThrowsExceptionAsync` | **MSTest 4 已移除该 API**；按本仓既有约定迁移到 `Assert.ThrowsExactlyAsync<T>`（证据：`PuddingCoreTests/ResponsesLlmGatewayTests.cs:109` 等多处） |
| 3 | `Tools/TaskToolsTests.cs(572) CS0103: 不存在名称"Mutation"` | 该辅助方法实为嵌套类 `TaskToolsTests.FakeTaskAgentCommandService` 的成员（:193），调用点 :572 在外层测试类的 lambda 中 → 先限定为 `FakeTaskAgentCommandService.Mutation(...)`（暴露 CS0122），再将成员由 `private static` 提升为 `internal static` |

### 套件结果

```
失败: 5，通过: 1348，已跳过: 0，总计: 1353，持续时间: 2 m 13 s   ← 无挂起 ✅
```

### 5 项失败清单

| # | 用例 | 症状 |
| --- | --- | --- |
| 1 | `A91Review_IncompleteOrContradictorySchema_IsDeferred` | `Assert.IsNotNull(result.ReasonCode)` 失败 |
| 2 | `ToolApprovalReviewParser_Returns_DeferredDependency_For_Invalid_Json` | `CollectionAssert.Contains` 失败 |
| 3 | `RequestToolApprovalTool_Accepts_Structured_Checklist_With_Snake_Case_Steps` | `StringAssert.Contains` 失败 |
| 4 | `RequestToolApprovalTool_Accepts_String_Operation_Steps` | `StringAssert.Contains` 失败 |
| 5 | `InvocationToolApprovalLlmClient_PropagatesCallerCancellation` | 期望 `OperationCanceledException`，实际 `TaskCanceledException` |

- **★ 一致性发现**：5 项**全部落在工具审批域**（`A91Review_*` / `ToolApprovalReviewParser_*` /
  `RequestToolApprovalTool_*` / `InvocationToolApprovalLlmClient_*`）
  → 与本会话新立的平台门禁缺陷卡 `d356c701…`（`approval_review_profile_not_configured`，多次阻塞执行类工具）**同域**，
  提示该特性处于**半完成状态**：审查模型未配置 + 相关用例期望值未对齐。
- 第 5 项值得单列：`TaskCanceledException` **派生自** `OperationCanceledException`，
  故产品「调用者取消继续向上抛」的行为**是正确的** ✅，失败源于断言要求**精确类型**。
  该用例自带注释「F06：调用者取消必须继续向上抛，不能变成依赖等待或不建待恢复动作」
  → 意图是「OCE 家族异常向上抛」，建议改用 `Assert.ThrowsAsync<OperationCanceledException>`（允许派生类型），
  属**测试陈旧**而非产品缺陷。
- ★ **第 5 项已修复（2026-09-17 05:35，commit 5b75f57）**：`InvocationToolApprovalLlmClient_PropagatesCallerCancellation`
  原断言 `ThrowsExactlyAsync<OperationCanceledException>` 要求**精确类型**，而实际抛出 `TaskCanceledException`
  （**派生自** OCE，取消路径上 `Task.Delay`/`HttpClient` 抛出的就是它）→ 产品行为**正确**，
  用例注释「F06：调用者取消必须继续向上抛」表明意图是 **OCE 家族**向上抛 ⇒ 断言过严。
  改为 `ThrowsAsync<OperationCanceledException>`（允许派生）+ 补注释。
  验证：单测过滤 → 失败 0 / 通过 1 ✅；全量复测 → **失败 4 / 通过 1349 / 总计 1353 / 2 m 16 s**。
  注：该用例此前**从未真正通过**（ progetto 长期不可构建），先修构建才暴露此问题；若产品侧希望精确抛调用者原始 OCE，应改产品而非断言。
- 第 1–4 项均为工具审批域，与平台门禁缺陷卡 `d356c701…`（`ToolApproval:Llm` 缺配）**同域**，
  提示该特性处于**半完成状态**；其中 `RequestToolApprovalTool_Accepts_String_Operation_Steps` 失败位置为
  `PuddingToolInfrastructureTests.cs:3494`（下一轮取证入口）。

### 过程自纠（两处，均为本轮发现）

1. **构建报「N 个错误」不可据此推断不存在其他错误**：首轮构建只报 1 个错误（Moq），据此误判 `using Moq;` 为残留未用并删除；
   删除后重新构建才暴露 3 个错误、其中 :197 确实使用 `Mock<>`。已当轮撤回删除。
2. **C# 嵌套类访问规则方向**：嵌套类型**可以**访问外层类型的私有成员；但**外层类型不能**访问嵌套类型的 `private` 成员
   （由 CS0122 纠正了反向假设）。

### 最终 8 项目全景

| 项目 | 总计 | 通过 | 失败 | 耗时 | 可构建 |
| --- | --- | --- | --- | --- | --- |
| Core | 863 | 859 | 4 | 56 s | ✅ |
| Platform | 1185 | 1185 | 1 真 + 1 flaky | — | ✅ |
| FullTextIndex | 54 | 50 | 0（4 跳过） | 5 s | ✅ |
| MemoryEngine | 164 | 161 | 3 | 53 s | ✅ |
| CodexService | 4 | 4 | 0 | 0.2 s | ✅ |
| WebApi | 144 | 62 | 82（76 工具假失败 / 6 真） | 11 s | ✅ |
| CodeIntelligence | 96 | 95 | 1 | 11 s | ✅ |
| **RuntimeTests** | **1353** | **1348** | **5** | **2 m 13 s** | ✅（本轮修复） |

**8 个项目全部可构建；8 个套件全部无挂起** ✅

## 六之补五、B 项裁定修正：`git worktree` 用例是**产品缺陷**，不是「用例漏初始化」

（本节取代前文 §五 B 项的「两种解释」表述；证据链已闭合。）

### 用例自己明确了契约

`Source/PuddingCoreTests/Swarm/SwarmOrchestratorTests.cs:386-404`：

```csharp
Exception? thrownException = null;
try
{
    await foreach (var evt in orchestrator.ProcessSwarmAsync("Test task"))
        events.Add(evt);
}
catch (Exception ex) { thrownException = ex; }

// Assert: Should either succeed or handle error gracefully
if (thrownException != null)
    Assert.Fail($"Should not throw exception: {thrownException.Message}");
Assert.IsTrue(events.Any(e => e is SwarmCompletedEvent), "Should complete workflow");
```

→ 契约是**明文**的：传入无效 swarm 目录时**不得抛异常**，且**须完成流程**（产出 `SwarmCompletedEvent`）。
这也解释了为何该用例**唯独不调用** `InitializeTestGitRepoAsync()` —— 它是**故意**拿非 git 目录做输入。

### 产品侧违反契约的确切位置

`Source/PuddingCore/Swarm/WorkerManager.cs`：

```csharp
// :206-222
private async Task<string> RunGitAsync(string[] args, CancellationToken ct)
{ … throw new InvalidOperationException($"git {string.Join(' ', args)} failed: {stderr}"); }

// :137（CreateWorktreeAsync）—— 直接调用、无任何防护
var existingBranches = await RunGitAsync(["worktree", "list"], ct);

// :105-127（CleanupExistingWorktreeAsync）—— 同一文件里却是容错的：
try { await RunGitAsync(["worktree", "remove", worktreePath, "--force"], ct); }
catch { /* 忽略删除失败 */ }
```

→ 非 git 目录下 `git worktree list` 失败 → `RunGitAsync` 抛出
`git worktree list failed: fatal: not a git repository`
→ 异常冒出 `ProcessSwarmAsync` → 测试的 `Assert.Fail` 触发；**错误消息与实测逐字吻合**。

### 结论与建议（需所有者定夺处理语义）

- **定性：产品缺陷**（同文件尚有容错先例，且 `SwarmOrchestrator.cs:193/231` 已有 `ErrorEvent` 通道可用），
  而非「测试夹具遗漏」—— 之前的两种解释中，「用例补初始化」已可排除。
- 修法方向（两种，需所有者选一）：
  1. **局部容错**：`CreateWorktreeAsync` 的 `worktree list` 调用改为与 `CleanupExistingWorktreeAsync` 一致的 try/catch
     （失败则按「无已存分支」继续，或转为可被上层识别的错误）；
  2. **编排层容错**：`SwarmOrchestrator.ProcessSwarmAsync` 捕获 worker 创建失败，
     以 `ErrorEvent` 告知并继续跑到 `SwarmCompletedEvent`（与用例注释「handle error gracefully」完全一致）。
- 建议 ②（更贴合用例契约），但属**产品行为变更**（错误语义取舍），故**未擅自实施**。

## 六之补六、本会话执行阻塞（`approval_review_profile_not_configured`）根因：**配置缺失**（非平台代码缺陷）

### 现象

执行类工具**间歇性完全不可用**，同一调用在分钟级内一次可成功、一次被拒：

```
High-risk tool 'terminal_start' is waiting for the approval review dependency
(reasonCode=approval_review_profile_not_configured):
No approval review model profile is configured (ToolApproval:Llm).
This is a dependency wait, not a human authorization request; retry the same invocation
once the review model is available.
```

`shell`（同 reasonCode）、`file_patch` 亦出现过。影响：套件探针多次被拦、`dotnet test` 全量尝试被迫放弃。

### 证据链

1. `Source/PuddingAgent/appsettings.json:105-107` —— 仅声明 reviewer，**无 `Llm` 子节点**：

```json
"ToolApproval": {
  "Reviewer": "llm"
},
```

2. `Source/PuddingRuntime/Tools/Platform/PuddingToolServiceCollectionExtensions.cs`：

```csharp
:89  services.Configure<ToolApprovalRuntimeOptions>(configuration.GetSection(ToolApprovalRuntimeOptions.SectionName));
:91  services.Configure<ToolApprovalLlmOptions>(configuration.GetSection($"{ToolApprovalRuntimeOptions.SectionName}:Llm"));
:100 services.TryAddSingleton<IToolApprovalLlmProfileResolver, StrictConfiguredToolApprovalLlmProfileResolver>();
:114 if (string.Equals(reviewer, ToolApprovalRuntimeOptions.LlmReviewer, …)) return ActivatorUtilities.CreateInstance<LlmToolApprovalReviewer>(sp);
:120 … "The production registration only supports 'llm'; inject test doubles through test DI." …
```

→ `ToolApproval:Llm` 绑定到 `ToolApprovalLlmOptions`；profile 解析器是**严格配置型**（未配置即拒绝）；
生产注册**只支持 `llm` reviewer** ⇒ 审查必须有可用 LLM profile，而该节点缺配 ⇒ **需审查的高危调用一律 fail-closed**。

3. **「间歇」观感的解释**：未命中审批链路的调用（经防火墙/白名单放行，见 `PuddingToolRegistry.cs:651`
   `fwDecision.Disposition == ToolApprovalDecision.DeferredDependency` 分支）仍可正常通过，命中者才报错。

### 修复方向（需所有者决定）

1. 在 `appsettings.json` 的 `ToolApproval` 下补 `Llm` 配置（字段形状见 `ToolApprovalLlmOptions`）；
2. 或让**只读/低风险**工具（跑测试/构建类 `shell`、`terminal_start`）不经 LLM 审查直接放行，仅对真正高危调用要求审查；
3. 至少把「缺配」与「审查模型暂时不可用」区分开，并给出可操作指引（配哪个项、在哪配）。

### 关联

同域用例失败见「六之补四」中 `PuddingRuntimeTests` 工具审批域 5 项，提示该特性处于**半完成状态**。

## 六之补七、E 项进展：`CompactSession` ×2 的 400 已定位为「缺必需字段 `agentId`」

### 端点与校验

`Source/PuddingPlatform/Controllers/Api/SessionEventsController.cs`：

```csharp
:684  [HttpPost("{sessionId}/compact")]
:685  public async Task<ActionResult<CompactSessionResponse>> Compact(string sessionId, [FromBody] CompactSessionRequest request, CancellationToken ct)
:689  var workspaceId = string.IsNullOrWhiteSpace(request.WorkspaceId) ? "default" : request.WorkspaceId;
:692  if (string.IsNullOrWhiteSpace(request.AgentId))
:694      return BadRequest(new {
:695          errorCode = "agent_id_required",
:696          message = "Compaction requires the current Agent instance ID.", });
:1108 public sealed record CompactSessionRequest(…, ContextCompactionLevel? Level, …, string? CompactionId);
```

### 失败用例发的请求

`Source/PuddingWebApiTests/SessionApiControllerTests.cs`：

```csharp
:340  var response = await _client.PostAsJsonAsync($"/api/sessions/{sessionId}/compact", new
:341  {  workspaceId = "default",
:342     level = "Full",          // ← 缺 agentId
:343     reason = "test compact", });
:375  （第二个用例同形，sessionId 改为真实创建的 oldSession.SessionId，仍缺 agentId）
```

### ★ 对照实验（同路由、通过者）

`Source/PuddingWebApiTests/SessionEventsControllerTests.cs:134-139`（该用例**通过** ✅）：

```csharp
var compactResp = await client.PostAsJsonAsync($"/api/sessions/{created!.SessionId}/compact", new
{
    workspaceId = "default",
    agentId = "default.global_research-assistant.c1",   // ← 有 agentId
    reason = "manual slash command"
});
Assert.AreEqual(HttpStatusCode.OK, compactResp.StatusCode);
```

→ **定性：测试陈旧**（端点新增了必需字段 `agentId`，两条用例未同步；期望 `OK` 实得 `BadRequest` 与
`errorCode=agent_id_required` 完全一致），**不是产品缺陷** ✅

### 修法（需所有者或后续会话执行，本轮不擅自改写测试前置）

- 两条用例均需补齐 `agentId`；且仅补字段可能还不够：若该 agent 无有效配置，会落到
  `:741` 的第二个 400 分支（`AgentConfigurationException`）；
- 参考兄弟用例的前置：**先用真实模板创建会话**（`agentTemplateId="global:research-assistant"`）
  再用其 `SessionId` 与派生 `agentId` 发起压缩；
- 另：`level="Full"` 走 `ContextCompactionLevel?` 反序列化（`:1111`），枚举仍有 `Full`（`:719` 默认值）
  → **该字段本身不是失败源**。

## 六之补八、E 项进展（续）：`Complete_CreatesAdminProviderAndDefaultModel` 的 500 已定位

### 取到服务端异常（单测 + detailed 日志）

```bash
dotnet test Source/PuddingWebApiTests/PuddingWebApiTests.csproj --no-build \
  --filter "FullyQualifiedName~Complete_CreatesAdminProviderAndDefaultModel" \
  --logger "console;verbosity=detailed" > temp/webapi-bootstrap1.log 2>&1
```

日志（`temp/webapi-bootstrap1.log:5-9`）：

```
[05:19:11 ERR] [UnhandledException] errorId=d4149e64ebc4 traceId=null sessionId=null path=/api/bootstrap/complete
System.InvalidOperationException: 模型 'gpt-test' 不能选择 openai、responses 或 anthropic 协议。
   at PuddingPlatform.Controllers.Api.BootstrapApiController.CreateModelRequest(String modelId, String protocol, Boolean isDefault, List`1 modelRequests) :407
   at PuddingPlatform.Controllers.Api.BootstrapApiController.ConfigureProviderAsync(BootstrapProviderRequest provider, CancellationToken ct) :373
   at PuddingPlatform.Controllers.Api.BootstrapApiController.Complete(BootstrapCompleteRequest request, CancellationToken ct) :192
```

> ★ 日志中该异常消息行为 GBK/UTF-8 混写的乱码，中文部分按其字节重建；**英文、方法签名与行号为原文** ✅
> （`Select-String` 按 GBK 解码 UTF-8 日志会产生同样乱码；`file_read` 也回放乱码，因该行字节本身非 UTF-8。）

### 两个结论

1. **测试陈旧**：用例（`BootstrapApiControllerTests.cs:16`）用**虚构模型 id**
   （`chatModelId = "gpt-test"` / `memoryModelId = "gpt-test-mini"`）✗，而
   `BootstrapApiController.CreateModelRequest:407` 会按**模型目录**校验「模型 ↔ 协议」组合 ✗
   → 虚构模型被拒 → 抛异常 → **500** ✗。
   修法：改用已注册模型 id（见 `data/config/llm.providers.json` / `list_llm_providers`），或让用例走测试替身。
2. **产品健壮性（次要，建议）**：这属于**用户输入校验失败**，却以 `InvalidOperationException` 抛出并变成
   **500 InternalServerError** ✗，而不是像同文件已有的 `agent_id_required` 那样返回 **400 + 结构化 errorCode** ✅
   → 建议在 `CreateModelRequest`/`Complete` 边界把该情形转为 400 + 明确 code（如 `model_protocol_mismatch`）。

### E 项现状

| # | 项 | 结论 |
| --- | --- | --- |
| 1 | `CompactSession` ×2 | ✅ 测试陈旧（缺必需字段 `agentId`，对照兄弟用例得 OK） |
| 2 | `Complete_CreatesAdminProviderAndDefaultModel` | ✅ 测试陈旧（虚构模型 id 被模型目录校验拒绝）+ 产品应改返 400 |
| 3 | Goals 域 ×3 | ⏳ 与 Platform `GoalCheckRunnerTests` 同源（**他方在途改动**），不介入 |

→ 结论：E 项**已无任何「疑似产品功能性缺陷」项**；剩余 3 项归他方在途改动。

## 六之补九、C 项（`MemoryEngine` ×3）裁定完成：断言的是**已被废弃的一代命名**（并更正前文一处表述）

### 产品实际发出的快照标签全集（证据：`ContextPipelineOrchestrator.cs`）

`RecordLayer(StringBuilder sb, string content, string snapshotLabel, string layerName, …)` 的约定是
**`layers` 收中文显示名、`layerInfos` 收层 ID**（`:61-204`），部分层直接 `layers.Add(new ContextLayerSnapshot(...))`：

| 快照名（进入 `layers`） | 层 ID（进入 `layerInfos`） |
| --- | --- |
| 静态上下文 | L0-STATIC |
| **运行环境不变量** | L0-ENVIRONMENT |
| 工作区 Agents | L0-AGENTS-ROSTER |
| 任务规划约束（:81） | —— |
| **工具使用规则** | L1-TOOLS |
| **技能使用规则** | L2-SKILLS |
| 工作区环境 | L3-WORKSPACE-ENVIRONMENT |
| 继承上下文 | L2-INHERITED |
| **历史上下文**（:145） | ——（直接 `layers.Add`，**无层 ID**） |
| 用户偏好 / 用户偏好记忆 | —— |
| 重要记忆 | L4-PINNED |
| 上下文增强 / 运行时指令 / 入站消息上下文 / 当前消息 | —— |

### 测试期望的那一代名字在产品里已经不存在

```
search_grep "环境信息|动态工具|动态技能"  →  Source/PuddingMemoryEngine：**0 命中** ✅
search_grep "近期历史"                   →  Source/PuddingRuntime：**仅 1 处注释**
                                          （ContextPipelineLayers.cs:727 `// L5: 近期历史`），
                                          从未作为快照标签出现
```

而期望这些名字的地方**只在测试里**：`PuddingMemoryEngineTests/MemoryLibraryTests.cs:671-675`、`:797`
（注释本身就写着「L1 动态工具」「L2 动态技能」——旧代命名）。

### 结论与修法

- **C 项定性：测试陈旧** —— 断言的是产品中已完全不存在的一代层显示名，**不是功能缺失**（提示词侧断言均绿）。
- 修法（二选一，均需所有者执行）：
  1. **按现行名更新**：`环境信息`→`运行环境不变量`、`动态工具`→`工具使用规则`、`动态技能`→`技能使用规则`、
     `近期历史`→**`历史上下文`**；
  2. **改断言层 ID**（更稳）：`L0-ENVIRONMENT`/`L1-TOOLS`/`L2-SKILLS` 走 `layerInfos`；
     但注意**历史层无层 ID**（直接 `layers.Add`），该条仍需按显示名断言。

### ★ 更正前文一处表述

前文曾说「`:753` 断言的『近期历史』快照在实现中**无对应物**」——
**表述不准确**：该层**确实存在**，只是产品的快照名是 **`历史上下文`**（`:145`），
而生成函数叫 `BuildRecentHistoryLayer`（`ContextPipelineLayers.cs:725`，输出 `--- LAYER: RECENT ---`）。
准确说法是：**「近期历史」这个显示名无对应物，但同一层以「历史上下文」被记入快照**，
因此该条同样属于**命名陈旧**，而非「产品漏记快照」。

## 六之补十、A 项（`LlmProfileResolver` ×3）裁定完成：断言的是**已被有意移除的全局回退**

> 本报告自本节起附章节地图：「六之补」（门禁/WebApi 真失败）、「六之补三」（CodeIntelligence）、
> 「六之补四」（RuntimeTests：构建修复 + 审批域 5 项）、「六之补五」（B 项：git worktree = 产品缺陷）、
> 「六之补六」（执行阻塞根因 = 配置缺失）、「六之补七」（E：CompactSession 缺 agentId）、
> 「六之补八」（E：引导流程虚构模型 id）、「六之补九」（C：废弃命名）、本节（A：移除的全局回退）。

### 产品侧证据（`Source/PuddingCore/Configuration/LlmProfileResolver.cs`，测试指向的正是这个类）

```csharp
/// <summary>
/// Agent LLM Profile 解析器 — 按优先级链解析 Conscious/Subconscious 绑定：
///   1. 模板默认 profiles            ← ★ 悬空的「1.」：原第 2 项（全局回退）已被删除
/// 不抛异常：缺失角色返回 null profile。
///
/// [Obsolete] 全局 roles（roles.conscious / roles.subconscious）不再作为回退。
/// 主 Agent 运行时不使用此解析器；其模型身份来自实例 manifest.json 的
/// preferredProviderId/preferredModelId。
/// </summary>

private static ResolvedLlmProfile? ResolveRole(string roleName, PuddingLlmProvidersConfig llmConfig, string? templateProfileId)
{
    var profileId = templateProfileId;
    if (string.IsNullOrWhiteSpace(profileId))
        return null;          // ← 无 templateProfileId 即 null，**不回退全局 roles**
    …
}
```

### 失败用例断言的是已被移除的行为

`Source/PuddingCoreTests/Configuration/LlmProfileResolverTests.cs`：

```csharp
:115 public void Resolve_Falls_Back_To_Global_Role_Defaults()
     llm = CreateLlmConfig();
     var resolved = LlmProfileResolver.Resolve(llm, template: null, instance: null);
     Assert.IsNotNull(resolved.Conscious);                          // ← 现在必定为 null
     Assert.AreEqual("mimo-v2.5-pro", resolved.Conscious.ModelId);

:136 public void Resolve_Ignores_Instance_Profile_And_Falls_Back_To_Role_Defaults()
     // 名字就已自认：依赖「回退到 role 默认值」
```

→ **定性：测试陈旧**（断言已废弃的全局回退链），与产品的明确声明完全相反；**不是产品回归** ✅
（同文件断言新行为的用例通过 —— 这是最直接的旁证。）

### ★ 附带发现（产品侧 API 异味，建议一并清理）

1. `Resolve(..., AgentInstanceLlmConfig? instance)` 的 **`instance` 形参在实现中未被使用** ✗
   —— 三个失败用例正是按「instance 生效」的旧行为写的；建议**要么移除该形参、要么实现它**，避免误导调用方。
2. XML 文档的「优先级链：1. 模板默认 profiles」**悬空**（无第 2 项）✗ —— 删除回退时未清理，建议补完或删掉列表号。

### 修法（需所有者执行，属改他人测试）

- 删除或重写三个「回退」用例；若需保留回归保护，应改为断言**当前契约**：
  无 `templateProfileId` → `Conscious/Subconscious` 均为 `null`（且不抛异常）。

## 六之补十一、★ 重大关联发现：RuntimeTests 审批域 4 项与「执行阻塞」**同一根因**（`ToolApproval:Llm` 缺配）

### 实测输出（逐个审批域失败用例）

```
dotnet test Source/PuddingRuntimeTests/PuddingRuntimeTests.csproj --no-build \
  --filter "FullyQualifiedName~RequestToolApprovalTool_Accepts|FullyQualifiedName~A91Review_IncompleteOrContradictorySchema|FullyQualifiedName~ToolApprovalReviewParser_Returns_DeferredDependency" \
  --logger "console;verbosity=minimal" > temp/runtimetests-approval4.log 2>&1
```

`RequestToolApprovalTool_Accepts_Structured_Checklist_With_Snake_Case_Steps`（`:3444`）失败消息原文：

```
StringAssert.Contains 失败。字符串“{
  "ticketId": "tap_b9e17acbfd284da6b9c62683a90578c5",
  "status": "deferred_dependency",
  "decision": "deferred_dependency",
  "reasonCode": "approval_review_profile_not_configured",
  "decisionReason": "No approval review model profile is configured (ToolApproval:Llm).",
  "argumentsHash": "44136fa355b3678a1146ad16f7e8649e94fb4fc21fe77e8310c060f61caaff8a",
  "allowedScope": null,
  "expiresAtUtc": null,
  "allowlistRuleId": null,
  "recommendedNextStep": "Waiting for the approval review dependency to recover (configure or fix the ToolApproval:Llm review profile/model). This is not a human authorization request; retry the same invocation once the dependency is healthy."
}”不包含字符串“"decision": "approved"”。
```

`RequestToolApprovalTool_Accepts_String_Operation_Steps`（`:3494`）失败消息同形（同样得到 `deferred_dependency`）。

### ⇒ 结论：不是测试逻辑错，也不是产品逻辑错，而是**测试夹具未提供审批审查依赖**

- 这四个用例在**裸 DI 容器**里跑（`new ServiceCollection()` + `AddPuddingTool<…>()` + `AddPuddingToolRegistry()`），
  既没绑定 `ToolApproval:Llm` 配置，也没注入 `IToolApprovalReviewer`；
- 而生产注册（`PuddingToolServiceCollectionExtensions.cs:100/:107`）是 **`TryAddSingleton` + 严格配置型解析器**
  ⇒ 无配置即 `deferred_dependency` ⇒ 断言 `"decision": "approved"` 必然失败；
- **与「六之补六」的 `approval_review_profile_not_configured` 是同一个 reasonCode、同一段产品逻辑** ✅。

### 关键含义（两个方向）

1. **对测试**：修法是让夹具提供审查依赖 —— 产品已自带 `Source/PuddingRuntime/Tools/Approval/FakeToolApprovalReviewer.cs`
   （供测试注入），或绑定一份 `ToolApproval:Llm` 选项；属改他人测试，**未擅自实施**。
2. **对产品/配置**：这四个用例同时证明「未配置即 fail-closed」在**裸容器**下也发生 ⇒
   进一步印证门禁卡 `d356c701…` 的根因判断与修复方向（补配置 / 低风险工具免审查 / 区分缺配与模型不可用）。
   值得一提的是：产品的**降级输出质量很好** —— 返回了 `recommendedNextStep` 与「这不是人工授权请求」的明确说明 ✅。

### 另两项已全部定性（2026-09-17 05:45）

**① `ToolApprovalReviewParser_Returns_DeferredDependency_For_Invalid_Json`（`:1952`）= 测试陈旧（纯字面文案漂移）**

用例：

```csharp
// ADR-091 §4.1 步骤 5：非法 JSON 属于依赖/协议失败，不是人工决定，也不得伪造批准。
var result = ToolApprovalReviewParser.Parse("not json");
Assert.AreEqual(ToolApprovalDecision.DeferredDependency, result.Decision);          // ✅ 通过
StringAssert.Contains(result.DecisionReason, "Invalid approval reviewer JSON");     // ✅ 通过
Assert.IsFalse(result.RequiresHumanAuthorization);                                 // ✅ 通过
CollectionAssert.Contains(result.MissingRequirements.ToArray(), "valid reviewer JSON"); // ✗ 失败
```

产品侧唯一的赋值处（`Source/PuddingRuntime/Tools/Approval/ToolApprovalReviewParser.cs`）：

```csharp
:21  return ProtocolFailure(ToolApprovalWire.CodeInvalidJson, "Invalid approval reviewer JSON: " + ex.Message);
:100 MissingRequirements = GetStringArray(root, "missingRequirements"),   // 审查器返回时动态读取
:119 MissingRequirements = ["schema-valid review response"],              // ← 协议失败分支的硬编码
```

⇒ 产品写的是 **`schema-valid review response`**，用例断言 **`valid reviewer JSON`** ✗ ⇒
**解析器行为正确，仅字面文案不同** ⇒ 测试陈旧。
（该用例其余三断言全通过 —— 包括 `"Invalid approval reviewer JSON"` 与 `:21` 字面一致 —— 反向证明只是这一条的文案未同步。）
修法（一行，属改他人测试，未擅自实施）：改为 `"schema-valid review response"`，或改为断言集合非空。

**② `A91Review_IncompleteOrContradictorySchema_IsDeferred`（`PuddingToolInfrastructureTests.A91Review.cs:35`）
= 与 ① ② 同源（夹具未提供审查依赖）**

实际得到 `{"decision":"deferred_dependency","reason":"unavailable","requiresHumanAuthorization":true}`，
用例断言 `Assert.IsNotNull(result.ReasonCode)` —— 在「审查根本未开始（无 profile）」时自然为空；
先让其夹具注入 `FakeToolApprovalReviewer` / 绑定 `ToolApproval:Llm` 后才能判定其断言是否也需更新。

### 本轮小计

RuntimeTests 审批域 5 项：**1 项已修复（OCE 家族断言）**、**2 项定为夹具缺审查依赖**、
**1 项定为字面文案陈旧**、**1 项待配好审查依赖后复判** ⇒ **无一项是产品逻辑缺陷** ✅

## 七、工具与排查手册（本环境实测）

## 七、工具与排查手册（本环境实测）

### 测试运行

```bash
# 取失败清单：日志仅数十行，只含失败用例名 + 错误消息 + 堆栈，file_read 尾部即可读完
dotnet test <proj>.csproj --no-build --artifacts-path temp/hbk-artifacts2 \
  --logger "console;verbosity=minimal" > temp/x.log 2>&1

# 区分「慢」与「卡死」：需要逐条输出
dotnet test <proj>.csproj --logger "console;verbosity=normal"

# 定位挂起：点名阻塞测试并落盘 hangdump
dotnet test <proj>.csproj --blame-hang --blame-hang-timeout 300s
```

- `--artifacts-path temp/hbk-artifacts2`：绕开残留 testhost 对 `bin` 下 DLL 的锁（否则构建阶段报 MSB3021/MSB3027）。
- `--no-build`：复用已构建产物，零构建开销（Core 全量仅 56 秒）。
- `-v:q` 的零输出**不能**作为挂起判据。
- ⚠️ **`--artifacts-path` 的副作用（必读）**：它把 bin 移到 `temp/...`，从而改变 `AppContext.BaseDirectory`
  的层级 —— **凡以 bin 相对路径定位仓库数据的测试都会假失败**（实例与对照实验见 §6.C 的更正）。
  即该绕过手段与「路径类测试的可信度」之间存在取舍：验证路径类测试时应改用默认 bin 布局，
  但需先确认没有残留 testhost 占用；判断失败原因时，优先做「换布局重跑」的对照实验，
  以先排除「失败由自己的运行参数引起」。

### 检索与取证

- **`findstr` 不可用于日志检索**：对 UTF-8 无 BOM 文件，中文与部分 ASCII（实测 `Exception`、`Assert`）均 NoMatch。
- **`search_grep` 在 `temp/` 会超时**（文件数过多，`scanned 0/2000`，`coverage: partial`）：须先把目标复制到干净小目录再检索；**partial 结果不得当全集**。
- `terminal` 走 cmd：PowerShell 专属 cmdlet（`Select-String`）与多 `/C:` 模式（含空格）均失败。
- `shell` 工具对**非零退出码不回传 stdout**（仅报 `exit code 1`）→ 一律 `> temp/x.log 2>&1` 重定向后 `file_read` 取结果。
- 平台门禁已知缺陷：`terminal_start` / `shell` / `file_patch` 会间歇报 `approval_review_profile_not_configured`
  （**依赖等待，非人工授权**）。不要盲目重试，改用等价手段或稍后再试。
- `git_log` 的 `file_path` 过滤在本环境未生效（返回分支最近提交，未按路径过滤），需注意勿据此下结论。

### 提交纪律（共享工作目录）

- 永远**显式文件清单**暂存，禁止 `git add -A`、禁止 amend/改写历史；错话用后续提交显式更正。
- 改他方文件前先 `git_diff` 确认工作区干净；不代为提交他方在途改动。
- 临时产物一律写 gitignored 的 `temp/`。

## 八、本会话推送的 9 个修复

```
0ce4f3de  fix(tests): GitSnapshotServiceTests 断言参数写反
38d0c5ce  fix(tests): WorkerManagerTests 断言参数写反
0137c4ea  fix(tests): 解除 FileSwarmTransportTests 永久挂起
47dd39e8  fix(tests): FileSwarmTransportTests 补齐 catch OCE + 放宽超时（该组 11/11）
412feabe  fix(tests): IsGreaterThanOrEqualTo 与 IsLessThan 两族参数写反（2 用例转绿）
32b34962  fix(swarm): ProcessSwarmAsync 空输入不再抛 NRE（产品缺陷）
08302d70  fix(tests): 消除 ListSnapshots_ReturnsMostRecentFirst 时序 flaky
ba6d8961  fix(tests): 消除 FileSystemWatcher_DeliversNewMessages 负载 flaky
81f73da9  fix(tests): 修正 DefaultDistillerTests 两处同类写反，并更正先前分类
```
