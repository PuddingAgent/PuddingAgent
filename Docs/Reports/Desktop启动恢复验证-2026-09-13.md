# Desktop 启动恢复验证（2026-09-13）

用户报告运行中心错误：`Core did not emit a Ready signal or renew its startup lease within 60 seconds.`

检查时 Desktop/Core 均未运行，无法取得截图对应进程的内存输出环形缓冲。既有系统日志最后写入约 20:37，不能据此确定该次超时的阻塞阶段。

启动当前 `Source/PuddingDesktop/bin/Debug/net10.0-windows10.0.17763.0/PuddingDesktop.exe` 后，Desktop PID 25068 托管 Core PID 2124，DataRoot 为 `D:\data`，Core 为 `Source/PuddingAgent/bin/Debug/net10.0/PuddingAgent.exe`。

- 22:55:06 开始启动；Platform/Memory schema、Workspace Catalog、jieba 初始化继续推进。
- 已观察到合法且递增的 `PUDDING_DESKTOP_STARTING`，22:55:28 发出 `PUDDING_DESKTOP_READY`。
- 22:55:36 Desktop 状态进入 Ready；22:55:44 `/health/ready` 返回 HTTP 200、`status=ready`，`lastError=null`。

本次属于运行恢复，未复现原故障，根因未确定；未修改产品代码、超时配置或手工修补数据库。原有源码改动和运行数据保留。若再次发生，应在退出 Desktop 前通过受 Control Token 保护的 `/desktop/bootstrap/diagnostics` 捕获 `coreLogTail` 和当前状态，避免失去失败周期证据。Program.cs 的租约从 Host Build 后开始，这是待验证边界，尚不能认定为此次根因。
