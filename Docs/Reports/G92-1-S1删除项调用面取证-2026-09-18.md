# S1 删除项调用面取证（只读，2026-09-18）

范围：`Source/PuddingCore`、`Source/PuddingPlatform`、`Source/PuddingRuntime`、`Source/PuddingPlatformTests`、`Source/PuddingCoreTests`。
方法：`search_grep`（目录级缩小 + 命中上限公告）+ `file_read` 逐点确证；无构建、无 git 写、无记忆写。
依据：`Docs/07架构/106ADR-092...md` 决策 5/6；`Docs/Features/Goal目标驱动执行与分层验证闭环设计-2026-09-15.md:150`（S1 完成门槛=「删工程门禁兜底完成及Goal步骤advance/remaining/scope分支」）。

> 口径说明：委派已定位的两处（`GoalAcceptanceContractPlanner`、`ConservativeGoalIterationVerifier`）计入 **A**。另两项按 ADR-092 决策 5 拆分：
> **B = Goal 步骤引擎 advance/remaining**；**C = 两级 Verifier / verification scope（work_unit↔goal 双层裁决）**。
> 若父级要的 A/B/C 映射不同，见文末「口径待确认」。

---

## A：工程门禁兜底完成（决策 6；通用 build/test 不得兜底为完整合同）

### A1 合同生成点（已定位，不重复推导）
- `Source/PuddingPlatform/Services/Goals/GoalAcceptanceContractPlanner.cs`
  - `:18` 类声明（DI 单例）；`:19-21` 构造依赖 `GoalAcceptanceContractStore` / `IOptions<GoalRunOptions>` / logger。
  - `:23` `Source = "bounded_planning"`；`:26` `SourceWithObjectiveEvidence = "bounded_planning:objective_evidence"`。
  - `:39-52` `EnsureContractAsync` 入口；`:47-52` 已有条件的合同不重写（条件变化必须升 Revision）。
  - `:54-66` `ResolveProjects()` + `GoalObjectiveEvidenceParser.Extract`；**两类输入皆空则保持空合同**（fail-closed，只 LogInformation）。
  - `:88-93` 回归门禁循环 `AddBoundedPair`（显式 `CheckProjects` → build/test 条件）——即「工程门禁」来源。
  - **`:95-99`（核心兜底点）** `if (!evidence.HasAny) logger?.LogWarning("[GoalContract] objective declares no evidence; contract covers engineering gates only goal={GoalRunId}")` —— 仅告警，不阻断落库。
  - `:98` `source = evidence.HasAny ? SourceWithObjectiveEvidence : Source`。
  - `:104-116` `contractStore.SaveAsync(...)` 落库 + LogInformation。
  - `:120-138` `ResolveProjects()`：仅接受 `_options.CheckProjects` 中通过 `GoalCheckDefinitionRegistry.IsSafeTarget` 的相对路径（拒绝通配/绝对路径）。
  - `:180-232`（尾部）`AddEvidenceCheck` / `AddEvidencePair` / `AddFileEvidenceCheck` / `AddBoundedPair` 条件与检查定义构造。

### A2 判定末端（已定位；补充分支族）
- `Source/PuddingPlatform/Services/Goals/ConservativeGoalIterationVerifier.cs`
  - `:13` 类声明；`:41-46` `boundToTask / goalScope / RemainingWorkUnits==0` 判定。
  - **`:134-139`** `else if (allRequiredPassed && goalScope)` → `Verdict = Complete`（兜底完成的最终裁决）。
  - `:142-160` `goalScope && RemainingWorkUnits==0` 且整体条件未通过 → `Continue`（合法中间态）。
- `Source/PuddingPlatform/Services/Goals/GoalSettlementStore.cs`（裁决落地口径）
  - `:696-708` `proposed.Verdict==Complete && IsTaskTerminalNonCompletable(task.Status)` → 降级 `Blocked("task_blocked")`（唯一否决口）。
  - `:885-1020` `ApplyBoundPlanGates`：`:905-911` 收敛态 + `AllRequiredCriteriaPassed` → `Complete`；`:985-993` 同一完成判据的第二条路径。
  - `:1020-1086` `ApplyBoundPlanVerdict`：`verifiedWorkUnit` → 回写 Plan/Root Completed（仅当 `AllRequiredCriteriaPassed`）。
  - `:1088-1135` `ApplyCurrentVerdict` 的 `Verdict==Complete` 分支：`goal.Status=Completed` + `CompleteBoundTask`（Task 终态唯一写入口）。

### A3 上下游消费点（本轮新增，A 的重点）
上游（谁触发）
- `Source/PuddingPlatform/Services/Goals/GoalSettlementWorker.cs`
  - `:11-13` 构造注入 `IGoalIterationVerifier verifier, GoalAcceptanceContractPlanner planner, GoalAcceptanceContractStore contractStore`。
  - `:85-107` `SettleCandidateAsync`：`capsule.Criteria.Count==0` → `planner.EnsureContractAsync(...)`；`planned==true` → `contractStore.LoadAsync(...)` 回填 `Criteria/Checks`。
  - `:110-140` 检查执行与报告合并（只认持久 passed/failed，waiting 合并）。
  - `:141` `verifier.VerifyAsync(capsule)`；`:143` `store.ApplyAsync(candidate, decision)`。
  - `:170-199` `RunChecksAsync`：`Scope = capsule.VerificationScope`（`:175`），调用 `IGoalCheckRunner.RunAsync`。
上游（候选构造 / 合同读取）
- `Source/PuddingPlatform/Services/Goals/GoalSettlementStore.cs`
  - `:117` `GetCandidatesAsync`；`:247-252` `ResolveVerificationScope(await LoadBoundPlanAsync(...))`。
  - `:278-281` 候选填 `VerificationScope / RemainingWorkUnits / Criteria = ReadCriteria(contract?.CriteriaJson) / Checks = ReadChecks(contract?.ChecksJson)`。
  - `:69-95` `ToCapsule()`（capsule 的字段来源）。
持久化 / schema / 配置
- `Source/PuddingPlatform/Services/Goals/GoalAcceptanceContractStore.cs`：`:16` 类；`:19-31` `LoadAsync`；`:36-95` `SaveAsync`（版本化 upsert，默认 `source="bounded_planning"`）。
- `Source/PuddingPlatform/Services/Goals/GoalSchemaBootstrapper.cs:172-185`：`goal_acceptance_contracts`，`source TEXT NOT NULL DEFAULT 'bounded_planning'`。
- `Source/PuddingPlatform/Data/Entities/GoalAcceptanceContractEntity.cs:42-44`：`Source` 默认 `bounded_planning`。
- `Source/PuddingCore/Goals/GoalRunOptions.cs:52-58` `CheckProjects[]`（有界规划的受检目标，>16 项 / 不安全路径被拒）；`:40-46` `CheckWorkingDirectory` / `CheckTimeoutSeconds`。
- `Source/PuddingPlatform/Services/Goals/GoalVerificationPersistence.cs:62-66` `ReadCriteria/ReadChecks`。
下游（读投影 / 续行信封）
- `Source/PuddingPlatform/Services/Goals/GoalQueryService.cs:108+` `GetStepsAsync` 的 `checks` 投影（目标级 checks，非 per-step）。
- `Source/PuddingPlatform/Services/Goals/GoalContinuationWorker.cs:348` `BuildLastVerdict`；`:396` `TryLoadLastVerificationAsync`（把上一轮裁决摘要注入续行 payload）。
- `Source/PuddingPlatform/Services/Goals/GoalContinuationWorker.cs:541+`（提示词中「server verifier decides terminal state」）。
顶点注册
- `Source/PuddingHost/Extensions/PuddingServiceCollectionExtensions.Platform.cs:157-171`：`GoalSettlementStore / IGoalIterationVerifier→ConservativeGoalIterationVerifier / GoalAcceptanceContractStore / GoalAcceptanceContractPlanner / IGoalCheckRunner→GoalCheckRunner / GoalAcceptanceContractStore` + `Configure<GoalRunOptions>` + `AddHostedService<GoalSettlementWorker>`；`:208-209` 启动期 `Get<GoalRunOptions>()` 校验。

### A 命中数
- 生产代码命中：3 个属主文件（Planner / Verifier / SettlementStore）+ 1 个 Worker + 2 个持久化/schema + 1 个 DI 顶点；下游读投影 2 处；配置面 1 处（`CheckProjects`）。合计 **约 10 处消费点**，无「未找到」。

---

## B：Goal 步骤引擎 advance/remaining（决策 5）

契约与投影（Core）
- `Source/PuddingCore/Goals/GoalVerificationContracts.cs`
  - `:42-45` `VerificationScope`（默认 `work_unit`）；`:47-51` `RemainingWorkUnits`（`int?`，null=未知保守）。
  - `:148-153` `GoalVerificationScopes { WorkUnit="work_unit", Goal="goal" }`。
  - `:171-185` `GoalSettlementDispositions`；`:187+` `GoalSettlementDecisionCalculator`（`:198-215` `AllRequiredCriteriaPassed`，`:326-347` `ComputeDisposition` 返回 `Advance/Complete/...`）。
- `Source/PuddingCore/Goals/IGoalCommandService.cs:75-105`：`GoalStepProgressSnapshot { StepsTotal, StepsPassed, StepsFailed, StepsInProgress, CurrentStepId }` + `GoalStepSnapshot`（depth1 叶投影）。

步骤选定（Platform）
- `Source/PuddingPlatform/Services/Goals/GoalRunStore.cs`
  - `:78-90` `FindCurrentTaskWorkUnitAsync(taskPlanId)`：首个非终态 depth1 叶子（Completed/Cancelled/Superseded 之外），按 `SequenceNo`。
  - `:92-101` `FindPlanStepsAsync(taskPlanId)`：全部 depth1 叶（供 stepsPassed/stepsTotal）。
- `Source/PuddingPlatform/Services/Goals/GoalQueryService.cs`
  - `:108` `GetStepsAsync`；`:140-143` `currentStepId` 复用 `FindCurrentTaskWorkUnitAsync`（W2 硬约束：不得另写判断）；`:170-180` `StepsTotal/Passed/Failed/InProgress/CurrentStepId` 组装。
- `Source/PuddingPlatform/Services/Goals/GoalContinuationWorker.cs`
  - `:110-115` 续行前取 `workUnit = FindCurrentTaskWorkUnitAsync(...)` / `planSteps = FindPlanStepsAsync(...)`。
  - `:447-530` 续行 payload：`loop { phases, current, stepIndex, stepTotal }`、`currentStep { nodeId, kind, title, sequenceNo, status }`、`progress { stepsPassed, stepsTotal }`。
- `Source/PuddingPlatform/Services/Scheduling/TaskExecutionTracker.cs:75-78`：`currentWorkUnits`（按 plan 取首个非终态 depth1 叶）；`:135` 传入绑定评估。
- `Source/PuddingPlatform/Services/Goals/GoalSettlementStore.cs:846-863` `ResolveVerificationScope`：`remaining==0 → (Goal,0)`；否则 `(WorkUnit,remaining)`；计划缺失/非法 → `(WorkUnit,null)`。
- `Source/PuddingPlatform/Controllers/Api/GoalQueriesController.cs:76-95` `GET goals/{goalId}/steps`；`:142-146` 响应 DTO（`StepsTotal/StepsPassed/.../CurrentStepId`）。

「advance」落点（无独立方法，命名为处置枚举）
- `GoalVerificationContracts.cs:187-347` `GoalSettlementDispositions.Advance`；`GoalSettlementStore.cs:1441` `success = typedDisposition is Complete or Advance`。

### B 命中数
- 生产命中：Core 2 文件（契约 + 命令面 DTO）、Platform 5 文件（RunStore/QueryService/ContinuationWorker/Tracker/SettlementStore）+ 1 控制器；测试 1 文件（`GoalContinuationTests.cs:167,198` 断言 stepsTotal）。合计 **约 9 处消费点**。
- **「未找到」**：代码中不存在 `CurrentWorkUnit` 属性/字段（仅存在于设计文档措辞）；等价载体是 `FindCurrentTaskWorkUnitAsync` + `GoalStepProgressSnapshot.CurrentStepId`。也不存在独立 `AdvanceStep`/`GoalStep` 推进方法。

---

## C：两级 Verifier / verification scope 双层裁决（决策 5）

接口与唯一实现
- `Source/PuddingCore/Goals/GoalVerificationContracts.cs:76-84`：`IGoalIterationVerifier.VerifyAsync(GoalEvidenceCapsule, ct)`（只读、不写状态、不执行工具）。
- `Source/PuddingPlatform/Services/Goals/ConservativeGoalIterationVerifier.cs:13`：**唯一实现**（`:41-61` scope 判定；`:134` Complete；`:142` Continue；其余为 Blocked 分支族 `:65-132`）。
- 注册：`Source/PuddingHost/Extensions/PuddingServiceCollectionExtensions.Platform.cs:158` `AddSingleton<IGoalIterationVerifier, ConservativeGoalIterationVerifier>()`。

两级（scope）载体
- `Source/PuddingCore/Goals/GoalVerificationContracts.cs:148-153` `GoalVerificationScopes.WorkUnit|Goal`（`:150` `WorkUnit`）。
- `Source/PuddingCore/Goals/GoalCheckContracts.cs:119` `GoalCheckSpec.Scope`（默认 `work_unit`）。
- `Source/PuddingPlatform/Services/Goals/GoalSettlementStore.cs:44-47` `GoalSettlementCandidate.VerificationScope`；`:69-95` `ToCapsule()`；`:247-281` 填入候选。
- `Source/PuddingPlatform/Services/Goals/GoalSettlementWorker.cs:175` `GoalCheckContext.Scope = capsule.VerificationScope` → `GoalCheckRunner.cs:65` 消费。
- `Source/PuddingPlatform/Services/Goals/GoalSettlementStore.cs:853-863` scope 计算（见 B）。

审计落库
- `Source/PuddingCore/Goals/GoalEventTypes.cs:45-46` `goal.verification.requested/completed`；`:69-73` `GoalProducerComponents.Verifier="goal.verifier"`。
- `Source/PuddingPlatform/Services/Goals/GoalSettlementStore.cs:521-533` `GoalVerifications.Add(...)`；`:577-590` 两类 verifier 事件。
- `Source/PuddingPlatform/Services/Goals/GoalSchemaBootstrapper.cs:131-162`（`goal_verifications` 表 + 去重索引）；`Source/PuddingPlatform/Data/Entities/GoalVerificationEntity.cs:7-11`；`Source/PuddingPlatform/Data/PlatformDbContext.cs:137,769-771`。
- `Source/PuddingPlatform/Services/Goals/GoalRunStore.cs:115` `FindLatestVerificationAsync`；`GoalContinuationWorker.cs:396` 读取。

### C 命中数
- 生产命中：Core 2 文件（接口 + check scope）、Platform 4 文件（Verifier/Worker/SettlementStore/Schema）+ 1 Entity + DbContext + 1 DI 顶点。合计 **约 9 处消费点**。
- **「未找到」**：仓库内**不存在第二个 `IGoalIterationVerifier` 实现**，也不存在独立的「步骤审阅 Agent / 整体审阅 Agent」类。设计里的「两级」在现有代码中表现为 **scope 两级（work_unit ↔ goal）单 Verifier**，外加 `GoalSettlementStore.ApplyBoundPlanGates` 的第二道计划闸（`:885-1020`）。若 S1 的「两级 Verifier 移除」指删除第二道计划闸/scope 分支，则范围 = C2 列出的 `GoalSettlementStore.cs:846-1020` + `ConservativeGoalIterationVerifier.cs:41-46,134,142`。

---

## 测试面（只读；用于回归锁定位）
- `Source/PuddingPlatformTests/Services/Goals/ConservativeGoalIterationVerifierTests.cs`（`:88,109,132,183,196,237...` 构造 capsule / `remainingWorkUnits`，多处 `:132-544` 断言 scope 分支）。
- `Source/PuddingPlatformTests/Services/Goals/GoalAcceptanceContractPlannerTests.cs`（`:15-19,52,191,261,318` 断言 `Source`/`SourceWithObjectiveEvidence`）。
- `Source/PuddingPlatformTests/Services/Goals/GoalSettlementAcceptanceRegressionTests.cs:350`（生产 verifier）。
- `Source/PuddingPlatformTests/Services/Goals/GoalSettlementWorkerCheckIntegrationTests.cs:194-198,404-451`（透传 verifier + 真实 planner/contract Store）。
- `Source/PuddingPlatformTests/Services/Goals/GoalContinuationTests.cs:167,198,601,679,741,813,919,1023,1158,1257`（stepsTotal 断言 + verifier 调用）。
- `Source/PuddingPlatformTests/Services/Goals/GoalCheckEvidenceUnavailableTests.cs:174`、`GoalCheckRunnerTests.cs:248`（`Scope=WorkUnit`）。
- `Source/PuddingCoreTests/GoalCheckEvidencePolicyTests.cs:348-360`；`GoalSettlementCriterionAggregationTests.cs`（Advance/Complete/Wait/Stop）；`GoalSettlementDecisionCalculatorTests.cs:54-181`（处置轴，覆盖 `Advance`/`Complete`）。
- 在途未提交、只读未动：`Source/PuddingPlatformTests/Services/Goals/GoalCommandServiceTests.cs`。

## 未触碰 / 未找到汇总
- 三节均无「整节未找到」；A 无未找到项，B、C 各有一条命名级「未找到」（见上）。
- `Source/PuddingRuntime`：三个目标符号（CurrentWorkUnit/RemainingWorkUnits、GoalAcceptanceContractPlanner、IGoalIterationVerifier）**零命中**（该层不参与 Goal 结算/验证；Goal 相关仅出现在 Host 注册与 Platform 服务）。
- 搜索边界：`search_grep` 在整目录 `Source` / `Source/PuddingPlatformTests` 会触发 2000 文件枚举上限（假阴性），已改为子目录级检索；`Source/PuddingPlatformTests` 顶层、`Source/PuddingCoreTests` 顶层已完成子目录化覆盖（Goals/ 两目录 + 根级 Goal* 测试文件）。

## 口径待确认（NOTES）
- 「三处代码」的 A/B/C 归属按本文顶部口径拆分。若父级本意是「A=兜底合同、B=Goal 步骤引擎、C=scope 分支」或「A/B=已定位两点、C=剩余一处」，第三节可按需重排——素材已按文件:行完整给出，无需再次取证。
