# Goal 检查器依赖注册与 Core 启动修复（2026-09-15）

## 现场与根因

截图运行中心为 CircuitOpen，最近 PID38660、退出码 -532462766。Desktop `bootstrap/status` 确认 22:55:46 发起源码 rebuild-restart，构建退出码 0，22:56:34 返回 coreRestarted=false。Windows Application/.NET Runtime 1026 事件按 XML Execution.ProcessID 确认 22:56:29、36、44、55，PID37776、10040、28472、38660 均因相同依赖注入异常退出。

异常在 `PuddingApplicationHost.Build`/ValidateOnBuild：GoalCheckRunner 与 GoalSettlementWorker 无法解析 `ITerminalCommandAdmission`。Runtime.DependencyInjection 已注册接口，但产品 PuddingHost 不调用 AddPuddingRuntime，仍只注册 ITerminalCommandPolicy。上轮视觉故障修复的完整工作树测试已记录此独立缺口；当时部署的是隔离旧基线，因此可 Ready，本次源码 rebuild 将新 Goal 代码加载后暴露。

## 修改

- PuddingHost 注册 DefaultTerminalCommandPolicy 单例，并映射 ITerminalCommandPolicy、ITerminalCommandAdmission 到同一实例。
- 产品组合根测试解析真实 GoalCheckRunner，断言两个接口和具体类型实例一致；校验 dotnet test 可准入、taskkill 被拒绝，仅调用策略而不启动测试命令。
- 保留 ValidateOnBuild、现有命令准入规则和 Desktop 熔断策略。

## 验证

- 当前工作树产品组合根测试 1/1 通过（原先因缺依赖失败）。
- Core 定向构建 `dotnet build Source/PuddingAgent/PuddingAgent.csproj --no-restore` 到 `.tmp-build/goal-admission-fix`，0 errors。
- 通过 Desktop `core/deploy-restart` 部署预构建包，返回 success=true、coreRestarted=true、errors=[]，熔断状态恢复为 Ready。
- 新 Core PID6940，启动时间 23:03:20；23:04:09、23:04:43、23:05:17 三次检查均为 Ready 与 `/health/ready` HTTP200，最后已持续运行超过116秒，无新 .NET Runtime 崩溃事件。
- PuddingAgent/PuddingHost/PuddingRuntime/PuddingPlatform DLL 哈希与构建包一致；prepared/loaded managed artifact manifest 一致：`5550e4f4ccbb3003195725f96612c34397cb0bf39e75e4ebda104baa12150a01`。
- 证据保存在 `temp/goal-admission-host-tests.log`、`temp/goal-admission-build.log`、`temp/goal-admission-deployment-result.json`、`temp/goal-admission-verification.json`。本轮验证产品启动、策略共享和稳定健康，不代表完整 Goal 业务流程验收。
- 保留工作树原有投影等修改，提交只包含本次注册、测试及文档。
