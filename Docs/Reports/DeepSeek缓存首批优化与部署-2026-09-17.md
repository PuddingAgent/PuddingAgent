# DeepSeek 缓存首批优化与部署（2026-09-17）

## 范围

沿用[隔夜诊断](隔夜缓存命中评估与Harness-Reasonix优化方案-2026-09-17.md)和ADR-084，以DeepSeek为主要验收模型；不改变资源池选型、不降低模型输出上限、不改变实际工具协议或权限。

## 工具能力包（源码验证完成，待部署）

- ToolExposurePlanner在首次发现某项能力时一次曝光同组已授权工具：文件读取3项、编辑2项及读取依赖、代码2项及读取依赖、终端6项、Git只读3项。
- 不从用户文本猜测能力，不全量预加载；读取不会触发写入或Shell，Git只读不会触发提交/推送。未授权/已撤销定义不进入工具包。
- 使用现有轮边界与稳定追加序；已提交可见清单参与恢复基线，避免loaded集合为空时工具组收缩。实际工具执行继续经过原权限/审批/参数校验链。
- 这是需求出现后的成组发现，不是首轮任务分类预加载；后者需要可信任务类型与成本实测再做。
- 定向测试28/28通过（含新增9项工具包场景），TRX `.tmp-test-out/cache-bundles-results/cache-bundles.trx`。没有发起模型付费调用；不能据此给出线上命中提升百分点。

新增工具会使已有会话在首次触发工具包时发生一次前缀变化，首轮输入也可能变大。验收必须按整个任务的输入/miss/输出/延迟/成功率衡量，不能把相关miss全部视作收益。
