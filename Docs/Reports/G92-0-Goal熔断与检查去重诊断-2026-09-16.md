# G92-0 Goal 熔断与「检查结果去重」诊断（2026-09-16）

> 结论先行：重启确实加载了修复，但 Goal **不会再自动跑下一轮** —— 它已因连续 3 轮同一 blocker 熔断（`Blocked` + `human decision required`）。
> 同时本轮查清了此前被忽略的**第二层障碍**：受控检查结果按 `dedup_key` 复用，修复代码若不变更 `input_fingerprint` 将无法生效；而当前 GoalRun 已处于 `activation_epoch=2`，**恰好意味着下一次迭代会重跑检查**。

## 一、重启生效验证（四条独立证据）

| 证据 | 值 |
|---|---|
| 修复代码在位 | `Source/PuddingPlatform/Services/Goals/GoalCheckRunner.cs:204` `var readOffset = Math.Max(0, (probe?.TotalLines ?? 0) - MaxCapturedLines);` |
| 修复文件 mtime | `GoalCheckRunner.cs` = **2026-09-16 22:19:58** |
| 部署产物时间 | `.tmp-build/agent-model-output/PuddingPlatform.dll` = **23:02:56**（晚于修复 ⇒ 必含修复） |
| 部署结果 | `D:\data\config\rebuild.signal.result.json`：`preparedAssemblySha256 == loadedAssemblySha256 = 8d29163770d5ffc92e22afb9a01c0efa252b632502e53002afc2517cabc45137`、`assembliesReloaded=true`、`coreRestarted=true`、`errors=[]` |
| 新 Core 启动 | `D:\data\logs\system\pudding-20260916_057.log` 首行 **23:05:18** |

## 二、Goal 熔断真相（GoalRun `7ef90f2c66f449389fc0046eb9662cce`）

```
status                        = 3  (GoalPhase.Blocked)
blocked_code                  = no_progress_circuit_open
blocked_message               = "Circuit breaker opened after 0 settlements without progress
                                 (sameBlocker=3, infraFailures=0, threshold=3); human decision required."
resume_policy                 = paused          ← 重启不自动恢复（ADR-074 §12 默认）
activation_epoch              = 2
iterations_started/settled    = 4 / 4
consecutive_same_blocker      = 3
consecutive_no_progress       = 0
max_iterations                = 32
terminal_at_utc               = null
```

四轮裁决**完全一致**（`goal_verifications`）：

| iteration | completed_at_utc | verdict | blocker_code | unmet_criteria |
|---|---|---|---|---|
| 1 | 13:56:41Z | blocked | criterion_failed | `test:…PuddingPlatformTests.csproj: test_count_unknown` |
| 2 | 14:09:42Z | blocked | criterion_failed | 同上 |
| 3 | 14:26:23Z | blocked | criterion_failed | 同上 |
| 4 | 14:28:43Z | blocked | criterion_failed | 同上 |

`goal_iterations` 四轮均 `status=settled`、`stop_reason=completed`、**`progress_fingerprint` 全为 null** ⇒ 无进展指纹 ⇒ 与 `consecutive_same_blocker=3` 一致 ⇒ 触发熔断。

## 三、第二层障碍：受控检查结果被去重复用

`goal_check_records` **只有 2 条记录，且均在 `iteration_no=1 / activation_epoch=1`**：

| check_id | status | failure_code |
|---|---|---|
| `bounded:build:Source/PuddingPlatformTests/PuddingPlatformTests.csproj` | finished | `null`（通过） |
| `bounded:test:Source/PuddingPlatformTests/PuddingPlatformTests.csproj` | finished | `test_count_unknown` |

即：**迭代 2/3/4 没有产生新的检查记录，却各自拿到了 `criterion_failed` 裁决** ⇒ 复用迭代 1 的结果。

去重规则（代码事实）：

- `GoalCheckRecordStore.cs:12` — 注释原文：“`(scope|criterionRevision|definitionHash|inputFingerprint)` 相同则复用执行结果”
- `GoalCheckRecordStore.cs:45-51` — 以 `GoalVerificationPersistence.BuildScopedDedupKey(scope, criterionRevision, definitionHash, inputFingerprint)` 构造 `dedup_key`
- 实测 `dedup_key` = `7ef90f2c…|1|work_unit|1|sha256:4357c6fa09c…|epoch:1:objective:1`

`input_fingerprint` 的生成（`GoalAcceptanceContractPlanner.cs:150-156`）：

```csharp
=> string.IsNullOrWhiteSpace(planFingerprint)
    ? $"epoch:{activationEpoch}:objective:{objectiveVersion}"   // 无绑定 Plan 时的退化值
    : planFingerprint.Trim();
```

**本项目该 Goal 的 `task` 为 null（缺陷 2b60040d）⇒ 无绑定 Plan ⇒ 退化为 `epoch:N:objective:N`**，实测正是 `epoch:1:objective:1`。

⇒ 推论：**仅修 `GoalCheckRunner` 不改 `definitionHash`，也不会改变 `input_fingerprint` ⇒ 检查会被去重复用，修复"看起来没生效"。**

## 四、解锁条件已具备（本轮最重要的正面发现）

`GoalRun.activation_epoch` 当前 **= 2**，而全部 `check_records` / `goal_verifications` 都是 **epoch 1**。

⇒ 下一次迭代将以 `epoch:2:objective:1` 构造 `input_fingerprint` ⇒ `dedup_key` 与历史不同 ⇒ **检查会真实重跑** ⇒ `46b9aa76` 的尾部读取修复将在该轮生效。

`ActivationEpoch++` 的产生点（`search_grep` 实测）：

- `GoalCommandService.cs:180 / :220 / :262 / :323 / :433 / :546`（命令层：edit / replace / resume / policy / extend 等）
- `GoalRestartReconciler.cs:65 / :95`（重启协调；`:16` 注释：“`auto_resume_on_restart`：保持 Active，但 `ActivationEpoch++` 且 `ActivationBootId=bootId`”）
- `GoalSettlementStore.cs:1118 / :1223 / :1313`（结算层）
- `TaskGoalDispatchTransactionStore.cs:393`（任务派发层）

## 五、当前阻塞与下一步

**唯一缺口：Goal 处于 `Blocked` 且熔断语义为 `human decision required`，需要一次显式 resume。** 平台侧状态机允许 `blocked → active`（`GoalStateMachine.cs:19`），resume 是 `paused / blocked` 的合法目标（`:48`）。但：

- 平台**没有** agent 侧 resume 工具（本轮实测工具目录无 `goal_resume`）；
- `GoalCommandKind.Extend` 注释明确：“仅用户 slash / HTTP 入口（与 `/goal policy` 同级人类权能），**不暴露为 agent 侧工具**”（`GoalContracts.cs`）；
- 用户已明确要求：**不得绕过 admin API 授权机制**。

⇒ 因此**不由 Agent 自行恢复**，需用户在 Web 面板点击「恢复」（或执行 `/goal resume`）。

恢复后预期：迭代 5 以 epoch 2 重跑检查 → `bounded:test` 读到输出尾部（实测全量 451 行、汇总行在末尾、`Passed: 1125`）→ `test_count_unknown` 消除 → 裁决转为通过/继续。

## 六、证据来源（可复现）

- DB：`D:\data\databases\pudding_platform.db`（只读连接 `mode=ro`），查询脚本 `temp/query-goal-state.py`、`temp/query-goal-checks.py`、`temp/query-goal-seq.py`
- 部署：`D:\data\config\rebuild.signal.result.json`
- 日志：`D:\data\logs\system\pudding-20260916_057.log`
- 代码：`GoalCheckRunner.cs`、`GoalCheckRecordStore.cs`、`GoalAcceptanceContractPlanner.cs`、`GoalStateMachine.cs`、`GoalContracts.cs`、`GoalCommandService.cs`

## 七、侦察补全：去重机制完整因果链（只读代码侦察，file:line 齐备）

### 7.1 去重键的唯一构造点

`GoalVerificationPersistence.BuildScopedDedupKey`（`GoalVerificationPersistence.cs:43-50`）产出：

```
{goalRunId}|{epoch}|{scope}|{criterionRevision}|{definitionHash}|{inputFingerprint}
```

逐段对照实测键 `7ef90f2c…|1|work_unit|1|sha256:4357c6fa09c…|epoch:1:objective:1`：

| 段 | 取值来源 |
|---|---|
| `scope` | **常量** `GoalVerificationScopes.WorkUnit = "work_unit"`（`GoalVerificationContracts.cs:148-151`）⇒ 键内**无 WorkUnit 身份** |
| `criterionRevision` | 规划器硬编码 `1`（`GoalAcceptanceContractPlanner.cs:170-217`） |
| `definitionHash` | `{DefinitionRef}|{Kind}|{CommandTemplate}` 的 SHA256（`GoalCheckDefinitionRegistry.cs:52-60`）⇒ **runner 采集逻辑的修复不改此值** |
| `inputFingerprint` | 无绑定 Plan 时退化为 `epoch:{e}:objective:{v}`（`GoalAcceptanceContractPlanner.cs:150-156`） |

唯一索引 `UX_goal_check_records_dedup(dedup_key)`（`GoalSchemaBootstrapper.cs:208`）。

### 7.2 迭代 2/3/4 为何没有记录却仍有裁决

`GoalCheckRecordStore.EnqueueAsync`（`GoalCheckRecordStore.cs:44-76`）是**跨迭代幂等**的：先按含 `iterationNo` 的 `CheckRecordId` 查（`:52-61`），未命中再**按 `DedupKey` 全表查**，命中即 `continue`（不建行、不执行）（`:69-76`）。执行侧认领只接受 `pending/leased`（`:126-146`、`:164-168`），而 iter1 两行已 `finished` ⇒ **后续轮一次都不启动**。

裁决侧却照常推进：`GoalCheckRunner.RunAsync` 末尾以「`persisted ∪ produced`」为准（`GoalCheckRunner.cs:104-119`），结算按 `(goalRunId, epoch)` **不按 iterationNo** 全量读取（`GoalSettlementStore.cs:170-173`、`GoalSettlementWorker.cs:107-141`）⇒ iter2/3/4 各用 iter1 的失败证据产出 `criterion_failed`。

### 7.3 `activation_epoch` 1→2 的真实来源（修正上一节推断）

**不是某次 resume，而是熔断本身**：`GoalSettlementStore.cs:1203-1223` 在“不可恢复”分支同事务内 `Status=Blocked` **且** `ActivationEpoch++`（`:1223`）；熔断置码在同文件 `:1157-1180`，事件 `goal.circuit_opened` 在 `:1182-1195`。

记录侧一律取 `iteration.ActivationEpoch`（`GoalSettlementStore.cs:164-173`、`:260`），而 iter1–4 创建/结算时该值仍是 1 ⇒ 全部记录停在 epoch 1。

### 7.4 为什么 resume 后检查必然真实重跑

当前无 epoch=2 记录的原因是：熔断后 Goal=Blocked，而**受理新迭代要求 Active**（`GoalStateMachine.cs:78-80`）⇒ 未创建 iter5 ⇒ 从未以 epoch 2 调用 `EnqueueAsync`。一旦 resume：

1. 契约按 `(goalRunId, 2, objectiveVersion)` 查无行 ⇒ 重建（`GoalSettlementWorker.cs:77-106`、`GoalAcceptanceContractPlanner.cs:47-52`）；
2. `inputFingerprint` 变为 `epoch:2:objective:1`；
3. 新 `CheckRecordId`（`iterationNo=5`）与 `|2|` 前缀的 `dedup_key` **均无冲突** ⇒ 检查真实重跑。

⇒ `46b9aa76` 的尾部读取修复将在该轮生效（该轮全量实测输出 451–473 行、汇总行在末行、`Passed: 1125`）。

### 7.5 侦察暴露的三个**新**缺陷（本轮新增，均附证据）

**A. 无绑定 Plan 的 Goal：检查结果指纹退化，同 epoch 内永久复用**
`inputFingerprint` 的设计意图是绑定 `planFingerprint`（注释原文「工作树/计划变化即失效」），但无绑定 Plan 时退化为 `epoch:objective` ⇒ **完全失去内容变化检测**。后果：同一 epoch 内即使代码已改，同名检查也不会重跑，直至熔断。这与既有缺陷 `2b60040d`（Task-bound Goal 未注入 ActiveTask 元数据）构成**同一条因果链的下游后果**。

**B. 查询侧与结算侧 epoch 口径不一致 ⇒ 查询面看不到检查记录**
结算侧用 `iteration.ActivationEpoch`（=1）；查询侧 `GoalQueryService.cs:184-185` 用 `goal.ActivationEpoch`（=2）。二者不一致时，**按 Goal 查询不到任何 epoch-1 的检查记录**。属可复现的查询/面板缺陷（口径事实，非推断）。

**C. 去重键不含 WorkUnit 身份**
`scope` 恒为常量 `"work_unit"`（`GoalVerificationContracts.cs:148-151`）⇒ 同一 epoch 内不同工作单元、不同迭代的同名检查（如 `bounded:test:{project}`）共享同一 `dedup_key`，结论互相顶替。是否为有意设计需 ADR 裁决。

### 7.6 明确「未证实」项（不得当作结论）

- epoch 1→2 的归因系排除法 + 字段吻合，未直接读事件审计行点名校验；
- 「修复后仍恒失败是否**仅**由去重冻结造成、抑或叠加 locale/编码因素」未做运行时验证（侦察被约束为只读）；
- “该 GoalRun 无 `task_goal_bindings` 行”系由 `Status=Blocked(3)`（task-bound 时应为 `Failed(7)`）与 fingerprint 退化推断，未直接查库确认。
