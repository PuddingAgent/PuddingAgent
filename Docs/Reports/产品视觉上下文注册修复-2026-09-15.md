# 产品视觉上下文注册修复（2026-09-15）

## 故障证据

- Session：`206a9b48ec904ebb93e7541131fbb835`。
- 页面 Message ID：`2d1a5cc62075431e8f5c355576dfb12a`；实际 Turn：`3283ad5719ee42569bf733270b0b34f4`；command：`6d9e8dce9d8a4052bb0f1406742f323f`；run：`c008d5afa4fc4da798bec547090edf2e`；trace：`65ff8a351644452db56bfebdc57fe881`。
- canonical `turn.started` sequence 879570；`turn.failed` sequence 879572，时间 `2026-09-15T12:19:04.8091914Z`。
- `D:/data/logs/error/pudding-error-20260915_005.log:148–175`：图片请求准备 31ms 即抛出 `VisionPipelineException: Visual inputs require a workspace and a vision-capable route.`，Error ID `llm-82e36578910949509a0f9509e1a0fd55`。尚未发出 Provider HTTP 请求。

## 根因与修改

先前逐次 LLM MoveNext 绑定快照的修复已在部署源码内。遗漏是产品组合根：`PuddingHost` 不调用 `AddPuddingRuntime`，而 `FrozenVisionContextAccessor` 只在 Runtime 注册。AgentExecutionService 和 DirectLlmClient 的可选构造参数因此均为 null；ValidateOnBuild 也无法识别这种语义缺失。

1. 在 `PuddingServiceCollectionExtensions.Runtime.cs` 注册共享 accessor singleton，保持冻结快照单源与无快照 fail-closed。
2. 产品组合根测试解析实际 AgentExecutionService/IRuntimeLlmClient，并核验两个实例的 accessor 与容器 singleton 相同。
3. `TurnExecutorAdapter` 优先读取 Runtime `errorCode`，再支持已有 `code` 合同，避免具体错误一律降级为 `runtime_execution_failed`。保留消息与无代码时的兜底。

没有删除原会话历史图片或修改模型能力配置。

## 验证与范围

- 线上源码基线（74ae4e0 + b0bcf21 缓存修复、既有心跳投影）叠加本次两处生产修改；详细源清单 `temp/vision-host-build/build-source.json`。不将当前其他 Goal 开发改动发布。
- 产品组合根测试 1/1；FrozenVisionContextAccessor、LlmStreamObservability、TurnExecutorAdapter 合计 22/22。包括模拟 Provider HTTP 请求中的用户 input_image 与工具 function_call_output 图片、连续两轮、AsyncLocal 隔离和错误码投影。
- 当前完整工作树的 Host 测试被既有 `GoalCheckRunner` 缺少 `ITerminalCommandAdmission` 注册阻挡。记录于 `temp/vision-host-registration-test.log`；未绕过这项校验，改用线上源码基线执行产品组合根测试。不能表述为当前全仓测试通过。
- 本次不发送原任务续行或真实模型消息；模拟请求体测试与部署健康不等于真实图片理解验收。

## 部署

- 隔离 Core 构建 0 errors；产物 `.tmp-build/vision-host-fix`。
- 部署前 `chat_execution_commands` 与 `execution_runs` 无 running/queued/pending 项；通过 Desktop `core/deploy-restart` 安装预构建包。
- Desktop 返回 success=true、coreRestarted=true、errors=[]、Ready。Core PID 35964 → 14548，`/health/ready` HTTP 200。
- prepared/loaded managed artifact manifest SHA-256 一致：`3afad8f63320074ca9c68d5ab23644d371050d9705cdcb0d57d0d56b073cccc8`。PuddingAgent/PuddingHost/PuddingRuntime 三个 DLL 的磁盘 SHA-256 均与构建包一致。证据：`temp/vision-host-deployment-result.json`、`temp/vision-host-verification.json`。
- 已完成源码定向回归和产品部署；原会话真实模型重试尚未执行。
