# GLM 优化批次1实施证据（F01 / S01-A / T01 / A01）

> 2026-09-11 独立复审补注：下文保留 GLM 原交付记录。当前结论以 [04 独立审计](04-批次1独立审计与下一步.md) 为准：F01/S01-A/T01 尚有 10 个边界探针失败，不能整体按 source-complete 放行；A01 删除与构建层面通过。后续任务已登记，见 [05 看板回执](05-后续任务与看板回执.md)。本轮未核验运行实例程序集，下文“仍加载旧程序集”是原交付方声明，不作为独立部署证据。

日期：2026-09-11  
状态：source-complete（代码与聚焦测试完成；未部署、未做生产验收）。  
基线：HEAD 731d5e0 + 审阅时既有 dirty 工作树（实施过程完整保留无关 dirty 文件与运行数据）。

## WorkPackage: F01 — 真正有界的明细水合

- SourceFiles:
  - 新增 `Source/PuddingPlatformAdmin/src/pages/chat/runtime/detailHydrationScheduler.ts`
  - 新增 `Source/PuddingPlatformAdmin/src/pages/chat/runtime/detailHydrationScheduler.test.ts`
  - 修改 `Source/PuddingPlatformAdmin/src/pages/chat/hooks/useTurnSurfaceStore.ts`（并发状态全部移交调度器）
  - 修改 `hooks/__tests__/turnSurfaceStore.hydration.test.ts`（新增 5 个并发/生命周期用例）
  - 修改 `components/MessageRow.tsx`、`components/MessageList.tsx`、`components/ChatMain.tsx`、`components/ChatLayout.tsx`、`index.tsx`（配对注销接线）
  - 修改 `components/MessageRow.focus.test.tsx`（视口观察合同更新）
- ProblemTrigger: 水合 effect 的 `slice(0,2)` 只限制单批新启动数；投影更新/注册/完成回调都能让在飞请求叠加（实测交错：effect#1 两个 pending → 新投影 → effect#2 再发两个）。
- BehaviorBefore: 总在飞请求数无上界；失败集合随每次投影清空导致 1.2s 轮询反复重试；会话切换不 abort 旧请求；A→B→A 仅比较 conversationId。
- BehaviorAfter: 页面级未完成明细请求 ≤2（含被取消但未结束的“僵尸”占位）；稳定 key=`workspace|agent|conversation|message|detailsRevision`；generation 递增 + owner token 校验，旧代迟到回调不改共享状态；401/403 等待显式恢复、404 对该 revision 终止、transient 指数退避 + jitter（默认 4 次），sync 不重置预算；可见回合引用计数注册/注销，离视口剪枝未开始预取。
- PreservedContracts: hook 公开接口不变（新增 `unregisterVisibleTurn` 为增量）；TurnSurfaceStore 与明细接口未动；已取得明细不因离视口回滚。
- RemovedDuplicateResponsibilities: hook 内的 inFlight/failedMessageIds/失败清空 effect/hydrationDrainRevision 全部删除。
- Tests: `npx jest --runInBand --runTestsByPath src/pages/chat/runtime/detailHydrationScheduler.test.ts src/pages/chat/hooks/__tests__/turnSurfaceStore.hydration.test.ts` → 14/14 通过；`MessageRow.focus` 9/9、`MessageList`、`ChatMain`、`messageTurnMerge`、`turnSurfaceStore`（projections）、`chatPerfScenario` 3/3 通过；`npm run tsc` 零错误。
- 关键交错验证: 8 条可见消息 × 20 次新投影对象重渲染在飞恒为 2；释放一槽只补一；A→B→A 后两个旧代僵尸占满槽位、新代排队不叠加；旧代迟到成功/失败被忽略；unmount 后迟到完成无状态泄漏。

## WorkPackage: S01-A — usage 并发幂等与原子月聚合

- SourceFiles:
  - 修改 `Source/PuddingPlatform/Services/TokenUsageRecorder.cs`
  - 新增 `Source/PuddingPlatform/Services/UsageWriteConflict.cs`
  - 修改 `Source/PuddingPlatform/Services/LlmGatewayUsageRecorder.cs`
  - 新增 `Source/PuddingPlatformTests/Services/TokenUsageRecorderConcurrencyTests.cs`
- ProblemTrigger: 两个独立 context 先 `AnyAsync` 再 `Add`；月度聚合读出后在 CLR `+=` 再 SaveChanges——并发下事件双计/唯一约束炸、聚合丢失更新（读 100 各写 101）。
- BehaviorBefore: 读改写窗口不受保护；重复 source 第二次 SaveChanges 撞唯一索引直接抛错。
- BehaviorAfter: `RecordCoreAsync` 使用 `BeginTransactionAsync(IsolationLevel.Serializable)`（SQLite BEGIN IMMEDIATE）单写事务：幂等读取（同 payload → 幂等跳过；异 payload → `InvalidOperationException` 冲突拒绝）、明细插入、context layer 明细、月度聚合读改写同事务提交；失败事务零残留。金额保持 decimal CLR 语义不经 SQLite 数值转换。unique 索引作为兜底（SQLITE_CONSTRAINT 19/2067/1555）。`LlmGatewayUsageRecorder` 的 SaveChanges 捕获唯一冲突按幂等成功。
- 额外修复（实施中发现）: dirty 工作树遗留的 `occurredAtUtc` 半成品重构使该文件无法编译（非可空参数 + `.Value` 混用、三个调用方不匹配）；本卡按原意图收尾（参数恢复可空、函数内规范化 UTC）。
- PreservedContracts: `ITokenUsageRecorder` 三个公开方法签名与语义不变；表结构、索引、金额列类型不变；遥测/timeline 投递路径不变。
- Tests: `dotnet test Source/PuddingPlatformTests/PuddingPlatformTests.csproj --filter "FullyQualifiedName~TokenUsageRecorderConcurrency"` → 5/5（50 并发独立 source 事件/sum/RequestCount/cost 完全对齐、8 并发重复 source 计一次、同 source 异 payload Required 抛冲突且账本完好、best-effort 冲突不抛、同 payload 重复调用幂等）；usage 相关全量回归 `--filter "FullyQualifiedName~TokenUsage|FullyQualifiedName~LlmGatewayUsage"` → 39/39 通过。真实 SQLite 临时文件、独立连接，未用 InMemory provider。
- 未实施: S01-B（invocationId/attemptId/RequestUsageAttribution 请求级身份与迟到归因）仍为 Proposed。

## WorkPackage: T01 — 记忆工具闭合合同（增量）

- SourceFiles:
  - 修改 `Source/PuddingRuntime/Tools/BuiltIns/Memory/SaveMemoryTool.cs`
  - 修改 `Source/PuddingRuntimeTests/Tools/SaveMemoryToolContractTests.cs`
  - 修改 `Source/PuddingRuntimeTests/Tools/MemoryToolsTests.cs`
- 实施前状态: HEAD 已含未知 action fail-closed（`SupportedActions`）、`ToResult` 把 status=error 映射 Success=false、取消传播（含测试）；本卡补齐剩余缺口。
- ProblemTrigger: `set_important`/`get_important` 从 root 读取 `agent_instance_id`，但 `BuildRoot` 从不写入该键、`SaveMemoryArgs` 未声明该字段——分支恒报“agent_instance_id is required”，不可达且不完整。
- BehaviorAfter: important 读写身份改由 `ToolExecutionContext.AgentInstanceId` 派生（模型不能经参数指定身份绕过作用域）；缺失身份为业务失败。upsert 参数组合前置校验：preference 必须有 key；非 preference 必须有非空 content——空 content 从“quality 警告后照写空章节”改为 fail-closed 零写入（`MemoryToolsTests` 对应用例同步改为断言失败）。
- Tests: `dotnet test Source/PuddingRuntimeTests --filter "FullyQualifiedName~SaveMemoryToolContract|FullyQualifiedName~MemoryToolsTests"` → 27/27 通过（新增：preference 缺 key 零写入、fact 缺 content 零写入、set/get_important 使用 context 身份）。

## WorkPackage: A01 — 唯一组合根与入口文档

- SourceFiles:
  - 删除 `Source/PuddingAgent/Services/PuddingServiceCollectionExtensions.Runtime.cs`（400 行）与 `Connectors.cs`（255 行）
  - 修改 `Docs/架构.md`、`code_map.md`、`Docs/README.md`
- 实施前证明: `dotnet msbuild Source/PuddingAgent/PuddingAgent.csproj -getItem:Compile` 求值 3 项 = Program.cs + 两个旧副本；两副本均为 `private static` 方法且全仓无调用方（Host 的同名 `PuddingAgent.Services` 分部类 26 个文件是活的组合根，入口项目副本是死拷贝；`FeishuConnector` 的 `using PuddingAgent.Services;` 解析到 Host，不受影响）。
- BehaviorAfter: Compile 仅剩 Program.cs；`dotnet build Source/PuddingAgent/PuddingAgent.csproj` 0 错误；`Docs/架构.md` 开头显式标注当前 Desktop + Core 产品形态，2026-05-02 单进程/P2P 叙述降级为历史背景（不批量改写 ADR 历史）。
- Tests: `dotnet test Tests/PuddingHost.Tests --filter "FullyQualifiedName~Composition"` → 1/1（DesktopChild 组合守卫）；`PuddingDesktop.csproj` 引用仅 Core + Browser.*，不引用 Host/ASP.NET Core。

## 全局声明

- Tests (exact commands, passed/failed): 见各卡；总计新增/更新 33 个测试用例全部通过，未观察到无关回归（chat 相关套件 66+9+3 通过）。
- ProductBuildIdentity: 仅源码与测试变更；未构建产品发布包。
- DeploymentStatus: 未部署。现有 D:\data 运行实例仍加载旧程序集；两段式验收（ready-for-external-deploy → 外部重启 → 产品内 smoke）待执行。
- ProductionAcceptanceStatus: 未测（无生产指标可宣称）。
- RemainingRisks / NextDependency: F01 的明细 LRU 字节上限未做（ hydrated 详情不淘汰）；S01-B 未实施前 attribution 仍读 session 最新调试快照；C01 依赖 S01 身份合同。行为变化点：空 content 的 save_memory 现在失败（原为警告+写入）；水合 transient 失败耗尽预算后不再随轮询自动重试。
