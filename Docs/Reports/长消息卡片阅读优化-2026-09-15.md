# 长消息卡片阅读优化

## 问题与改动

用户截图中的长卡片来自 `TurnContentStream` 的交错正文与活动组：旧逻辑最多展示 40 个内容块，正文全部平铺；最新组默认挂载 24 项，内联思考没有高度上限。

本次增加按实际高度展开的正文/过程预览，历史交错过程就地收拢，最新组初次窗口缩为 6 项。保留原始顺序、唯一正文、完整复制/TTS 和手动展开优先。具体交互与阈值已同步到 [实施方案 §15](../Features/Agent消息交错内容流与最新行为组披露完整实施方案.md) 和 [ADR-079](../07架构/93ADR-079Agent消息交错内容流与最新行为组披露ADR.md)。

## 验证边界

- TypeScript `tsc --noEmit` 通过。
- 使用 HEAD `f934352` 的独立前端目录作为对照，然后仅覆盖本次 8 个前端文件进行生产构建和回归，未夹带其他未提交改动。
- `AgentMessageBubble.test.tsx` 的 3 项既有失败，在未修改 HEAD 的对照中逐项复现：旧 reasoning summary、旧完整轨迹展开、工具开始后旧 reasoning 标题断言。两边失败名称一致；本次不修改这些无关断言。
- 浏览器连接两次均返回 `nodeRepl.fetch request failed`，无法取得页面 DOM 或截图；因此不宣称浏览器视觉、真实长回合滚动或屏幕阅读器验收已完成。
- 原有 unrelated 工作树变更保留；不修改配置、数据库、Agent 提示词或运行中的 Core。

## 最终结果

- 隔离目录的 4 个套件共 51 项：48 通过、3 项既有失败；3 个相关套件全部通过，覆盖长内容测量/晚到尺寸变化、展开保持、键盘焦点自动展开、原序恢复、最新回复可见、失败提示和渐进挂载。
- 生产构建成功，bundle 预算通过：sync=1,375,316、chat=478,172、common=192,915 字节。
- 发布 148 个静态文件至源码 dist 和运行 Core 的 wwwroot/admin；先发布资源，再替换 HTML，保留旧 hash 资源，旧 HTML 备份于仓库 temp。
- HTTP `/admin/chat` 已返回 `/admin/umi.7a16a6ef.js`；入口及 Chat chunk `fd4d19be-async.6186d98d.js` 的 HTTP 下载 SHA-256 均与隔离构建一致。Core 仍为 PID24908，未重启。
- 已打开页面刷新后加载新资源。静态发布和自动化回归完成，浏览器视觉及长回合滚动仍待实际验收。
