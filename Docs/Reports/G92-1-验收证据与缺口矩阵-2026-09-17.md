# G92-1 验收证据矩阵（v2 · 更正版）

- 日期：2026-09-17（v2 修订）｜ 载体卡：`b849ef8d750143fdaaa2a70d7b024a6c`（p0）
- 分派：`2026-09-17T04:12:25Z task.reserved` → `04:12:29Z task.assigned`，assignment `c3682263b09d4aba991dc69ac15d1599`，agent = `default.global_general-assistant.6a8`（用户明确授权）
- 依赖卡：G92-0 `30eb371e7527424c83107dde5bf99d94`
- 取证工具（**受版本控制**）：`Tools/Diagnostics/goal-ledger/goal_ledger_probe.py` + `README.md`

---

## 0. 本版相对 v1 的更正（Codex 评审意见的逐条落实）

| # | v1 的错误/不足 | v2 的更正 |
|---|---|---|
| 1 | 统计写成「**4 项已具备**（1、2、7 + 2 的全链闭环）」 | **标准 2 被重复计数**。按行标签只能是 **3（✅）/ 3（◐）/ 1（❌）**，且这只是**报告分类**，不是独立验收结论 |
| 2 | 用 `dedup_key` 命中证明「无关变化有依据复用」 | **去重命中只证明去重**，不能证明「依据明确依赖范围的可复用性」。该论断归入标准 4，且**当前不成立** |
| 3 | 标准 1 记 ✅ 过于笼统 | **分子项**：旧报告拒绝一项**受标准 4 缺陷牵连** ⇒ 记为「证据不足」。有解析器/失败码/方法名 ≠ 端到端证明 |
| 4 | 标准 5 记 ◐ 无细目 | **逐项列**：Verifier 无写权限 / 超时 / 取消 / 租约回收 / 依赖等待——分别给证据或标缺 |
| 5 | 标准 7 拿历史构建与 mtime 当证据 | **历史构建不能验收后续新增代码**；mtime **仅辅助**，须有「构建/输入身份 ↔ 实际加载组件」的对应关系 |
| 6 | 标准 4 只写「需裁决」 | **裁决已下**：保留原始要求，**必须补输入指纹**；不得把标准缩为「仅约束绑定 Plan 的 Goal」。合同先入 ADR，见 §6 |
| 7 | 证据大量引用 `temp/` 未版本化脚本 | 证据工具移入 `Tools/Diagnostics/goal-ledger/`（受版本控制），报告引用该路径 |
| 8 | （自查）探针首版把表列名硬编码 ⇒ `no such column: definition_ref` 崩溃；任务表定位误命中非任务表 | 探针第二版改为**架构漂移容忍**（PRAGMA 取列名、缺失列显式入 `*_missing_columns`）与**任务表需同时具备 `task_id` + `board_column`**；已跑通 exit 0 |
| 9 | （自查）`leased>0` 一律禁构建过于粗糙 | 拆为 `leased_current_epoch_count` / `stale_leased` / `build_safe`：仅**当前 epoch** 悬挂租约代表存在活跃检查者（`LeaseAsync` 按 epoch 过滤） |

---

## 1. canonical 状态与取证途径（含受阻项）

### 1.1 已取得的 canonical 事实

| 来源 | 事实 |
|---|---|
| `agent_status`（canonical API） | 本实例 `active_goal_run_id = null`、`active_task_id = null` ⇒ **当前无活跃 Goal** |
| `manage_tasks get`（canonical API，**已作废，见 §8.2**） | ~~G92-1 = `InProgress` / **v24** / progress 70% / assignment `c3682263b09d4aba991dc69ac15d1599`~~ |
| `manage_tasks get`（canonical API，**已作废，见 §8.2**） | ~~G92-0 = `NeedsReview` / **v21** / progress 70% / `parent_task_id = d85a107b89da4bdf8fabbb6ecb82237a`~~ |
| 上一轮 `goal-state` 探针（**当时** terminal 可用） | GoalRun `7ef90f2c`：`status=5`、`status_reason="All goal-scope required criteria passed for this standalone Goal."`、`activation_epoch=12`、iterations 15/15、`blocked_code` 空；epoch 11 的 `bounded:build` 与 `bounded:test` 记录均为 `status=finished / failure_code=None` |

### 1.2 ⚠️ 本轮取证受阻（**真实阻塞，如实报告**）

`terminal_start` / `shell` 当前返回：

```
High-risk tool 'terminal_start' is waiting for the approval review dependency
(reasonCode=approval_review_profile_not_configured):
No approval review model profile is configured (ToolApproval:Llm).
```

**后果**：本轮**无法重新读取 GoalRun 账本**（§1.1 中该行是上一轮的观测，标注为"当时"）。
**所需动作**（二选一，均由外部/用户侧完成，我不擅自绕过）：

* **A（推荐，自服务）**：放宽 `LlmToolApprovalReviewer` 路径 B 的守卫，使 `ProviderId+ModelId`
  可直接解析（该校验语句代码中已存在），随后纯仓库配置即可生效 ⇒ 需一次部署；
* **B**：在 `D:\data\config\llm.providers.json` 的 `"profiles": {}` 中补一个命名 profile，
  并在 `appsettings.json` 写 `ToolApproval:Llm:ProfileId`（跨出仓库写入，需授权）。

**当前纪律**：在 A/B 落地前，凡需要 shell 的证据（重建账本、字节级取证、进程枚举）**一律暂停**，
不再以"重试"代替修复。

### 1.3 独立 Goal 的实际必需条件（回答"Completed 到底证明了什么"）

上一轮探针显示每个 activation epoch 的 **合同只有 2 条检查**：
`bounded:build:Source/PuddingPlatformTests/PuddingPlatformTests.csproj` 与
`bounded:test:Source/PuddingPlatformTests/PuddingPlatformTests.csproj`，
检查记录也严格成对（每 epoch 2 条）。

⇒ `7ef90f2c` 的 Completed **只证明**：该 Goal 的**冻结合同**（2 条回归门禁）通过，
即「构建成功 + 测试真实执行且全通过」。

⇒ **不能**等价为：G92-0 整张卡的验收通过，或 G92-1 的七条标准通过，或
「回合结束 ≠ 目标完成」的全部语义已验收。**v1 报告中任何"联合功能完成"的暗示一律撤回。**

### 1.4 为什么 `goal_payload.task` 为 `null`

`7ef90f2c` 是**standalone Goal**（无 Task 绑定）⇒ 平台未注入 ActiveTask 元数据。
其**受控检查合同**因此走 `inputFingerprint = epoch:{e}:objective:{v}` 的退化分支（见标准 4）。
Task-bound 路径缺正面证据的根因即缺陷 `2b60040d`，**不在本卡擅自接管**（归属见 §2）。

---

## 2. 缺口登记表（负责人 / 复现步骤 / 原始证据 / 期望断言 / 完成门槛）

约定：**负责人**一栏只登记允许我承接的范围；他人归属只做协调建议。

| ID | 缺口 | 负责人 | 可复现步骤 | 原始证据位置 | 期望断言 | 完成门槛 |
|---|---|---|---|---|---|---|
| **G1** | 标准 4：输入失效机制缺失 | 本实例（G92-1） | `python Tools/Diagnostics/goal-ledger/goal_ledger_probe.py --goal-run-id 7ef90f2c… --out temp/g.json`，读 `contracts[].checks[].inputFingerprint` | `Source/PuddingPlatform/Services/Goals/GoalAcceptanceContractPlanner.cs:151-156`（注释自述「缺失时…不宣称能检测内容变化」）；`GoalContinuationWorker.cs:485`；`TaskGoalDispatchTransactionStore.cs:239` | ① 相关源码/配置/依赖/检查定义变化 ⇒ 旧成功与**旧失败**结论均失效并重跑；② 明确依赖范围内的无关变化 ⇒ 可复用；③ 检查**执行期间**输入变化 ⇒ 旧结果不得用于当前完成裁决 | 合同写入 ADR-092（§6）+ 专项测试覆盖上述 3 条断言 + 账本中可观测到指纹随输入变化 |
| **G2** | 标准 3：Task-bound 正面路径 | **待分派**（缺陷卡 `2b60040d6eb94db7800d0522698e3975`，p0，`preferred_agent_id` = 本实例，**无 active assignment**） | 以 Task-bound Goal 运行受控检查，观察是否注入 ActiveTask 元数据 | 缺陷卡描述；`active_context_missing`（stage=lookup/not_visible） | Task-bound Goal 可 canonical 认领/上报，并与独立 Goal 走同一验证内核 | 缺陷卡经授权分派后修复 + Task-bound Goal 端到端跑通 |
| **G3** | 标准 3：双终态原子证据 | 本实例（G92-1） | 构造 CAS 竞争下的完成写入口，检查 Task/Plan/Goal 是否同事务终态 | `GoalSettlementStore`（结算器）；`TaskGoalDispatchTransactionStore.cs` | CAS 失败时不产生半个终态；旧验证只留历史 | 专项并发测试 + 账本终态一致性断言 |
| **G4** | 标准 5：租约回收端到端 | 本实例（G92-1） | 需**隔离环境或显式安全窗口**，受控故障注入使租约过期后验证回收重跑 | `GoalCheckRecordStore.IsClaimable:173-177`；`LeaseAsync:140-142` | 过期租约被回收并**重跑成功**；超时/取消路径可见 | 隔离环境证据（**不得**在主证据链上重启 Core） |
| **G5** | 标准 5：依赖不可用进等待 | 本实例（G92-1） | 断开依赖后观察 Goal 是否保持 Goal 身份并进入等待而非失败 | `dependency_wait` 失败码；ADR-092 决策 6 | 依赖不可用 ⇒ 持久化等待 + 释放执行资源；事件到达后重新准入 | 专项测试 + 账本状态断言 |
| **G6** | 标准 6：写入口径一致性 | **待协调**（缺陷卡 `bf7ef5f5`，p0，`preferred_agent_id` = 本实例，无 active assignment） | 观测 `tracker-legacy-blocked-*` 写入 blocked 后 worker 侧能否自恢复 | G92-1 事件链 `tracker-legacy-blocked-…-18`；G92-0 事件链 `…-9` | 所有完成/阻塞写入口一致；Blocked 可 canonical 自恢复 | 缺陷卡经授权分派后修复 |
| **G7** | 标准 7：构建身份 ↔ 加载组件对应 | 本实例（G92-1） | 部署后比对构建输入身份与**实际加载**组件 | `D:\data\config\rebuild.signal.result.json`（`preparedAssemblySha256` / `loadedAssemblySha256` / `coreRestarted`） | 每个待验收提交都有「构建/输入身份 ↔ 实际加载组件」的对应关系；**历史构建不得用于验收后续新增代码** | 对每个候选提交单独产出对应关系证据 |
| **G8** | 标准 1：旧报告拒绝的端到端 | 本实例（G92-1） | 需要标准 4 到位后，构造"输入已变但旧报告为绿"的场景 | `GoalCheckOutputParser.cs:36-42`；`GoalCheckRecordStore.EnqueueAsync:57-66` | 旧报告在输入变化后**不得**被用作当前完成裁决 | **依赖 G1**（当前证据不足） |

---

## 3. 四类状态**分别**报告（不合并叙述）

| 类别 | 结论 | 依据 |
|---|---|---|
| **源码验证**（本仓代码 + 单元/集成测试） | 已跑：`dotnet test Source/PuddingPlatformTests --filter FullyQualifiedName~Goal` @ HEAD `5e63b569` → `Failed 0 / Passed 244 / Total 244`，exit 0 | 该结果由**上一个可用窗口**采集；本轮 shell 受阻未复跑（见 §1.2） |
| **ready-for-external-deploy** | 有候选改动，**未**做外部部署验收 | 部署与 Core 生命周期属进程外控制器 |
| **新构建功能 smoke** | 已取得 1 次平台侧证据：epoch 11 的 `bounded:build` / `bounded:test` 均 `finished / failure_code=None` | 属"当时"观测；需在 §1.2 解除后重取 |
| **平台终态** | GoalRun `7ef90f2c` = `Completed`（独立 Goal 的冻结合同通过）；G92-0 Task = `NeedsReview`；G92-1 Task = `InProgress` | `agent_status` 确认无活跃 Goal；任务态来自 canonical task API |

---

## 4. 执行与互斥纪律（按 Codex 指令 4）

1. **执行前实时核对**：`Goal` 是否 Active、`leased_check_count` 是否为 0、是否有实际存活的
   检查/测试进程。三者任一不满足 ⇒ 不执行构建/测试。
2. **Goal 非 Active 不代表安全**（旧 epoch 残留进程与新会话可能并发）。
3. **`--no-build` 不是并发运行测试的豁免**（VSTest 宿主与结果目录仍冲突）。
4. **检查运行期间不部署重启**。
5. 需要受控故障注入（G4）时，用**隔离环境**或显式安全窗口，且由**外部控制器**处理
   承载自身的 Desktop/Core 生命周期 ⇒ 避免再次打断主证据链。

---

## 5. 报告纪律

* 不再使用「已 Assigned」「已提交报告」「单组测试通过」「turnkey」等措辞描述未闭合状态。
* 「已具备证据」只在**有端到端断言**时使用；仅有源码/方法名/失败码的记「证据不足」。
* 矩阵统计分档仅作**分类**，不构成验收结论；终态由服务端 verifier 裁决。

---

## 6. 标准 4 的合同（先固化，后实现）

已按 Codex 指令写入权威设计与 ADR：

* `Docs/07架构/106ADR-092目标驱动执行与分层验证闭环ADR.md` —— 新增决策 10「输入身份与结果失效」；
* `Docs/Features/Goal目标驱动执行与分层验证闭环设计-2026-09-15.md` —— 新增对应实施条款。

合同要点（摘要）：

1. **输入清单**必须显式枚举并覆盖：相关源码、配置、依赖、检查定义。
2. 以**内容哈希**表达输入身份；`HEAD` 仅作**来源信息**记录。
3. **仅凭 HEAD 或模糊 dirty 标记不足以判定安全复用**。
4. 无法判定相关性时 ⇒ **保守重跑**。
5. 必须证明：相关变化 ⇒ 旧成功**与旧失败**均失效；无关变化 ⇒ 按明确依赖范围可复用。
6. 检查**执行前后**输入发生变化 ⇒ 旧结果**不得**用于当前完成裁决。
7. 对**独立 Goal 与 Task-bound Goal 使用同一机制**（不设"仅绑定 Plan"的例外）。

---

## 7. 本轮回补与更正（2026-09-17 04:56–04:58Z）

### 7.1 B1（取证阻塞）为**间歇性**，本轮已恢复

`terminal_start` 在 04:56Z 起重新可用 ⇒ **§1.2 的"一律暂停"降级为"先探测再执行"**：
每次需 shell 前先跑一次探针；失败则立即停手，不以重试代替修复。所需动作（A/B）**仍然有效**，因为根因未修。

### 7.2 §1.1 的 GoalRun 证据已升级为**本轮时点证据**（exit 0）

```
OUT=...\temp\goal-ledger.json BYTES=34820
GOAL status=5 phase=Completed
GOAL epoch=12 iterations=15/15 same_blocker=0 blocked=None
CONTRACTS epochs=[1, 3, 5, 7, 9, 11]
CHECKS total=12 current_epoch_leased=0 stale_leased=3 build_safe=True
FAILCODES=['None','non_zero_exit_code','test_count_unknown']
FINGERPRINTS=['epoch:11:objective:1','epoch:1:objective:1','epoch:3:objective:1','epoch:5:objective:1','epoch:7:objective:1','epoch:9:objective:1']
```

本轮新增可核对事实：

* `phase_name=Completed` 由**枚举映射**得出（`GoalContracts.cs:7-17`），不再依赖字符串直读；
* 合同 epoch 完整序列 `[1,3,5,7,9,11]` —— epoch 变更确实每次都重建了合同；
* **`stale_leased=3`**：epoch 5/7/9 各 1 条 `leased` 是**旧 epoch 残留**（检查执行期被重启打断，属已解释现象）；
  当前 epoch（12）**无**悬挂租约 ⇒ `build_safe=true`；
* 指纹集合**始终是 `epoch:{e}:objective:1` 形态** —— 这直接坐实标准 4 未达成（没有任何内容哈希分量）。

### 7.3 仍缺（不因本轮回补而消失）

* 任务表**不在 Goal 库**内（探针已如实输出空任务集，不再误报）；任务态继续以 canonical task API 为准；
* goal_check_records 的 `report_json` 中 `executed_test_count` 等字段本轮**未落盘到报告**（需逐条展开 `report` 子对象）——列为本轮遗留，下轮补齐；
* G1–G8 缺口状态不变。

---

## 8. 2026-09-17T18:04+08:00 外部只读复核后的更正（**接受**）

对账来源：Codex 只读取证（Task API v1 + `D:/data/databases/pudding_platform.db` `mode=ro`/`query_only=on` + 源码），报告 `Docs/Reports/Goal多轮效率与缓存命中评估-2026-09-17.md`。

### 8.1 结论性更正（**我原来的判断错了两处**）

| 项 | 我原判 | 更正为 | 依据 |
|---|---|---|---|
| 七条标准汇总 | 「3（✅）/3（◐）/1（❌）」 | **1（通过：标准 2）/ 5（部分：1、3、5、6、7）/ 1（未达成：4）** | 标准 1 与 7 在 v2 中已被我**自己**降为分子项部分（旧报告受标准 4 牵连、历史构建不得验收后续代码），行标签却仍记 ✅ ⇒ 数错。往后**从结构化子项自动汇总**，不手写档位 |
| 业务目标 | 隐含「Goal Completed 即原目标达成」 | **原业务目标未达成**；这是「**验收合同覆盖不足导致的业务误完成**」 | 该 Goal 的 `TaskGoalBinding` **0 条**；最后冻结合同仅 `bounded:build`+`bounded:test`（`Source=bounded_planning`），未覆盖 G92-0 的业务验收条件与 G92-1 联合门禁 |

**机理（源码级）**：`GoalAcceptanceContractPlanner` 在目标无显式证据时**只写 engineering-gates** 合同并发 warning；`ConservativeGoalIterationVerifier` 在 `allRequiredPassed && goalScope` 时即可完成独立 Goal。两者组合 ⇒ 非空合同只能防「**空集通过**」，**不能防「验错了东西」**。

> 补充：不得把 **15/32** 当进度读——它是**预算消耗 46.875%**。

### 8.2 canonical 实时状态（18:04+08:00 快照，**以此为准**）

| 对象 | 结构化事实 | 我此前记载（**作废**） |
|---|---|---|
| GoalRun `7ef90f2c` | `Completed`，15/32，epoch 12，12:10:16 终止 | 同，但**不得当进度** |
| **TaskGoalBinding** | **0 条** | 未记载 |
| G92-0 | `NeedsReview` / v21 / 70% / **`activeAssignmentId=null`** / `completedAtUtc=null`；description 的父任务编号与结构化 `parentTaskId=null` **不一致** | 仅记 v21 |
| G92-1 | **`Blocked`** / v26 / **70%** / **`activeAssignmentId=null`**；`blocker=execution_claim_orphaned` | 仍写「InProgress / 72% / assignment `c3682263…`」⇒ **把历史叙述当成了当前认领，作废** |

⇒ 纪律：**只引用结构化字段**；description 属历史叙述，引用时必须显式标注时间戳。

### 8.3 效率与缓存（外部实测，作为后续优化基线；**非本卡验收项**）

- Goal 15 轮：主模型调用 **270**、工具调用 **360**、输入 **52,427,866** tokens；墙钟 9/16 21:52 → 9/17 12:10（≈14h18m）；`active_elapsed_ms` ≈84 分 17 秒（平台记账口径，非全链 CPU/等待）。
- 结算分布：`criterion_failed` **8** 轮、`check_results_pending` **6** 轮、`complete` **1** 轮。
- **第 9–14 轮（pending）**：143 次主调用 / 185 次工具调用 / **29,889,447** 输入 tokens ≈ 全部输入 **57.0%**；**第 12 轮**命中 **99.5434%** 却仍耗 **12,192,370** 输入 tokens 且最终仍 `pending` ⇒ **缓存命中与推进效率必须分开评估**。
- 缓存：本 Goal 270 次主调用加权命中 **98.8814%**；今日 00:00–18:04 全量 **96.7106%**；9/11–9/16 六日 ≈**95.6205%**（9/10 缺失原因未查明，**不填 0**）。**既未达 99%，也不具备连续 7 天完整覆盖证据。**
- 成本归集缺口：Goal aggregate `cost=0.0`，而 Gateway 272 条记录合计约 5.0913 ⇒ **不能把 0 当免费**。
- 分层 token 遥测**不是账单**：本 Goal 主调用 `HistoryMessageTokens` 累计 71.12M、`ToolDefinitionTokens` 累计 14.23M，**已超过**实际 52.43M 输入 ⇒ 不可直接当输入构成。

### 8.4 新登记缺口（本卡范围外，另立卡）

**G9 · 目标合同覆盖不足（业务误完成）**
- 现象：显式 Task 目标未在 admission 时解析并绑定权威任务；验收条件与依赖版本未冻结入合同；纯回归门禁替代了目标级证明。
- 期望断言：**build/test 全绿但 G92-1 未通过或 Task 未完成时，Goal 不得宣称原目标完成**；独立 Goal 可无步骤，但**必须**有充分目标条件。
- 完成门槛：合同覆盖校验 + 上述回归用例。

**G10 · 等待期不得反复调用模型**
- 现象：检查 `pending` / 依赖不可用期间反复产生相同续行与全文汇报（实测第 9–14 轮占全部输入 57.0%）。
- 期望断言：等待期间**无周期性 LLM 调用**；恢复后**恰好一次**有效续行；每轮须说明新增证据或义务变化。
- 完成门槛：pending/依赖不可用持久化为等待 + 事件唤醒 + 回归断言。

### 8.5 代理侧补充证据（供 P0「先恢复依赖与认领链」采用）

本实例实测：在**外部 API 轮次**（非 task 派发轮）调用 `task_claim` / `task_update` 返回
`task.active_context_missing`（`context_rebuild: stage=lookup, outcome=not_visible`）⇒
worker 无法在外部触发轮次内认领/上报，只能改用管理面 `manage_tasks` 更新 description。
这与 G92-1 的 `execution_claim_orphaned` 同属**认领链断裂**，建议在修复时一并覆盖
「Assignment / Run / Reservation / fence」四元组，而**不是**改 description 或反复 reopen。

### 8.6 B1 影响半径扩大（**实测更正**）与一次失败的委派

原记载「B1 只阻塞 shell/terminal」**不准确**。实测（2026-09-17T18:54+08:00）：

| 工具 | 目标 | 结果 |
|---|---|---|
| `terminal_start` / `shell` | 任意 | ❌ 被 `approval_review_profile_not_configured` 阻塞 |
| `file_write` | **工作区之外**（如 agent 私有 `D:\data\agents\<id>\goal.md`） | ❌ **同样被阻塞** |
| `file_write` / `file_patch` | **工作区内**（`Docs/`、`temp/`） | ✅ 可用 |
| `goal_update` / `manage_tasks` / `save_memory` | 专用工具 | ✅ 可用 |

**失败委派**：子代理 `sub-14d18d05`（goal.md 归档）因 `file_write` 命中同一依赖而 `failed`，
`resumable=false`，`tool_failure_count=4`，`tool_output_chars=703109` —— **烧掉大量上下文且零产出**。

⇒ 三条派生纪律：
1. **派委派前先校目标路径是否在工作区内**；需写工作区外路径的任务在 B1 未解除前**直接判为阻塞，不得派**。
2. **goal.md（26.7 KB，超 `goal_read` 16 KB 上限）的字节级归档在 B1 解除前无法完成**：
   skill `oversized-goalmd-archival-and-slim-down` 依赖的 `shell` 与 `file_write` 双双不可用。
3. **已知可行的工作区内的替代路径**（待专用预算轮执行）：`file_read` 读全文（工作区外读是否放行待验）
   → `file_write` 写入仓库内被 gitignore 的 `memory/` 作为归档 → `goal_update(content_base64)` 覆盖为精简版。
   不得为了「完成归档」而做**未留回滚点的压缩重写**。

**结构性缺陷（单点耦合）**：写**工作区外**文件被判为「高风险」，与执行通道共用同一审批依赖
⇒ **Agent 自身的长期状态维护（goal.md）与执行能力共用一个故障点**。已另立卡跟踪。

---

## §9　2026-09-18 迭代记录：S1-a 规格落地与一次委派事故

### 9.1 本卡范围重整（ADR-092 第二版）
旧口径「CheckRunner 与步骤/整体两级 Verifier」已被第二版整体废止（设计 §10 明示「G92-0/1/2/3 是历史工作载体，可对应 S1–S4 重整实施范围」）。本卡现定位 **S1「合同与纯决策」**，卡标题/描述/验收标准已按设计 §10/§11 改写（事件 `seq30 task.updated @2026-09-18T04:15:17Z`）。

### 9.2 「工程门禁兜底完成」的确切代码位置（S1-a 的靶点）
| 环节 | 位置 | 事实 |
|---|---|---|
| 生成兜底合同 | `Source/PuddingPlatform/Services/Goals/GoalAcceptanceContractPlanner.cs:39-118` | `GoalObjectiveEvidenceParser.Extract` 未解析出 objective 证据时，**只记 `LogWarning`，无功能后果**；合同 `source` 退化为 `"bounded_planning"`（:23；含证据时为 `"bounded_planning:objective_evidence"`，:26） |
| 判定完成 | `Source/PuddingPlatform/Services/Goals/ConservativeGoalIterationVerifier.cs` | 判定链末端 `allRequiredPassed && goalScope → Verdict=Complete`；**纯门禁合同照样完成** |

**实测量化**：GoalRun `7ef90f2c` 全部 epoch 合同 `source=bounded_planning`、`criteria_json≈1247` / `checks_json≈1281` 字符（仅 `bounded:build`+`bounded:test`），`TaskGoalBinding = 0` 条，最终 `status=5 Completed` ⇒ 载体卡 `30eb371e` 的业务验收从未被验证。此为设计 §11 **不可删的固定负例**。

### 9.3 S1-a 规格（已提交 `731b8eb`，已 push）
`Docs/Reports/G92-1-S1a-合同覆盖门设计与施工规格-2026-09-18.md`
目标行为：`source == "bounded_planning"` ⇒ 即使全部必需条件通过也**不得 Complete**，改 `Blocked` + `blockerCode="contract_coverage_insufficient"` + NextAction 指向「一次有界合同整理」；`"bounded_planning:objective_evidence"` 行为不变。判定表 A–F 与优先级纪律（覆盖门只能插在 Complete 分支之前、失败/等待分支之后）。

### 9.4 委派事故（如实记录，不掩盖）
| 子代理 | 模型 | 结果 | 代价 |
|---|---|---|---|
`sub-8be04809` | deepseek-flash | **failed** —— 取证已完成，但一个无关的 `save_memory` 参数错误（`'content' is required for type='fact'`）导致整轮失败；因规格只要求**内联返回**大清单，交付物**未落盘且不可回收**（`query_sub_agents` 仅留摘要首段；`query_session_logs` FTS 无命中） | 1 361 819 tok / 94.9% 缓存 / $0.4041 / 23 请求 |
`sub-6733d5ac` | deepseek-flash | 按**修正规格**重发：交付物必须写盘 `temp/s1-deletion-callsites-2026-09-18.md`，最终回复只给 `FILE/LINES/SUMMARY(≤10行)/NOTES`；显式禁止记忆类工具；明确「中途工具报错不要停止」 | 在途 |
`sub-a5271f7a` | glm-5.3-flash | 实现 S1-a 覆盖门（四步：胶囊字段 → 结算填充 → verifier 分支 → 前端受阻码映射） | 在途 |

**根因归于本次委派规格缺陷，而非子代理能力**。已固化为硬规则：大交付物必须要求写盘并只返回路径。

### 9.5 S1-a 尚未取得的证据（不得拔高）
- 无编译证据、无测试证据、无「表 A–F 逐行成立」证据 ⇒ **S1-a 未完成、未交付**。
- S1 其余门槛（S1-b 删除步骤引擎 `advance/remaining/scope` 分支、单入口收敛、真实失败→修复循环）未开始。
- 独立阻塞：`task_goal_start` 对本卡返回 `execution_plan_unavailable`（新缺陷卡 `ef7d6378…`）。

---

## §10　2026-09-18 续：S1-b 输入取证落地 + S1-a 规格充分性独立验证

### 10.1 取证交付物已提升为受跟踪证据
- 文件：`Docs/Reports/G92-1-S1删除项调用面取证-2026-09-18.md`（143 行）
- 来源：子代理 `sub-6733d5ac`（deepseek-flash，**completed / tool_failure_count=0**），按修正规格**写盘后只返回路径**
- 原落点 `temp/s1-deletion-callsites-2026-09-18.md` 受 `.gitignore:21 temp/` 保护（`git check-ignore` 已验），为避免丢失而提升到受跟踪目录
- 上游事故（对照）：首次同类委派 `sub-8be04809` 因规格要求**内联返回**大清单，被一个无关 `save_memory` 参数错误整轮打断，交付物不可回收（1.36M tok / $0.4041）

### 10.2 A 节要点（「工程门禁兜底完成」的完整调用面）
属主 3 文件 + Worker + 持久化/schema + DI 顶点 + 下游读投影 2 处，合计约 10 处消费点。完成路径是**两阶段管线**：
1. `ConservativeGoalIterationVerifier.cs:134-139` → `Verdict=Complete`
2. `GoalSettlementStore.cs` `ApplyBoundPlanGates` 可把非阻塞裁决**升级**为 `Complete`（`:905-911`、`:985-993`）

**我对 S1-a 充分性的独立验证（读码，非采信自述）**：`GoalSettlementStore.cs:881-886` 的守卫为
```csharp
if (!string.Equals(candidate.TerminalKind, "completed", ...)
    || decision.Verdict is Blocked or NeedsUser or Unsafe) { return decision; }
```
⇒ 覆盖门返回的 `Blocked("contract_coverage_insufficient")` 会被**原样返回**，第二阶段只做「Continue→Complete 升级」，**无法**把 Blocked 改写为 Complete。**结论：S1-a 只需在 Verifier 插入覆盖门即可，规格充分，无旁路。**

### 10.3 纠正我方两处口径（原表述过强）
| 原口径 | 代码事实（本轮取证） |
|---|---|
「两级 Verifier」= 步骤级 + 整体级两套 Verifier | **仓库内不存在第二个 `IGoalIterationVerifier` 实现**，也无独立的步骤/整体审阅 Agent 类；「两级」实为**单 Verifier 的 scope 两级**（`WorkUnit ↔ Goal`）加一道计划闸（`ApplyBoundPlanGates`） |
「Goal 步骤引擎 CurrentWorkUnit / advance」 | 仓库内**不存在** `CurrentWorkUnit` 属性，也无独立 Advance 方法（命名级未找到）；对应实现是 `RemainingWorkUnits` / `VerificationScope`（`GoalVerificationContracts.cs:42-51/148-153`）与 `GoalSettlementDecisionCalculator` 的 Advance 处置 |
另：`Source/PuddingRuntime` 对三个目标符号**零命中** ⇒ S1 改动不触及 Runtime 层。

### 10.4 状态（不因取证而拔高）
- S1-a：**仍无编译/测试证据**，子代理在途 ⇒ §9.5 的「未完成、未交付」结论不变。
- S1-b：**输入已就绪**（上表 + 取证文档），尚未开工。
- 未触碰：`GoalCommandServiceTests.cs`（他人在途）本轮只读、未改、未提交。

---

## §11　2026-09-18 S1-a 落地与独立验证

### 11.1 实现（子代理 `sub-a5271f7a`，glm-5.3-flash，completed / tool_failure_count=0）
| 文件 | 变更 |
|---|---|
`Source/PuddingCore/Goals/GoalVerificationContracts.cs` | +31 —— `GoalEvidenceCapsule` 增只读 `AcceptanceContractSource` 与派生 `IsEngineeringGatesOnly`；新增受控词表 `GoalAcceptanceContractSources`；纯新增成员，既有构造调用点零改动 |
`Source/PuddingPlatform/Services/Goals/GoalSettlementStore.cs` | +8 —— 候选增 `AcceptanceContractSource`，`ToCapsule()` 透传，候选构造处取 `contract?.Source`（复用同处已加载的合同行，零新增查询） |
`Source/PuddingPlatform/Services/Goals/ConservativeGoalIterationVerifier.cs` | +15 —— 覆盖门分支 |
Admin `goalBlockerCodes.ts` | +5 —— 新受阻码中文映射（不落「未知受阻码」兜底） |
Admin `goalBlockerCodes.test.ts` | +14/-2 —— 守护清单纳入新码 + 专项断言 |
`Source/PuddingPlatformTests/Services/Goals/GoalContractCoverageGateTests.cs` | 新增约 390 行 —— 判定表 A–F 用例 |

### 11.2 本 Agent 的独立验证（不采信子代理自述）
- **改动面实测**（`git diff --stat`）：+31 / +15 / +8 / +5 / +14-2 / 新增文件，与自述一致；
  并确认**未触碰**同目录他人在途 WIP：`MessageRow.tsx`、`MessageList.tsx`、`MessageRow.memo.test.ts`、
  `messageTurnMerge.test.ts`、`DevPanel/PerfTab.test.tsx`、`GoalCommandServiceTests.cs`。
- **读码确认插入位置**：覆盖门位于 `task_terminal_without_completion` 之后、`allRequiredPassed && goalScope`（Complete）之前 ——
  符合规格 §2.1 的优先级纪律（失败/等待/阻塞分支优先级不变）。
- **测试由本 Agent 亲自运行**（比子代理的过滤更宽，故计数更大）：
  - `dotnet test PuddingPlatformTests --filter FullyQualifiedName~Goal` ⇒ **失败 0 / 通过 269 / 总计 269**，exit_code=0
  - `dotnet test PuddingCoreTests --filter FullyQualifiedName~GoalSettlement` ⇒ **失败 0 / 通过 22 / 总计 22**，exit_code=0
  - `npx jest src/pages/chat/components/goalBlockerCodes.test.ts` ⇒ **Tests: 5 passed, 5 total**

### 11.3 遗留决策点（显式登记，未掩盖）
实现取「**精确匹配 `bounded_planning` 才拦截**」（`IsEngineeringGatesOnly`），而非规格建议的
「非 `bounded_planning:objective_evidence` 即拦截」。差异：**未知 source（null / 未来新值）不触发覆盖门**（fail-open）。
- 缓冲依据：`goal_acceptance_contracts.source` 为 NOT NULL 且 Planner 只写两种受控值；合同行缺失时必然先走
  `acceptance_contract_missing` / `check_contract_missing`，故 null 无法绕过。
- 未采用严格语义的原因：既有 PlatformTests 用例构造 capsule 时不设 source（null），严格语义会使其**全绿用例翻红**，
  而该测试文件不在本次可改清单内（不得为通过而改既有预期）。
- **判定**：当前实现已满足标准 1 的指定反例（build/test 绿 + 业务条件缺失 ⇒ 不得 Complete）；
  unknown-source fail-open 作为**显式遗留项**，需设计裁决后再收紧（收紧时须同步修改既有 Capsule helper 与相应用例）。

### 11.4 未部署（线上行为未变）
本次仅源码 + 本地测试，**未部署、未重启 Core、未改配置/数据库**。按设计 §10，部署由外部控制器在安全窗口进行。

### 11.5 未覆盖项
- `GoalSettlementWorker` 落库 → `GoalSnapshot.BlockedCode` → 前端渲染的**端到端链路无集成测试**（本次止于裁决 + 处置映射 + 前端映射注册）。
- 未跑两套件全量（仅为定向过滤 + 构建 0 错误）。

### 11.6 S1-a 之后的 S1 剩余
S1 完成门槛中的 S1-b（删除 `RemainingWorkUnits` / `VerificationScope` 与 Advance 处置、计划闸收敛）、
及「单入口」「真实失败→修复循环」尚未开工 ⇒ **S1 未完成，本卡不得关闭**。

---

## §12　2026-09-18 S1-b 前置证据（SQLite 实测）

### 12.1 合同 source 的真实分布
```
select source, count(*) from goal_acceptance_contracts group by source
→ [('bounded_planning', 9)]
```
**全部 9 行历史合同都是 `bounded_planning`，`bounded_planning:objective_evidence` 一次都未产生。**

### 12.2 推论 A：§11.3 的 unknown-source fail-open 属**理论风险**（降级）
既然生产只有受控值，覆盖门「精确匹配 `bounded_planning`」已能拦住**全部现存合同**。
§11.3 的遗留项保留为「将来新增 source 取值时需同步收紧」的提醒，不再视为当前风险。

### 12.3 推论 B（重要，必须显式再确认）：覆盖门部署后**全部现存 Goal 都会被拦**
因为 9/9 合同是纯工程门禁 ⇒ 部署后任何 Goal 想完成都必须先取得目标级证据。这是设计 §6「覆盖不足不得完成」的**预期结果**，但它使「**一次有界合同整理**」（设计 §6）成为**承载路径**：

- 若 Agent 无法在迭代内为 objective 补齐目标级证据声明，Goal 将进入**永久阻塞**（每次都命中 `contract_coverage_insufficient`）。
- 机制上这条路径存在：`GoalObjectiveEvidenceParser.Extract(objective)` 解析 objective 中**显式声明**的项目证据 / 文件证据目标。
- **待验证（下一轮做）**：① 该显式声明语法是否文档化且可被 Agent 在 objective 文本中稳定使用；② 覆盖门返回的 `NextAction` 文案是否足以让 Agent 完成这次整理（否则会把「可修复」变成「不可达」）。
- 这一条**不是缺陷**，但属于**部署前必须确认的操作前提**；否则上线后表现为「Goal 模式突然无法完成」。

### 12.4 S1-b 去风险证据：`VerificationScope` / `RemainingWorkUnits` **无持久化列**
```
pragma table_info(goal_verifications)
→ verification_id, goal_run_id, activation_epoch, iteration_no, source_turn_id,
  source_terminal_sequence, contract_version, route_snapshot_json, status, verdict,
  summary, unmet_criteria_json, next_action, blocker_code, blocker_message,
  evidence_refs_json, raw_output_artifact_ref, raw_output_sha256,
  input_tokens, output_tokens, cost, error_id, created_at_utc, completed_at_utc
pragma table_info(goal_iterations)
→ goal_iteration_id, goal_run_id, activation_epoch, iteration_no, status, command_id,
  turn_id, run_id, trace_id, accepted_sequence, terminal_sequence, stop_reason, error_id,
  started_at_utc, settled_at_utc, llm_rounds, tool_calls, input_tokens, output_tokens,
  progress_fingerprint, created_at_utc
```
⇒ 两表**都没有** scope / remaining 相关列。因此 `VerificationScope` 与 `RemainingWorkUnits` 是**运行时从绑定计划计算**的字段，
**删除它们不需要版本化 Schema 升级、不需要数据迁移**（与设计 §10「数据库按设计 §10 显式升级」的顾虑解耦）。
这把 S1-b 从「跨持久化层的危险重构」降级为**纯代码删除**。

### 12.5 在途
`sub-ff4efe52`（deepseek-flash）正在产出 S1-b 影响分析与最小可删集，交付物写盘 `temp/s1b-deletion-impact-2026-09-18.md`（按修正规格：只返回路径）。

---

## §13　2026-09-18 S1-b 影响分析与最小可删集 MS-1

### 13.1 交付物
`temp/s1b-deletion-impact-2026-09-18.md`（170 行，五节齐全），来源 `sub-ff4efe52`（deepseek-flash，**completed / tool_failure_count=0**），按修正规格写盘后只返回路径。

### 13.2 本 Agent 的独立核验（关键断言抽查）
- **K2 判定成立**：`GoalVerificationPersistence.cs:32-35` 实测 `BuildDedupKey(scope, criterionRevision, definitionHash, inputFingerprint) => $"{scope}|..."`，
  且 `BuildScopedDedupKey` 再叠加 `(goalRunId, activationEpoch)` ⇒ **`dedup_key` 含 scope**。
  故「检查作用域轴」删除会使既有 `goal_check_records` 去重键全量失配（重复执行受控检查）⇒ 必须保留（K2 正确）。
- **前进方向正确**：D5 只把 S1-a 覆盖门条件里的 `goalScope &&` 摘除（**保留判定体**），不构成对 S1-a 的回退（K1 正确）。

### 13.3 MS-1 = 删除 Goal **完成门**的作用域/剩余 WorkUnit 轴
- 可删 D1–D5（+可选 D6）：capsule/candidate 的 `VerificationScope`+`RemainingWorkUnits`、`ToCapsule` 映射、
  候选填充与 `ResolveVerificationScope`、verifier 的 `goalScope`/`boundToTask`/scope 三分支与 `taskCompleted`，
  以及覆盖门条件中的 `goalScope &&`。
- 必须保留 K1–K7：S1-a 覆盖门判定体、检查身份轴（`GoalVerificationScopes`/`GoalCheckSpec.Scope`/dedup key）、
  `ApplyBoundPlanGates`/`ApplyBoundPlanVerdict`/`BoundPlanState`、Advance 常量、`/goals/{id}/steps` 对外契约与前端。
- **删除面全部在进程内**：`goal_verifications` / `goal_iterations` / `goal_acceptance_contracts` schema 均无 scope/remaining 列
  （§12.4 已实测），无 HTTP/DTO 外泄、无 outbox 字段 ⇒ 可独立提交、单 commit、`git revert` 可回滚，**不需 DB 迁移**。

### 13.4 风险与依赖（不掩盖）
- **N1（最高）**：MS-1 后完成判据只剩「全必需条件同版本 passed（且非纯工程门禁合同）」，**不再要求 `remaining==0`**。
  该偏差在 Task-bound Goal 不可达（F1，本 Agent 实测：`task_type=general` 205/213 卡 → `execution_plan_unavailable`）时无生产影响；
  若 F1 不成立，则直接改变在跑目标的完成时点 ⇒ **F1 是 MS-1 生效前提**，实施前须复核。
- **悬空耦合**：Advance 常量与 gates 的 `:963-978` 臂不可同刀删除（后者会把带已验证据的裁决降级为 Continue，继续产出 Advance；
  先删常量会破坏 `:1449-1450` 记账语义）⇒ Advance 轴本刀不动（A5 锁定）。
- **本 Agent 追加的实施陷阱（不在子代理原报告内）**：`GoalSettlementWorker.cs:175` 现为 `Scope = capsule.VerificationScope`；
  capsule 字段删除后**不得一律硬编码 `work_unit`**，须取**该次检查自己的 `GoalCheckSpec.Scope`**，
  否则 `Scope != work_unit` 的检查项会因 dedup key 不匹配而被重复执行。要求实施方给出「哪些检查的 Scope 非 work_unit」的代码证据。

### 13.5 在途
MS-1 实施已委派（原子目标，任务书引用本报告 §13 与影响分析文档）；要求交付：逐文件改动、被改写的既有断言清单、
A1–A7 逐条证据、亲自运行的测试输出。**实施方不得 commit**（提交由本 Agent 在复核后逐文件执行）。

---

## §14　2026-09-18 用户设计决定 + 由它暴露的**阻断性缺口**（S1-c）

### 14.1 用户决定（已记录，作为后续设计与实施边界）
> 「因为后面主要由前端的 UI 控件开启 Goal 模式，同时你也不需要给自己提供一个 Goal 工具。以便可以开启 Goal 模式和恢复、暂停和停止。」

- **Goal 生命周期由前端 UI 控件驱动**：新建 / 恢复 / 暂停 / 停止（截图证据：Goal 面板含「新建 Goal」「清除记录」及刷新控件；目标创建物「目标仅一行，完整内容即上方标题」）。
- **不提供 Agent 侧 Goal 工具**。
- **本 Agent 据此撤回**此前反复提出的建议（「请你在 Web 端发 `/goal <目标>`」）—— 该建议已作废，不再重复。
- 附带边界：`task_goal_start` / `goal_resume` 等既有 Agent 侧工具的去留属设计决策，**本 Agent 不擅自删除也不继续扩建**（ADR-092 第二版决策 9 的口径：Task 是可选适配器；工具确需暴露时只做薄适配、不拥有状态）。

### 14.2 🔴 阻断性缺口：UI 一行目标 × 覆盖门 = **永久无法完成**（已实测确认）
`GoalObjectiveEvidenceParser.cs` 全文已读，能力边界确定：
- 唯一入口是**显式标记**：`证据:` / `证据：` / `evidence:` / `evidence：`（同文件 :40-46）；
- 标记之后的 token 必须通过 `GoalCheckDefinitionRegistry.IsSafeTarget`（`.csproj/.sln/.slnx`）或
  `IsSafeEvidenceFilePath`（安全相对路径）才被接受，否则**丢弃并记日志，绝不生成条件**（:80-96）；
- **不存在任何文本断言 / 内容断言路径**。

因此形成如下必然链条：

| 步骤 | 事实 |
|---|---|
1 | UI 创建目标 = 一行自然语言（截图原文：「完成 30eb371e ——『P0 G92-0 Goal 结算修复（回合结束 ≠ 步骤/目标完成）』，并更新任务看板的状态」，面板标注「目标仅一行，完整内容即上方标题」） |
2 | 该 objective 无 `证据:` 标记 ⇒ `evidence.HasAny == false` |
3 | `GoalAcceptanceContractPlanner.cs:98` ⇒ `source = "bounded_planning"`，合同仅 `bounded:build` + `bounded:test` |
4 | `ConservativeGoalIterationVerifier` 覆盖门（S1-a，commit `be04d37`）条件 `allRequiredPassed && IsEngineeringGatesOnly` 成立 |
5 | ⇒ **`Blocked("contract_coverage_insufficient")`，且此后每次迭代都命中同一分支 ⇒ 目标永久无法完成** |

**§12.3 的定性需上调**：那一条不是「部署前需确认的操作前提」，而是**已确认的阻断性缺口**。

### 14.3 两条逃生通道**都缺失**（这是缺口成立的原因）
| 通道 | 设计要求 | 现状 |
|---|---|---|
a | 设计 §6「**简单文本用文本断言**」；设计 §11「只输出指定文本 ⇒ 一轮完成、零工具调用、零 build/test、无需Task/Plan」 | **不存在文本断言条件类型，也无生成路径** ⇒ 该场景当前**不可实现** |
b | 设计 §6「**允许一次有界合同整理**」；S1-a 覆盖门的 `NextAction` 已指向它 | objective 是**用户创作、迭代内不可改**；且**没有 Agent 侧可用于补齐目标级条件的通道** ⇒ 该整理当前**不可达** |

⇒ **结论（部署纪律）**：**S1-a 不得单独部署**。它与 S1-c（文本断言 + 可达的合同整理通道）必须同批生效，否则上线即表现为「Goal 模式彻底无法完成」。这比「语义漂移」级风险高一个量级，已写入本记录的显式禁令。

### 14.4 记入 S1 待办
- **S1-c（新增，S1-a 的部署前置）**：为「简单文本目标」提供文本断言合同路径；并为「一次有界合同整理」提供可达通道。
  需设计裁决的点：文本断言的判据来源（迭代输出文本？指定工件？）、条件/检查定义的版本化表示、
  以及整理通道是走 objective 预声明规范（例如 UI 引导用户填写证据行）还是走 Agent 可写的合同修订接口。
  **本 Agent 不擅自选定** —— 先出规格，再实施。
- S1-b（MS-1）在途，不受本缺口影响（它删的是完成门的作用域轴，不涉及合同生成）。

### 14.5 关联修订
- 缺陷卡 `ef7d6378`（Task-bound Goal 不可达）的**定位随本决定调整**：Agent 侧 `task_goal_start` 不再是 Goal 的主入口，
  该卡的实际价值收敛为「调度器无法为 `general` 类型卡（205/213）生成执行计划」这一**看板派发面**问题；本 Agent 暂不改其优先级，仅记录重新定位。
- 记忆固化：用户决定（UI 驱动生命周期、不给 Agent Goal 工具）+ 本节缺口结论。

---

## §15　2026-09-18 S1-b MS-1 交付与独立验证

### 15.1 交付
来源 `sub-a254ebdd`（glm-5.3-flash，completed），交付物 `temp/s1b-ms1-report.md`。
改动面（`git diff --numstat` 实测，**合计 +28/−144**）：

| 文件 | 增/删 |
|---|---|
`Source/PuddingCore/Goals/GoalVerificationContracts.cs` | +0/−12（删 capsule 两属性与 doc；`GoalVerificationScopes` 词表**保留**） |
`Source/PuddingPlatform/Services/Goals/ConservativeGoalIterationVerifier.cs` | +9/−47（删 `boundToTask`/`goalScope`/`taskCompleted` 与收敛 Continue 臂、advance 臂；Complete 收敛为 `allRequiredPassed` 唯一路径；覆盖门摘 `goalScope &&`、**判定体保留**） |
`Source/PuddingPlatform/Services/Goals/GoalSettlementStore.cs` | +2/−43（删候选两属性、`ToCapsule` 两映射行、`ResolveVerificationScope` 及其调用；每次结算少一次计划读取） |
`Source/PuddingPlatform/Services/Goals/GoalSettlementWorker.cs` | +4/−1（`:175` 常量化） |
`Source/PuddingPlatformTests/Services/Goals/ConservativeGoalIterationVerifierTests.cs` | +6/−32 |
`Source/PuddingPlatformTests/Services/Goals/GoalContractCoverageGateTests.cs` | +7/−9 |

未触碰：同目录他人在途 `GoalCommandServiceTests.cs`（本轮从 0 涨到 +201 行，是**别人**的 WIP）、前端 5 文件。

### 15.2 本 Agent 的独立验证（**亲自运行，非采信自述**）
- `dotnet test PuddingPlatformTests --no-build --filter FullyQualifiedName~Goals` ⇒ **失败 0 / 通过 247 / 总计 247**，exit **0**
- `dotnet test PuddingCoreTests --no-build --filter FullyQualifiedName~GoalSettlement` ⇒ **失败 0 / 通过 22 / 总计 22**，exit **0**
- **A1 静态归零（我 grep 复核）**：`RemainingWorkUnits` / `.VerificationScope` 在 Platform `Services/Goals` **0 命中**；Core 仅剩两个 **K2 必须保留的幸存者**（`GoalCheckContracts.cs:119` 检查身份 `Scope`、`GoalVerificationContracts.cs:167` `GoalVerificationScopes` 词表）
- **A5 交付物未触碰 Advance 轴**：`git diff --stat` 显示 PuddingCoreTests 的 `GoalSettlementDecisionCalculatorTests` / `GoalSettlementCriterionAggregationTests` **零改动**（不在变更面内）

### 15.3 被改写的既有断言（逐条，不得以改预期掩饰回归）
1. **删** `UnknownRemainingWorkUnits_OnlyAdvances` —— remaining 轴消亡后与新用例完全同形，保留即重复。
2. `VerifiedCriteria_WithoutTaskCompletion_Advances` → `_Completes` —— 锁定的语义是「**Task 非终态不阻止完成**」（非放松）。
3. `EngineeringGatesContract_WorkUnitPassed_StillAdvances` → `_BlockedByCoverageGate` —— scope 旁路消亡后，纯门禁合同 + 全绿一律命中覆盖门，**属 S1-a 强化而非回退**。
4. 其余 **9 处仅删 `remainingWorkUnits: 0` 实参，断言全部不变**（含 `ZeroTests_CannotPass`、`StaleGreenReport_IsNotCompletion`、`UnverifiedRevision_DoesNotCount`、`IncompleteEvidence_BlocksBeforeAnyCompletionClaim` 等**不可删负例族**）。
⇒ **判定：不可删负例族未被削弱**；S1-a 判定表 A 用例预期未修改。

### 15.4 本 Agent 的自我更正（我那条「实施陷阱」前提是错的）
我此前要求实施方「`GoalSettlementWorker.cs:175` 不得硬编码，须取**该次检查自己的 `GoalCheckSpec.Scope`**」。
**读码更正**：`GoalCheckSpec`（`GoalCheckContracts.cs:6-45`）**没有 `Scope` 字段**；
`:119` 的 `Scope` 属于 `GoalCheckContext`（doc：「验证作用域：work_unit | goal（去重键的一部分）」）。
故该要求**不可实现**，实施方改为词表常量化是唯一正确解（K2 保留：dedup key 结构与唯一索引不动）。
副作用（实施方已如实披露、本 Agent 认可为有界）：对「绑定计划已收敛」epoch，scope 由 `goal` 变为 `work_unit`，该 (goal, epoch) 会有**一次 dedup miss、重跑一次受控检查**；而该类 epoch 只可能出现在 Task-bound Goal（当前生产不可达）⇒ **生产影响约等于零**。
**连带更正**：`Docs/Reports/G92-1-S1删除项调用面取证-2026-09-18.md` 把 :119 标为 `GoalCheckSpec.Scope`，同属笔误。

### 15.5 遗留（不掩盖）
- **N1（继承，最高）**：若 F1「Task-bound Goal 生产不可达」被推翻，在跑目标的完成时点会改变（不再要求 `remaining==0`）⇒ **部署前须复核 `task_plan_runs`**。
- CoreTests 全量有 **4 个环境型预存失败**（`LlmProfileResolver`×3 依赖本机 `llm.providers.json` role defaults；`SwarmOrchestrator`×1 需 git worktree），与 Goal 完成门零交叉、非本刀引入。
- 收敛态 Continue 臂文案由 goal-scope 专用改为通用（`BlockerCode` 仍为 `acceptance_not_verified`）。

### 15.6 状态与部署纪律
- **MS-1 完成并提交**；S1-b 的 MS-2（Advance 轴）/ MS-3（步骤投影与前端契约）**未开工**。
- **S1 未完成**；S1-a + S1-b 均**未部署**。
- §14.3 硬禁令仍生效：**S1-a 不得单独部署**，须与 S1-c 同批。

---

## 16. 规划与片 0a 评审（2026-09-18，loop 冲刺前）

### 16.1 规划交付物
- `Docs/Reports/G92-1-S1-implementation-plan-gpt6-2026-09-18.md`（179 行 + 父级核验）
- 生成方：**fastrouter/gpt-6-astra**（`fallback_model` 设为同模型 ⇒ **回退禁用**，产出确为 GPT-6），`sub-9d51cfce`，status=completed
- 提交：**`8a833c7`**（已 push）
- 内容：P1 三裁决（选项/后果/反例/推荐）· P2 切片序列 · P3 发布批次 B0–B3 与回退裁决 · P4 §10/§11 逐条对齐表 · P5 风险清单

### 16.2 用户批准的三点裁决（D1–D5，后续实施不得偏离）
| 编号 | 决策 |
|---|---|
| D1 | 文本断言判据 = 本 Goal/epoch/iteration 绑定的**已结束 canonical Turn 最终 assistant 输出**；**ordinal 精确匹配**；**拒绝 contains** |
| D2 | **新增受控 `text_assertion` kind**（不复用 Semantic/FileEvidence）；期望文本**必须进入哈希载荷**，采用**独立 DefinitionRef 方案 A**（旧三条目 hash 字节不变） |
| D3 | 合同整理通道 = **A1**（现有 Agent 回合提出结构化合同提议 → 平台验证提交）；**禁止** Agent 侧 Goal 工具 |
| D4 | **S1-a 不得单独部署**，须与 S1-c 同批 |
| D5 | 文本断言场景要求**零工具调用**，且**不得只信模型自述** |

### 16.3 父级对规划的独立核验
- 实测 `GoalCheckDefinitionRegistry.ComputeDefinitionHash` 载荷为 `"{DefinitionRef}|{Kind}|{CommandTemplate}"`，**不含期望文本** ⇒ 规划「事实 7」成立，直接决定 D2 的实现方式。
- **未核验项（如实声明）**：规划 P5 中标注“新”的多数静态风险（Worker 来源未刷新、合同审计版本不可靠、一次性预算伪实现、输入采集 TOCTOU、file-exists 冒充覆盖等）**尚未逐条读码验证**，不得作为已验证结论使用。

### 16.4 片 0a 只读评审（持久化与身份协议）
- `Docs/Reports/G92-1-S1c-slice0a-persistence-review-2026-09-18.md`（108 行）；生成方 deepseek/deepseek-flash，`sub-da134914`；提交 **`93d2d32`**（已 push）。
- 关键结论：
 1. 新 kind 完整接线面 **9 层**，需改 **4 处生产 + 1 处测试**。
 2. **哈希方案 A**：无条件向公共 payload 追加字段会让 3 条既有 DefinitionRef hash 全变（一旦 `GoalCheckInputIdentity` 接线即全量 stale）⇒ 推荐新增独立 DefinitionRef、期望文本仅进该条目载荷。
 3. 三轴核查：`CriterionRevision` 生产**无递增点**（planner 硬编码 1）；`ContractVersion` **不被任何失效链读取**（仅审计）；`GoalCheckInputIdentity` **零生产调用点**。
 4. canonical Turn 最终 assistant 输出**可取到**（`turn.completed` payload `reply`，`TurnExecutorAdapter.cs:151-165`），但 **Goals 路径当前未读**。
 5. 工具调用数记账（`GoalSettlementStore.cs:214-215/:276`）**只覆盖父 Turn，不含子代理调用**。

### 16.5 当前状态
- **S1 未完成**：S1-a + S1-b/MS-1 已提交未部署；S1-c 进入施工规格编制（gpt-6-astra 异步，`sub-10568b21`）；S1-b 的 MS-2/MS-3 未开工。
- 协作边界：蜜糖(258) 与 dsh(0e0) **已休眠**（用户 2026-09-18 告知）⇒ 无并发协作者，但「逐文件显式提交、不覆盖他人未提交 WIP」纪律不变。
- 工作模式：进入 **loop 循环 Goal 冲刺**（选任务 → gpt-6-astra 规划 → 实现 → 验证 → 提交 push → 下一个）。

---

## 17. S1-c 实施前基线证据（2026-09-18，Goal iteration 1）

### 17.1 基线命令与结果（由父级亲自运行，非采信子代理）
```
dotnet test Source/PuddingPlatformTests/PuddingPlatformTests.csproj --filter "FullyQualifiedName~Goals" --nologo -v q
→ 已通过! - 失败: 0，通过: 247，已跳过: 0，总计: 247，持续时间: 18 s (net10.0)
→ exit code = 0
```
- HEAD：**`64eaba1`**（仅文档变更，未部署）
- 用途：**S1-c 实施后必须与此基线对比**；规划 P3/B0 明确要求“禁止用历史 269/247 代替新验证”，故本次为新跑。
- 备注：编译阶段有 MStest 分析器 warning（MSTEST0037 等），**非错误**，与 Goal 域无关。

### 17.2 本轮 iteration 状态
- 在途：`sub-10568b21`（**fastrouter/gpt-6-astra**）正在编制 `temp/s1c-implementation-spec-2026-09-18.md`（S1-c 施工规格），状态 running；产出尚未落盘。
- 看板：`b849ef8d`（G92-1 / S1）Ready、Todo、**is_container=true**、progress 70%、version 34；子卡 `4fcd4aa5`（S1-c）Backlog。
- 阻塞（未解除，已上报用户）：`ToolApproval:Llm` 未配置 ⇒ 非白名单 shell 命令（`powershell -File …`）被 `deferred_dependency` 阻断（新票 `tap_7599e139…`）。**白名单命令不受影响**：`dotnet` / `git` / `copy` / `Get-Date` 实测可用。

---

## 18. S1-c 片 1+2 实施与父级复核（2026-09-18）

### 18.1 实施交付
- 实施方：**bigmodel/glm-5.3-flash**（`sub-f1756ce8`）；提交 **`711f109`**（已 push）。
- 改动文件（逐文件核对，共 4 个）：
 1. `Source/PuddingCore/Goals/GoalVerificationContracts.cs`（+7）词表 `TextAssertion="text-assertion"`
 2. `Source/PuddingCore/Goals/GoalCheckContracts.cs`（+7）`GoalCheckSpec.ExpectedText`
 3. `Source/PuddingPlatform/Services/Goals/GoalCheckDefinitionRegistry.cs`（+28/-）条目 + record 兼容参数 + **方案 A 哈希** + 拒绝集
 4. 新增 `Source/PuddingPlatformTests/Services/Goals/GoalCheckTextAssertionDefinitionTests.cs`（10 用例）

### 18.2 父级独立复核（四项，均亲自执行）
1. **改动面**：`git status --porcelain` + `git diff` —— 仅上述 4 文件属我方；他人在途 WIP（`GoalCommandServiceTests.cs` +201、前端 5 文件等）**未被触碰**。
2. `dotnet test …PuddingPlatformTests --filter "FullyQualifiedName~Goals"` → **失败 0 / 通过 257**（基线 247 + 新增 10），15 s。
3. `dotnet test …PuddingCoreTests --filter "FullyQualifiedName~GoalSettlement"` → **失败 0 / 通过 22**。
4. **读码核实**（不采信自述）：`GoalCheckDefinitionRegistry.cs:65-79` 确实为 `!string.IsNullOrEmpty(ExpectedText)` 追加，注释明确禁用 `IsNullOrWhiteSpace`；`GoalVerificationPersistence.SerializeChecks/ReadChecks`（**Platform 层**）确实用 `JsonSerializer.Serialize`（`JsonSerializerDefaults.Web`）属性自动序列化 ⇒ 新字段无需登记即可往返；新测试①④直接调用**生产方法**而非自造路径。

### 18.3 纠正与诚实记录
- **路径纠正**：`GoalVerificationPersistence.cs` 实际位于 **`Source/PuddingPlatform/Services/Goals/`（Platform 层）**，不在 Core 层；规格任务书该路径为笔误。
- 子代理因此产生 1 类工具失败（`file_read: File not found`，共 9 次失败），其“自动序列化”结论**未经读码即给出** —— 父级已独立读码证实该结论**正确**，但“读错路径仍下结论”的行为模式已记录。
- 子代理如实披露：金样 hash 首版误写双尾管道，单测红后按实现输更正；父级复核确认现行三值自洽（null ⇒ 单尾管道 `|kind|`；非空 ⇒ `|kind||{text}`）。
- 金样（改动前三条目，固化入测试）：build `sha256:4f6c397e…ffa9e`、test `sha256:4357c6fa…d782`、file-evidence `sha256:28f1b16e…7a39`。

### 18.4 遗留
- **片 3+4 未开工**（Runner 纯文本分支与文本供给链 / Policy 放行）。
- **S6 五项待用户裁决**（blocker 形态 / 子代理计数是否并入零工具判定 / EvidenceRef 字面量 / 期望文本长度上限 / 与 S1-a 同车部署窗口）；父级已给推荐，用户尚未回复。
- **未部署**：S1-a + S1-b + S1-c 片1+2 均未上线（D4 约束不变）。

---

## §19 2026-09-18 晚 —— 片3 交付 / 看板归档 / 并行实验 / B1 定位

### 19.1 S1-c 片3 交付与独立复核（commit `fe36249`）
- **3a 上下文载体**：`GoalCheckContext.FinalAssistantReply` + 新 record `GoalFinalAssistantReply{TurnId, Sequence, Text}`（Text 为原文，不 Trim、不归一）
- **3b Runner 分支**：在 `WorkingDirectory` 门禁**之前**分派；`EvaluateTextAssertion` 用 `StringComparison.Ordinal`；不可得 ⇒ 终态 `FailedReport(evidence_missing)`（不回 pending）；证据格式 `assistant-output:{turnId}@{sequence}`
- **3c 供给链**：`GoalSettlementStore.LoadFinalAssistantReplyAsync`（conversationId + turnId + Type==TurnCompleted + Sequence<=terminalSequence 取最后一条）；`TryReadReplyText` 全程容错（非法 JSON / 缺 reply / 非 string ⇒ null）
- **复核证据（父级亲跑）**：`PlatformTests ~Goals` **263/0**；`CoreTests ~GoalSettlement` **22/0**
- **遗留**：片4 未做 ⇒ text-assertion 报告仍会落 `unsupported_check_kind`，**功能未闭环**

### 19.2 看板卫生：已完成任务归档
- 36/36 归档成功，**全程未用 `force=true`**，在途卡零误伤
- 独立核验：`status=Completed` ⇒ `total=0, items=[]`；`status=Ready` ⇒ `total=21`（未减少）
- **残留观察**：多条已归档卡仍带 `active_assignment`（历史 Completed 卡未走 canonical 释放路径的脏数据）——未清理，属另一卡范围

### 19.3 看板全量盘点（实测，逐状态分页遍历完整）
| 状态 | 数量 | p0 | p1 |
|---|---|---|---|
| Ready | 21 | 11 | 5 |
| NeedsReview | 15 | 3 | 8 |
| Blocked | 7 | 2 | 5 |
| InProgress | 1 | 1 | 0 |
| Backlog | 125 | 21 | 68 |
| **合计非终态** | **169** | 38 | 86 |

- A 类（立即可执行）16 条；B 类（待裁决）15 条；C 类（受阻）7 条；D 类（Backlog）125 条；E 类（容器）1 张 = `b849ef8d`
- **高杠杆结论**：C 类 7 张中 **5 张**阻塞根因同一个 —— 审批门禁（`d356c701` / `133711b4`）
- 报告留存：`Docs/Reports/看板全量盘点-169非终态-2026-09-18.md`

### 19.4 并行实验（第一批，文件域隔离）
- 在途三路：片4（`sub-b3a61bbe`，Goals 域）/ 路由卫生试点（`sub-679b3587`，文档·预置）/ B1 修复（`sub-63d1ced3`，Tools/Approval + appsettings）
- 并行纪律：①一组内文件域**不重叠** ②子代理**禁止 git 写** ③**验收留父级**（逐批复核 + 亲跑测试 + 逐文件提交）④写入路径优先 `apply_patch`/`file_write`

### 19.5 B1 根因定位（审批 LLM 档案不可达 —— 死锁结构）
- 文件：`Source/PuddingRuntime/Tools/Approval/LlmToolApprovalReviewer.cs`
- **死锁**：分支A（`ProfileId` 非空）因生产 `profiles` 为空且 `_llmConfigService != null` ⇒ 返回 null；分支B（`:176-179`）仍**强制要求 `ProfileId` 非空** ⇒ **永不可达**
- **影响面**：所有灰区工具（`file_patch`、`powershell`）返回 `deferred_dependency` / `approval_review_profile_not_configured`；本日实测**单次子代理因反复重试浪费 15,609,087 token / $15.91**
- **新增事实**：子代理侧被拒 20 次，而**父代理对同一产品文件 `file_patch` 成功** ⇒ 权限不对称，属 B1 范围
- **修复方案（选项B）**：放宽分支B 允许仅 `ProviderId+ModelId`（`ProfileId` 合成）；分支A 语义不变；`appsettings.json` 的 `ToolApproval:Llm` 补 `ProviderId`+`ModelId`（**必须是已注册路由**）；**不放宽安全语义**（仍走独立 LLM 审查、不得静默自动批准、不得 fallback 默认模型）
- **部署纪律**：须**重启**生效；重启前必须等在途子代理收口（重启会杀子代理）

### 19.6 平台事实（新增，供后续复用）
- `manage_tasks list` 的 `total` 是**本页条数**而非匹配总数 ⇒ 必须靠 `next_cursor` 翻页遍历
- 平台单次工具响应约 **20KB 即截断**，大结果需利用 artifact 文件或换成小分页
- 子代理与父代理的**工具可用性可不对称**（同一工具、同一路径）；委派前应假设子代理受限并预置替代路径

---

## §20 2026-09-18 晚（续）—— B1 修复 / 片4+5 / 路由卫生试点 / 前置 plan 门禁

### 20.1 S1-c 片4 + 片5（commit `123f821`）
- **片4（EvidencePolicy 放行）**：`Source/PuddingCore/Goals/GoalCheckEvidencePolicy.cs` 新增 `isTextAssertion`；3.6 节证据绑定（要求 `assistant-output:` 前缀 + turnId 一致，否则 Downgrade Failed(evidence_missing)）；第 10 步放行（位于 `unsupported_check_kind` 之前，不要求 InvocationId/ReportRef/ExitCode）；既有 build/test/file-evidence 零改动。新增 `GoalCheckEvidencePolicyTextAssertionTests.cs`（双入口 + 4 类用例）
- **片5（父级补接线 —— 多轮验证发现的致命缺口）**：唯一调用点 `ConservativeGoalIterationVerifier.cs:23` **未传 `assistantOutputTurnId`** ⇒ 恒 null ⇒ text-assertion 永远 fail-closed，而片4测试直接传参故全绿（**测试绿 ≠ 功能生效**）。修复：`GoalEvidenceCapsule` 新增可空 `AssistantOutputTurnId`（刻意不复用恒非空的 `TurnId`，否则无法表达"reply 不可得"）+ `ToCapsule()` 填充 + verifier 传参
- 验证：`PlatformTests ~Goals` **268/0**；片5 接线目前仅【读码+编译】验证，**缺端到端回归测试**（待补）

### 20.2 B1 修复（commit `c604caf`）—— 成本闸门解除
- **根因**：`StrictConfiguredToolApprovalLlmProfileResolver.ResolveAsync` 分支A 因生产 `profiles` 为空且 `_llmConfigService` 非 null ⇒ null；分支B 入口仍强制 `ProfileId` 非空 ⇒ 无 ProfileId 时**永不可达**
- **修复**：分支B 删除 `ProfileId` 前置条件；`ProfileId` 缺省合成 `"{providerId}/{modelId}"`（仅日志标识）；分支A 逐行未动；**安全语义零改动**（仍独立 LLM 审查、不静默批准、不回退默认模型）
- **配置**：`Source/PuddingAgent/appsettings.json` → `ToolApproval:Llm = { ProviderId: "deepseek", ModelId: "deepseek-flash" }`（不写 ProfileId）；选 deepseek-flash 取延迟优先（高频小调用）
- **验证**：`PuddingRuntimeTests ~Approval` 81/78 通过/3 失败；相关 10/10 通过（新增 4 + 既有 6 回归锁）
- **3 个失败的父级独立归因**（读源码判定，非采信子代理）：Parser 那条为纯 `Parse("not json")` 不经过 resolver 且 Parser 未改；两条 `RequestToolApprovalTool_Accepts_*` 构造 DI 时未配 `ToolApproval:Llm` ⇒ 必然 null。单调性佐证：改动仅"删除一个返回 null 的条件"，不可能引入 null 结果 ⇒ **预存失败**（建议另开卡治理）
- **待部署**：需重启加载 appsettings 才生效

### 20.3 路由卫生试点（卡 `0278f106`，commit `8fd3d1e`）
- 7 文件 22 处对称替换（文档/预置/Skill/速查表），以 `list_llm_providers` 实测为唯一事实源
- 验收：`AgentTemplateFileService` 测试 **7/0**（该预置文件有测试覆盖，必须实跑）
- **审批墙的重要修正**：本次子代理 `file_patch` **8 次全部成功**（改文档/预置），而片3 改产品代码时 **20 次全被拒** ⇒ 判据更可能是**写入目标**（产品代码 vs 文档），非单纯调用者身份
- 遗留：①卡面"验收2：smart_* 无回退告警"未闭环，根因在产品代码默认值（`SmartExploreTool.cs:37`、`SmartDevelopTool.cs:31`），需另开代码卡 ②最高价值点位 `D:\data\agents\<id>\manifest.json`（工作区外）未动，待父级处理 ③`memory/llm-providers-cheatsheet.md` 已改但**该文件 gitignored**（故不在 git status，需用 search_grep 验证内容）

### 20.4 流程升级：任务前置 plan 门禁（用户 2026-09-18 要求）
- 取卡后**第一动作是 plan**，回答四问：①任务是否正确 ②方向是否正确 ③是否有更好实现方式/方向 ④是否必要
- 结论标签：`继续实现` / `修正方向后实现` / `合并到<卡号>` / `否决（无必要）` / `已由现有代码解决`
- 与"未完成卡不得直接关闭"的交叉：plan 判"无需实现"时**只能上报裁决**，不得自行关卡
- 首次落地：`sub-b810287d` 评估 6 张 A 类 p0 卡（`ef7d6378`/`2b60040d`/`e2c35d6e`/`dc0ac9a8`/`d85a107b`/`bf7ef5f5`），产出 `temp/task-pre-plan-2026-09-18.md`

### 20.5 重启前状态（供新会话恢复）
- 已推送提交序列：`711f109` → `fe36249` → `62a9151` → `12eaf154` → `b7fba30` → `123f821` → `8fd3d1e` → `c604caf`
- 待办：①等 plan 子代理收口 ②`bootstrap_reboot`（B1 生效 + 验证子代理写入是否恢复）③重启后补片5 端到端回归测试

---

## §21 2026-09-18 夜 —— 重启后四项验证 + gpt-5.6-sol 接入（父级亲自执行）

### 21.1 重启与资源池（✅ 已验证）
- `bootstrap_reboot` 已执行（**默认 desktop-build** ⇒ 本次重编译并部署了工作区当前代码：S1-c 片1-5 + B1 + 路由卫生）
- `list_llm_providers` 实测：`fastrouter` 下现有两条 —— `gpt-6-astra`(sortOrder 20)、**`gpt-5.6-sol`(sortOrder 30, protocol=responses, ctx 1,050,000, tags 含 function-calling/streaming)** ⇒ **接入生效，无需改配置**
- 附带修正：`fastrouter/gpt-6`（无后缀，此前 404 死配置）**已不在资源池列表** ⇒ 死配置隐患解除
- 父级方法错误更正：`list_llm_providers` 的 `model_id` 是**精确匹配**（非模糊），此前用 `gpt-5.6` 查不到 `gpt-5.6-sol` 属我的查询方式错误，不是配置缺失

### 21.2 部署后回归（✅ 268/268，exit 0）
```
dotnet test Source/PuddingPlatformTests/PuddingPlatformTests.csproj --filter "FullyQualifiedName~Goals" --nologo -v q
→ 已通过! - 失败: 0，通过: 268，已跳过: 0，总计: 268，持续时间: 21 s (net10.0)
→ exit_code = 0
```
- 对照基线：重启前 247 → 片1-5 新增 21 → **268**，**零回归**
- 结论：desktop-build 部署的 S1-c 片1-5 + B1 + 路由卫生**未破坏任何既有 Goal 用例**

### 21.3 gpt-5.6-sol 烟测（⚠️ 未通过，需继续观察）
- 派发（禁用回退：`fallback_model` 设为同模型）：`sub-e81725be`，任务=仅回两行文本、禁用全部工具
- 实测：**2 请求 / 77,285 token / 缓存命中 0.0% / 费用 $0.0000（价格配置为 0）**，长时间 `running`，诊断显示 `totalRounds: 0, totalToolCalls: 0`
- 与 `gpt-6-astra` 的 0% 缓存一致 ⇒ **fastrouter 侧很可能不回报 cached_tokens**，或该供应商前缀缓存不生效（**待确认，勿当结论**）
- 判断：**该模型作为子代理的可用性尚未被证实**，本轮不下"可用/不可用"结论

### 21.4 前置 plan 门禁首次落地（卡 `f1d45a15`）
- 派 `sub-52752ee2`（`fastrouter/gpt-5.6-sol`）：对 `f1d45a1501f04b62bc25e6c2afedf8f0` 做四问评估 + 验证诊断文档的 V1–V4（**任务书明确要求自解析完整 task_id，禁止父级转述短号** —— 针对前一轮 `task.not_found` 事故的直接对策）

### 21.5 六卡前置 plan 结论（来源 `sub-714aa39d`，已入库 commit `a74e77f`）
| 卡 | 结论标签 | 要点 |
|---|---|---|
| `ef7d6378` | 修正方向后实现（两阶段） | `TaskExecutionPlanCompiler.cs:26/:110/:112` general 落 `_ => []` ⇒ 编译必败；建议方案B=general→单一轻量 WorkUnit + `unsupported_task_type` 结构化拒绝，并把 16/16 Failed 独立定位 |
| `2b60040d` | **已由现有代码解决**（附证据） | F3a 五文件已进 master（`git_diff HEAD` 为空）；残余仅端到端验收 ⇒ **不得自行关卡，待验收后归档** |
| `e2c35d6e` | **合并到 `bf7ef5f5`** | 与后者验收逐条同构，单独做会双卡改同文件 |
| `dc0ac9a8` | 继续实现（两刀） | 刀1=BudgetExhausted 改暂停（可独立先做）；刀2=AwaitHandle 持久等待 |
| `d85a107b` | 继续实现（伞卡） | 前置完成前不派发 |
| `bf7ef5f5` | 继续实现（吸收 `e2c35d6e`） | BoardColumn 读侧投影**已实现** ⇒ 残余=一致性测试 + DB 存量，**不得重做投影改造** |

### 21.6 父级操作失误（本轮自查，已固化）
- **`terminal_wait` 读法错误**：长任务里我反复从 `offset=0` 读、且未先查 `terminal_status`，导致三次长等待（240/300/420/600s）都停在编译告警段，**没读到早已存在的尾部结果行**。实际该 job **早已 `Exited, exit_code=0`**
  - 教训：①长任务先 `terminal_status` 判断是否 `Exited` ②读尾部用 `terminal_read` 从 `total_lines-N` 起读 ③`terminal_cancel` 对已退出 job 返回 `cancelled:false` 即"已结束"信号
- 子代理近 3h 效率（`agent_diagnostics`）：14 runs / 成功率 **50%**；`glm-5.3-flash` 10 runs / 70% / avg 1036s / 33 rounds / 55.7 tool calls；`gpt-5.6-sol` 2 runs 均未收口

### 21.7 本轮在途
- `sub-e81725be`（gpt-5.6-sol 烟测）running
- `sub-52752ee2`（gpt-5.6-sol / `f1d45a15` 前置 plan）running
- 待补：S1-c 片5 端到端回归测试；B1 生效实证（需一次「子代理改产品代码」的实际调用作为证据）
