# G92-1 下一行动：接通真实检查、结算与目标续行

日期：2026-09-15。源码查看基线：`4d5980f`。本次按用户要求核对当前进展并发送下一行动指令，**仅做定向静态检查，未运行构建/测试，未修改产品代码或部署**。

## 已有进展

当前可见六个新提交：`4f8561a`、`fcca460`、`eb5352c`、`ad6312a`、`ea97998`、`4d5980f`，涉及 A91 typed dependency、G92-0 结算与真实 Store 测试代码、G92-1 检查合同/证据策略。上一消息 `extmsg-da3ce2ac0143172916f3178d61b0177c` 于北京时间 08:59:48 返回 canonical `succeeded`，Agent 明确未运行 build/test；该回执不是完整 Goal 完成证据。

已读取任务 `b849ef8d750143fdaaa2a70d7b024a6c`，标题“P0 G92-1 Goal真实验收：CheckRunner与步骤/整体两级Verifier”，状态 InProgress、v8；进展说明也承认 CheckRunner、持久合同填充、完成入口和独立 Goal 尚未接通。

## 当前代码缺口与下一顺序

| 顺序 | 静态证据 | 下一行动 |
|---|---|---|
| 1 | `GoalSettlementStore.cs:51` 的 ToCapsule 未填入 Criteria/Checks/CheckReports | 从持久合同/检查事实装配生产 capsule，补真实读取链回归 |
| 1 | 同文件 `ApplyCurrentVerdict` 仍按粗粒度 Blocked 将 task-bound Goal 置 Failed；节点计算器改动没有接到最终 Goal 结算 | 用统一 typed disposition 结算 Goal/Plan/Task；可修复继续当前单元，等待保持目标归属 |
| 1 | `acceptance_contract_missing` 被列为 WaitBlockerCodes，缺少合同生成触发 | 改为有界规划动作；有检查未执行先建立检查工作，不能等待不存在的事件 |
| 2 | `AllRequiredCriteriaPassed` 对同一条件只取首条结果，verifier 在 failed-check 分支前判断全通过 | 对一个条件关联的所有必需检查正确归并；build pass/test fail 不得通过，结果顺序不能改变结论 |
| 2 | EvidencePolicy 只按 CheckId 选报告，缺 CriterionId 关联；空 hash/指纹/ExpectedEvidence 可跳过校验，未知失败数未被拒绝 | 完整身份、新鲜度、可信来源和计数一致性校验；非空 ReportRef 不等于真实运行 |
| 3 | 当前 IGoalCheckRunner 仅有接口，未检索到 Runtime/Platform/Host 的生产实现或注入调用 | 实现经既有准入的真实检查、持久 job/lease、运行报告和恢复信号 |
| 4 | verifier 仍用 `allRequiredPassed && taskCompleted`；Store deterministic gate 仍要求 Task 先 Completed | 明确步骤/整体 scope、完成提议、原子 Goal/Task 结算，打通独立 Goal |
| 5 | 当前只有测试代码与静态语法检查，尚无实际运行报告 | 定向构建/测试及新构建真实多轮闭环；未跑不得标通过 |

以上是当前代码路径和缺失连接点的定向检查，不冒充完整代码审计、线上复现或测试结论。

沿用 [ADR-092](../07架构/106ADR-092目标驱动执行与分层验证闭环ADR.md) 与[完整施工方案](../Features/Goal目标驱动执行与分层验证闭环设计-2026-09-15.md)，不重新扩张设计或重复建立任务。发送内容将在下方完整保留，便于交接。

## 派发回执

目标 Agent：`default.global_general-assistant.6a8`。本次消息 `extmsg-da729184af07c2dc883f6c9d136f6668` 的 canonical acceptedAt 为北京时间 11:17:15，HTTP 202 响应约为 11:17:16。

45 秒有界等待后的回执：deliveryStatus=`accepted`，delivery=`delivered`，attemptCount=1，executionStatus=`running`，conversationId=`206a9b48ec904ebb93e7541131fbb835`。reply、replyIsError、completedAtUtc 均为空，尚无终态答复或完成证据。没有重复发送；后续可用同一 messageId 跟踪。

已向现有 G92-1 任务追加下一行动和运行中回执评论，未改写为 Completed。此记录只证明实施消息正在执行，不证明已经建立新的 canonical GoalRun 或通过目标验收。

## 实际发送内容

```text
继续推进 ADR-092 的完整目标，不重做已经提交的合同/纯函数切片。下一行动是 G92-1 的生产运行闭环，主任务 b849ef8d750143fdaaa2a70d7b024a6c（已读到 InProgress v8），并修复它依赖的 G92-0/G92-2 真实结算路径。不是再交付一批只有接口和模拟输入的代码。

我检查了当前 HEAD 4d5980f 和上一条 canonical 回执。4f8561a、fcca460、eb5352c、ad6312a、ea97998、4d5980f 均已存在；认可这是源码进展，但你明确尚未跑 build/test，我没有把它们判为验收通过。上一消息 succeeded 只表示那一轮结束，不是 Goal 达成。

请按以下次序直接实施，每个已验证原子改动提交后继续下一项：

1. 先堵住真实结算断链，写真实 Store 回归再修。
- GoalSettlementStore.cs:51 的 GoalSettlementCandidate.ToCapsule 仍不填 Criteria/Checks/CheckReports，生产输入恒为空。扩展 candidate 并从持久合同/检查记录按 goal+epoch+objective/plan revision 读取；不得在生产或测试中直接注入假 passed 代替这条读取链。
- GoalSettlementStore.cs:841-853 的 ApplyCurrentVerdict 仍将所有 Blocked/NeedsUser/Unsafe 的 task-bound Goal 置 Failed，未消费新 ComputeDisposition；所以 criterion_failed/check_results_pending/acceptance_contract_missing 仍会结束目标。将 typed disposition 接到唯一最终 writer：Repair/ContinueCurrent 留在当前单元并投递下一轮，Await 有持久事件/截止时间并保留逻辑 Goal，只有明确终止才走 Failed/Cancel。Goal、Plan、Task、binding、reservation、assignment 必须一致；不能只保留节点状态。
- acceptance_contract_missing 是系统能执行的有界规划工作，应安排一次合同生成步骤；从 WaitBlockerCodes 的无触发等待中移除。已有检查但还没结果必须创建/复用实际 check job 后等待，不能永远等不存在的事件。

2. 完成检查结果归并的正确性，避免新假绿灯。
- 当前 AllRequiredCriteriaPassed 用 FirstOrDefault，而 ToCriterionResults 可为同一 Criterion 产生多项检查结果；verifier 又先判断 allRequiredPassed，再判断 HasFailedChecks。增加“同一必需条件 build passed + test failed/waiting”的回归：绝不能 Advance/Complete。按 criterion 聚合它关联的全部必需 checks；定义明确的失败/等待/缺失优先级，与列表顺序无关。
- GoalCheckEvidencePolicy.FindReport 只按 CheckId 选记录，EvaluateOne 未核对 CriterionId；补检查定义与报告的身份关联、revision/输入/定义 hash 及可信来源校验。定义缺失必要 hash/指纹不能跳过新鲜度校验；ExpectedEvidence 为空不等于允许零证据；FailedTestCount 未知/负数、通过/失败数量矛盾不能判通过。只认受控执行产生的报告，不能仅凭非空 ReportRef 字符串或 self-reported Status=passed。

3. 接入真实 CheckRunner 与持久工作。
- 当前只有 IGoalCheckRunner 接口，没有生产实现、DI 或调用链。实现首个可用的 build/test/postcondition 路径，经既有 CommandExecutionPlan/工具准入运行；解析本次运行报告和后置结果，记录 canonical InvocationId/RunId、输入指纹、报告引用和实际测试数量。
- verification/check 的 pending→leased→finished 状态、去重键、租约/取消/超时与恢复要贯通 GoalSettlementWorker；不持 SQLite 写事务等待模型/进程。不增加自由 shell 旁路，不把测试模型 mock 当真实检查。
- 调用者取消/用户取消不能吞成自动恢复 waiting；测试超时可能需要修复、依赖未就绪才是依赖等待，任何 wait 必须有实际恢复来源。

4. 解除 Task 先完成的循环依赖，区分步骤与整个目标。
- ConservativeGoalIterationVerifier.cs:89 的 allRequiredPassed && taskCompleted、GoalSettlementStore.ApplyDeterministicGates:588 的 Task 必须先 Completed 仍存在。增加显式 work_unit/goal 验证 scope；步骤全部通过只能推进，整体条件全部通过才可完成。独立 Goal 不依赖 Task，必须有可达完成路径。
- 绑定 Goal 的 Task 完成请求统一为 completion proposal；所有写入口共用 gate。由唯一结算事务验证版本后原子结束 Goal/Plan/Task、释放资源。不得仅删 TaskCompleted 判断导致“一个单元通过即整个目标完成”。

5. 交付贯通链的证据，之后继续 G92-2/3。
- 必测：生产 candidate/capsule 有持久合同；空合同自动规划；一个条件多个检查有失败不通过；实际测试失败→Repair→下一轮复验→整体完成；pending 事件恢复；独立 Goal；旧 epoch/取消后迟到报告；Task/Goal 双终态。
- 定向项目：Source/PuddingCoreTests/PuddingCoreTests.csproj（Goal），Source/PuddingPlatformTests/PuddingPlatformTests.csproj（Goal/TaskExecutionTracker），Source/PuddingRuntimeTests/PuddingRuntimeTests.csproj（Goal/AgentInvocationDispatchFactory）；--no-restore --nologo。必须记录真实发现/运行数量，code_outline 无语法错误不等于编译/测试通过。
- 如现有 shell/terminal 准入仍阻塞，准确记录一次 reason/Invocation/待外部验证命令，继续可做的源码工作；不要反复申请或包装命令绕过。测试未跑就标未跑，不宣称 A91/G92 已验证。外部构建/部署后，才能做真实三轮 Goal 功能验收。

沿用 Docs/Features/Goal目标驱动执行与分层验证闭环设计-2026-09-15.md 与 ADR-092；本指令是接线顺序细化，不扩大目标，不接管 8df6a4d6 预算卡。先保留现有他方 WIP、精确提交自己的文件。更新现有卡进展和证据，不重复建任务。不要以 commit、回复结束、goal.md 或 sleep 充当 Goal 完成；只在真实依赖/授权预算/用户控制要求下暂停，给出具体恢复条件。
```
