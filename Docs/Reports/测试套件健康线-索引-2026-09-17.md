# 测试套件健康线 · 索引（dsh / default.global_general-assistant.0e0）

> 建立：2026-09-17｜用途：把本 Agent「测试套件健康治理」这条线的全部交付物与结论做成**一处可达的入口**。
>
> ⚠️ **版本控制说明**：本文件是**受 Git 跟踪的权威副本**（`Docs/Reports/`）。
> 同内容的 `memory/dsh-testsuite-index-20260917.md` 是**本机便利副本** —— 仓库 `memory/` 下**新建文件被 gitignore**，
> 因此 `memory/` 那份**不会入库**（该事实已由 `2026-09-17 19:15` 的核对确认：`git_status` 不列出该新文件、`git_add` 后 `git_commit` 报 "No changes"）。
> 另：共享索引 `memory/INDEX.md` 由 6a8 维护（头部「最后更新 2026-09-13」），本 Agent **不并发改写**。

## 一、交付物（均可直接打开）

| 文件 | 内容 | 备注 |
| --- | --- | --- |
| `Docs/Reports/测试套件健康与缺陷类诊断-2026-09-17.md` | 主诊断报告（1130+ 行）：§一 结论摘要 · §一之二 交付清单/状态总表 · §二~§六 分组诊断 · 六之补系列 · §七 工具与排查手册 · §八 修复提交清单（13 项） | 全部数据本机可复现 |
| `Docs/Reports/测试套件待裁定项-决策备忘-2026-09-17.md` | **拍板用**：速览表 + 逐项「现象原文 / 判定依据 / 精确改法草案 / 影响面 / 验证命令」，覆盖 B 产品缺陷 · A/C/E-1/E-2 四组测试陈旧 · 审批域 4 项 · 平台 `ToolApproval:Llm` 缺配 | 不做任何代码修改，全部为待批准草案 |
| 本文件 | 本线单一入口（结论 / 指标 / 方法论 / 复核 / 待办） | 受版本控制 |

## 二、一句话结论

- Core 套件从「**永久挂起、从未跑完**」→ **55 秒跑完**；失败 **12 → 4**（−67%）。
- **8 个测试项目全部可构建、8 个套件全部无挂起**（起始：1 套件永久挂起 + 2 项目不可构建）。
- 剩余失败**全部**落在「所有者裁定」或「他方在途」，**机械修复空间已用尽**。

## 三、健康指标（最近实测，2026-09-17）

| 套件 | 通过 / 失败 / 总计 | 备注 |
| --- | --- | --- |
| Core | **859 / 4 / 863** · 55 s | 4 项 = A×3（`LlmProfileResolverTests`）+ B×1（`SwarmOrchestratorTests:403`）；距首次全量 10 小时复测无回归 |
| Platform | 1187 / 1 / 1188 | 该 1 项属 Goals 域他方在途 |
| MemoryEngine | 161 / 3 / 164 | C 项（一代旧层名） |
| CodeIntelligence | **96 / 0 / 96** | ✅ 已修全绿（DI 夹具补 `AddLogging()`） |
| RuntimeTests | **1367 / 4 / 1371** | 总数由 1353→1371（他方新增 18 项），失败仍为 4（审批域，无回归） |
| FullTextIndex | 50 / 0 / 54 | 4 跳过 |
| CodexService | 4 / 0 / 4 | — |
| WebApi | **138 / 6 / 144**（2026-09-17 23:27 三轮全量、关闭并行，13 s） | 稳定失败仅 **3 项**（E-1 ×2 + E-2 ×1）；其余属**套件级不稳定**（三轮集合 5/7/6 各不相同；首现 `ChatCompletions_WithTools_Stream_ReturnsSseToolCallDeltas`；**关闭并行也未能稳定**）—— 详见主报告 §一 19:46 复测块 |

## 四、★ 三条方法论（本环境硬经验）

1. **`--artifacts-path` 下的任何失败清单都必须先用默认布局复核** —— 它改变 `AppContext.BaseDirectory` 的层级形状，凡按 bin 相对路径取数据的测试都会假失败（本会话已证伪 3 例 + 1 个整夹具族共 76 处）。
2. **`exit code 1` ≠ 被门禁拒绝** —— 先读重定向日志；有内容就说明命令跑过（`dotnet test` 有失败用例即返回 1）。
   真门禁拒绝有两种明确文案：`Runtime approval required for high-risk tool …` / `High-risk tool 'X' is waiting for the approval review dependency (reasonCode=approval_review_profile_not_configured)`。
3. **门禁是「逐调用间歇」** —— `shell` 与 `terminal_start` 互为等价工具；两者同时不可用时应改为产出**无需命令**的交付物（文档 / 证据复核 / 记忆维护），不要空转或盲目重试。

## 五、独立复核（文档记载 vs 工作树实态，2026-09-17 19:12）

| 记载项 | 复核方式 | 结果 |
| --- | --- | --- |
| `5b75f57` 审批域取消断言改 OCE 家族 | 直读 `Source/PuddingRuntimeTests/Tools/PuddingToolInfrastructureTests.cs`（约 `:4377`） | ✅ 实到：`await Assert.ThrowsAsync<OperationCanceledException>(…)` 在位（非 `ThrowsExactlyAsync`） |
| `cc66f7ca` CodeIntelligence DI 夹具补 `AddLogging()` | 直读 `Source/PuddingCodeIntelligenceTests/Services/DependencyInjectionTests.cs` | ✅ 实到：**两个**用例的裸 `ServiceCollection` 均已有 `services.AddLogging();` |
| 清理：合并我留下的重复 F06 注释 | — | commit `67c917d4`（纯注释，零行为变更） |

> 目的：防止「文档声称已修、而工作树实际未修」的漂移。命令类工具被门禁阻塞时，优先做这类**静态复核**。

## 六、待办与阻塞

| 项 | 状态 |
| --- | --- |
| 补跑 FullTextIndex / CodexService / WebApi / RuntimeTests 四套件，刷新「全景表」 | ⏳ **门禁阻塞**（2026-09-17 17:25 / 17:58 / 18:40 / 19:11 四轮 `terminal_start` + `shell` 均被拒） |
| 待所有者裁定 7 组（B 产品缺陷 · A/C/E-1/E-2 测试陈旧 · 审批域 4 项 · 平台门禁配置） | ⏳ 已附证据链与改法草案（见决策备忘） |
| 心跳「报飞书」 | ⏳ 未投递：未建立飞书会话绑定时**任何**主动投递路径均不可用（需主人先在飞书给本 Agent 发消息）；要点按契约写入 `sleep` 的 `work_summary` 下轮补报 |
| 平台门禁 `ToolApproval:Llm` 缺配（含**恢复路径自指**） | ⏳ 建议补配置 / 缺配时降级为人工授权工单 / 放行只读构建类工具 |

## 七、更正记录

| 时间 | 更正内容 |
| --- | --- |
| 2026-09-17 18:45 | 「`memory/INDEX.md` 因门禁未建成」→ 实为**共享索引**（6a8 维护），已改为新建自有索引 |
| 2026-09-17 19:20 | 更正：仓库 `memory/` 下**新建文件被 gitignore**，故提交 `781a6fe9` 说明称「新增 memory/dsh-testsuite-index-…」**不准确**（未入库）；权威副本已放到受跟踪的 `Docs/Reports/测试套件健康线-索引-2026-09-17.md` |
| 2026-09-17 19:48 | 更正：提交 `28abc7fe` 说明末尾一句「主报告对应行同步更正」**已做到**；同时**撤回**「WebApi 76 项假失败由 `--artifacts-path` 布局引起」的断言——同布局**全新构建**后仅 5 项失败，成因更可能是当时 artifacts 目录内**陈旧/不完整构建** |
| 2026-09-17 19:48 | 新增事实：**默认 bin 布局下 WebApi 无法构建**，因为运行中的产品进程 `PuddingAgent (PID 26356)` 锁定 `bin\Debug\net10.0\Pudding*.dll`（`MSB3027`/`MSB3021`）⇒ 需要 `--artifacts-path` 或 `--no-build` |
| 2026-09-17 20:20 | 更正：上一行「早前 3 项 Goals 域失败已消解」**撤回** —— TRX 取证显示 WebApi 5 项失败含 `GoalCommands_Replay_With_Same_ClientRequestId_Returns_First_Goal`（**全量运行失败、过滤运行通过** ⇒ 顺序/状态相关） |
| 2026-09-17 20:20 | 新增工具手法：取失败名单改用 **TRX 日志**（`--results-directory temp\watrx --logger trx` → `search_grep` 查 `outcome="Failed"`）；⚠️ `--logger "console;verbosity=quiet"` 会因分号被逃逸成 `%3b` 触发 `MSB4177` |
| 2026-09-17 23:27 | **三轮全量（关闭并行 `-- RunConfiguration.MaxCpuCount=1`）：失败 6 / 通过 138 / 144** ⇒ 三轮集合 5/7/6 各不相同、首现 `ChatCompletions_WithTools_Stream_ReturnsSseToolCallDeltas`；⇒ **关闭并行不能消除不稳定**，不支持「单纯并行干扰」，更可能是**同程序集内共享宿主/数据根状态污染**（待查） |

## 八、记忆库对应条目

`PuddingAgent` 书内：测试套件基线（Core/Platform 数字与命令）· 工具坑汇编 · 协作边界 · 投递约束 · 交付物索引。
