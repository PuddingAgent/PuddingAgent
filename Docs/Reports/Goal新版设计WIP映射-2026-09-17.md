# Goal 新版设计（ADR-092 第二版）落地映射：既有 WIP 保留 / 改造 / 删除

日期：2026-09-17（北京时间 19:1x）
对应设计：`Docs/07架构/106ADR-092目标驱动执行与分层验证闭环ADR.md`（第二版）、`Docs/Features/Goal目标驱动执行与分层验证闭环设计-2026-09-15.md`（第二版）
对应提交：`62fe53238c8b3b541b199f7d347d7be3ddcc4055`「docs(goal): simplify lifecycle and separate agent goal storage」（**仅文档**）
状态：**已收到设计；本文件只是落地映射与影响范围，不代表实现完成**

---

## 0. 授权边界（本文的自我约束）

- 本文**不**改代码、**不**改运行配置、**不**动数据库、**不**部署、**不**变更任务状态、**不**接收/覆盖他人 WIP。
- 设计更新、报告提交、收信成功**都不是**实现完成；本文按此口径书写。
- 不因收到本设计而绕过审批；不自动重开任何任务。

---

## 1. 我此前判断被新版否决 / 更正之处（**必须先更正，避免沿用旧前提**）

| # | 我此前的立场 | 新版规定 | 我的更正 |
|---|---|---|---|
| 1 | 为解 B1，我倾向 **A：放宽 `LlmToolApprovalReviewer` 路径 B 守卫**，使 `ProviderId+ModelId` 可解析 | ADR-092 第二版：「**本次不修改其策略，不选择放宽审批来解决依赖故障**」；设计 §1：审批「返回准入结果；Goal 无旁路、无自己的审批系统」 | **撤回 A/B 倾向**。B1 属 **ADR-091 审批安全域**的依赖可用性问题，不在 Goal 域内解决，**不得以放宽门禁换效率** |
| 2 | 我把 `goal.md`（超 `goal_read` 16 KB 上限）的归档当作需要优先扫清的**前置阻塞** | 设计 §1：Goal 的启动/执行/续行/等待/暂停/恢复/预算/完成判定「**只依赖自己的结构化持久数据**」；「**不得要求读取、压缩、归档或写入 `goal.md` 才能推进**」；「缺失、损坏、超限、拒绝访问该文件均**不改变** Goal 生命周期」 | **降级**：goal.md 归档是 **Agent 自身卫生**（影响我自己的上下文恢复），**不是** Goal 模式的修复前置，不阻塞任何 Goal 工作 |
| 3 | 我把「独立 Goal 因工程门禁合同通过而 Completed」记为需补合同覆盖的**缺口** | 设计 §11 把它列为**固定负例**：「G92反例：build/test绿而业务条件缺失 ⇒ 不得 Complete」；且「固定负例覆盖**不可删**」 | **完全一致，保留**。卡 `600d5a9c` 的回归断言与新 §11 同一语义，仅需**对齐编号与门槛措辞** |

> 结论：我对「业务误完成」的判定与新版一致；但我对**修复路径**的两条倾向（放宽审批、goal.md 归档优先）**已被用户明确否决**，不再沿用。

---

## 2. WIP 映射：保留 / 改造 / 删除

### 2.1 可保留（与新版一致，不需改造）

| 既有物 | 位置 | 新版依据 |
|---|---|---|
| Goal 专属持久表（`goal_runs` / `goal_acceptance_contracts` / `goal_iterations` / `goal_check_records` / `goal_verifications` / `goal_outbox`） | 平台库 | 设计 §4 表结构职责；ADR 决策 2 |
| 版本 CAS、epoch 隔离、执行租约 fence、事务 outbox 与幂等 | `GoalSettlementStore` 等 | ADR 决策 10「可靠性机制内聚」 |
| canonical Turn 作为唯一回合执行体 | Agent Runtime | 设计 §2「执行回合」 |
| 受控检查的**准入 / 超时 / 取消 / 租约** | `GoalCheckRunner`、`GoalCheckRecordStore` | 设计 §6「受控检查复用既有准入、超时、取消和租约」 |
| 检查输出读取的既有修复（`46b9aa76` 读尾部、`fde428d7` 字符预算、`bca022c8` 输出排空、`abe29e5a` UTF-8 解码） | `GoalCheckRunner` | 设计 §6「优先读结构化报告，文本只作诊断」——这四项让文本诊断可用，**保留** |
| 用户控制与既有预算服务 | `/goal`、HTTP 命令、预算服务 | ADR 决策 8 / 设计 §8、§9 |
| `Tools/Diagnostics/goal-ledger/`（只读账本探针，受版本控制） | 本仓库 | 中性诊断工具，用于核验新旧枚举映射与账本事实 |

### 2.2 应改造（有价值但需按新版重定语义）

| 既有物 | 现状 | 需要改成 | 归属切片 |
|---|---|---|---|
| `GoalCheckInputIdentity.cs` + `GoalCheckInputIdentityTests.cs` | 已提交 `e3418b44`（单元级 11/11）并完成 **§6 对齐改造**（与本行同批提交）：4 分量（sources/config/deps/checks）按项目闭包采集，**新增声明分量** `declaredRelevantPaths`（存在→原始字节；缺席→`declared-absent:<path>` 条目，使其随后出现也改变身份）与**环境分量** `buildId`（只记 SHA-256，不落原始值）；单元级 **15/15** | ① ✅ 按 §6「检查声明的相关文件」；② ✅ 补「适用环境 / BuildId」；③ ✅ 维持不归一源码字节、`HEAD` 仅作来源；④ **仍待**：接线（S2）与「执行前后输入身份未被受管写操作改变」的冻结校验（§6 后段） | S1/S2 |
| `GoalAcceptanceContractPlanner` | 目标无显式证据时**只写 engineering-gates 合同 + warning** | 工程门禁**只能补充合同，不得兜底成为未知目标的完整合同**；未覆盖义务必须可见 | **S1** |
| `ConservativeGoalIterationVerifier` | `allRequiredPassed && goalScope` 即可完成独立 Goal | 收敛为**唯一决策入口**的判定（`Complete` 必须同时满足 §5 四项） | **S1** |
| `GoalResumeService` / `goal_resume` 工具 | 独立 resume 链 | 保留用户控制语义，但恢复`按持久 ResumePolicy`、走统一入口；恢复扫描只恢复同一持久工作 | **S2** |
| Task ↔ Goal 绑定与结算 | 绑定 + 多处终态写入 | 适配器只提供四项能力；**存活绑定唯一写入口**；Task/Goal 同事务完成并校验绑定版本与 fence | **S3** |

### 2.3 应删除 / 停止扩建（新版明确移除）

| 被移除物 | 我的关联情况（如实） | 处理 |
|---|---|---|
| Goal 强制 Plan / WorkUnit / `CurrentWorkUnit` / `RemainingWorkUnits` / 推进 `task_nodes` | 我在 v2 矩阵与卡 `b849ef8d` 中把它当作验收对象之一 | 停止扩建；验收口径改为新 §11 |
| **两级 Verifier**（步骤级 + 整体级） | 我此前的七条标准里含「步骤/整体两级」口径 | 停止沿用该口径 |
| **工程门禁兜底完成** | 我在卡 `600d5a9c` 中已登记为缺陷（方向一致） | 作为**固定负例**保留（不可删） |
| `goal_queue.json` 旧循环的生产注册 / 提示注入 | 与本实例无直接 WIP | 由 S4 关闭/删除；Runtime 旧 GoalMode 一并处理 |
| 常驻双实现兼容层 | 我未新增兼容层（本次未获授权） | 不得新增 |
| 多处 Task 终态写入（Tracker / Repair / 其他结算服务） | 我在 G6 登记的写入入口口径一致性（缺陷 `bf7ef5f5`）**同向** | 归 **S3** |
| 模型轮询式等待、同一拒绝反复委派 | 我在卡 `71c11991` 登记的 G10 **同向** | 归 **S2/S8** |

### 2.4 非我所有、**不得触碰**的既有 WIP

`code_map.md`、`Source/PuddingCore/Platform/AgentProjectionDtos.cs`、`Source/PuddingCoreTests/ResponsesLlmGatewayTests.cs`、`external/github.hyfree.GM`、`How-Debuge.md`、`Docs/Reports/` 下他人新增文件、以及工作区中他人未提交的 Goal 相关契约文件。

---

## 3. 状态枚举迁移映射（实现时必须显式，**不得重解释旧数字**）

| 旧（第一版） | 新（第二版） | 处置 |
|---|---|---|
| `Active` | `Running` | 显式映射 |
| `Blocked` | `Waiting` 或 `Paused` | **按原因**选择，不能一律映射 |
| `BudgetExhausted` | `Paused(budget)` | 预算不足是 `Paused` 的原因，不是独立持久状态 |
| `Completed` / `Cancelled` / `Failed` | 同名 | 保留原始状态、合同与证据；**不**批量改成新合同验收成功 |
| 无法判定的旧活跃目标 | — | **暂停待核**，不默认为可运行 |

迁移纪律（设计 §10）：未完成目标须**补齐合同并重新准入**才能续行；迁移本身不保留常驻旧模型兼容层。

---

## 4. 与新 §11 验收门槛的对应关系

| 新版 §11 场景 | 我已登记的载体 | 状态 |
|---|---|---|
| Agent `goal.md` 缺失/损坏/超限/无访问权限 ⇒ 相同 Goal 生命周期 | 卡 `516a19e6`（我登记为「审批依赖阻塞 agent 私有状态维护」） | **同向**；本设计明确「其维护故障单独处理」⇒ 该卡**独立成立** |
| G92 反例：build/test 绿而业务条件缺失 ⇒ 不得 Complete | 卡 `600d5a9c`（G9 合同覆盖不足） | **同向**，按 §11 对齐 |
| pending 检查 / 审批依赖未变化 ⇒ 无轮询式 LLM、无必败委派，事件后**恰好一次**有效决策 | 卡 `71c11991`（G10） | **同向** |
| 相关/无关输入变化、执行期间改动 ⇒ 旧证据按依赖范围失效 | `GoalCheckInputIdentity`（2.2 改造项） | 需补 BuildId/环境分量后重测 |
| 重复事件/超时/崩溃/暂停取消后旧结果 ⇒ 单次结算、无复活、fence 拒绝 | G3/G4（旧矩阵缺口） | 映射进 **S2** |
| Task-bound 完成与 Tracker 竞争、CAS 冲突 ⇒ 无双写、无半终态、无孤儿认领 | G2/G6（`2b60040d` / `bf7ef5f5`） | 映射进 **S3** |

---

## 5. 我**不会**做的事（授权边界，明确声明）

1. 不因本设计而改代码、部署、重启、清库或改 Schema —— 切换须由**外部控制器**在**无存活执行者**的安全窗口进行（设计 §10）。
2. 不自动重开 / 不擅自恢复任何任务（含 G92-0/G92-1）—— 本轮设计变更**未**变更看板或执行授权。
3. 不覆盖他人 WIP；不动 `code_map.md` 等非我文件。
4. 不用放宽审批的方式解决依赖故障。
5. 不把本映射、文档提交或收信成功称为实现完成。

---

## 6. 核对项状态（**执行通道于 19:21 恢复后已补做**）

| # | 项 | 状态 | 结果 |
|---|---|---|---|
| 1 | Goal 路径未提交清点 | ✅ **已完成** | 见 §6.1 |
| 2 | 他人 WIP 归属复核 | ⚠️ **部分完成** | 见 §6.2；发现一张 Goal 测试文件被他人改动 |
| 3 | `GoalResumeContracts.cs` 等是否在途 | ✅ **已核实** | `Source/PuddingCore/Goals` 全 12 文件 mtime 均 ≤ 2026-09-16T15:48:56Z，且 `git diff HEAD -- Source` 未列出 ⇒ **无未提交在途** |
| 4 | S1–S4 工作量与依赖顺序评估 | ❌ 未做 | 需在授权实施轮内做 |

### 6.1 Goal 路径清点结果（`git -C <repo> status --porcelain -- <两个 Goals 目录>`）

```
   M Source/PuddingPlatformTests/Services/Goals/GoalCommandServiceTests.cs      ← 他人在途，⚠️ 不是我的
  ?? Source/PuddingPlatform/Services/Goals/GoalCheckInputIdentity.cs            ← 我的（已提交 e3418b44）
  ?? Source/PuddingPlatformTests/Services/Goals/GoalCheckInputIdentityTests.cs  ← 我的（已提交 e3418b44）
```

`git diff --name-only HEAD -- Source` 列出 19 行（含 9 行 CRLF 警告），去重后 **11 个被修改的已跟踪文件**，其中：

- **Goal 相关仅 1 个**：`Source/PuddingPlatformTests/Services/Goals/GoalCommandServiceTests.cs`（**非我所有**）
- 其余 10 个属 Chat/投影/前端/ContextPipeline 域（`AgentProjectionDtos.cs`、`ResponsesLlmGatewayTests.cs`、
  `AgentConversationProjectionService.cs`、`PuddingPlatformAdmin` 下 6 个 ts/tsx、`ContextPipelineSkillLayerTests.cs`）
  ⇒ **均非我所有，本文未提交任何一项**。

⇒ 结论：**Goal 域的在途工作只有我这两个文件**（本轮已提交）+ 他人 1 个测试文件（不得触碰）。

### 6.2 归属风险（新增）

`GoalCommandServiceTests.cs` 被并发工作流修改且尚未提交 ⇒ ① 我不得提交它；② 它就在
`Source/PuddingPlatformTests/Services/Goals/` 目录内，**后续任何人做“提交该目录”的批量操作都会误带走它**。
建议：提交一律显式逐文件列举，禁止目录级批量提交（本轮已按此执行）。

### 6.3 工作区已前进（需知悉）

本文件写入时的 HEAD 为 `4adc418d`；补做清点时已前进到 `28abc7fe`（他人提交：
`28abc7fe docs(reports): 索引权威副本改放受跟踪位置`、`67c917d4 chore(tests): 合并 OCE 断言处重复的 F06 注释`）。
⇒ 工作区存在并发提交者，我的每次提交均以**显式文件列表**限定范围。

---

## 7. 本轮新增的执行证据（原「未验证」项已兑现）

19:21 执行通道（`terminal_start`）恢复，已补做编译与单元测试：

```
dotnet test Source/PuddingPlatformTests/PuddingPlatformTests.csproj
  --filter "FullyQualifiedName~GoalCheckInputIdentity"
→ Passed! - Failed: 0, Passed: 11, Skipped: 0, Total: 11, Duration: 3 s
→ exit_code = 0；error lines = 0
```

- 验证时 HEAD = `28abc7fecd436e2f27e2125542816af698bec09c`；提交为 `e3418b44`。
- 覆盖 T1–T11（含 T10 二层传递引用失效、T11 A↔B 环保护终止）。
- 口径声明：这只达到 **单元级已验证**；仍**不**等于交付、**不**等于已接线、也**不**满足
  设计 §6 的完整输入身份定义（尚缺「检查声明的相关文件」与「适用环境 / BuildId」分量）。

## 8. 本轮**未**做的事（边界重申）

未改运行配置、未部署、未重启、未改数据库/Schema、未变更任何任务状态、
未自动重开或恢复任务、未触碰他人 WIP、未以放宽审批方式处理依赖故障。

### 8.1 `code_map.md` **本轮未更新**（有意为之，附原因）

操作规则要求「修改代码后检查 code_map 已更新」。但 `code_map.md` 当前处于
**他人在途未提交状态**（`git diff --name-only HEAD -- Source` 之外的 worktree 修改），
而 Git 提交以**整文件**为单位 ⇒ 一旦我编辑并提交 `code_map.md`，会**连带把他人未完成的改动一并提交**。

⇒ 取舍：**不触碰** `code_map.md`，并在下一次提交说明里明示；待其归属人提交或明确授权后再补 Go​al 相关条目。
（这与「不提交他人 WIP」的硬约束一致，也与「code_map 应更新」的期望冲突时，**优先保护他人 WIP**。）

