# P0 卡依赖图与拓扑执行序（2026-09-19）

> 取证方式：`manage_tasks(action=list/get)` 只读快照（P0 全量 60 张：非终态 41 + Archived 19）。
> 未做任何写操作、未改卡状态、未跑 build/test。以下「依据」均为卡面原文摘录，无依据的推断单列并标「推测」。
> 口径校验：待规划 Backlog=22 ✓、未开始 Ready10+NeedsReview6=16 ✓、进行中 Blocked2+InProgress1=3 ✓，与用户描述一致。

---

## ① 依赖边清单（有卡面依据）

| # | 前置卡 ID | 后置卡 ID | 依据（卡面原文摘录） |
|---|---|---|---|
| G1 | `30eb371e` G92-0 | `b849ef8d` G92-1 | 「与 G92-1 按合同→回归→实现顺序交付」 |
| G2 | `30eb371e` G92-0 | `dc0ac9a8` G92-2 | 「依赖 G92-0/1 的 typed outcome」 |
| G3 | `b849ef8d` G92-1 | `dc0ac9a8` G92-2 | 「依赖 G92-0/1 的 typed outcome」 |
| G4 | `dc0ac9a8` G92-2 | `d85a107b` G92-3 | 「实施顺序：G92-0…G92-1…G92-2；本卡G92-3收口」 |
| G5 | `ef7d6378` | `2b60040d` | 「2b60040d 在本轮尚未被触达（卡在更早的计划编译阶段就失败）」 |
| G6 | `2b60040d` | `e2c35d6e` | 「根因层 3：无人认领 → 单向闩锁（已单独立卡）」 |
| G7 | `2b60040d` | `b849ef8d` G92-1 | 「标准 3 受缺陷 2b60040d…阻塞」 |
| G8 | `bf7ef5f5` | `b849ef8d` G92-1 | 「标准 5/6 缺端到端证据（…Blocked 单向闩锁 bf7ef5f5）」 |
| G9 | `4fcd4aa5` S1-c | `b849ef8d` G92-1 | 「S1-c…是 S1-a 的部署前置」「S1-c 与 S1-a 同批生效」 |
| G10 | `57809f83` ADR-076 S0 | `99fc3747` ADR-076 S1 | 「S0 可用性任务：57809f83…前置安全/正确性发布门禁」 |
| G11 | `8335911d` M02 | `a951d353` M01 | 「依赖 M02-A 最小快照读写后」 |
| G12 | `8335911d` M02 | `de986391` C03 | 「顺序修订：M02-A优先→M01…→M02-B/C03」 |
| G13 | `a951d353` M01 | `de986391` C03 | 「顺序修订：M02-A优先→M01…→M02-B/C03」 |
| G14 | `9494be33` C01 | `a8e5340e` C02 | 「先验收已有C01-B…再实施C02最终manifest」 |
| G15 | `a8e5340e` C02 | `a951d353` M01 | 「再实施C02最终manifest、M01启动上下文、C03后台miss」 |
| G16 | `f8a9c164` A91-0 | `8cc96d64` A91-1 | 「先修 A91-0 并复验，再进入 A91-1」 |
| G17 | `f8a9c164` A91-0 | `e187a8bb` ADR-091 | 「本阶段单独完成不能宣称自动审计系统完成」 |
| G18 | `8cc96d64` A91-1 | `e187a8bb` ADR-091 | 「真实开发工作流放行须 A91-0、A91-1 同时部署验收」 |
| G19 | `6f49d33e` 总任务 | `3bd2a4b0` | 「父任务：6f49d33e…」 |
| G20 | `6f49d33e` 总任务 | `c7dc8f5d` | 「父任务：6f49d33e…」 |
| G21 | `3bd2a4b0` | `bf7ef5f5` | 「原卡 3bd2a4b0 前提不成立…本卡替代原卡的方向」 |
| G22 | `38da54f1` | `ca80f48d` P0-S1a | 「父卡：38da54f1…」 |
| G23 | `38da54f1` | `3f987cef` P0-S1b | 「父卡：38da54f1…」 |
| G24 | `600d5a9c` G9 | `71c11991` G10 | 「与 G10…互为前提」／G10 侧「上游之一：G9」 |
| G25 | `666b07e0` V5 | `8ee47996` V6-T2 | 「与 V5 已落地的视觉单源化链路对齐：能力判定只允许一个来源」 |
| G26 | `06580c4d`(已完成) | `183b8f73` | 「承接已完成卡 06580c4d…的新证据 fix-forward」 |
| G27 | `06580c4d`(已完成) | `38da54f1` | 「本卡是已完成任务 06580c4d 的新证据 fix-forward」 |
| G28 | `6b3707d7`(非本清单) | `c7dc8f5d` | 「Blocked 恢复子任务 6b3707d7…全部完成并部署」（显式前置） |

### 推测边（无直接卡面依据，不得当事实用）

| # | 推测前置 | 推测后置 | 推测理由（须人工确认） |
|---|---|---|---|
| S1 | `a2849320` V6-T1 | `8ee47996` V6-T2 | V6-T1 修「唯一 capability 判定入口」，V6-T2 的「不得工具层二次判定」依赖它 |
| S2 | `600d5a9c` G9 | `30eb371e` G92-0 | 合同覆盖是 G92 家族「误完成」结算修复的前置，但两卡互未引用 |
| S3 | `9bdf9f06` 审批信任根 | 无方向 | 与 A91 家族同域（审批/授权），卡面只写「审批已改 LLM 单点把关，信任根零落地」 |
| S4 | `066f8c76`／`183b8f73`／`791d062f` | — | 三者与 `38da54f1` 同域（lease_lost／父Turn终态／子事件回放），无方向声明，判定为并列 |

---

## ② 拓扑分组

### A 组：无前置，可立即执行（18 张）
`ef7d6378`、`bf7ef5f5`、`e2c35d6e`、`d43e61db`、`066f8c76`、`183b8f73`、`600d5a9c`、`57809f83`、
`8335911d`、`9494be33`*、`a6a74a5a`、`a2849320`、`9bdf9f06`、`2e309562`、`791d062f`、`6495cee8`、
`f1d45a15`、`af25d72c`(InProgress 续跑)

> *`9494be33` 状态为 NeedsReview，作为 C01 链起点无前置，但需先裁决才能推进。

### B 组：需等待 A 组（15 张）

| 卡 ID | 标题摘要 | 等待 |
|---|---|---|
| `2b60040d` | 派发未注入 ActiveTask 元数据 | ← `ef7d6378` (G5) |
| `e2c35d6e` | Blocked 单向闩锁（legacy 写路径） | ← `2b60040d` (G6) |
| `b849ef8d` | G92-1 母卡 | ← `30eb371e`(G1)、`2b60040d`(G7)、`bf7ef5f5`(G8)、`4fcd4aa5`(G9) |
| `dc0ac9a8` | G92-2 Goal 持久等待与恢复 | ← `30eb371e`+`b849ef8d` (G2/G3) |
| `d85a107b` | G92-3 收口 | ← `dc0ac9a8` (G4) |
| `99fc3747` | ADR-076 S1 | ← `57809f83` (G10) |
| `a951d353` | M01 新Session减负 | ← `8335911d`(G11)、`a8e5340e`(G15) |
| `de986391` | C03 后台 miss 减量 | ← `8335911d`(G12)、`a951d353`(G13) |
| `a8e5340e` | C02 最终 Provider Manifest | ← `9494be33` (G14) |
| `8cc96d64` | A91-1 统一工具执行准入 | ← `f8a9c164` (G16) |
| `e187a8bb` | ADR-091 自动审计总卡 | ← `f8a9c164`+`8cc96d64` 联合部署门 (G17/G18) |
| `3bd2a4b0` | 统一 Scheduler 内核 | ← `6f49d33e`(G19)；且前提已被证伪 |
| `c7dc8f5d` | Scheduler 真实自动派发验收 | ← `6f49d33e`(G20) + 4 项前置 + 外卡 `6b3707d7` (G28) |
| `71c11991` | G10 等待期不得反复调模型 | ← `600d5a9c` (G24) |
| `8ee47996` | V6-T2 image_reader 去外挂化 | ← `666b07e0` (G25)；+ 推测 S1 |

### C 组：已阻塞／依赖外部决策或外部动作（8 张）

| 卡 ID | 状态 | 阻塞/外部依赖原因（卡面） |
|---|---|---|
| `3bd2a4b0` | Blocked（board_column=InProgress，**双口径背离**） | 前提「内核待新建」已被证伪；待重定范围或关闭，方向由 `bf7ef5f5` 替代 |
| `f8a9c164` A91-0 | Blocked（tracker-legacy-blocked） | 独立复审 needs_changes，F01–F06 未闭环；卡面自陈「未部署」 |
| `af25d72c` | InProgress | 7 日验收未通过（实测 98.32%，需再减 ≥1910 miss）；外部部署+跨日观测 |
| `2b60040d`／`4fcd4aa5`／`30eb371e`／`666b07e0`／`791d062f`／`9494be33` | NeedsReview | 均需人工裁决 disposition 才能转 Ready |
| `ad069621` | Backlog | 依赖 ①上游模型真实名探测 ②工作区外 `llm.providers.json` 修改 ③重启；与 P1 `b03caf63` 同批 |
| `2e309562`／`9bdf9f06`／`e187a8bb`／`b849ef8d` | Ready/Backlog | 验收均要求「外部控制器部署 + 新会话 smoke」，非源码级可闭环 |

---

## ③ 关键路径（最长依赖链 → 主线 G92-1）

**最长链（5 卡 / 4 边）**：

```
ef7d6378  →  2b60040d  →  b849ef8d  →  dc0ac9a8  →  d85a107b
(计划编译        (派发未注入        (G92-1        (G92-2        (G92-3 收口)
 general无kinds)  ActiveTask)        母卡·目标)     持久等待)
```

逐跳依据：G5（编译阶段先于派发）→ G7（标准3 阻塞母卡）→ G4（母卡声明 G92-2 在后）← G2/G3。

**并行支线（汇入母卡）**：
- `bf7ef5f5` →(G8)→ `b849ef8d`（标准 5/6 端到端证据）
- `3bd2a4b0` →(G21，取代关系)→ `bf7ef5f5`
- `4fcd4aa5` S1-c →(G9，部署门)→ `b849ef8d`
- 家族交付序（卡面直接声明）：`30eb371e`(G92-0) → `b849ef8d`(G92-1) → `dc0ac9a8`(G92-2) → `d85a107b`(G92-3)

**最长端到端链（跨簇并列最长，3 边）**：
- `9494be33`(C01) → `a8e5340e`(C02) → `a951d353`(M01) → `de986391`(C03)
- `f8a9c164`(A91-0) → `8cc96d64`(A91-1) → `e187a8bb`(ADR-091 总卡)

---

## ④ 建议执行顺序（前 10 张）

| 序 | 卡 ID | 一句话理由 |
|---|---|---|
| 1 | `ef7d6378` | 最上游结构性根因：`TryResolveKinds` 不含 general ⇒ 96.2% 卡结构性无法启动 Task-bound Goal，不修则下游全部在更早阶段死掉 |
| 2 | `2b60040d` | 紧随其后的第二道门：派发未注入 ActiveTask ⇒ G92-1 标准3 直接被堵；先裁决 NeedsReview 再转 Ready |
| 3 | `bf7ef5f5` | Blocked 逃生通道；既解 G92-1 标准5/6 端到端证据，又解锁 16 张僵死卡（**最高杠杆**） |
| 4 | `38da54f1` | 已 93%，最接近收口；其子片 `ca80f48d`/`3f987cef` 由此解锁（G22/G23） |
| 5 | `8335911d` M02-A | Token/Memory 链最上游（M02-A 优先→M01→C03），且是 InProgress 卡 `af25d72c` 的后续依赖源 |
| 6 | `9494be33` C01 | Token 链起点（先验收 C01-B 稳定工具定义），NeedsReview 需先裁决 |
| 7 | `57809f83` ADR-076 S0 | `99fc3747`(S1) 的显式前置；Web `/admin/storage` 三类 500 阻断可用性 |
| 8 | `f8a9c164` A91-0 | Blocked，须先闭环 F01–F06；它是 A91-1 与 ADR-091 总卡的唯一入口 |
| 9 | `a2849320` V6-T1 | 视觉链最上游 BUG（能力声明与运行期不一致却静默降级）；不修则 V5/V6-T2 判定口径分裂 |
| 10 | `e2c35d6e` | legacy 闩锁写路径，与 `d43e61db` 分界；不修则「无人认领→Blocked」永久化 |

> 未列入的次优项：`30eb371e`(G92-0，70%) 与 `dc0ac9a8` 需等 1–3 完成后按家族交付序推进；
> `c7dc8f5d` 需等 4 项前置 + 外卡 `6b3707d7`。

---

## ⑤ 孤立卡（既无前置也无后置，可自由并行）

`066f8c76`（崩溃恢复链 handoff）、`183b8f73`（Turn terminal 前置提交）、`a6a74a5a`（并行子代理写隔离）、
`9bdf9f06`（审批信任根加固）、`791d062f`（SA-TRACE 轨迹不显示）、`2e309562`（鉴权与配置加固）、
`6495cee8`（子代理弹性预算）、`f1d45a15`（终端 runner/file_patch bug）

- 共 **8 张**（不含推测边 S1/S3/S4 被采纳的情形）。
- `a2849320` 若采纳推测 S1 则退出孤立集；`ad069621` 与 P1 卡 `b03caf63` 属「同批处理」而非前后置，亦计孤立但需与 P1 联动。

---

## ⑥ 汇总计数

| 指标 | 数值 |
|---|---|
| P0 卡总数（原始） | 60（含 Archived 19） |
| **非终态 P0 卡总数（本次分析域）** | **41**（Backlog 22 / Ready 10 / NeedsReview 6 / Blocked 2 / InProgress 1） |
| 有依赖关系的卡数 | **32** |
| 依赖边数 | **28 条有依据**（含 2 条指向已完成卡 G26/G27、1 条指向清单外卡 G28）+ **4 条推测边** |
| 关键路径长度 | **4 边 / 5 节点**（`ef7d6378` → `2b60040d` → `b849ef8d` → `dc0ac9a8` → `d85a107b`） |
| 孤立卡 | 8 |
| A / B / C 组 | 18 / 15 / 8 |

---

## 附：取证风险与需人工确认项

1. **命名冲突（高优先确认）**：`4fcd4aa5`（「G92-1 [S1-c]」）文中出现的 **S1-a** 指 G92-1 S1 内的覆盖门切片
   （commit be04d37），而 `ca80f48d` 是另一体系的「P0-S1a」（Execution monitor fault）。两者**不是同一物**；
   边 G9 的落点若被误读为 `ca80f48d`，会跨接到完全不相关的租约域。建议在卡面显式消除歧义。
2. **母卡无 child 覆盖 S1-a/S1-b**：`b849ef8d` 的 `child_count=2` 仅为 `4fcd4aa5`(S1-c) 与 `b03caf63`(P1)；
   S1-a/S1-b 在卡片树上缺失（`ca80f48d`/`3f987cef` 的父是 `38da54f1`）。
3. **双口径背离实证**：`3bd2a4b0` 与 `f8a9c164` 均为 `status=Blocked` + `board_column=InProgress`，
   正是 `bf7ef5f5` 登记的口径分裂现场。
4. **陈旧 blocker 语义**：`d85a107b`、`666b07e0`、`6495cee8` 等卡携带
   `assignment_execution_missing` / `execution_terminal_without_task_settlement` 的 blocker 字段，
   `d43e61db` 已登记此类可能为旧语义残留 ⇒ **阻塞面可能虚高，不应据此判死**。
5. **调度开关**：41 张非终态 P0 卡中，仅 `b849ef8d`、`3bd2a4b0` 的 `auto_dispatch_enabled=true`。
   ⇒ **拓扑序确定 ≠ 会被自动执行**，仍需管理面 assign/run_now 或改配置。
6. **B 组内互相门控**：`30eb371e`(G92-0) 与 `b849ef8d`(G92-1) 在卡面同时存在
   「实施顺序 0 先于 1」与「整个 Goal 的完成等待 G92-1 真正验证」两条表述
   ⇒ 代码交付序明确，但**终态验收序是反向门控**，排期时勿按单向链理解。
