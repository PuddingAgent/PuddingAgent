# A 类 p0 六卡前置规划评估（pre-implementation plan）· 2026-09-18

- 评估人：glm-5.3-flash 代行（gpt-6-astra 上游仅接受流式不可用，用户已知并同意降级）
- 范围：纯只读（manage_tasks get/list + 代码只读），唯一写入物为本报告
- 基线：master HEAD ≈ `c604caf7`（2026-09-18 20:31 B1 审批修复）之后的工作树；行号基于当前文件，±3 行容差
- 方法：逐卡 manage_tasks get 读卡面 → 关键断言逐条对当前代码取证 → 四问裁决 → 结论标签
- v2 增补（同日第二轮独立复核）：全部关键断言二次取证；精确化行号、修正卡⑥一处细节、升级 1 项不确定性，见 §10

---

## 1. 全局事实基线（评估前提，全部已提交 master）

| # | 事实 | 证据 |
|---|---|---|
| B1 | **F3a ActiveTask 载体链已全部进 master**（09-14 工作区改动已被收编提交）：`Source/PuddingCore/Tasks/ActiveTaskMetadata.cs` 存在；`ExecutionCommandReader.cs:179` 注入 `ActiveTask = ActiveTaskMetadata.TryBuild(...)`；`AgentInvocationDispatchFactory.cs:134/:140-142` 携带并委托唯一映射器；`TurnExecutorAdapter.cs:56` 透传 `ActiveTask = context.ActiveTask` | git_log(file) + file_read 四处 |
| B2 | **GoalContinuationWorker 的 task.auto 键集写入仍在**：`GoalContinuationWorker.cs:139-176`（task_id/assignment_id/expected_version/priority/execution_window/dispatch_idempotency_key/reservation_fencing_token/task_plan_*/task_node_*） | file_read |
| B3 | **卡 813ad427 修复已落地**（commit `5bf1792`，09-14）：`TaskUpdateTool.cs:47 allowBlockedRecovery:true`、`TaskClaimTool.cs:47 allowBlockedRecovery:false`；`TaskToolModels.cs:317-325` 状态门槛 update 受理 InProgress/Blocked，claim 受理 Assigned/InProgress；注释明引 813ad427 与 3133b149（诊断上下文 inputs/lookup/ownership 已落地） | file_read |
| B4 | **RepairCoordinator 已非"只写 Blocked"**：`TaskExecutionRepairCoordinator.cs:36-53` decision 路由 5 类——`terminal/blocked_binding_still_active`→清理、`legacy_*`4 类→写 Blocked+释放（:140-255）、`continuation_lease_expired`→**re-arm**（:257+，outbox 重置 Pending+Signal）、`continuation_intent_missing/next_iteration_intent_missing`→**re-arm**（补写 GoalOutbox+Signal） | file_read |
| B5 | **GoalSettlementStore 已有 recoverableOutcome 通道**：`:1246-1248` Repair/ContinueCurrent/Wait=可恢复 ⇒ 不置 Failed、不释放 binding/reservation/assignment（`:1342-1357`）；仍单向的：Blocked/NeedsUser/Unsafe verdict 的 Task-bound attempt（`:1250-1346`：Goal Failed+binding terminal+`ReleaseReservation("goal_blocked")`:1316+`ReleaseAssignment(Failed)`:1319+task Blocked）与 BudgetExhausted（`:1359-1381`，:1365/:1367 释放） | file_read |
| B6 | **计划编译对 general 类型结构性拒绝**：`TaskExecutionPlanCompiler.cs:26` 默认码 `execution_plan_unavailable`；`TryResolveKinds`（`_ => []` :110、`return kinds.Length > 0` :112，v2 精确化）⇒ general→false。调用点：`TaskAutoDispatchEvaluator.cs:320`（派发评估 Denied）与 `TaskGoalDispatchTransactionStore.cs:95`（Goal 绑定事务） | file_read/grep |
| B7 | G92-1 S1-c 五片已交付：`711f109`（片1+2 text-assertion 定义+哈希方案A）、`fe36249`（片3 Runner 纯文本分支+终态文本供给链）、`123f821`（片4+5 EvidencePolicy 放行+证据锚）；`43898c1`（S1-b MS-1 删 scope/remaining 轴）；矩阵 §19（`b7fba30`） | git_log + 矩阵 §19 |
| B8 | B1 审批死锁已修（`c604caf7` 选项B：LlmToolApprovalReviewer 允许仅 ProviderId+ModelId）——C 类审批门禁卡的解锁条件在收敛 | git_log |
| B9 | 看板治理进度：09-18 已归档 36/36（矩阵 §19.2），当前 Blocked 全量 7 张（盘点报告 §5） | 盘点/矩阵 |
| B10 | `WorkUnitAwaitHandleEntity` 存在（`Source/PuddingPlatform/Data/Entities/WorkUnitAwaitHandleEntity.cs`）——dc0ac9a8 复用前提成立 | file_search |

---

## 2. 卡 `ef7d6378814e421482c41e42fcbf6e5b` —— Task-bound Goal 不可达：general 无执行计划

**卡面**：task_goal_start 对 general 卡三次实测 `execution_plan_unavailable`（decision=denied）；SQLite 实测 general=205/213（96.2%）结构性无法启动 Task-bound Goal；implementation 计划 16/16 Failed；Agent 侧无独立 Goal 入口（S3 缺口）。

- **① 任务是否正确：成立。** `TaskExecutionPlanCompiler.cs:26`（默认失败码）+ `TryResolveKinds`（`_ => []` :110、`return kinds.Length > 0` :112）⇒ general 编译必败；两处调用点 `TaskAutoDispatchEvaluator.cs:320`、`TaskGoalDispatchTransactionStore.cs:95` 意味着派发评估与 Goal 绑定事务**双闸**拦截。本次评估的 6 张卡本身全部 task_type=general——G92 全家族都在被此闸拦住。
- **② 方向是否正确：方向正确（修根因：类型→kinds 映射缺失），但卡面把两个缺陷捆在一起**：general 无 kinds（编译失败）与 implementation 计划 16/16 Failed（编译成功后的**运行**失败）是不同阶段的独立缺陷面，实施时必须拆分，否则单卡范围失控。
- **③ 更好的方式：有。** 候选：
  - 方案A：general 直接映射 `[Explore,Plan,Review]`（research 同款）——最小改动，但 205 张 general 卡语义异质，Plan/Review 单元多为空转，且按 `Budget()`（约 :123-141）每计划叠加 2-4.5 USD 成本上限，对轻卡过重；
  - **方案B（推荐）**：general → 单一轻量 WorkUnit（复用 Explore 或新增 `General` kind，轻 Budget），worker 在迭代内自主推进，验证依赖 G92-1 已落地的 text-assertion 合同而非硬性 Review 单元；与 ADR-092"Task 是可选适配器"决策一致；未知/显式不支持的 task_type 保留 `_ => []` 并**结构化拒绝**（code=unsupported_task_type + 可执行建议），满足卡面验收 2；
  - 方案C：只做显式拒绝——与 Goal 模式目标冲突，否决。
  - implementation 16/16 Failed 需先取 task_plan_runs 失败阶段证据（编译后哪个环节死）再定位，列为独立第二阶段。
- **④ 是否必要：必要，不可合并。** 它是 Task-bound Goal 全家族的前置闸：不修则 `2b60040d` 的注入链、`dc0ac9a8` 的等待/恢复在真实链路上不可达（卡面自己也记录"2b60040d 在本轮尚未被触达"）。
- **结论标签：`修正方向后实现`**（拆两阶段：阶段1=general kinds 决策（方案B）+ Denied 原因结构化；阶段2=implementation 16/16 Failed 独立定位）。
- **建议文件域**：`TaskExecutionPlanCompiler.cs`（kinds 映射 + Budget）、`PuddingCode.Scheduling` 的 `TaskWorkUnitKind` 枚举、`TaskAutoDispatchEvaluator`（denied reason 结构化）、`TaskGoalDispatchTransactionStore`（错误透传）、`Source/PuddingPlatformTests`（编译矩阵测试）。

---

## 3. 卡 `2b60040d6eb94db7800d0522698e3975` —— 派发未注入 ActiveTask 元数据

**卡面**：GoalRun tg-d685… payload 带 task 块但 task_claim/task_update 全报 `active_context_missing(stage=lookup/not_visible)`；根因层1=派发未注入；层2=反查身份口径不一致；层3=无人认领→闩锁（另立卡）。

- **① 任务是否正确：卡面根因层1 已不成立——F3a 载体链已全部进 master**（基线 B1/B2）：worker 写键（`GoalContinuationWorker.cs:139-176`）→ `ExecutionCommandReader.cs:179` 注入 → `AgentInvocationDispatchFactory.cs:134/:140-142` → `TurnExecutorAdapter.cs:56` 透传 → 工具侧 `context.ActiveTask` 非空直走注入路径（`TaskToolModels.cs:273-276`：注入存在时不做重建）。卡面引用的旧行号（AgentInvocationDispatchFactory.cs:141-153 的"任一为空 return null"内联实现）已被 `ActiveTaskMetadata.TryBuild` 唯一映射器替代。
- **② 方向是否正确：原始方向（补载体）正确且已实施。** 残余方向应是**端到端验收**与根因层2 的裁决：层2（写侧 `assignment.AgentId`=持久实例 id vs 读侧 `context.AgentInstanceId`，`TaskToolModels.cs` 约 :288-306）对 Goal 续跑主链不触发（`GoalContinuationWorker.cs:180` `AgentIds=[goal.AgentInstanceId]` 与写侧同源，口径一致）；仅 delegated/子会话场景（`PuddingToolContracts.cs:155-162` 语义）可能 not_visible——该残余属 G92-1 S3"Task 适配与入口统一"范围，不属本卡。
- **③ 更好的方式：无需新实现。** 本卡应 re-scope 为"Task-bound 派发 ActiveTask 端到端验收"：一张 general 卡（在 `ef7d6378` 修复后）从 Ready→reserved→assigned→accepted→progress 全链在同一 GoalRun 内 canonical 跑通，观察 `context_rebuild` 不再出现 stage=lookup/not_visible。这与 G92-1 标准 3 是同一次验证，可与 `ef7d6378` 成组派发、一次验收双卡。
- **④ 是否必要：修复已存在；验收必要。** 按用户硬约束，不得以"已解决"直接关闭——须以端到端验收完成后收口。
- **结论标签：`已由现有代码解决（附证据）`——残余工作收敛为端到端验收，验收完成后方可关闭；身份口径残余转 G92-1/S3。**
- **证据**：基线 B1/B2/B3 行号；`TaskToolModels.cs:273-276`（ActiveTask 非空即返回，不反查）。
- **建议文件域**：无代码改动；验收测试可加 `Source/PuddingRuntimeTests`（TurnExecutorAdapter 透传 stub）与 `Source/PuddingCoreTests`（ActiveTaskMetadata 键别名/缺键）各一条回归锁。

---

## 4. 卡 `e2c35d6eeae244c191ed508ccd85b6fe` —— Blocked 单向闩锁（RepairCoordinator 只写 Blocked 不 re-arm）

- **① 任务是否正确：卡面主断言（"只写 Blocked、从不 re-arm"）已部分过时。** 基线 B4：`TaskExecutionRepairCoordinator.cs:36-53` 现有 5 类 decision，其中 `continuation_lease_expired`→`TryRecoverContinuationLeaseAsync`（约 :257+，outbox 重置 Pending+`outboxSignal.Signal()`）与 `continuation_intent_missing/next_iteration_intent_missing`→`TryRestoreContinuationIntentAsync`（补写 GoalOutbox Pending+Signal）——**Goal 绑定路径已有两种自动 re-arm**。仍成立的残余：`legacy_*` 4 类（:140-255）依旧 Blocked+`ActiveAssignmentId=null`（约 :224）+assignment Failed（:227-231）+`tracker-legacy-blocked-*` 事件（约 :245-252），不回 Ready；worker 侧对"Blocked 且无 active assignment"仍无恢复通道（`TaskToolModels.cs` 约 :288-296 反查要求 `ActiveAssignment` 非空且匹配，置 null 后必然 assignment_stale/not_visible）——**闩锁终态只能管理面 resume/requeue**。派发扫描仅取 Ready|Deferred 的断言（卡面引 `TaskAutoDispatchEvaluator.cs:196`）本次未复核，倾向未变。
- **② 方向是否正确：方向正确（修根因：恢复通道缺失），但范围须按现状重划**：Goal 路径 re-arm 已做；剩余=legacy 闩锁终态的解除语义 + Blocked 分类语义（transient/needs_input）+ 双口径收敛。其中"Blocked 分类语义"在 Goal 侧已有 typed disposition 词汇表（基线 B5：Repair/ContinueCurrent/Wait=可恢复），task 侧 BlockerKind 应对齐。
- **③ 更好的方式：与本卡验收逐条对比后合并进 `bf7ef5f5`**（该卡验收 1-4 与本卡验收 1-4 几乎逐条同构：re-arm 幂等 / Status 单一事实源 / Blocked 态 task_get/update 契约化 / 僵死卡回填）。单独实施会造成同文件双卡并行。legacy 路径的处置建议：能自动重试的 transient→re-arm 回 Ready；需人工的→挂起投影+needs_attention；并显式声明"legacy Blocked+null 的 canonical 恢复=disposition=todo→Ready 白名单"（服务端状态机已允许 Blocked→Ready，`TaskToolModels.cs:317-318` 注释），或声明 legacy 只走管理面并写契约测试——二选一，须设计裁决。
- **④ 是否必要：残余部分必要，但应合并。** Goal 路径的闩锁主体已被 B4/B5 拦截（27 秒 blocked 的旧现场——Goal 结算打回——现在非 recoverable verdict 才走 task Blocked，且此后 Goal 已终态、由管理面 resume 创建新 fenced attempt 是 `GoalSettlementStore.cs:1254-1261` 注释的**有意设计**）。
- **结论标签：`合并到 bf7ef5f5`**（保留本卡全部代码证据与 legacy re-arm 残余点，作为合并后的实施输入；不得自行关闭，由父级执行合并）。
- **建议文件域（并入 bf7ef5f5 后）**：`TaskExecutionRepairCoordinator.cs`（legacy 分支 re-arm/挂起二分）、`TaskToolModels.cs`/`TaskAgentCommandService.cs`（Blocked+null 的 todo→Ready 白名单与契约）、`TaskAutoDispatchEvaluator.cs`（Blocked 分类与扫描口径）、`Source/PuddingPlatformTests`。

---

## 5. 卡 `dc0ac9a8236d4994ad04f0d9f860779c` —— G92-2 持久等待与恢复

- **① 任务是否正确：卡面所修的旧 F1 主体已被 G92-1 实现，剩余部分成立。** 基线 B5：`GoalSettlementStore.cs:1246-1248` recoverableOutcome（Repair/ContinueCurrent/Wait）⇒ 不置 Failed、不释放、Goal 保持 Active、outbox 续投（:1342-1415，DueAtUtc 按可恢复性决定）——旧 F1"恢复性结局一律关闭通道"已不存在。仍单向的恢复性结局：**Blocked/NeedsUser/Unsafe verdict**（:1250-1346，Task-bound attempt 直接 Goal Failed+释放全套；注释 :1254-1261 说明这是为保 active-Goal invariant 的有意设计，恢复靠 Task Resume/Requeue 新建 fenced attempt）与 **BudgetExhausted**（:1359-1381）。即"等待释放执行资源、事件到达重验再预约"的持久等待语义（卡面核心诉求）尚未实现——当前 Wait 只是"立即重试"。
- **② 方向是否正确：正确。** 复用 `WorkUnitAwaitHandleEntity`（基线 B10 存在）+ 统一 admission 重验 + 新 fence，与 ADR-092 一致；卡面已修订旧 F1 并声明"禁止放宽 assignment.stale"（正确，`TaskAgentCommandService` 的 stale 判据是归属安全边界）。
- **③ 更好的方式：范围收敛为两刀。** 刀1（小）：BudgetExhausted 从"Failed+释放"改为"暂停+保留归属"（对应卡面验收 1"等待或预算暂停不是 Goal Failed"）——只动 `GoalSettlementStore.cs:1359-1381` 一个分支+测试；刀2（大）：AwaitHandle 持久等待/依赖事件/deadline/resume/重启恢复。刀2 的真实链路依赖 `ef7d6378`（general 可编译）与 `2b60040d` 端到端验收，否则不可达。卡面"与 8df6a4d6 共享合同不接管所有权"维持。
- **④ 是否必要：必要，不可合并**（G92 家族唯一覆盖"长程等待与恢复"的卡）；排期必须排在 ef7d6378 / 2b60040d 验收之后。
- **结论标签：`继续实现`**（先刀1 后刀2；刀1 可独立先行）。
- **建议文件域**：`GoalSettlementStore.cs`（BudgetExhausted 分支 + AwaitHandle 结算交互）、`WorkUnitAwaitHandleEntity` 相关 store、`GoalContinuationWorker`（等待→续行触发）、admission/`TaskGoalDispatchTransactionStore`（重验+新 fence）、`Source/PuddingPlatformTests ~Goals`。

---

## 6. 卡 `d85a107b89da4bdf8fabbb6ecb82237a` —— G92-3 Goal 模式总收口

- **① 任务是否正确：成立。** 这是目标级收口卡（端到端结果门禁），8 条验收（同一 Goal ≥3 轮失败→修复→达成全程无"继续"、关浏览器+sleep=3600 仍按 outbox 推进、UI 区分步骤完成与整体达成、预算 600/2000 配置化、24h 无人值守证据、BuildId/canonical ID）均无兑现证据；G92-0（30eb371e，NeedsReview）与 G92-1（b849ef8d，Ready，active assignment d228340d）尚未完成。
- **② 方向是否正确：正确。** 实施顺序卡面自述 G92-0→G92-1→G92-2→本卡，依赖结构清晰；作为验收门禁卡不指向单一代码缺陷，不需要"更好实现"，但**验收条件需随架构演进微调**：①24h 观察窗与 ADR-092 typed wait/deadline 的交互要明确（等待期是否计入观察窗）；②"UI 能区分"一条应与 S3"Task 适配与入口统一"对齐（`ef7d6378` 卡面已登记 Agent 侧无独立 Goal 入口的 S3 缺口）；③预算两条与 `6495cee8`（子代理弹性预算纠偏）有部分重叠，验收时应避免双卡重复实现同一预算轴。
- **③ 更好的方式：维持收口定位，把验收 8 条映射到前置卡的交付物**（G92-0→结算正确性、G92-1→验证合同、G92-2→等待恢复、ef7d6378→入口可达），本卡只做"总验收+遗留缺口登记"，避免在本卡内重新实现。
- **④ 是否必要：必要，不可合并**（家族唯一端到端门禁）。
- **结论标签：`继续实现`**（前置依赖未完成前不派发实施，仅维持 Ready 作为收口锚点）。
- **建议文件域**：验收为主；代码面仅 UI（GoalBanner）与配置授权（256→正整数）两项，均已在卡面声明。

---

## 7. 卡 `bf7ef5f5e2d04d23ac3aed224ee539c3` —— Blocked 逃生通道 + Status/BoardColumn 双口径

- **① 任务是否正确：三条腿中一条已修、一条被治理消化大半、一条仍成立。**
  - "Blocked 态 task_update 直接拒绝 active_context_missing"——**已过时**：813ad427 修复落地（基线 B3：`TaskUpdateTool.cs:47` allowBlockedRecovery:true；`TaskToolModels.cs:317-325` Blocked 受理+归属校验），Blocked+active assignment 已可 canonical 上报；
  - "16 张僵死卡（Blocked+InProgress）回填"——存量已被 09-18 归档治理大幅消化（基线 B9：当前 Blocked 全量仅 7 张）；卡面的 Blocked 45/双口径分裂统计是 09-12 快照，**现状需重测 DB**（本任务无 DB 读权限）；
  - 仍成立：Blocked+`ActiveAssignmentId=null`（legacy cleanup 终态）的 worker 侧恢复缺失（同卡 e2c35d6e ①）；Status/BoardColumn **读侧收敛已投影化**（v2 修正：`WorkspaceTaskEntity` 无 BoardColumn 列，7 处读路径投影自 Status，见 §10.3），残余=一致性校验测试与 DB 存量清理；方案文档 §8 Blocked UI 无代码落地（前端本次未核查，列为不确定）。
- **② 方向是否正确：正确**（Blocked 分类语义=transient 自动回 Ready/needs_input 挂起投影；恢复路径白名单化；Status 单一事实源、BoardColumn 投影）。且 Goal 侧已有 typed disposition 词汇表（B5）可直接对齐，避免自造第二套语义。
- **③ 更好的方式：吸收 e2c35d6e 后单卡实施**（见卡 4 ③）；实施顺序建议：先服务端契约（Blocked 分类+todo→Ready 白名单+测试）→ 再双口径一致性校验/投影 → 最后 UI 或显式删除 §8。
- **④ 是否必要：必要**（Blocked 家族唯一幸存的实施壳卡）。
- **结论标签：`继续实现`（吸收 e2c35d6e 残余后实施）。**
- **建议文件域**：同卡 4 文件域 + 前端 Blocked UI 承载/删除决策 + `Source/PuddingPlatformTests`（双口径一致性校验）。

---

## 8. 依赖顺序与派发建议（供父级裁决）

```
ef7d6378（general 计划，方案B）──┐
                                ├─→ 2b60040d（端到端验收收口）──→ dc0ac9a8 刀2（AwaitHandle）
                                │        │
                                └────────┴─→ bf7ef5f5（吸收 e2c35d6e，可并行先行）
dc0ac9a8 刀1（BudgetExhausted 改暂停）可独立先行
d85a107b（G92-3）＝ 终点收口，前置全部完成后启动
```
- 高杠杆排序：**ef7d6378 → 2b60040d 验收**（解锁 G92 全家族真实链路）与 **bf7ef5f5**（解锁 Blocked 家族）两线并行；
- 全部结论为"建议改向/合并/继续"，**未对任何看板卡执行写操作**，关闭/合并/改卡由父级与用户裁决（遵守硬约束 a）。

## 9. 不确定性清单

1. 行号容差 ±3 行（基线为 2026-09-18 晚 master 工作树，后续提交可能位移）。
2. F3a 载体链的**具体落库 commit 未逐一定位**（git_log 显示 `ActiveTaskMetadata.cs` 至少自 09-18 12:22 起已在系列提交中；关键事实是当前 HEAD 已含全部四处接线）。
3. implementation 计划 16/16 Failed 的失败阶段无 DB 证据，仅引自卡面。
4. Blocked+InProgress 双口径与 Status/BoardColumn 分裂的**当前**量级未重测（无 DB 只读探针通道）。
5. （v2 已复核）`TaskAutoDispatchEvaluator.cs:196` = `AND status IN ({(int)WorkspaceTaskStatus.Ready}, {(int)WorkspaceTaskStatus.Deferred})`，「派发扫描仅取 Ready|Deferred」断言成立。
6. 前端 §8 Blocked UI 是否已有承载未核查。
7. 模型降级：本评估由 glm-5.3-flash 代行（gpt-6-astra 上游仅流式不可用），深度受 Flash 档限制，关键结论均以代码行号为锚降低误判风险。


---

## 10. v2 独立复核增补（2026-09-18 第二次评估运行）

本节由第二轮独立取证运行追加：对 §1–§9 的关键断言逐条重新读码/git 取证。**六卡结论标签全部维持不变**；修正 1 处细节（卡⑥第三腿）、精确化 2 处行号、升级 1 项不确定性。

### 10.1 逐项复核结果（本运行亲测，非继承上一轮）
| 断言 | 复核证据 | 结果 |
|---|---|---|
| F3a 载体链已进 master | 5 文件（ActiveTaskMetadata.cs / ITurnExecutor.cs / ExecutionCommandReader.cs / TurnExecutorAdapter.cs / AgentInvocationDispatchFactory.cs）`git_diff HEAD` 为**空**；`git_status` 无对应未跟踪条目；四文件逐一读码确认：`ActiveTaskMetadata.TryBuild` 唯一映射器（全文件 73 行）、`ExecutionCommandReader` 末尾 `ActiveTask = ActiveTaskMetadata.TryBuild(entity.WorkspaceId, entity.AgentInstanceId, metadata)`、`TurnExecutorAdapter` 请求构造含 `ActiveTask = context.ActiveTask`、DispatchFactory `BuildActiveTask` 委托唯一映射器 | ✅ 维持 B1 |
| F3a 具体 SHA | 近 60 条提交信息（09-17 18:11 → 09-18 20:32）无 ActiveTask/F3a 关键词 ⇒ 落库时间 ≤ 09-17 18:11，精确 SHA 未定位 | ⚠ 不确定性维持（§9-2） |
| general 编译必败 | `TaskExecutionPlanCompiler.cs:26`（默认码 `execution_plan_unavailable`）+ `:110`（`_ => []`）+ `:112`（`return kinds.Length > 0`） | ✅ 行号精确化 |
| 派发扫描仅 Ready\|Deferred | `TaskAutoDispatchEvaluator.cs:196` = `AND status IN ({(int)WorkspaceTaskStatus.Ready}, {(int)WorkspaceTaskStatus.Deferred})` | ✅ 原 §9-5 升级为已复核 |
| 813ad427 修复落地 | `TaskUpdateTool.cs:47`（allowBlockedRecovery:true）、`TaskClaimTool.cs:47`（false）、`TaskToolModels.cs:277`（签名）/:326-333（statusOk：update 受理 InProgress/Blocked，claim 受理 Assigned/InProgress） | ✅ 维持 B3 |
| mine 过滤现状 | `TaskAgentCommandService.cs:179`：`task is null || task.ActiveAssignmentId is null → null`（无 assignment 状态过滤、无 task.Status 过滤）——ActiveAssignmentId 已被置 null 的闩锁态必然 not_visible | ✅ 新增锚点 |
| 结算已分可恢复/不可恢复 | `GoalSettlementStore.cs:1246-1250`（recoverableOutcome = Repair/ContinueCurrent/Wait）、`:1342`（可恢复：不置 Failed、不释放 binding/reservation/assignment）、`:1319`/`:1367`（不可恢复 verdict / BudgetExhausted 才 `ReleaseAssignment(..., Failed)`，定义 :1693）、`:735`（`ApplyDeterministicGates`：evidence_incomplete → **BlockedDecision**，非 Failed）、`:764/:778`（IsTaskTerminalNonCompletable 否决完成）、`:1222`（NeedsUser 判决已存在） | ✅ 维持 B5 并加密行号锚 |
| 修复器对 task 闩锁无 re-arm | legacy 分支直读：`CanTransition→Blocked`、写 BlockerKind/Reason、`task.ActiveAssignmentId = null`、`assignment.Status=Failed + ReleasedAtUtc`、释放 legacy reservation、`AppendLegacyTaskBlockedEventAsync`——**无回 Ready 分支**；`TryRecoverContinuationLeaseAsync`（GoalOutbox 重置 Pending+Signal）是 Goal 续跑租约恢复，不是 task 闩锁恢复 | ✅ 维持 B4 主断言 |
| BoardColumn 双口径 | `WorkspaceTaskEntity` **无 BoardColumn 属性**；读侧 7 处全部投影自 Status：`TaskAgentCommandService.cs:709/726/748`、`WorkspaceTaskAdminService.cs:521/554`、`ExternalTaskController.cs:599`、`TaskController.cs:562` | ⚠ 修正卡⑥第三腿（见 10.3） |
| 6 张卡 ID 解析 | 六卡均以完整 32 位 ID `manage_tasks get` 成功（0 not_found）：`ef7d6378814e421482c41e42fcbf6e5b` / `2b60040d6eb94db7800d0522698e3975` / `e2c35d6eeae244c191ed508ccd85b6fe` / `dc0ac9a8236d4994ad04f0d9f860779c` / `d85a107b89da4bdf8fabbb6ecb82237a` / `bf7ef5f5e2d04d23ac3aed224ee539c3` | ✅ |

### 10.2 行号对照（v1 → v2）
- `TaskExecutionPlanCompiler.cs`：默认码 :23 → **:26**；`_ => []` 「约 :70-85」→ **:110**；`return kinds.Length > 0` → **:112**
- `TaskAutoDispatchEvaluator.cs:196`：未复核 → **已复核成立**
- 其余行号维持（±3 行容差不变）

### 10.3 卡⑥第三腿修正（不改结论标签，收窄实施范围）
v1 写「Status/BoardColumn 收敛未见代码承载」。v2 修正：**读侧收敛已实现**——BoardColumn 在实体层不存在独立列，所有读路径由 Status 实时投影（7 处证据见 10.1）。因此卡 `bf7ef5f5` 的「收敛双口径」残余实际为：
1. 双口径**一致性校验测试**缺失；
2. DB 存量是否残留 BoardColumn 列/脏行未实测（本轮无 shell/DB 通道）；
3. Blocked 分类语义与存量回填（Blocked 现 7 张，含 `3bd2a4b0`/`f8a9c164` 两张 p0）。
实施时**不得重做投影改造**。

### 10.4 v2 新增不确定性
8. `TaskExecutionRepairCoordinator` 的 `TryRestoreContinuationIntentAsync` 与 :36-53 decision 路由未逐行直读（本轮只直读了 legacy 写 Blocked 分支与 `TryRecoverContinuationLeaseAsync`）；B4 的「5 类 decision」框架采信 v1 记录。
9. `TaskAutoDispatchEvaluator.cs:320`、`TaskGoalDispatchTransactionStore.cs:95` 两个调用点行号未复核（引用 v1）。
10. DB 侧事实（workspace_tasks 是否残留 BoardColumn 列、Blocked 7 张的 blocker 字段现状、task_plan_runs 16/16 Failed 的失败阶段）无只读探针通道，全部维持卡面/盘点报告引用，派发实施前建议先用只读 SQL 复测。
