# G5-a 去噪影响评估（只读预演）

生成时间：2026-09-21 18:36:41 +08:00
技能根目录：D:\data\agents\default.global_general-assistant.6a8\skills

> **只读预演**：本报告不改 `CollectKeywords`、不写任何技能文件、不动 enabled 状态。
> 目的：在动生产行为之前，先量出每个候选口径的**收益（恢复的机会）**与**风险（变成零关键词死数据的技能）**。

基准 S0 = 现状口径，139 个启用技能。来源白名单指 CollectKeywords 的四类来源（显式 Keywords / Tags / SkillId / Name 与 Name 分词）。

| 场景 | 口径 | 槽位 | 去重 | 共享关键词 | 被挤掉机会 | 恢复 | 零关键词技能 | 平均每技能 |
|------|------|------|------|------------|------------|------|--------------|------------|
| S0 | 现状（全部来源，无过滤） | 2490 | 755 | 165 | 1735 | 0 | 0 | 17.91 |
| S1 | 去 D1：丢掉 Tags 来源 | 1690 | 601 | 125 | 1089 | 646 | 0 | 12.16 |
| S2 | 去 D1+D2：再去掉工具名 | 744 | 540 | 75 | 204 | 1531 | 0 | 5.35 |
| S3 | 去 D1+D2+D3：只留语义 | 269 | 269 | 0 | 0 | 1735 | 0 | 1.94 |
| S4 | 仅显式 keywords 且去噪 | 173 | 173 | 0 | 0 | 1735 | 4 | 1.24 |
| S5 | 仅显式 keywords（原样） | 1119 | 234 | 50 | 885 | 850 | 1 | 8.05 |

## 怎么读这张表

- **恢复** = S0 被挤掉机会 − 本口径被挤掉机会：去噪真正的收益（噪声不再抢占映射位）。
- **零关键词技能** = 该口径下没有任何关键词能把它拉进上下文的技能数。**这是唯一会伤到能力的指标**：
  它意味着「技能还在、但永远不会被触发」。任何口径若让该数显著大于 0，就必须先为这些技能补写**语义**关键词，
  否则等同于**静默停用**一批技能——这正是本评估要拦住的事故。

### 无「零关键词」风险的候选口径

| 场景 | 口径 | 恢复 | 被挤掉机会 | 平均每技能 |
|------|------|------|------------|------------|
| S1 | 去 D1：丢掉 Tags 来源 | 646 | 1089 | 12.16 |
| S2 | 去 D1+D2：再去掉工具名 | 1531 | 204 | 5.35 |
| S3 | 去 D1+D2+D3：只留语义 | 1735 | 0 | 1.94 |

选择仍须由人/判据决定：本报告只提供事实，不给建议值。

### ⚠️ 重要：上面的「零关键词技能 = 0」很可能是**假安全**

该指标只检查「是否还剩至少一个关键词」，**不检查剩下的关键词是否可能出现在真实输入里**。
S2/S3 剩余的关键词主要是 `Name` 全句与 `SkillId` 这类 slug（例如 `agent-repo-health-check`），
它们几乎不会出现在用户文本中 ⇒ 技能「有键但永不命中」，等价于静默停用。

**这说明单靠关键词形态过滤无法证明去噪安全。** 要回答真正的问题——
「现在到底是哪些关键词在真实触发注入？」——必须有**使用遥测（G2）**，本报告回答不了。

### 由此得到的重述：去噪不是「删关键词」，而是「给关键词定主」

1735 次被挤掉的机会，本质是**共享关键词的归属未裁决**（`map[kw]` 先到先得），
而不是「关键词太多」。因此正确动作可能是：

1. **保留**有真实命中的关键词（哪怕它是工具名），但让它**有主**——同一关键词只归属一个技能；
2. 被挤掉的技能改用**各自的语义关键词**（而非共享的工具名）来触发；
3. 只有确认「无命中、且无主」的关键词才删。

⚠️ 走哪条路**取决于 G2 遥测**：若真实注入主要靠 D2 工具名命中，则 S2/S3 会**直接切断现有注入路径**，
那是比噪声更严重的能力回退。⇒ **G5 的实施前置条件是 G2 遥测**：先有命中事实，再谈删或定主。

## 方法与边界

- **只读**：全程不写技能文件、不改 manifest、不动 enabled。
- **口径复用**：与 G1 报告共用同一份 `CollectKeywords` 复现逻辑（同一脚本的 `-Mode portfolio`），避免两处真相各自漂移。
- **确定值**：槽位、去重、共享数、被挤掉机会、零关键词技能数、平均每技能数。
- **未覆盖**：本报告不模拟「用户输入命中哪几个关键词」，因此**不能**回答「某个具体输入下注入集合如何变化」；
  那是 G5-b 实施时必须补的注入行为对比（需要运行时链路）。
- **不改判据**：本报告不设定任何阈值，不宣布哪个口径「正确」。
