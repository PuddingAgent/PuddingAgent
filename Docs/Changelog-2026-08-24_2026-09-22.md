# PuddingAgent 升级日志（2026-08-24 ~ 2026-09-22）

> 统计口径：该区间共 **938 条提交**（无 merge 提交），分支 `master`，作者为 hyfree（含多个 Agent 身份）。
> 其中 `feat` 163、`fix` 168、`perf` 9、`chat` 8；`docs` 371、`test` 52、`chore` 13、`refactor` 8、`style` 3 等属内部工程提交，已归并或不再逐条列出。
> 每条功能点为一句话，代表提交以短 sha 标注。区间内 2026-09-05 ~ 09-10 无提交。

## 新增功能

- 新增 SKILL Hub 中央技能库：后端服务 + Admin 六页签控制台 + `skill_hub` 工具，支持跨 Agent 分享与安装技能。`ff15a1b, 70c83d3, d23ab5c`
- SKILL Hub 打通写入路径与启动链 Schema 注册，宿主机凭据按 scheme 解析，不再静默回退到 `:5000`。`6798f5e, 4ae9bd6, 723ac54`
- 新增潜意识 `skill.curate` 整理作业：自动抽取会话轨迹并按 C1–C5 门禁筛选，只报告不改技能。`ffdaa04, 6de869b, 8d8fb22`
- 新增技能家族能力：家族判据、名称分词聚类、家族内数量上限与关键词归属裁决。`0023baa, 67a8731, 14e09b3`
- 新增技能组合预算判定器，并接入技能晋升路径，置换副作用可回滚。`25c88f9, 99faf03, 200fbe6`
- 新增技能价值打分器与技能使用遥测，并提供技能命中榜只读报表。`b66e0f5, 8c86a6e, accb805`
- 新增工具调用安全分类器分层：规则分类器、规则策展器、确定性求值管线与临时完全访问授予（TTL + 服务端计时 + 审计）。`57863dc, 19bfd5f, efa83cd, 2cf1c9d`
- 新增审批门户工具 `request_tool_approval` 与稳定拒绝码，工具审批改由分类器 + 评审器接管。`a1a526b, 1a5293f, f24f1b2`
- 接入 Jev 决策模型作为审批评审器与仲裁分类器，支持资源池 apiKey 与真实联网校验。`114bbe8, 008591d, 392c4fc`
- 新增 Agent 级访问级别存储：运行模式跨重启持久化，聊天权限简化为两档并支持 5 分钟临时授权（`/yolo`）。`b45dcec, bba41b6, a0e7902`
- 新增 Goal 模式：Agent 可自主进入、恢复与延长目标（`task_goal_start`、`goal_resume`、`/goal extend`）。`ab10af5, a64c436, 3d4661c`
- 新增 Goal 受控检查与证据防伪：验收合同 upsert、真实进程执行器，0 测试或未结束后台进程不得通过。`17ca5ee, 30ea122, 4d5980f`
- 新增 Goal 恢复与熔断策略：持久 ResumePolicy、无进展熔断、裁决降级与预算收尾。`cdbf48a, 1a5268e, 1c0e9e7`
- 新增拆解 TODO 工具族 `todo_write` / `todo_read` / `todo_check`，带服务端约束与 Agent 隔离。`5389947, 5ec7ba3`
- 新增任务看板母子任务层级：容器母卡、子卡依赖与执行者只读投影。`6423c52, ed2f3de, e8e14f4`
- 新增任务看板自动派发：事件驱动调度、持久化派发决策、扫描日志与统一结算。`2d8d02d, e2d3e15, fa0e936`
- 新增 Goal 生命周期工具 `goal_start` / `goal_pause` / `goal_cancel`，并让 `manage_tasks` 暴露 `task_type` 与卡片依赖。`d401879, ed00a68`
- 新增 Goal 持久控制面：`/goal` 命令、重启 disarm、Web 状态条与自主续行。`12dfd28, d7156f2, 41abffe`
- 新增 goodput 归因聚合与阶段感知模型路由确定性求值器（`agent_diagnostics` 提供只读归因动作）。`d23ef52, fa9a853, 7cbd416`
- 新增图片本地预处理能力，供 Agent 原生视觉读取。`74ae4e0, 52c0aae`
- 视觉链路升级：大图走 Files API（`file_id` 复用 + 生命周期管理），`image_reader` 支持裁剪、缩放、灰度等变换。`41917ac, 44c022b, 0a9807b`
- 新增供应商余额查询（DeepSeek 适配器）与 Chat 余额指示器、悬浮卡。`366191a, 99b3a86`
- 新增存储管理：语义目录、单 writer 协调器、自动保留调度与 Web 存储管理页（ADR-076）。`c3deb2d`
- 新增外部 Workspace Agent API V1。`68df30f`
- 新增用户头像完整链路（唯一上传契约 `/api/users/{userId}/avatar`，管理端可为指定用户上传）。`5d612ef`
- 新增 steering 按 `target_turn_id` 精确受理与消费。`f2ace33`
- 新增 Desktop 引导自启与核心启动进度展示、调试模式前端构建部署服务。`667bb21, 3696b5f`
- 新增工具审批防火墙与 LLM 复核员，并在授权时记录工具定义哈希。`7fbac38, 42e3c19`
- 新增工具曝光计划在用户轮边界冻结，并按工具定义身份精确恢复。`7b6233c, b29355f, defd1dd`
- 新增文件搜索与检索的统一匹配合同与覆盖率声明（ADR-089 RetrievalGlobMatcher）。`bf6f91e, ff79f3b, 2ac62ad`
- Chat/Admin 新增插嘴、Turn 取消、消息队列、模型重试与行为链交错渲染。`5a462e7, 14feceb, 20de2a0`
- 新增办公文档读写能力盘点，`ppt-master` 原生可编辑 PPTX 能力进入集成。`88b8e8d, d8507f4`

## 体验优化

- 上下文面板改为分层彩色进度条并新增图例，工具栏以圆环替代旧指示条。`fec9e99, bd37cb8, 4ab56dc`
- 压缩卡重新设计，修正假压缩态并显示真实 compaction 身份。`b3d1e50, e01da55, aab7f5c`
- 消息卡运行态显示实时时长，气泡头部上移至卡外、推理段可折叠、操作行常驻以消除抖动。`c41af70, a800b95, 8ed4c5b`
- 工具步骤行统一语言标签与结果态，折叠行直接显示 diff 摘要。`458176f, 1b84946, 4ee5af8`
- 消息卡宽度响应式适配，剩余上下文口径与窗口对齐，图片超限后保留文本继续输入。`2d7921c, adc09ff, db654d3`
- 压缩卡与 GoalStepsPanel 瘦身，Chat 路由首屏体积通过门禁。`2cf63ba, e53c79d`
- 长消息卡片支持折叠显示并可展开阅读。`e1075ad`
- 工具 IN/OUT 面板按主题感知配色，浅色/深色终端各自适配。`1597389`
- 前缀缓存优化：追加可变目录不再重写 system prefix、跨轮保持工具顺序、保留已验证 warm 前缀、会话驻留 4 小时。`b0bcf21, 9fabee4, 7c96d1f`
- 压缩策略抑制无收益的重复压缩，改为按模型压力与当前用量触发。`c582009, 572c394`
- 重启链路支持可选前端重建（`frontend_mode`），避免重启后界面仍是旧版。`69e0b8a`
- git、终端、记忆、测试类工具降为低风险，免运行时审批（AutoAllowed）。`8e6200e, 911cbaa, 7698788`
- Admin 新增分类器健康面与 `classifier_status` 只读探查，补齐故障提示与图标。`5defb57, ee62531, 3836db5`
- 任务看板 UI 与执行追踪、backlog 精炼接线。`446bcbb, 40b3105`
- 记忆调度遥测降噪：按 workspace + 5 分钟聚合，telemetry 收归单一 owner。`8377491`
- 全文索引与历史检索跨重启可用，检索查询有界。`4680cc5, 95a0cd3, 8ddd0d3`

## 问题修复

- 修复 SKILL Hub 白屏、CJK 关键词/标签检索失效，以及旧 SPA 响应被浏览器长期缓存复用。`85b05cd, ec3af47, 89318a4`
- 修复 139+ 个 SKILL.md 元数据与摘要退化，官方校验通过数从 0 提升到 133/144。`a17cac4, 6bad17b, 6b6f228`
- 技能索引重建改为自愈 ContentHash，并清理 141 个垃圾摘要。`f48a3a2, eaeca39, ecd921c`
- 修复审批死接口与过期记录回收，并补齐 Chat 审批决定端点的契约回归。`c1c2abf, 8d5fc2d, 7991acc`
- 审批防伪加固：`ResolvedBy` 改由认证上下文决定，确认码恒时比较并限制失败次数。`3e6aa0f, 65b0de5, bfbf745`
- 审批接口补鉴权、脱敏与去匿名化，bootstrap 不再匿名可读。`6fb90d5, f20a854, f1f7c69`
- 评审输入按不可信数据处理，超出预算时 fail-closed 升级为人工审批。`2e4f728`
- 命令防火墙覆盖 `terminal_execute`，审批命令改为「成界完整」匹配。`cfc0d1b, fd51e35`
- 分类器权威与来源修正、覆盖审计失败告警，审计事件 wire 名收归单一来源（修复 13 个事件筛选失效）。`3b28114, 7012e34, 4d5347a`
- SSE 建立「回放 vs 实时」不变量：游标显式化，410 走可恢复分流。`08860a4, 1a34291, 8d2bbb8`
- 上下文用量六桶落账与 DB 回填，修复 SQLite 排序导致读取静默失效。`cf48f85, 01a6641`
- Goal 结算语义修正：回合结束或单单元通过不再等同于目标完成。`fcca460, 87c1b17, aabd222`
- Goal 状态判定修正：Failed/Killed 不再误报等待，UnmetCriteria 给出真实清单。`67f27d2, b539c08`
- Goal 受阻码提供中文映射与结构化受阻说明，并在 Admin 暴露。`46d00eb, 6daff42`
- 修复受控检查 Test 类恒报 `test_count_unknown`（改读输出尾部并支持中文解析）。`46b9aa7, bd77214, fde428d`
- 修复看板 Blocked 卡的 canonical 恢复通道与 ActiveTask 载体。`5bf1792, d7ec01f`
- 修复 Goal waiting 报告可见性、租约 EF/SQLite 翻译失败与回合失败不终结。`273d2d5, 3f2acd1, c5a87d9`
- 修复文件搜索/检索遍历无界且无法响应取消。`4ce9d6f, f8dfafb`
- 修复终端退出后未等待 stdout 排空，重定向输出改为按 UTF-8 解码。`bca022c, abe29e5`
- 修复运行时健壮性问题：取消前结算、按次输入容量上限、不可投递回复不阻塞批次。`a18702b, 990673e, 8d2ad3b`
- 修复子代理归属、终态幂等、父 Turn 唤醒与轨迹丢行（A01 系列）。`a7526b3, da389c5, 18587ea`
- 修复工具审批 `need_human` 未保持 Pending、`/authorize` 人工批准闭环不通。`3df7d2a, b0cfa3a`
- 记忆工具改为 fail-closed：未知 action、type 归一化、preference value 必填。`3d74ae7, bd28a11, 33c1489`
- 修复任务工具与看板可用性问题：上下文重建、CAS 冲突、占位导致误报繁忙。`11b1295, f33a010, 766747d`
- 修复 `file_patch` 不接受 camelCase 参数名，并保真缩进与换行。`2841238, 2c249e5`
- 修复用量账本幂等与归因错误，Goal 结算错误归档链透传。`15842ff, 9537f60, 8007e7f`
- 修复飞书连接器超时与启动 drain、子代理摘要乱码、用户图片投影。`02fa3d9, 9365788, 3b30acd`
- 修复 Admin 密钥保管箱页头不可读、假揭示与非枚举化分类。`7ae0cc1`
- 修复工具审批 A91-0：审查模型与审计 Agent 解耦，依赖按类型分类。`943270d, d871ed9, 4f8561a`
- 修复接入 Jev 后父级回退 Reviewer 默认值带来的回归。`b65e2b5`
- 修复 `task-tools` 注入快照与实时版本的双重 CAS 互锁，并在宿主重启后按服务端归属重建任务上下文。`f33a010, 11b1295`
- 修复 Jev 模型档案仅配置 ProviderId + ModelId 时评审链路 400。`231eb23, c604caf`

## 其他变更

- 出站 HTTP 统一 User-Agent 为 `PuddingAgent/1.0`。`a452eb7`
- 工具调用安全分类器成为默认 Reviewer，取代原直接放行路径，审批语义有变化。`57863dc, efa83cd, 3b92fdf`
- 聊天权限模式由多档收敛为两档 + 临时授权，`/yolo` 语义并入 Agent 级访问级别。`b45dcec, bba41b6, 3a285dd`
- 技能库体检结论：89.1% 关键词为噪声、1735 次技能注入机会被挤掉、23.7% 为近重复。`b4f0d0f, 3d8390a, 4ab573e`
- 审批信任根 P0 结论：确认 AgentFirewall 存在 3 条 bypass 通道与 4 个审批持久化面，已登记待修。`574851b, 04d70d3, eea9687`
- 子代理协作协议更新至 v1.1–v1.3，明确单 Agent 优先原则与成本窗口纪律。`15469ad, ab10ab6, faa99dd`
- `goal.md` 轮转脚本上线，默认 dry-run 并带无损校验。`8d65273`
