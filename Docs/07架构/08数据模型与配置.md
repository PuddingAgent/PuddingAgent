# 数据模型与配置

## 核心数据实体

- AgentTemplate：角色、默认能力、运行画像、记忆策略。
- AgentInstance / AgentLifecycleRecord / AgentArchiveRecord / AgentFreezeRecord：模板派生出的实际运行实例，以及生命周期、冻结与归档对象。
- SkillRegistryEntry / McpRegistryEntry：全局 Skill / MCP 注册对象。
- Session：ServiceSession、ExecutionSession、AuditSession、ChildSession。
- WorkflowDefinition / WorkflowNode / WorkflowEdge / WorkflowTrigger / WorkflowRun：显式工作流、节点、边、触发器与运行实例定义。
- MemoryRecord / MemoryPolicy：正式记忆、候选记忆与记忆治理策略。
- ApprovalRecord：高风险执行与确认链路的核心对象。
- ChannelBinding：渠道绑定到 Workspace 的治理关系。
- EventEnvelope / EventRoute：统一事件对象与路由结果。
- EventSubscription：Agent、Runtime、审计链路对事件的订阅声明。
- ApprovedEventSubscription / WakeupBinding / InterruptionRecord / ResumeAnchor：Runtime 侧事件命中、消息打断与恢复锚点对象。
- EventCursor / ReplayCheckpoint：长连接消费、补偿消费与重放游标。
- DeadLetterEvent：无法投递、无法消费或超限事件的隔离对象。
- RuntimeNodeDescriptor / RuntimeNodeHeartbeat：Runtime 节点静态画像与动态心跳对象。
- RuntimePlacementPolicy / RuntimePlacementDecision：节点选址规则与选址结果对象。
- SandboxBinding / SandboxInstanceRecord / SandboxFault：沙箱绑定、实例台账与故障对象。
- TaskBoardItem / TaskBoardColumn：共享任务板对象。
- SpreadsheetSheet / SpreadsheetRangeRef：共享表格对象。
- WikiEntry / KnowledgePageVersion：共享知识页面对象。
- ObjectAsset / ObjectAssetVersion：对象存储中的文件与版本对象。
- AgentActivitySnapshot / TaskMapSummary / KeyEventRecord：Workspace 协作驾驶舱的聚合视图对象。
- AgentAccessToken：Agent 通过 HTTP API 访问共享工具时的身份令牌对象。
- AgentExecutionLog / EventLogRecord / WorkspaceLogProjection：Workspace 日志系统的局部日志、事件日志与聚合日志对象。
- LogQuery / LogRetentionPolicy：日志查询条件与日志保留策略对象。

## 建模原则

- 模板与实例必须分离。
- Workspace 是配置与治理边界，Session 是运行时边界。
- 记忆必须同时建模正式存储与候选提升。
- 路由、审批、权限、审计需要有可查询的显式对象。
- 事件模型必须同时支持外部接入事件与内部状态事件，不能只覆盖消息渠道。
- 订阅关系、事件可见性与重放权限必须可治理、可审计、可查询。

## Workspace 最小配置对象

- AgentTemplate
- ChannelBinding
- Workflow
- MemoryPolicy
- PermissionPolicy
- BudgetPolicy
- PluginBinding
- EventPolicy
- EventSubscriptionPolicy

## AgentTemplate 建议最小配置

- `systemPrompt`
- `roleProfile`
- `memoryPolicy`
- `skillRefs`
- `mcpRefs`
- `hostCapabilityRefs`
- `requiredRuntimeTags`
- `preferredRuntimeTags`
- `excludedRuntimeTags`
- `runtimeAffinityPolicy`
- `defaultIsolationLevel`
- `autoRebuildSandbox`
- `eventSubscriptionPreferences`
- `riskProfile`
- `approvalProfile`
- `lifecyclePolicy`

### `AgentInstance`

- `agentId`
- `templateId`
- `workspaceId`
- `runtimeId`
- `sessionId`
- `status`
- `createdAt`
- `lastActiveAt`
- `hibernatedAt`
- `frozenAt`
- `archivedAt`
- `destroyedAt`

建议状态至少包括：

- `Created`
- `Running`
- `Busy`
- `Sleeping`
- `Hibernated`
- `Frozen`
- `Archived`
- `Destroyed`

### `AgentLifecycleRecord`

- `recordId`
- `agentId`
- `workspaceId`
- `fromStatus`
- `toStatus`
- `triggerType`
- `triggeredBy`
- `reason`
- `occurredAt`

建议用途：

- 记录 Agent 生命周期迁移，例如创建、任务完成回到 Running、进入睡眠、进入休眠、归档、销毁

### `AgentArchiveRecord`

- `archiveId`
- `agentId`
- `workspaceId`
- `archiveStorageType`
- `archiveObjectRef`
- `compressed`
- `channelBindingsReleased`
- `archivedAt`
- `restoredAt`
- `status`

建议用途：

- 记录 Agent 进入归档后的打包、压缩、上传与恢复信息

### `AgentFreezeRecord`

- `freezeId`
- `workspaceId`
- `agentId`
- `scope`：`agent | workspace`
- `triggeredBy`
- `reason`
- `containerStopped`
- `stoppedContainerCount`
- `frozenAt`
- `resumedAt`
- `status`

建议用途：

- 记录单 Agent 冻结或 Workspace 全局冻结的安全治理动作
- 记录是否已停止对应容器、冻结范围和恢复结果

### `LifecyclePolicy`

- `allowSelfSleep`
- `allowSelfHibernate`
- `allowPeerHibernate`
- `allowArchive`
- `allowDestroy`
- `allowAuditFreeze`
- `allowWorkspaceEmergencyStop`
- `sleepKeepsEventWakeup`
- `hibernateDisablesEventWakeup`
- `freezeStopsContainers`
- `archiveToColdStorage`

建议用途：

- 约束 Agent 自主请求睡眠、休眠、归档、销毁的权限边界
- 约束 Sleeping / Hibernated / Frozen / Archived 的事件接收能力与存储策略

### `FreezeAgentRequest`

- `requestId`
- `workspaceId`
- `agentId`
- `requestedBy`
- `reason`
- `stopContainers`
- `requestedAt`

### `ResumeAgentRequest`

- `requestId`
- `workspaceId`
- `agentId`
- `requestedBy`
- `restartContainers`
- `requestedAt`

### `FreezeWorkspaceRequest`

- `requestId`
- `workspaceId`
- `requestedBy`
- `reason`
- `stopContainers`
- `requestedAt`

建议用途：

- 既可作为 Platform -> Controller 的治理命令对象
- 也可作为 `workspace.freeze.requested` 全局事件的 payload

### `ResumeWorkspaceRequest`

- `requestId`
- `workspaceId`
- `requestedBy`
- `restartContainers`
- `requestedAt`

建议用途：

- 既可作为 Platform -> Controller 的治理命令对象
- 也可作为 `workspace.resume.requested` 全局事件的 payload

建议用途：

- 作为 Controller -> Runtime 的冻结 / 恢复治理指令对象
- 支持单 Agent 冻结与 Workspace 级全局 stop / recover

## 配置来源

第一阶段采用 Workspace YAML 加平台持久化的混合模式。配置变更优先手动 reload，而不是一开始就做自动热更新。

其中：

- 全局 Skill / MCP Registry 建议由 Controller 管理并持久化到 PostgreSQL。
- Workspace 内的 AgentTemplate 引用这些全局能力，而不是复制它们的定义。

## Workflow 最小建模建议

### `WorkflowDefinition`

- `workflowId`
- `workspaceId`
- `name`
- `version`
- `triggers`
- `nodes`
- `edges`
- `variables`
- `policies`

### `WorkflowNode`

- `nodeId`
- `type`
- `componentRef`
- `agentTemplateRef`
- `agentLifecycle`
- `data`
- `resourceBindings`
- `eventHooks`

### `WorkflowTrigger`

- `triggerType`：`agent | event | user | workflow`
- `eventPattern`
- `sourceWorkflowId`
- `enabled`

### `WorkflowRun`

- `runId`
- `workflowId`
- `workspaceId`
- `status`
- `input`
- `startedAt`
- `finishedAt`

### `WorkflowNodeState`

- `Pending`
- `Ready`
- `Claimed`
- `Running`
- `Blocked`
- `Completed`
- `Failed`
- `Released`
- `Skipped`

### `WorkflowScopedAgentLease`

- `runId`
- `nodeId`
- `agentId`
- `agentLifecycle`
- `sandboxId`
- `expiresAt`

## Workspace 协作工具最小建模建议

### `TaskBoardItem`

- `taskId`
- `workspaceId`
- `title`
- `status`
- `priority`
- `assigneeAgentId`
- `dependsOn`

### `SpreadsheetSheet`

- `sheetId`
- `workspaceId`
- `name`
- `columns`
- `version`

### `WikiEntry`

- `entryId`
- `workspaceId`
- `title`
- `tags`
- `latestVersion`

### `ObjectAsset`

- `assetId`
- `workspaceId`
- `name`
- `contentType`
- `version`
- `labels`

### `AgentActivitySnapshot`

- `agentId`
- `workspaceId`
- `currentNodeId`
- `role`
- `status`
- `workloadScore`
- `lastActionAt`

### `TaskMapSummary`

- `workspaceId`
- `mapId`
- `pendingCount`
- `runningCount`
- `doneCount`
- `failedCount`
- `blockedCount`

### `KeyEventRecord`

- `eventId`
- `workspaceId`
- `eventType`
- `subjectId`
- `agentId`
- `occurredAt`

### `AgentAccessToken`

- `tokenId`
- `agentId`
- `workspaceId`
- `scopes`
- `expiresAt`

建议用途：

- 用于 Agent 通过 HTTP API 访问共享工具时声明身份
- 避免 Agent 直接以系统级密钥访问底层服务

## Workspace 日志系统最小建模建议

### `AgentExecutionLog`

- `logId`
- `workspaceId`
- `agentId`
- `sessionId`
- `nodeId`
- `resourceId`
- `logLevel`
- `status`
- `message`
- `errorCode`
- `occurredAt`

### `EventLogRecord`

- `recordId`
- `workspaceId`
- `eventId`
- `eventType`
- `scope`
- `agentId`
- `taskId`
- `correlationId`
- `causationId`
- `occurredAt`

### `WorkspaceLogProjection`

- `projectionId`
- `workspaceId`
- `timeBucket`
- `activeAgentCount`
- `completedNodeCount`
- `failedNodeCount`
- `eventDensityScore`
- `hotspotTags`

### `LogQuery`

- `workspaceId`
- `agentId`
- `taskId`
- `nodeId`
- `eventTypes`
- `timeRange`
- `levels`
- `includeRawPayload`

### `LogRetentionPolicy`

- `workspaceId`
- `agentLogRetentionDays`
- `eventLogRetentionDays`
- `projectionRetentionDays`
- `archiveEnabled`
- `fullTextIndexEnabled`

建议用途：

- `AgentExecutionLog` 用于单 Agent 调试与轨迹回放
- `EventLogRecord` 用于跨 Agent 协作分析和事件时间轴
- `WorkspaceLogProjection` 用于驾驶舱、告警和汇总卡片

## 事件系统最小配置对象

- `EventPolicy`：声明允许进入本 Workspace 的事件类型、来源范围、信任等级和保留策略。
- `EventSubscriptionPolicy`：声明哪些 Agent / Workflow / Runtime 组件可以订阅哪些事件，以及是否允许直接唤醒。
- `EventRetentionPolicy`：声明事件日志、死信事件、重放窗口和归档策略。
- `AdapterEventMapping`：声明某个 Gateway Adapter 的原始协议字段如何映射到统一事件类型。

### `EventEnvelope`

- `eventId`
- `eventType`
- `scope`
- `workspaceId`
- `source`
- `subject`
- `payload`
- `trustLevel`
- `priority`：`P0 | P1 | P2`
- `correlationId`
- `causationId`
- `occurredAt`
- `publishedAt`
- `version`

建议约束：

- `scope=workspace` 应作为绝大多数业务事件的默认值
- `scope=global` 必须由治理策略显式允许，不能靠发布方随意自选

建议用途：

- `priority` 主要用于消息型事件的 Runtime 插入策略
- `P0` 代表紧急打断级，`P1` 代表当前轮插入级，`P2` 代表空闲后投递级

### `EventSubscription`

- `subscriptionId`
- `workspaceId`
- `subscriberType`
- `subscriberRef`
- `eventPattern`
- `filterExpression`
- `allowDirectWakeup`
- `allowedPriorities`
- `deliveryMode`
- `failurePolicy`
- `replayAllowed`

建议用途：

- 声明的是订阅偏好与治理目标，而不是 Runtime 已经执行的本地订阅实例

### `EventPublishPolicy`

- `workspaceId`
- `allowDefaultWorkspacePublish`
- `allowedGlobalPublishers`
- `globalEventPatterns`
- `requireApprovalForGlobalPublish`
- `auditGlobalPublish`

建议用途：

- 约束谁可以发布 `global` 事件
- 约束哪些事件类型允许从 `workspace` 升级为 `global`
- 默认要求：普通 Agent / Runtime 仅发布 `workspace` 事件，`global` 发布必须显式准入

### `ApprovedEventSubscription`

- `approvedSubscriptionId`
- `workspaceId`
- `runtimeId`
- `agentId`
- `eventPattern`
- `allowDirectWakeup`
- `allowedPriorities`
- `approvalRef`
- `expiresAt`

建议用途：

- 代表 Controller 已批准、可由 Runtime 实际落实的订阅对象

### `WakeupBinding`

- `bindingId`
- `runtimeId`
- `sessionId`
- `agentId`
- `approvedSubscriptionId`
- `deliveryMode`
- `interruptPolicy`
- `dedupeWindowSeconds`

建议用途：

- Runtime 本地事件命中绑定，用于把批准订阅转换为实际命中与唤醒行为

### `InterruptionRecord`

- `interruptionId`
- `workspaceId`
- `sessionId`
- `agentId`
- `eventId`
- `priority`
- `interruptionMode`
- `interruptedAt`
- `status`

建议用途：

- 记录一次 `P0` / `P1` 消息插入或打断的事实，供恢复、审计与调试使用

### `ResumeAnchor`

- `anchorId`
- `workspaceId`
- `sessionId`
- `agentId`
- `workflowRunId`
- `interruptedByEventId`
- `interruptedAt`
- `memoryPointer`
- `summary`
- `resumePriority`
- `status`

建议用途：

- 用于记录“消息到来时上一个任务做到哪里”
- 帮助 Runtime 在消息处理完成后快速恢复原执行流

## Runtime 节点与沙箱最小建模建议

### `RuntimeNodeDescriptor`

- `runtimeId`
- `hostType`
- `osType`
- `architecture`
- `purposeTags`
- `capabilityTags`
- `sandboxProviders`

### `RuntimeNodeHeartbeat`

- `runtimeId`
- `cpuUsage`
- `memoryUsage`
- `diskUsage`
- `activeAgentCount`
- `activeWorkspaceCount`
- `loadScore`
- `pressureLevel`
- `reportedAt`

### `RuntimePlacementDecision`

- `workspaceId`
- `agentId`
- `selectedRuntimeId`
- `decisionReason`
- `matchedRequiredTags`
- `matchedPreferredTags`
- `workspaceAffinityHit`

### `SandboxBinding`

- `agentId`
- `workspaceId`
- `sandboxId`
- `provider`
- `bindingMode`
- `status`

### `AgentContainerBinding`

- `agentId`
- `workspaceId`
- `runtimeId`
- `containerId`
- `containerName`
- `provider`
- `status`
- `startedAt`
- `stoppedAt`

建议用途：

- 维护 Agent 与 Docker 容器的直接绑定关系，支撑冻结与恢复

### `SandboxInstanceRecord`

- `sandboxId`
- `runtimeId`
- `provider`
- `containerName`
- `status`
- `rebuildCount`
- `failureReason`

### `DockerContainerRecord`

- `containerId`
- `runtimeId`
- `workspaceId`
- `ownerAgentId`
- `containerName`
- `status`
- `freezeReason`
- `resumeToken`
- `createdAt`
- `startedAt`
- `stoppedAt`
- `lastSeenAt`

建议用途：

- 为 Docker 首批实现提供明确的容器台账，便于按 Agent 或 Workspace 执行 stop / recover
