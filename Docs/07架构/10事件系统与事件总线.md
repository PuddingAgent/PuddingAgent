# 事件系统（P2P 实现）

> **2026-05-02**：事件总线从 RabbitMQ 改为 P2P 直连广播。

## V1 事件模型

- Agent 内部事件：进程内直接回调（无需中间件）
- Agent 间事件：通过 P2P 直连 HTTP/gRPC 广播
- 事件类型：agent.heartbeat、agent.completed、task.completed 等

## 未来（V2）

如果 P2P 广播不能满足大规模场景（>20 个 Agent），可引入轻量消息队列。但 V1 不需要。