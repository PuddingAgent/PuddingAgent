# 多轮会话与工具调用

> **2026-05-02**：执行模型不变——Runtime 驱动 loop，LLM 只做单轮决策。但 Runtime 是同进程内模块。

## 执行模型

- Runtime 模块控制循环
- LLM 每次只做单轮决策
- 工具在进程内直接执行
- 停止信号由 Controller 模块（同进程）下发

## 与之前设计的唯一区别

不再有 Controller → Runtime 的 HTTP 调用。改为同进程内方法调用，延迟更低。