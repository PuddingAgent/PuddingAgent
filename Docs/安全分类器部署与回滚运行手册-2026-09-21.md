# 安全分类器 · 部署与回滚运行手册（2026-09-21）

> 适用对象：安全分类器与工具调用准入方案 v2 的 P1 批次（S1～S6b）。
> 设计/契约细节见 `Docs/Features/安全分类器与工具调用准入方案-v2.md`（§14.10 切片表、§14.14.6 部署前核查）。
> 本手册只写**可执行步骤**：部署前核查 → 部署 → 部署后实证 → 回退。

## 0. 一句话结论
本批次的**实现已全部入 `origin/master`，但尚未部署**。因为选项（Options）与 DI 是**宿主启动时读取**，
所以必须**重启**才能生效；且本批次**包含 Admin 前端改动**，重启时**必须一并投放前端资产**，
否则会出现"后端已生效、前端看不到故障提示"的假象。

## 1. 待部署内容（行为相关）
| 提交 | 内容 | 生效方式 |
| --- | --- | --- |
| `3b92fdf4` | **默认值翻转**：`ToolApproval:Reviewer` → 分类器 | 重启读配置 |
| `169e8d26` | S5b 完全访问授予接入审批闸门（AgentFirewall Gate 4） | 重启（DI） |
| `5defb57f` / `a5df3ae5` / `f24f1b26` | S6a 健康面 + `classifier_status` 工具 + §14.9.2 退避；§8.2 契约修正；jev 回退路径补 `Instructions` | 重启（DI/工具注册） |
| `7012e342` | 管理 API 的 `classifier` 来源全线同步（含前端） | 后端重启 + **前端资产** |
| `ee625318` | S6b-1 `GET /api/classifier-health` 只读健康 API | 重启（控制器随宿主） |
| `4d5347a9` | 审计事件名 wire 收归单一来源（26 事件 snake_case） | 重启 |
| `3836db55` | S6b-2 前端「分类器故障明显提示」横幅 + 审计事件筛选 11→26 项 | **前端资产** |

> 另有纯文档提交（`6f2b467a` / `a6835032` / `815a0bf4` / `f7c3e2eb` / `ea39352a`）同样未部署，不影响运行行为。

## 2. 部署前核查（必须逐条确认，不可跳过）
1. **确认最终生效的 Reviewer 值** —— ✅ **已于 2026-09-21 查清并修正**（本条曾被忽略，最终发现是**部署阻断级**问题）：
   - 事实：`Source/PuddingAgent/appsettings.json:106` **显式写着 `"Reviewer": "llm"`**
     （由 `c604caf7` 于 **2026-09-18** 引入，早于分类器代码）；而运行中的 Core 是
     `Source\PuddingAgent\bin\Debug\net10.0\PuddingAgent.exe`，其 `appsettings.json` 与源文件
     **SHA-256 完全一致**（`AFA1000A…52684`）⇒ **进程实际读到的就是 `llm`**（是同一份字节，非推测）。
   - **结论**：仅翻转**代码默认值**（`3b92fdf4`）在生产上是个 **no-op** ——
     **配置的显式值优先于默认值**，分类器**从未接管**。这正是本核查条目的存在意义。
   - **已修正**：该配置已改为 `"classifier"`（独立提交）。
   - 另经全量排查：`D:\data` 下**无**任何 `ToolApproval`/`Reviewer` 配置；`Source` 树下该键**仅此一处**
     ⇒ **不存在第二处来源覆盖**。
   - **部署后必须复核部署目录中该键的生效值**（防止新构建未投放新配置 / 部署目录另有独立配置）。
2. **确认分类器仲裁实现已注册** —— ✅ **已于 2026-09-21 验证**：
   - `Source/PuddingRuntime/DependencyInjection.cs:145`：`services.AddSingleton<IJevDecisionService, JevDecisionService>()`
     ⇒ **无条件注册** ⇒ 部署后仲裁位是**真 Jev 分类器**（而非 `arbiter_unavailable` 的 fail-closed 占位）。
   - **端点/密钥可解析性**（这才是真正的风险点）：`JevDecisionOptionsProvider` 的解析链是
     **资源池优先**（`data/config/llm.providers.json` 的 `jev` provider → baseUrl + 首个未废弃模型 + apiKey/apiKeyRef），
     回退才是 `Jev:BaseUrl`/`JEV_BASE_URL` 与 `Jev:ApiKey`/`JEV_API_KEY`；
     **端点或密钥缺失时 fail-closed 抛 `JevDecisionException(NotConfigured)`** ⇒ 会表现为分类器不可用 → 大面积 deferred。
     实测：`appsettings.json` **无** `Jev` 节、环境中**无** `JEV_*` 变量 ⇒ **完全依赖资源池**，
     而资源池已确认有 `jev` provider（`isEnabled=true`、`baseUrl=https://jevtypesafeai.com`、
     `apiKey` 存在、模型 `jev-latest`）✅ ⇒ 不会 fail-closed。
   - 若日后出现大面积 deferred：优先怀疑此处（端点/密钥丢失或 provider 被禁用）。
   - **复核（2026-09-21 07:40 BJT，在 HEAD 上重做，非引用旧结论）**：
     * 生效值：`Source/PuddingAgent/appsettings.json` 的 `"ToolApproval": { "Reviewer": "classifier" }` ✅
       （文件 SHA256 `EBB9D9F57616093F0E3664BB11F89E73EE43A860044C4BE71D7E379D045B32F8`）；
       且 `git log a85c1216..HEAD -- Source/PuddingAgent/appsettings.json` 显示**手册创建后仅 `297293d`（那次翻转）动过它**
       ⇒ 无回退、无覆盖；代码侧默认值亦为 `Reviewer = ClassifierReviewer`（`LlmToolApprovalReviewer.cs:124`）。
     * 资源池：`D:\data\config\llm.providers.json`（SHA256 `6CDF2E657F5D66CE91667FC80382545A95629AA2ABBA002979BEE90AFBC56C9F`）
       中 `providerId=jev`、`enabled=True`、`baseUrl` 与 `apiKey` 均在、模型 `jev-latest` ✅ ⇒ **不会 fail-closed**。
     * 接线：`DependencyInjection.cs:145` 注册 `IJevDecisionService`；工具扩展 `:124` 注册 `IToolCallClassifier`（管线）、
       `:130` 健康面、`:141` 仲裁注册状态、`:179` `ClassifierStatusTool`、`:310+` `BuildToolCallClassifierPipeline` ✅。
3. **回退路径可用性**：确认 `Reviewer=llm`（以及 `jev`）路径仍存在且其模型 profile 可解析
   （`f24f1b26` 已修 jev 回退路径的 `Instructions` 缺失问题）。
4. **git 面**：`git status` 干净、`origin/master` 包含上表全部提交（`git log --oneline -12 origin/master`）。
5. **提交面卫生审计（2026-09-21 已通过；四项均空才算通过）**：
   - ① 被提交的临时/垃圾文件：`git log -45 --name-only --pretty=format:'' | ? { $_ -match '^temp/|\.log$|\.bak$|^\.pudding/' }`
   - ② **危险测试聚焦**（`.only(`/`.skip(`/`fdescribe(` 会让整套件被**静默跳过**，最隐蔽）：
     `git diff HEAD~45..HEAD | Select-String '\.only\(|\.skip\(|xdescribe\(|fdescribe\('
   - ③ debug 残留（只看**新增行**）：`git diff HEAD~45..HEAD | Select-String '^\+' | Select-String 'debugger|console\.log'`
   - ④ `git status --porcelain` 为空（工作区干净）。
   实测：**四项均为空**（同时也是“本轮临时 debug 已清除”的证据）。

## 3. 部署步骤
1. 选择**用户在场**的时段：重启会**终止所有运行中的子代理**，且平台存在"点火重启后恢复链曾断裂"的已知缺陷
   （可能需再发一条消息才能恢复会话）。
2. 触发重启（`bootstrap_reboot`，`deployment_mode=desktop-build` 默认即：停止 → 编译 → 事务部署 → 校验 SHA-256 → 重启）：
   - **前端必须一起走**：`frontend_mode=build`（从源码重建 Admin 前端并投放到 `wwwroot/admin`）
     或 `frontend_mode=load`（投放已构建好的 dist）。
   - **`frontend_mode=skip` 会导致前端改动不生效** ⇒ 本次部署**不要用 skip**。
   - **✅ 已在 HEAD 上重新预验证（2026-09-21 08:20 BJT）**：`pnpm run build` ⇒ **`BUILD_EXIT=0`**，
     包体预算检查通过（`[chat-bundle-budget] ok sync=1375364 chat=502405 common=201596`），
     新产物 `Source/PuddingPlatformAdmin/dist/index.html` **LastWriteTime 2026-09-21 08:20:50**、
     **SHA-256 `0F667A7B1C1151FD1F50014310C5F6386658D5C7ED277FCB4180A9EB79043C1B`**、dist 150 文件 / ≈31 MB。
     ⇒ **当前修订上构建可通过**（已含 `c90d7dc` 的图标映射）⇒ `frontend_mode=build` 的失败风险已排除；
     若改用 `frontend_mode=load`，**必须**用本次这组时间/SHA-256（或部署前重新构建）核对一致。
     （产物目录 `Source/PuddingPlatformAdmin/dist` 被 `.gitignore:10:/dist` 忽略 ⇒ 构建**不会污染工作区**。）
   - ⚠️ **历史记录（旧预验证，已过期，仅作留痕）**：`BUILD_EXIT=0`、`✓ Built in 25113ms`、`dist/` 150 文件 / 32.5MB 那次
     是在**更早的修订**上做的。其后**有生产级前端改动**：
     `src/layouts/AdminLayout/menuIcons.ts`（`c90d7dc`，补齐 `hdd`/`key` 图标映射）——
     该文件属于**产物面**（非测试），而其余后续提交均仅改 `*.test.ts(x)`。
     ⇒ **沿用旧 `dist/` 会把菜单图标修复静默丢掉**。因此：
       * **首选 `frontend_mode=build`**（部署时现场重建，慢但自证新鲜，不存在错配；失败会 fail-closed 中止）；
       * 若要用 `frontend_mode=load`：**必须先在当前修订上重跑 `pnpm run build`**，
         并核对 `dist/index.html` 的改动时间/SHA-256 与本次构建一致（不得沿用上表那次产物）。
   - 步骤失败会 fail-closed 中止（不会留下"后端新、前端旧"的半成品）。
   - **✅ 编译面预验证（2026-09-21 08:25 BJT）**：`dotnet build <proj> -t:Compile` ⇒
     `PuddingAgent`（**部署产物**，即 `desktop-build` 所编译者）**EXIT=0** ✅、`PuddingHost` **EXIT=0** ✅、`PuddingPlatform` **EXIT=0** ✅
     ⇒ 部署时的编译步骤**不会因代码错误失败**。
     * ⚠️ **`PuddingDesktop` 会报错，但那不是代码缺陷**：Desktop 进程（**PID 36768 正在运行**）占住了自身输出目录，
       报 `MSB3027`/`MSB3021`"文件被 PuddingDesktop 锁定" ⇒ **不要据此判定构建坏了**；
       `desktop-build` 编译的是 **Core 产物**而非 Desktop 自身（也**不要**在部署前手动去编译 Desktop）。
     * ⚠️ **方法学坑**：`-t:Compile` **不适用于 WPF 项目**（跳过 XAML 编译 pass）⇒ 会假报
       `CS5001`（无 `Main`）与 `CS0103 InitializeComponent`/`WebViewHost` ⇒ **属假阳性**，不代表工程有问题。
3. 重启完成后，确认进程加载的是新二进制（如已知的"进程加载最新产物"校验方式）。

## 4. 部署后实证（**不做这一步就不得宣称"已生效"**）
1. **分类器真的在当裁判**：用审批门户的分类动作（`request_tool_approval` 的 `classify` 入口，参数以工具 schema 为准）
   对一次工具调用请求裁决，确认返回四选一结论与**逐分类可信度**，而不是 `deferred`。
2. **降级契约未被误触发**：普通（非白名单命中）工具调用应被**正常放行**；
   若出现大面积 `deferred_dependency`，优先怀疑第 2 节核查项 2（仲裁未注册）。
3. **健康面**：调用 `classifier_status` 工具应与 `GET /api/classifier-health` 返回**同一档位**
   （服务端权威：两者都直读同一 `Snapshot()`）。
4. **前端可见**：打开 Admin「安全审批」页，确认分类器健康横幅出现，且
   `configured=false` / `unknown` 时**显示为"未知"而不是健康**（这是设计红线）。
5. **审计链**：管理 API 的审计列表应能看到 `classifier_invoked` / `classifier_unavailable` /
   `full_access_*` 等事件，并且**能按这些事件筛选**（本批修好的能力）。

## 5. 回退
| 场景 | 动作 | 是否需要重新构建 |
| --- | --- | --- |
| 分类器决策异常，想立刻回到旧审查器 | 配置 `ToolApproval:Reviewer=llm` 后重启 | **否**（免重建，最快） |
| 想回退到 jev 仲裁分类器 | 配置 `ToolApproval:Reviewer=jev` 后重启 | 否 |
| 前端展示异常想撤掉横幅 | 回退前端资产（`frontend_mode=load` 投放旧 dist） | 否 |
| 代码级回退 | `git revert` 相应提交后重新部署 | 是 |

> **回退优先级**：先用**配置回退**（免重建、秒级），确认稳定后再考虑代码级回退。

## 6. 已知风险与注意
- 重启**终止运行中的子代理**，其工作区改动会留在工作树但不会提交 ⇒ 部署前先确认没有在途子代理。
- **部署前需确认的未跟踪文件**：`git status` 显示 `Docs/Reports/PuddingAgent夜间效率与RSI评估-2026-09-21.metrics.json`
  为未跟踪（`??`）—— **非本批次产物**（疑为其他工作流/分析产出，本批次所有提交均无此文件）
  ⇒ 部署时**不要顺手 `git add .`**，避免把无关产物混入部署提交。
- 平台**飞书主动推送对本 Agent 不可用**（"无最近飞书会话"）⇒ 部署结果需在本回合回复中说明。
- 本批次**未做**端到端真机联调（无浏览器环境）：前端横幅的真实渲染未经人眼验证。
- **前端产物错配风险（本次审计新增）**：手册创建（`a85c1216`）之后，**唯一影响产物的前端改动**是
  `src/layouts/AdminLayout/menuIcons.ts`（`c90d7dc`）⇒ 旧的预构建 `dist/` **与 HEAD 不对应**。
  部署时若不重建，**菜单图标修复不会生效且无任何报错**（静默）。⇒ 见 §3 步骤 2 的处置。
- **残余风险 A（覆盖审计写入失败被静默吞；**已修**，非部署阻断）**：
  `Source/PuddingRuntime/Classification/ToolCallClassifierPipeline.cs:409-412` 的 `catch { }` 吞掉审计写入异常
  （注释自认“本切片不引入日志依赖”，并声称“溯源缺失风险在健康面/运维侧另行暴露”——**实际未实现**）
  ⇒ 若审计库写失败，**分类器覆盖 deny 这件事既无痕迹也无日志**；与设计意图（§14.13.2“覆盖必落审计”）
  以及用户要求④“故障要可提示、可探查”有落差。对照：`AgentFullAccessGrantService.cs:238-266` 的**授予**审计失败
  是“**回滚 + fail-closed**” ⇒ **两处口径不一致**。
  **修法（最小、加法式，已核可执行）**：给该类加**可空末位参数** `ILogger<ToolCallClassifierPipeline>? logger = null`，
  `catch (Exception ex) { _logger?.LogWarning(ex, "覆盖审计写入失败…"); }`；DI 构造处
  （`PuddingToolServiceCollectionExtensions.cs:310+`）补传 `serviceProvider.GetService<ILogger<ToolCallClassifierPipeline>>()`；
  **三处测试构造点因是可选参数无需改动**；新增用例：审计库抛异常 ⇒ 最终裁决**不变** 且**有 Warning**。
  → **已修（2026-09-21）**：管线加**可空末位** `ILogger<ToolCallClassifierPipeline>?`（三处测试构造点因可选参数无需改动）；
    `catch (Exception ex)` 改为记 **Warning**（带 tool/候选/终局/分类器 id，保留“裁决先于留痕”语义）；
    DI 组装处（`PuddingToolServiceCollectionExtensions.cs:310+`）补传 `GetService<ILogger<ToolCallClassifierPipeline>>()`；
    新增用例 `ClassifyAsync_OverrideAuditWriteFails_KeepsVerdict_AndLogsWarning`。
    **验证（原始计数）**：`~ToolCallClassifierPipeline` ⇒ **16 passed / 0 failed**（原 15 + 新 1）；
    **Runtime 门禁 ⇒ PASS，`已通过! - 失败: 0，通过: 1659`**（原 1658 ⇒ +1）。
- **残余风险 B（同一作用域重复授予不落 `FullAccessExpired`；已知语义，非缺陷）**：
  `AgentFullAccessGrantService.cs:210-221` 显示新授予**覆盖**同作用域旧授予（并清理旧撤销索引），
  但旧授予**不落** Expired ⇒ 审计链少了“旧授予被替代”这一环。影响面：**仅审计完整性**，
  **无功能性越权**（旧授予已被移出字典 ⇒ 不再生效）。可选修法：覆盖时对**新旧 id 不同**的情况补落一次 Expired（幂等标记先行）。
  → **状态：仍为已知语义（未修）**，与风险 A 不同：它不会造成“无痕迹且不可探查”，且不影响授权有效性。
- 测试门禁现状（2026-09-21 实测）：`Core` 910/1（唯一红＝`SwarmOrchestratorTests` 临时目录不在任何 git 仓库，
  已定性不修）、`Runtime` **1659/0**、`Platform` 1363/0、`AdminJest` **1349/3**（剩余 3 例**全是语音族**，
  根因＝孤儿组件 `VoiceConversationPanel.tsx` 生产内零引用，等用户定“接线 vs 移除”）、
  `WebApi` **SKIPPED_LOCKED**（Core 运行时不可测，留到部署窗口）。以上既存红均与本次部署改动无关，已逐例取证归因
  （`TestScripts/known-red-dispositions.md`）。

## 7. 交付前验收对照（§14.11 七条 → 代码 / 用例，2026-09-21 逐条取证）

| # | §14.11 验收项 | 代码/用例证据 | 结果 |
| --- | --- | --- | --- |
| 1 | 零厂商依赖（门户工具与服务不得出现厂商概念） | `Tools/Approval/ToolApprovalPortalService.cs` 与 `Tools/Approval/RequestToolApprovalTool.cs` 对 `Jev` / `Noul` / `choice` **命中均为 0** | ✅ |
| 2 | 完全访问时长：请求 600 秒⇒**被拒（非截断）**；授予后假时钟断言 300 秒到期自动失效 | `ToolApprovalPortalTests.cs:378-383`（600 秒拒 + `full_access.duration_exceeded` + `duration_exceeded=true` + `active=false`）；`AgentFullAccessGrantServiceTests.cs:97-116`（301 秒 typed 拒绝 + “绝不截断到 300”）、`:203-213`（假时钟 299.5s 仍有效 / 恰好 TTL 即失效） | ✅ |
| 3 | 分类器不可用：`classify` ⇒ `deferred`，**规则存储零新增**、**完全访问未授予** | `ToolApprovalPortalTests.cs:82-100`（`Classify_UnknownOutcome_ReturnsDeferred_NoRule_NoGrant`：outcome=deferred、`Source==Classifier` 规则计数 0、`active=false`）；`:104-117`（`ClassifierMissing…ZeroRules…` + `classifier.not_configured`） | ✅ |
| 4 | 缓存（永久裁决复用）**且两条互补用例必须同时存在** | `ToolApprovalPortalTests.cs:163` `Classify_ClassifierDenyRuleReused_ArbiterNotConsultedAgain_CallCountUnchanged`（计数不变）**与** `:126` `Classify_AfterManualDenyRule_ArbiterConsultedOnce_OverrideAllowed`（计数 +1 且覆盖生效）**两条同时在** | ✅ |
| 5 | 冲突策略：同键 allow+deny ⇒ 生效 `deny` + 审计含 `RuleConflictDetected` | `ClassificationRuleCuratorTests.cs:406/424/444`、`ToolApprovalPortalTests.cs:314` | ✅ |
| 6 | 向后兼容：不传 `action` ≡ `submit`；既有 36 个 `JevToolApprovalReviewer` 用例全绿 | `ToolApprovalPortalTests.cs:502-513`（`ExecuteAsync_WithoutAction_BehavesLikeSubmit`：两条路径均走提交流程）；`~JevToolApprovalReviewer` ⇒ **37 passed / 0 failed**（原 36 + 1 例后续护栏，无回归） | ✅ |
| 7 | 范围隔离：A 的完全访问不影响 B | `AgentFullAccessGrantServiceTests.cs:121-124` `GrantScope_IsolatedAcrossAgentsAndWorkspaces` | ✅ |

> **结论**：七条验收**均可在仓库内定位到用例**（全部为离线用例，不依赖网络）；其中第 6 条的计数已从 36 增至 **37**
> —— 属后续新增护栏用例，**无回归**。部署后仍需按 §4 做**真链路**实证（门户 `classify` 动作 + `classifier_status`）。

### 7.1 真链路探针（2026-09-21 08:06 BJT，在 HEAD 上重跑）

> 依据 §14.14.2 第 2/3 条：**桩全绿只能证明内部逻辑自洽，不能证明与外部契约一致**；
> 所以部署前必须在**当前 HEAD** 上做一次真实联网探针（不得引用早先结论）。

- **命令**（显式指定资源池，避免路径猜测）：
  ```powershell
  $env:JEV_LIVE_POOL_PATH = 'D:\data\config\llm.providers.json'
  dotnet test Source\PuddingRuntimeTests --filter "TestCategory=Live"
  ```
- **结果**：**`已通过! - 失败: 0，通过: 2`**（3 s，**无 Inconclusive 跳过** ⇒ 密钥从资源池成功解析、
  **真实联网调用成功**）。两个探针分别为：
  ① `Services/JevDecisionLiveTests`（服务级：资源池 → `JevDecisionOptionsProvider` → `JevDecisionService` → `https://jevtypesafeai.com`）；
  ② `Tools/JevToolCallClassifierLiveTests`（**分类器级**，即本方案真正消费的路径）。
- **分类器级探针断言的判据**（正是 §14.14.2 第 3 条要求的形状）：
  `Assert.AreNotEqual(Unknown, verdict.Outcome)`、`ReasonCode == ReasonCodeVerdict`（**不得**是 `classifier.jev.unparsed`）、
  且 `Outcome` 必属四选一；并输出原始形状（`answers["outcome"].type="choice"`、`choice` 字面量、逐分类可信度）作为证据。
- ⇒ **§14.14.2 三道前置的第 2、3 条在 HEAD 上复核通过** ✅（第 1 条实现清单亦已核实：`JevToolCallClassifier` 存在）。

> ⚠️ **部署后仍需做一次门户 `classify` 真上下文实证**（§4）——探针证明的是“外部契约一致”，
> 不能替代“本进程已加载新配置”。

### 7.2 规则层规范对照（§14.12 复核，2026-09-21）

| 条款 | 要求 | 证据 | 结果 |
| --- | --- | --- | --- |
| §14.12.1 | 5 元组键；**禁通配**，命中须逐字节相等 | `ClassificationRuleCuratorTests.CurateAsync_WildcardVariant_NeverTouchesExistingByteEqualRule`：通配变体键 **≠** 精确键，且既有权 `HitCount` 不变 | ✅ |
| §14.12.3 | 同键同 Effect ⇒ 更新不新增；`HitCount++` / `LastSeenAtUtc` 刷新；禁用不硬删除 | `ClassificationRuleCurator.cs:312-313`（`LastSeenAtUtc = now`、`HitCount = sameEffect.HitCount + 1`）、`:295`（Disabled 行视为已退役、不复活） | ✅ |
| §14.12.4 | 同键 allow+deny ⇒ 生效 `deny` + `RuleConflictDetected` | `ClassificationRuleCuratorTests.cs:406/424/444` | ✅ |
| §14.12.5 | **六条**尽窄校验，命中任一 ⇒ 拒绝落永久规则、退化为单次 | `NarrownessViolation` 枚举 1–6（`ClassificationRuleCurator.cs:27-48`）；`CheckNarrowness` 按编号返回**首个命中**（`:174-235`）；降级码 `narrow_1…narrow_6`（`:848-856`） | ✅ |
| §14.12.7 | 置信度 < 0.95 ⇒ 建议 30 天有效期；disable ⇒ `Disabled` + 审计，不硬删 | `SuggestedExpiryConfidenceThreshold = 0.95`（`:104`）+ `SuggestExpiryAtUtc`；`DisableAsync` 落 `AllowlistRuleDisabled`/`DenylistRuleDisabled` 审计（`:390-430`） | ✅ |
| §14.12.8 | 不得破坏既有语义（不改 `CheckAsync` 判定顺序） | `git log -S FindMatchingAllowlistRuleAsync` ⇒ 仅初始提交 `8e0ff89` 引入、**此后未改**；`a85c1216..HEAD` 对 `InMemoryToolApprovalService.cs` **无改动**（`CheckAsync:345` / 方法 `:1226` 仍与规格引用一致） | ✅ |
| §14.12.9 | §14.12.1–14.12.5 **每一条**都必须有离线单测 | 六个枚举成员在测试中**均被引用**：`ShellMetacharacter ×5`、`BareInterpreter ×4`、`NonStaticPlaceholder ×4`、`PathOutsideWorkspace ×3`、`WildcardOrRegex ×3`、`IrreversibleWithoutBackup ×1` | ✅ |

> **结论**：规则层七条**全部对上**，本轮**未发现落差**（与 §7 七条验收、§7.1 真链路探针共同构成交付前证据包）。

### 7.3 管线层规范对照（§14.13 复核，2026-09-21）

| 条款 | 要求 | 证据 | 结果 |
| --- | --- | --- | --- |
| §14.13.3 | 永久类需**逐分类可信度 ≥ 0.90**，否则降级为对应单次类；仲裁 `Unknown` ⇒ 按 ④ 处理 | 门槛默认 `DefaultPermanentConfidenceThreshold = 0.90`（`ToolCallClassifierPipelineOptions`）；降级由 `downgraded` 标记并写入覆盖审计 Reason；用例 08/08b/08c（含 0.899/0.900 边界）、05/06（`Unknown` ⇒ `override_unavailable`/`arbiter_unavailable`） | ✅ |
| §14.13.4 | 覆盖审计**必落**，至少含：原候选结论 / 覆盖后结论 / 分类器 id / 型号 / **逐分类可信度** / 理由 / 时间 / 身份四元组 / 工具 id 与参数 | `ToolApprovalAuditEvent`（`PuddingCore/Tools/ToolApproval.cs:428+`）：`ClassifierId`、`ClassifierConfidence`（注释：**§14.13.3 门槛校验用的那个值**）、`ReviewerModel`、`Reason`（含 `candidate=`/`final=`/`confidence=`/`threshold=`）、`CreatedAtUtc`、四元组、`ToolId`/`Command`/`ArgumentsJson`、`AllowlistRuleId`、`Effect` ⇒ **九项均可定位**；用例 02 逐字段断言 | ✅ |
| §14.13.5 | ① 分类器自产永久规则命中 ⇒ **不再回调仲裁**；② 同一次求值**只调用仲裁一次** | `ToolApprovalPortalTests.cs:163`（计数不变）与 `:126`（人工规则计数 +1）互补；管线用例 09（单次求值仲裁至多一次）、11（防循环） | ✅ |
| §14.13.6 | 仲裁用**独立 CTS** 默认 3000 ms；超时/异常/无 answers/解析失败 ⇒ 一律按 ④（**绝不冒泡**） | `DefaultArbiterTimeoutMs = 3000`；用例 07（60 ms 覆盖，断言不挂起）、05/06（异常不抛） | ✅ |
| §14.13.7 | 新增 `IToolApprovalReviewer` 实现；**不改** `CheckAsync`；**不删** `LlmToolApprovalReviewer`；**默认值翻转是单独一步且更新守护测试** | `ClassifierToolApprovalReviewer` 存在；`CheckAsync` 未改（见 §7.2 第 6 条）；`LlmToolApprovalReviewer` 保留且注释写明回退；**守护测试存在且最新**：`ClassifierActivationWiringTests.W3_DefaultReviewerOption_IsClassifier_AfterActivation` 断言 `"classifier"`，另有 `JevToolApprovalReviewerTests.cs:440` 同断言 | ✅ |
| §14.13.8 | 规则/仲裁分类器均可注入；求值序、覆盖审计、永久类降级、不可用分支、防循环**每条都要有离线用例**；假件可模拟七种情形 | `ToolCallClassifierPipelineTests`：①–⑫ 映射齐全（含 08/08b/08c 降级、05/06 不可用、09 单次、10 取消、11 防循环、12 审计字段可解析）；本轮实测 **16 passed / 0 failed** | ✅ |

> **本轮唯一发现并修掉的落差（文档类）**：`ClassifierActivationWiringTests.cs` **类头注释**仍写"W3 默认未翻转（仍 llm）"，
> 与同名用例（`…IsClassifier_AfterActivation`，断言 `"classifier"`）**相反** ⇒ 已改为"**已翻转**（= `classifier`，原 llm）+
> 回退方式"。**同文件内的用例与注释互相矛盾时，注释是骗人的那一方**——这类落差不会让任何测试变红，必须靠人工对照。
