# RSI-G6 验收记录（父代理独立核对）· 2026-09-22

> **状态：进行中快照** —— 子代理 `206a9b48ec904ebb93e7541131fbb835-sub-a52597a9`
> （`bigmodel/glm-5.3-flash`，创建 2026-09-21 18:08:52 UTC）**仍在运行**（截至 2026-09-22 02:36：122 请求 / $15.47 / 缓存 96.9%）。
>
> 本记录只记录**父代理独立观测**到的事实，**不采信子代理自报**；每条结论均标注取证手段。
> 因该子代理**正在实时改写工作树文件**，本快照只对**观测时刻**负责；凡与最终交付不一致处，以最终交付 + 重新复现为准。

---

## 1. 六处接入点核对（截至 02:36 磁盘状态）

| # | 接入点 | 结论 | 取证 |
|---|---|---|---|
| ① | 白名单 `SubconsciousWorkerService.cs:211` | ✅ 已加 `or SubconsciousJobTypes.SkillCurate))` | 两段 `file_read` + `search_grep` 行号 |
| ② | **dispatch `switch`** | ⚠️ **02:36 曾观测到缺 `case SubconsciousJobTypes.SkillCurate:`**（当时 `ImproveSkills` 之后直接落 `default: return false;`，约 `:236-256`）；**02:40 复观测已补齐于 `:258`** ⇒ 转「需变异取红确认」 | `search_grep SkillCurate\|SkillCuration` 无 case 行 |
| ③ | `loops.Add` `:82` | ✅ | 同上 |
| ④ | `SkillCurationLoopAsync` `:754` + 读调度配置（`SkillCurationInitialDelaySeconds/IntervalSeconds`） | ✅ | `search_grep` `:757-759` |
| ⑤ | 触发工具 `SubconsciousTriggerTool.cs` | ✅ **5 处全到位**：`:57`（`"skill_curate" =>`）、`:60`（错误文案）、`:145`（`RunSkillCurateAsync`）、`:193`（步骤表）、`:248`（`ToolParam`） | `search_grep` |
| ⑥ | 调度配置 `SubconsciousOptions.cs` | ✅ `SkillCurationInitialDelaySeconds = 1_200`、`SkillCurationIntervalSeconds = 14_400`（与 SkillImprovement 同量级 4h） | `git_diff` |

## 2. 缺陷清单（验收必修）

### D1 —— ② 缺失 ⇒ **作业永不被处理** + 死代码（功能性缺口，静默）

> **状态更新（02:40 复观测）**：`case SubconsciousJobTypes.SkillCurate:` **已出现于 `:258`**（白名单 `:208-211` 与 case **同时存在**）
> ⇒ 磁盘上已补齐。但**补齐 ≠ 验证过** —— 仍须做**变异取红**（删掉 case ⇒ `DurableWorker_SkillCurate_...` 必超时/必红）方可关闭本条。
> 下方「回归性质」分析保留，作为**整文件重写风险**的现场证据。


`skill.curate` **通过了白名单**，却在 `switch (queueItem.JobType)` 落到 `default: return false;`。
后果：该作业每 4h 入队一次、**永远不完成**；且 `CreateSkillCurationResultEnvelope`（`:328`）成为
**死代码**（`search_grep` 证实 worker 文件内已无 `SkillCurateAsync` 调用；**未用私有方法不产生编译错误/警告** ⇒ 静默）。

**回归性质**：早前（约 02:29）`git_diff` 显示的状态**恰好相反** —— 那时**有** `case`、**无**白名单项。
⇒ 判定为**"用陈旧内存快照重写文件"导致的回归**（整文件覆盖语义下，从旧副本重新落盘会丢掉期间新增内容）。
**该风险对任何"整文件重写"型子代理都成立。**

**修法**：补 `case SubconsciousJobTypes.SkillCurate:`（调 `_orchestrator.SkillCurateAsync(queueItem.Job.WorkspaceId, queueItem.Job.AgentId, memoryLlmConfig, ct)` → `CreateSkillCurationResultEnvelope`）⇒ 同时消灭死代码。

### D2 —— I1 不变式"无牙"（设计缺陷）

`Source/PuddingMemoryEngine/Services/SubconsciousOrchestrator.cs:1602` 起 `SkillCurateAsync` 内：
`var nAfter = enabledSkills.Count(skill => skill.Enabled);` —— `enabledSkills` 是**扫描开始时取的同一内存列表**
⇒ `nAfter` **恒等于** `nBefore` ⇒ I1 这条"防越界写盘"的防线**形同虚设**（只剩 A4 探针能证 I3）。

**修法**：`n_after` 必须在扫描**之后重新从 store 读一次**启用数 ⇒ 一旦中途有写盘，`n_after != n_before` 才可能暴露。
⚠️ 不得以"本片确实零写盘"为由放过：**字段的证明能力**与**当前事实**是两回事。

### D3 —— BOM 被剥（无关改动）

已确认 README 之外 4 个文件首行 `-﻿` → `+using`：`SubconsciousOptions.cs`、`SubconsciousWorkerService.cs`、
`SubconsciousTriggerTool.cs`、`SubconsciousDtos.cs`（原本有 BOM）。`tmp/bomcheck.ps1` 实测 7 个改动文件**当前全部无 BOM**（首 3 字节 `75 73 69`）。
**修法**：`[System.IO.File]::WriteAllText($p, (Get-Content -Raw -Encoding UTF8 $p), [System.Text.UTF8Encoding]::new($true))`；
其余文件按 `git diff` 首行是否出现 `-﻿` 判定（无需额外探测）。

## 3. 已确认正确的部分

| 项 | 结论 | 取证 |
|---|---|---|
| 冻结常量 | `SkillCurate = "skill.curate"`、`SkillCuration = "skill.curation_report.v1"` **逐字一致** | `git_diff` |
| 报告 DTO | `SkillCurationReport` 字段齐备（`NBefore`/`NAfter`/`NotReducedReason`/`CandidateCount`/`RetireSuggestionCount`） | `git_diff` |
| I3 现状 | handler 内**只调用** `_skillStore.ListAutoGeneratedAsync`（**读**）；`SetEnabledAsync` 在 `SubconsciousOrchestrator.cs` 内**零调用**（唯一命中在 `SkillEvolutionDeduplicationService.cs:357`，非本路径） | `search_grep` |
| I2 失败路径 | `catch` 分支给出非空 `NotReducedReason = "Curation scan failed before..."` ⇒ 失败时同样满足"跑了必须有解释" | `file_read :1664` |
| 幂等键（旁证） | `SubconsciousWorkerService.cs:770` `periodic:{jobType}:{workspaceId}:{agentInstanceId}:{bucket}` ⇒ 与任务书冻结契约一致 | `search_grep` |
| **A4 探针有牙** | `ProbeSkillStore` 三写方法均 `Interlocked.Increment` + **抛异常**，并断言 `WriteCallCount == 0` ⇒ I3 是**注入探针**证明，非读码结论 | `file_read :298-345` |
| **A3b 有牙** | `DurableWorker_SkillCurate_ShouldReportWithoutWritingSkills` `await queue.JobCompleted.Task.WaitAsync(5s)` ⇒ **case 缺失会导致超时即红** | `file_read :262-303` |
| 报告数值自洽 | 探针给 3 个启用技能（2 同名 + 1 异名）⇒ 断言 `n_before=3`/`candidate_count=3`/`retire_suggestion_count=1` ✓ | `file_read` + `file_read :305-320` |

## 4. 方法论记录（可复用）

1. **跨调用观测矛盾时，不假设自己看错** —— 引入**第三处独立观测**（`search_grep` 行号）定分晓，并以**最新状态**为准。
   原因：子代理**正在实时重写**同一文件，单次观测随时过期。
2. **"早期只读核对"性价比极高** —— 在子代理仍在跑时就抓出 D1/D2/D3，比最后返工便宜得多。
3. **"有牙"必须逐个证明**：D1 之所以能被抓到，正因为有人在用例里写了"等作业完成"的断言；
   若只断言"没崩"，这类缺口**不会有任何断言失败**。

## 5. 最终验收（待补）

- [ ] D1 补 case 后重跑 `DurableWorker_SkillCurate_ShouldReportWithoutWritingSkills`
- [ ] D1 **变异取红**：删掉 case ⇒ 该用例**必红**（超时）
- [ ] D2 `n_after` 改为扫描后重读 store；**变异取红**：注入一次写盘 ⇒ `n_after != n_before`
- [ ] D3 恢复 BOM 并核验 `git diff` 首行不再出现 `-﻿`
- [ ] A1/A2/A3a–A3d/A5/A6 逐条复现（含 `PeriodicLoops_SkillCuration_ShouldNotReopenSameTimeBucketAcrossRestarts` 的跨 bucket 幂等）
- [ ] `PuddingPlatformTests/Controllers/SubconsciousDebugApiControllerTests.cs:34-36` 是否需同步更新
- [ ] 白名单提交（**排除** `memory/INDEX.md`、`memory/agent-collaboration-agreement.md` 等他人 WIP）+ 推送
- [ ] 同步 `code_map` / 类注释（worker 类注释已更新为"四个定时循环"✓）

---

## 3.1 补充证据（02:40 复观测）

- **结果封装映射**：测试新增 `[DataRow(SubconsciousJobTypes.SkillCurate, SubconsciousJobResultKinds.SkillCuration, 0)]`
  （测试文件 `:467`）⇒ 封装 `kind` 与 **`OperationCount = 0`** 均被断言 ✓（与 I3 零写盘一致）
- **假编排器覆盖**：测试内 fake orchestrator 的 `switch` 亦新增 `case SubconsciousJobTypes.SkillCurate:`（`:533`）✓
- **worker 侧**：白名单 `:208-211` 与 `case` `:258` **同时存在** ✓（⇒ D1 的磁盘缺口已闭合，待变异取红）
- **平台测试面未变**：`PuddingPlatformTests/Controllers/SubconsciousDebugApiControllerTests.cs` **未被改动**（见 §6 裁决）

## 6. 边界裁决：debug API 的 `all` **不含** `skill.curate`

**事实**：`PuddingPlatformTests/Controllers/SubconsciousDebugApiControllerTests.cs:15`
`TriggerEvolution_All_ShouldEnqueueThreeScopedDurableJobsAndReuseRequestId` 断言 debug 面 `Action="all"` 只入队 **3 类**作业
（`AutoDream` / `ExtractPatterns` / `ImproveSkills`），且 `queue.Requests.Count == 3`。G6 **未改动** debug 控制器面。

**裁决：本次不改，且该测试保持有效（无需更新）。** 理由：

1. G6 的**冻结接入点是 6 处**（白名单 / `switch` / `loops.Add` / loop 方法 / 触发工具 / 调度配置），**不含 debug API 面**；
2. 用户可得的手动触发路径是 `SubconsciousTriggerTool`（**已含 `skill_curate`** ✓，5 处接入全到位）；
   debug 面属**内部调试入口**，不是用户路径；
3. 在冻结切片内改动第二个面 = **扩大范围** + 连带改该测试（3 → 4）+ 复核 `Reused`/`Jobs` 断言，风险大于收益。

**代价（显式记录，避免成为静默缺口）**：两个手动触发面**当前不对称** —— 用 debug 的 `all` 演练潜意识管线会
**静默跳过** curation。若后续要求一致性，应**单独开片**（含：控制器 `all` 列表 + 该测试 3 → 4 + 复核断言），
⛔ **不得**顺手塞进 G6 / L3-b 等其他切片。

> 记录方式说明：本裁决遵循「**裁决/规格结论 ≠ 执行载体**」纪律 —— 若未来决定要做，必须有看板卡；
> 若决定不做，也必须**留下带理由的显式记录**（本条即是），**不得静默失效**。
