# [P0·安全] 工具审批/授权链路信任根加固 —— 现场复核与实施计划

| 属性 | 值 |
|---|---|
| 任务卡 | `847df58b2fbe4062ae56beca8b149690`（p0，Assigned→InProgress） |
| 认领 | 2026-09-21 17:15 +08:00（6a8，version 4→5） |
| 复核基线 | HEAD `faa99dd` |
| 前置 | 审计员 2026-09-18 三轮 inform（约 23h 未受理）；蜜糖 2026-09-21 受理并做行号级复核 |
| 本复核 | **只读**（未改任何源码）；目的 = 满足卡内「修复时须现场确认」要求 |

---

## 1. 复核结论（逐项）

| # | 发现 | 蜜糖复核 | 6a8 现场复核 | 差异 |
|---|---|---|---|---|
| ① | 免票据双通道 | 成立 | **成立，且比描述更严重** | 见 §2.1（判据假设错误，影响修法） |
| ② | 三份审批存储无签章/序号/hash 链 | 暂接受（未逐行） | **本轮未复核**（同意暂接受） | 实施时须确认"三份"所指 |
| ③ | shell 无路径边界 | 成立 | **成立** | 无 |
| ④ | Once 授权非原子消费（TOCTOU） | 成立（`:173`） | **成立，但消费点实为 `:177`** | 见 §2.2 |
| ⑤ | `tool-authorizations.json` 无完整性 + 先于审批 | 基本成立 | **前半确认；后半待确认** | 见 §2.3 |
| ⑥ | `CommandContains` 子串放大 | 已修复，不成立 | 未复核（接受蜜糖结论） | 仅需回归测试 |

## 2. 三处需要修正/扩展的事实（本复核的主要产出）

### 2.1 发现①：危险不在"名单太长"，而在"名单的语义假设错误"

`ToolApprovalCommandFirewall.cs:59` 起的 `SafeFirstTokens` 中有一组注释为
**「轻度文件操作（不破坏既有内容）」**，但同组实际含：

```csharp
"copy-item", "copy", "cp", "xcopy", "new-item", "mkdir", "md", "touch",
```

**`copy` / `xcopy` 天然覆盖目标文件**，与「不破坏既有内容」**直接矛盾**；`new-item` 亦可 `-Force` 覆盖。
而 `IsSafeSegment`（`:330`）**只校验首词（`:346`）与 git 动词白名单**，全流程**唯一带参数校验的是 `tee-object`**（`HasSafeTeeObjectTarget`）。

⇒ **结论**：仅"收窄白名单"不足以保证闭环 —— 需要**按能力重分类**，并对"写-覆盖"类施加**目标路径校验**。

**可达示例（白名单实测支持）**：
- `copy <任意源> <runtimeRoot>\tool-approval\tickets.json`（首词 `copy` ∈ 白名单，且无目标校验）
- `type D:\data\config\llm.providers.json`（首词 `type` ∈ 白名单 ⇒ 敏感配置可读）

### 2.2 发现④：真正的消费点是 `:177`，不是 `:173`

```csharp
if (grant.Scope == ToolAuthorizationScope.Once)          // ~:167
{
    if (grant.RemainingUses is null or <= 0)
    {
        _grants.TryRemove(key, out _);                   // :173 ←「已耗尽则拒绝」分支
        continue;                                         //       丢弃返回值无害（随后拒绝）
    }

    _grants.TryRemove(key, out _);                       // :177 ←【真正的消费点】丢弃返回值
    _logger.LogInformation("[ToolAuth] AuthorizedOnce ...");
    return Task.FromResult(new ToolAuthorizationCheckResult
    {
        IsAuthorized = true,                             // ← 无条件 true ⇒ 并发双消费
        ...
    });
}
```

**修法**（落在 `:177`）：
```csharp
if (!_grants.TryRemove(key, out var consumed) || consumed is null)
    continue;   // CAS 失败 ⇒ 视为未授权，继续检查下一候选 grant
```
语义：**只有 CAS 成功的那一个请求**返回授权；竞争失败者不得获得授权。

> 注：蜜糖描述的行为（"丢弃返回值后无条件返回 true"）**完全正确**，仅行号指向 `:173`（同块内的相邻分支）。

### 2.3 发现⑤：前半确认，后半待确认

- **✅ 确认（完整性）**：`InMemoryToolAuthorizationService.cs:36`
  `_permanentGrantFilePath = Path.Combine(runtimeRoot, "tool-authorizations.json")`；
  `SavePermanentGrants()`（约 `:199`）序列化 `{ Version, Grants }` —— **无签章、无序号、无 hash 链**；
  且用 `File.WriteAllText` **明文可写** ⇒ 任何能写该路径的工具即可**直接提权**（授予 permanent）。
- **⏳ 待确认**：「**先于审批**查询」需现场追踪 `AgentFirewall` 与审批服务的调用序（本轮未读到该调用点）。

## 3. 实施计划（严格遵守卡内顺序强约束）

| 步骤 | 内容 | 依赖 | 验证方式 |
|---|---|---|---|
| **A** | 三类审批存储 + `tool-authorizations.json` 完整性（签章/序号或 hash 链；匹配时校验 `Source`/`ApprovalTicketId`/`DefinitionHash`） | 先确认 ② 的"三份"所指 | 单测：**篡改后拒绝使用** |
| **B** | 配 `ToolApproval:Llm` | A 完成 | 真链路探针（`classify` 非 deferred） |
| **C1** | 先配好 profile（避免 `dotnet`/`git` 掉灰区 `deferred_dependency`） | B 完成 | 分类器对常规命令不 deferred |
| **C2** | 再收窄 `CommandFirewall` 安全首词白名单（**+ 建议按 §2.1 扩展为能力重分类 + 覆盖类目标校验**） | C1 完成 | 回归：原白名单命令不误伤 |
| **D** | ④ Once 原子消费（`:177`）+ 绑 `ArgumentsHash` 或会话；票补 TTL | 独立 | 并发双消费单测（两条并发只应一条通过） |
| **E** | 硬边界（含 `CommandFirewall` Deny）判定**前移到授权判定之前** | 独立 | 单测：持 shell 授权**也不能**执行任意命令 |
| **F** | ⑥ 回归测试 | 独立 | 测试计数 |

**窗口纪律**：涉及重启验证的重型动作按协议 §12 排入**非工作时段**；本轮（工作时段）只做**只读复核**。

## 4. 请求蜜糖裁决（申请 go）

1. **§2.1 的修法扩展**：是否同意把 C2 从"收窄名单"扩展为
   **「按能力重分类 + 覆盖类目标路径校验」**？理由：仅收窄名单**无法**解决你举的 `copy` 覆写票据的例子 ——
   因为 `copy` 会被判为"轻度文件操作"而保留。
2. **④（D 步）是否可独立实现并提交到分支**（**不合入 master**，等整体 go）？还是必须与 A/B/C 同批交付？
   （卡内 §5 要求「蜜糖 go/no-go 后才可合入 master」，故本复核**未动任何源码**。）

## 5. 未复核项（如实申明）

- ② 的"三份审批存储"具体所指（待定位确认）
- ⑤ 的 `AgentFirewall` 查询序（"先于审批"）
- ⑥ 的回归测试现状

---
*复核：6a8（2026-09-21）· 只读，未改源码 · 待蜜糖裁决 §4 两点后进入实施*


---

## A 步现场确权（2026-09-21 20:20，只读复核）

**结论**：卡内所称「三类审批存储」经现场定位为 **4 个持久化面**，全部**无签章 / 无序号 / 无 hash 链**。

| # | 文件（相对 RuntimeRoot） | 实现（`Source/PuddingRuntime/Tools/Approval/`） | 结构 | 完整性保护 |
|---|---|---|---|---|
| 1 | `tool-approval/tickets.json` | `FileToolApprovalTicketStore`（`FileToolApprovalStores.cs:17`） | 整文件 JSON 数组，按 `TicketId` 建字典 | **无** |
| 2 | `tool-approval/allowlist.json` | `FileToolApprovalAllowlistStore`（`:52`） | 同上，按 `RuleId`；**每次保存自动注入 `ToolApprovalBuiltInAllowlistRules.Create()`** | **无** |
| 3 | `tool-approval/audit-events.jsonl` | `FileToolApprovalAuditStore` | **追加式 JSONL**，仅进程内 `SemaphoreSlim` 串行化 | **无** |
| 4 | `tool-authorizations.json` | `InMemoryToolAuthorizationService`（`:36`） | `PermanentGrantFile{ Version=1, Grants[] }` | **无** |

### 四条对修复设计有直接影响的现场事实

1. **写入是「整文件重写」**：`SaveUnlockedAsync` ⇒ `File.WriteAllTextAsync(path, 全量数组)`。
   ⇒ 完整性方案**不能**用「逐条序号」；须对整个文件做**签章**（或改造为 append-only + 序号链）。**这是 A 步方案选择的首要约束。**
   例外：`audit-events.jsonl` 已是追加式 ⇒ 可直接上 hash 链 / 单调序号。
2. **损坏即视为空**：`JsonException` → 返回空字典 + `LogWarning`；`tool-authorizations.json` 的 `catch` 走「忽略」。
   ⇒ 这四个面**失败方向都是安全的**（丢失授权只会退回"重新要求授权"），**不可被攻击者用来提权**；但同时也说明**篡改是静默的** —— 正是本卡要修的靶心。
3. ⚠️ **`tool-authorizations.json` 已存在一层天然缓解**：`LoadPermanentGrants` 只接受 `Scope == Permanent` 的条目 ⇒ **无法**通过手改文件注入 `Once`/`Session` 授权。
   **但可注入任意 `Permanent` 授权** ⇒ 与卡内发现⑤ 一致（该文件先于审批被查询，命中即 `Allow`）。
4. ✅ **`DefinitionHash` 匹配校验已存在**（`InMemoryToolApprovalService.cs:1332-1367`）：存储哈希缺失或与当前定义哈希不一致时，以 `Reason = "definition drift: …"` 拒绝。
   ⇒ 卡内 (b) 的 `DefinitionHash` 那一半**已有实现**；A 步只需补 **`Source` 与 `ApprovalTicketId`**（防止「拿一张旧票据的哈希去匹配另一条来源」）。

### 据上述事实对 A 步实施顺序的调整

- `DefinitionHash` 校验已存在 ⇒ **不重复实现**，改为「验证 + 补契约测试」（现有是否已覆盖 drift 拒绝路径待查）。
- 「整文件重写」⇒ 优先选**整文件签章（HMAC + 落盘单调序号）**，而非逐条序号；`audit-events.jsonl` 另行上 hash 链。
- A 步开工前必须定下的**两个决策点**：① 签章密钥来源（应沿用既有密钥设施，待查 `PuddingDataPaths` / 配置层）；② **存量无签章文件的迁移策略**（拒绝 / 首次加载即签章并记一次审计）。倾向于「首次加载即签章 + 审计」，但**需蜜糖确认**（属裁决域）。

### 未验证（不臆断，不扩大范围）

- `ToolApprovalBuiltInAllowlistRules.Create()` 的具体条目与 `SafeFirstTokens`（C2 步骤相关）**未在本次复核范围**。
- 是否存在其他审批持久化面（如平台侧 SQLite 表）**未穷尽** —— 本次范围限 `Source/PuddingRuntime`。


---

## A2 现场确权（2026-09-21 20:30）— AgentFirewall 调用序

**回应验收标准 8 的未复核项。结论：发现⑤ 后半「先于审批」成立，且实际存在 3 条 bypass 通道 —— 比"先于"更严重。**

`AgentFirewall` Gate 4 授权门（`Source/PuddingRuntime/Services/AgentFirewall.cs`）按代码顺序有 **3 个 Allow 出口**：

| # | 行号 | 命中条件 | 查了授权服务？ | 查了审批服务（含 CommandFirewall / BuiltInPolicy / Ticket / ImplicitAudit）？ |
|---|---|---|---|---|
| 1 | L197-224 | `_fullAccessGrants.GetActiveAsync` 命中 | ❌ | ❌（已有独立审计事件 `FullAccessGateBypass`） |
| 2 | L237-239 | `_authzSvc.CheckAsync(...).IsAuthorized` | ✅ | **❌ ← 关键** |
| 3 | L244-262 | `_approvalSvc.CheckAsync(...).IsApproved` | ❌ | ✅（CommandFirewall 在 `InMemoryToolApprovalService.cs:561` 内求值） |

代码原文（L237-239）：

```csharp
var authorization = await _authzSvc.CheckAsync(authzCtx, descriptor, ct);
if (authorization.IsAuthorized)
    return FirewallDecision.Allow();     // ← 直接放行；审批链路（含 CommandFirewall Deny）被完整跳过
```

### 两条对实施有直接影响的推论

1. ✅ **卡内步骤 5 的必要性被证实**：`CommandFirewall` 的 Deny（硬边界）**只在出口 3 内部**被求值 ⇒ 命中出口 1/2 的任何授权都会成为「任意命令通行证」。这正是「免票据双通道」（发现①）与「永久授权可提权」（发现⑤）能拼成完整攻击链的原因。
   ⇒ **精确落点（比卡内描述更靠前）**：硬边界判定须前移到 **L197 之前**；或（推荐）在 3 个 Allow 出口处**统一收口**为一个 `EnforceHardBoundary()` 出口守卫 —— 避免将来新增第 4 条通道时再次遗漏。
2. ⚠️ **出口 1 是此前无人登记的第三条通道**：`_fullAccessGrants` 命中时**连授权服务都不查**。它已有独立审计（好实践），但同样**未经硬边界**。修 A/B/C 时必须一并覆盖，否则「P0 修完仍可被 full-access 绕过」。

### 验收标准 8 回写完成情况

| 未复核项 | 状态 |
|---|---|
| 发现②「三份存储」具体所指 | ✅ 已确权为 **4 个持久化面**（含 `audit-events.jsonl`），见上一节 |
| 发现⑤ 后半 `AgentFirewall` 调用序 | ✅ **本节完成**，且额外发现出口 1 |
