# Agent 统一检索与渐进展开工具链

日期：2026-09-13。状态：Proposed；仅设计，未实施、未执行本地性能或产品验收。当前范围按用户要求限定为整合现有搜索能力，tgrep 仅作为设计参考，不引入其程序、依赖或服务。决策见 [ADR-089](../07架构/103ADR-089Agent统一检索与渐进展开工具链ADR.md)。

## 1. 目标与现状

Agent 用一个搜索入口发现文件、符号和文本，再沿同一结果引用读取源码、查看结构和追踪关系。引擎选择、路径解析、索引状态和分页由服务统一处理；保留各引擎专业能力，减少 Agent 选工具、搬运参数和重复查询的轮次。

本次基于当前源码核对。用户所说 code_search 对应代码索引检索能力；本次扫描到的原生注册 ID 是 code_symbol_search，不能将 PuddingCodexService 的 Codex 任务执行器误当代码索引引擎。

| 当前能力与入口 | 证据和整合点 |
| --- | --- |
| `Files/FileSearchTool.cs` | Everything SDK 优先、内置递归回退；按名称/路径搜索，不搜索内容；Provider、目录和回退暴露在工具参数/结果中 |
| `CodeIntelligence/CodeQueryTools.cs` | 符号、探索、调用方、被调用方、影响分析与索引状态分散；关系查询需要符号和项目身份 |
| `CodeIntelligence/CodeSummaryTool.cs` | 按名称搜索后取第一个结果，有重名歧义；同时输出文本和 JSON，重复呈现内容 |
| `CodeIntelligence/CodeOutlineTool.cs` | 直接解析当前文件，C# 使用 Roslyn，其他语言使用 IFileOutlinerRegistry；可作为无索引展开能力 |
| `CodeIntelligence/ProjectMapTool.cs` | 扫描目录并提取 C# 顶层类型，与 list_dir、Outline 有重复；不能视作完整多语言项目图 |
| `Files/FileTools.cs`、FileChunkService | 已有读取、分块、分页和大结果约束，应复用而非重写读取器 |
| `Search/SearchGrepTool.cs` | Lucene 分词查询命中直接返回，未按同一大小写/正则合同复核；回退是有界托管扫描，两条路径并不等价 |
| `Services/Search/SearchAttemptLedger.cs` | 去重键仅覆盖部分参数，版本来自进程内缓存的 Git HEAD；未提交编辑不会可靠失效，timeout 也会被短路 |
| `SmartWorkflow/SmartExploreTool.cs` 等 | 手工编排多个检索工具，提示中同时存在 grep 优先和索引优先流程，应统一为一条操作路径 |

上述 Runtime 文件均相对 `Source/PuddingRuntime/Tools/BuiltIns/`，Services 路径相对 `Source/PuddingRuntime/`。代码索引服务位于 `Source/PuddingCodeIntelligence/`。

## 2. Agent 表面：一个检索工具，一个展开工具

### workspace_search

必要参数只有 `query`；`scope` 默认执行快照的工作目录，`target=auto` 默认同时查询适用的文件、符号和文本来源。可选 `target=files|symbols|text` 表达搜索对象，而非引擎；`match=literal|regex` 默认 literal，`case=sensitive|insensitive` 默认 insensitive；`glob` 是独立文件过滤器。显式 target 不支持某种匹配模式时返回 contract_error；auto 可省略不支持的来源，但必须报告覆盖情况。

其他可选参数：`limit` 默认 20、上限 100；`cursor`；`freshness=indexed|current` 默认 indexed。初版只提供必要过滤与分页，不暴露底层引擎查询语法，也不自动调用模型改写查询。整句需求按原文匹配，不能承诺自然语言语义检索。未来语义检索需独立能力和证据标记。

auto 对同一词执行有界联合召回，例如 FileSearchTool 可同时命中文件名、类定义和引用文本。文件名匹配不是文本搜索前置过滤：不能只在文件名包含 query 的文件中搜内容。glob/scope 才是所有后端共享的过滤条件。

### workspace_open

必需 `target`，接受搜索返回的 ref 或显式路径；默认 `view=excerpt`。按目标支持 `outline|summary|relations|map|status`：

| view | 输入与输出 |
| --- | --- |
| excerpt | 命中附近文本；可指定行范围、方法范围或继续读取 cursor，复用 FileChunkService |
| outline | 文件/类型的结构树；按文件版本缓存，复用 Roslyn 与 IFileOutlinerRegistry |
| summary | 精确符号的签名、文档注释和少量关系；不隐藏调用 LLM，不按模糊名称取第一项 |
| relations | `relation=callers|callees|references|neighbors|impact`；默认 callers；默认 depth=1，最大 3，节点默认 20、上限 100；支持边来源和继续分页 |
| map | 目录树与已知符号；目录信息覆盖全部允许文件类型，类型信息注明语言支持和截断 |
| status | 指定 scope 的覆盖、后端健康与索引进度；只读，不触发注册、重建或新守护进程 |

references 需要补齐查询 facade，不能用字符串匹配冒充语义引用。关系 ref 定位到具体 workspace/project/symbol，重名返回 disambiguation 候选；解析精度不足的语言标注 syntactic/heuristic，不能输出伪精确调用图。impact 是基于已有关系图的有界影响遍历，不能承诺完整运行时影响面。

搜索与展开共用引用和服务，但分成两个小 schema：前者回答“在哪里”，后者回答“这里是什么、与谁相关”。不会同时向默认 Agent 暴露旧的多个平行检索入口。文件写入、补丁、Shell、索引管理保留独立权限与职责。

## 3. 典型流程与统一结果

```text
workspace_search(query="TryImportCurrentSessionTranscriptAsync")
  → ref=r1，符号定义，绝对路径/行范围/签名，file+symbol+text 命中证据
workspace_open(target="r1", view="summary")
  → 精确符号信息与可展开的关系引用
workspace_open(target="r1", view="relations", relation="callers")
  → 调用边、调用点和目标引用
workspace_open(target="r1", view="excerpt", extent="symbol")
  → 当前方法源码；较长则返回分页 cursor
```

示例 ref 为示意，以上工具尚未实现。简单定位可在一次 search 的紧凑预览中完成，不强制走完所有步骤。

统一 envelope 包括 `query_id/scope/status/items/coverage/continuation`。每项含 `ref/kind/path/range/symbol_id/signature/preview/match_sources/version`，按实际类型省略不适用字段。模型默认只接收紧凑单一表示；完整结构留在 canonical 工具结果和有界 artifact 中，避免文本树加同内容 JSON 的双份重放。

聚合同文件结果，以路径+范围+符号身份去重，保留匹配来源。精确名称/定义优先，其次名称部分匹配，再到文本命中；同级按规范路径和行号稳定排序。为不同来源预留首屏配额，不能让大量文本命中挤掉符号定义。各引擎原始分数不可直接相加。截断候选后再排序只保证已召回集合排序，必须声明其限制。

coverage 按来源返回 `available/index_generation/reconciled_at/freshness/complete/limit_reason`，区分搜索覆盖不全和展示分页。状态至少区分 ok/no_match/partial/index_unavailable/timeout/contract_error/stale_ref；某一路失败仍可返回其他路结果，但总体为 partial。未覆盖范围不能归纳成 no_match。总数未知时返回 lower_bound，不能用已返回数量冒充总数。

ref 是有界结果存储中的身份引用，绑定 workspace、授权范围、规范路径、可选项目/符号 ID、内容版本、索引 generation 和有效期。它不是访问凭证；每次展开重新执行既有权限检查。文件变化后按新文件验证、重定位并明确返回新版本，无法唯一重定位时返回 stale_ref，不能沿旧行号读错符号。cursor 绑定查询参数、排序和 generation；过期返回 cursor_expired，不跨新旧快照拼页。路径始终可作为独立入口，结果存储不是使用工具的前提。

## 4. 复用现有能力的服务结构

```text
workspace_search / workspace_open
  → admission + ScopeResolver + QueryPlanner
  → EverythingPathProvider / CodeIndexProvider / ContentSearchProvider
  → HitMerger + EvidenceVerifier + ResultStore
  → FileReader / OutlineProvider / SymbolSummaryProvider / RelationProvider
  → canonical ToolExecutionResult + bounded model presentation
```

复用 ICodeQueryService、ICodeIndexScopeResolver、CodePathIdentity、IFileOutlinerRegistry、FileChunkService 和现有工具执行/Artifact 管线。抽出 FileSearchTool 内的 Everything provider 与读取/Outline 逻辑为服务；工具不得通过拼接工具调用文本来调用旧工具，也不在内部重跑权限或创建第二套 Agent Loop。

tgrep 只提供设计启发：先缩小候选范围再验证真实匹配、复用索引并跟进变更、索引未就绪时仍可查询、统一控制查询与输出成本。当前阶段不接入 tgrep，不新增搜索守护进程，也不另写一套 trigram 引擎；将这些原则落实到已有组件的协同方式。

| 借鉴的思路 | 在现有组件中的落点 |
| --- | --- |
| 候选筛选后精确验证 | Everything 按 scope/glob 发现路径，代码索引定位符号；Lucene 可提供优先验证的内容候选，托管 literal/regex matcher 验证当前源码 |
| 索引复用与变更跟进 | 复用已有索引范围/调度与文件元数据；查询服务补齐变更失效、删除记录和校准状态，不让每个 Agent 重建索引 |
| 不依赖索引就绪 | Everything 不可用时用现有递归搜索，代码索引不可用时仍可文件/文本搜索，Outline 直接解析当前文件；关系能力不可用需明确报告 |
| 减少重复工作 | 同次查询复用按 scope/glob 得到的候选清单、文件读取与解析结果；跨次缓存绑定真实内容版本和范围 |
| 有界查询与输出 | 统一 deadline、取消、扫描量、结果数量与分页；每个后端不再各自消耗一整份预算 |

精确内容搜索以现有托管匹配器服务化后的实现为权威。Lucene 分词召回不是 trigram 完整候选集：只可用于候选优先级，不能直接作为 literal/regex 结果，也不能用它的零命中排除其余文件。除非查询计划能证明候选覆盖，否则必须继续扫描同 scope/glob 下的剩余允许文件；扫描预算不足返回 partial。非法正则明确失败，不静默改成字面搜索。规划器可在候选优势不足时直接扫描，避免每次查询固定多跑一遍 Lucene。

Everything 路径清单只有在该 scope 的覆盖和时效可验证时才可用作完整扫描清单，否则补充现有文件枚举。代码索引只用于已声明的符号/关系查询，不能将符号命中文件当成全文范围。复用上述能力并不意味着现有索引已具备 tgrep 的性能或完整性保证。

Core 拥有统一查询、索引协调与有界缓存，按实际物理仓库/worktree 隔离并共享给主子 Agent；Desktop 不承载索引业务。沿用现有索引生命周期；索引数据继续进入 PathHelper 指定的存储分类，纳入既有存储管理与回收。变更跟进先核对既有调度/监听能力，再在同一所有者内补齐；不在本设计中假定它们已经实现。

## 5. 范围、新鲜度与安全不变量

- 一次执行冻结 workspace、实际 working tree、scope、过滤规则、授权和输出预算；显式宿主路径沿用既有授权，不因 Everything 全盘可见就自动扩大内容读取范围。不同视图在统一 admission 中分别校验所需能力。
- 各 provider 共享文件范围合同，包括排除目录、glob、大小写、符号链接/重解析点和文件大小策略。Everything 结果需按规范真实路径后置校验。全局索引发现与内容读取保持各自权限，结果引用不能绕过权限。
- indexed 表示索引快照及已知增量，不能暗示当前文件系统完整；current 请求必须覆盖新增、修改、删除和未索引文件。仅复核已有候选不能发现新增命中。通过校准加增量扫描，或同范围全扫描满足；预算不够返回 partial，不能静默降级为 indexed。对并发写入仅声明扫描观察区间，不声称原子文件系统快照。
- 监听故障、溢出、轮询失败和过滤规则变化均使覆盖状态失效。代码、路径和文本各自版本独立报告，不能拼成虚构的统一事务快照。跨后端结果聚合后验证目标版本。
- 失败缓存键覆盖规范化后的全部语义参数、预算、授权范围与可验证内容 generation；未提交编辑、新文件、删除和配置变化必须失效。无可靠 generation 时禁用负结果短路；timeout 仅作诊断，不能无限抑制正常重试。延续有界 ledger，删除以缓存 Git HEAD 判断工作树不变的假设。
- 默认输出预算建议 8 KiB，可继续展开；Provider 共享总 deadline、取消令牌、并发与扫描预算，不能每个后端各占完整预算。以上预算属于 Proposed 默认值，需实测调整。

## 6. 文件级施工顺序

| 阶段 | 主要文件/模块 | 完成门禁 |
| --- | --- | --- |
| U0 合同与基线 | `Source/PuddingCore/Tools/Retrieval/`（新增 DTO/接口）；Runtime `Services/Search/`（scope、planner、coverage、ledger）；现有 SearchGrepTool/FileSearchTool | 同一请求跨后端语义一致；范围、错误、无匹配与截断可区分 |
| U1 统一搜索 | Runtime `Tools/BuiltIns/Search/WorkspaceSearchTool.cs`（新增）；Everything provider 抽出；CodeQueryService；现有 Lucene/托管搜索封装为 ContentSearchProvider | files/symbols/text/auto 可用，局部后端故障、分页和匹配来源正确；精确文本结果由统一 matcher 验证 |
| U2 渐进展开 | `WorkspaceOpenTool.cs`（新增）；FileTools/CodeOutlineTool/CodeSummaryTool/ProjectMapTool/CodeQueryTools 服务化；ResultStore | 重名不误选，旧 ref 不误读，outline/read/relations 共用身份与预算 |
| U3 后台异步索引维护 | Runtime 查询 planner/ledger/缓存；既有 CodeIndexScheduler、ICodeIndexStore、Lucene 索引更新与范围服务；Host DI；详见 §10 | FileSystemWatcher+有界队列、增量写入、执行期间二次变化、失效/删除/陈旧索引清理、崩溃后校准与队列恢复均有证据；查询不触发同步全量重建，全程不增加外部搜索依赖 |
| U4 入口收敛 | PuddingToolRegistry/SkillRuntime/ToolLoopInstructionBuilder/HarnessToolCompatibilityAdapter；BuiltInAgentTemplates/ToolProfileConfig；Host 预设；SmartWorkflow | 默认只暴露 search/open；旧工具授权逐项映射，禁止直接把任一旧权限升成全部新视图权限；无陈旧调用提示 |
| U5 产品验收 | RuntimeTests、CodeIntelligenceTests、HostTests 与外部部署流程 | 新构建外部重启后，新主/子 Agent 会话通过完整工具链，状态中心和退出回收由外部控制器验收 |

旧工具不是永久平行入口：U1–U3 可分支内验证，U4 同一原子切换更新调用方、预设、权限和持久配置（先备份、显式版本变更）。已有 rg/file_read 等 Harness 拼写仅在既有 admission 前归一化到新合同，不保留两套执行实现。注册/注销/重建索引仍为单独管理操作，不合并进 Low 只读工具。search_tools 是工具发现、search_memory 是知识记忆、会话日志是运行证据检索，本轮不强行并入代码/文件索引。

## 7. 验证与度量

正确性先行：相同范围对照不使用索引的全扫描基线验证路径、行号与匹配内容；覆盖短词、符号全名、大小写、中文、正则/非法正则、无匹配、隐藏/忽略文件、大文件、重名、worktree、重解析点，以及修改/新增/删除后立即查询。专门验证“Lucene 无候选但源码存在字面/正则命中”和“Everything 清单遗漏新文件”不被误报为 no_match。禁止用提速补偿漏检；基线与索引辅助路径必须采用同一匹配合同。

工具链测试：search → open → relations → excerpt；ref 过期/跨 workspace/授权收窄；部分引擎掉线；索引未就绪；展示分页与扫描不全；超时后可重试；未提交编辑使负缓存失效。复用已有搜索测试，新增能暴露真实错误的集成用例。

性能在固定 Pudding checkout、相同 query/范围/输出预算下记录冷启动、建索引、warm p50/p95、扫描字节、候选缩减率、更新可见延迟、常驻私有内存与磁盘占用。Agent 任务集合再记录总检索调用数、重复读取率、输出 Token、完成率与总耗时。冻结数据集后确定数值门槛；当前不宣称提升倍数。主子 Agent 共享服务还需验证并发取消与公平预算。

## 8. 参考

- [tgrep README 与架构](https://github.com/microsoft/tgrep#architecture)：候选筛选、索引增量、监听与回退的思路来源，仅作参考。
- [tgrep BENCHMARKS](https://github.com/microsoft/tgrep/blob/main/BENCHMARKS.md)：参考查询与资源测量方法，不作为接入门禁或 Pudding 性能承诺。本轮沿用本会话已读取资料。
- [工具基础设施分层](../07架构/tool-infrastructure-layering.md)：沿用契约/Runtime/Platform/组合根边界；其中旧 grep 实现描述存在漂移，现状以本文件 §1 和源码为准。

## 9. 任务看板登记与执行依赖

2026-09-13 已通过 External Task API v1 登记到 `default` 工作空间。创建前读取全部 160 条任务，未发现同主题实施任务。首次登记的 7 张卡已回读核对，创建时均为 P1、Backlog、version=1、autoDispatchEnabled=false、无 activeAssignmentId。后续 §10 扩展沿用 U3，并同步总任务/U5 的设计和验收条件；版本以最新回读为准。本次只登记设计与后续工作，未指派或启动 Agent，未实施产品代码。

| 阶段 | 看板任务标题 | Task ID | 前置 |
| --- | --- | --- | --- |
| 总任务 | ADR-089｜PuddingAgent 统一检索与渐进展开工具链（总任务） | `b74e561e5299479e9afdd5123f26490d` | 跟踪 U0–U5，避免重复认领实施 |
| U0 | ADR-089 U0｜统一检索合同、匹配语义与正确性基线 | `479bab22e9fe431e8217f72eb4f6329f` | 无 |
| U1 | ADR-089 U1｜实现 workspace_search 联合检索入口 | `205a678a7db14d189dc887c74e877315` | U0 |
| U2 | ADR-089 U2｜实现 workspace_open 与精确结果引用 | `52ac97d8231e40a59d2fa23214ee4dfb` | U1 |
| U3 | ADR-089 U3｜后台异步索引维护、文件监听与失效清理 | `8bddf9017b2d40049f8ea88823c2078a` | U1 |
| U4 | ADR-089 U4｜收敛旧工具、模板权限与 SmartWorkflow | `418495ede7ed4210b8e53cd143ee83cf` | U2、U3 |
| U5 | ADR-089 U5｜统一检索端到端评测与新构建产品验收 | `2d5bc2c901f249cba585b0a85c883465` | U4 |

总任务首次保存设计基线 `39b3e1f` 的完整快照，§10 后台维护扩展同步更新快照；阶段卡包含权威文档绝对路径、实施文件入口、验收条件与前置 Task ID。阶段索引评论保留历史，后续扩展追加说明。External V1 当前未暴露原生父子关系或依赖字段，上表与任务描述承担关联记录，不声称调度器已自动强制这些依赖。执行者认领阶段前须核对前置证据。

设计交付已完成；总任务只有在 U0–U5 的源码、部署、真实新会话和进程外验收全部完成后才能作为产品完成。登记状态仅表示本次回读时刻的事实，后续进度以看板最新记录为准。

## 10. 后台异步索引维护（2026-09-13 扩展）

### 10.1 现有基础与维护所有者

本节是 U3 的必做范围，并进入 U5 产品验收。已有 `CodeIndexScheduler` 使用 ConcurrentQueue/SemaphoreSlim 和后台 Task 执行索引；Enqueue 遇到同 scope 正在执行会直接返回，缺少“执行期间再次变化”的补跑记录。`LuceneSearchEngine.BuildIndexInternalAsync` 已有按修改时间增量筛选、stalePaths 删除和全量回退；不能将“尚无统一维护链路”写成“完全没有增量更新或删除”。但其枚举可跳过读取异常，清理时必须区分扫描不全与真实删除。

改造现有调度器及 provider 写入服务，由 Core 的单一 `BackgroundService` 生命周期统一驱动；避免原 Task worker 与新 HostedService 双写。`FileSystemWatcher` 负责变化提示，队列负责解耦，后台维护账本负责待办和恢复，校准扫描负责补足事件丢失。Everything 自身索引仍由 Everything 维护，Pudding 仅使路径缓存失效并核对可见性，不向其索引数据库写入。

```text
FileSystemWatcher Created / Changed / Deleted / Renamed / Error
  → scope observedVersion++ + dirty 标记 + Channel<IndexChange>.TryWrite
  → 合并/防抖 + 持久维护账本
  → Core 后台 worker（按 scope 合并、按物理索引单 writer）
  → 文件稳定读取/校验 → 增量更新 / 删除 / 范围重建
  → provider commit + committedVersion → 查询/Outline/ref/负缓存失效

启动校准 / 周期核对 / watcher 溢出 / 队列满 / 手工维护命令
  → needsReconcile → 同一后台 worker
```

### 10.2 文件监听、队列与合并

- 监听已登记且可访问的物理根；重叠 scope 复用监听，按各自规则分发，worktree/授权仍隔离。关注文件名、目录名、大小、最后写入及影响可读性的属性/权限；不监听 LastAccess，排除索引自身、临时产物及构建输出，避免自激循环。目录过滤需在事件处理/范围校验中执行，不能假定 Windows 递归 watcher 自动排除子树。
- 回调只记录路径、旧路径、事件类型、epoch/sequence 和时间，更新已登记 scope 的 dirty/version，再 TryWrite；不解析源码、不计算哈希、不写数据库、不阻塞等待。使用有界 Channel，FullMode=Wait 但回调仅 TryWrite；返回 false 时标记该 scope needsReconcile 并计数，不采用无法追踪损失的静默丢弃模式。dirty 标记保存在独立的有界 scope 状态中，不能依赖同一个已满的队列。
- worker 按 scope+规范路径合并，建议静默窗口 500ms、最长等待 2s；连续保存不无限推迟。Created/Changed 合并为“重读最终状态”；Renamed 同时核对旧路径清理和新路径写入；目录移动/删除触发对应子树或最近可读父目录校准，不能期待每个子文件都有事件。删除后迅速重建、编辑器临时文件替换均以执行时文件事实为准。
- Channel 初始容量建议 8192，待处理路径上限建议 20000；超过时将细粒度待办折叠为 scope 校准并释放路径记录。全局待办、批次字节数、解析并发均有界；scope 轮转保证大仓库持续写入不会饿死其他仓库。初始参数是待测默认值，不是已验证性能结论。

### 10.3 持久待办、增量提交与并发变化

Channel 不持久化。复用索引存储体系新增维护控制记录（由 ICodeIndexStore/既有 SQLite 基础设施承载，不存入配置表）：`scope/provider/epoch/desiredVersion/committedVersion/needsReconcile/lastSuccessfulScan`，以及按路径合并的 `operation/attempt/nextRetry/state`。配置仍在 system.json，维护账本属于运行状态。worker 先批量登记待办，再执行；已提交版本只能在对应 provider 真正 commit 后推进。

每次 Core 启动先将可用 scope 标记待校准，建立监听后扫描并处理扫描期间收到的变化；重启后的 epoch 使旧 cursor/ref/cache 不再被误认作当前。回调与持久写入之间崩溃仍可能丢事件，因此启动校准是必需恢复机制，不能宣称持久队列使 watcher 事件绝不丢失。

同 scope 在执行期间的新变化必须推进 desiredVersion/dirtyAgain；当前批次只可推进它实际处理的版本，结束时若仍落后则继续排队。不得沿用“in-flight 时丢弃 Enqueue”的行为。旧解析任务在提交前验证 epoch、配置/解析器版本与文件指纹，发现已经被新版本替代就放弃该结果并补跑；发布后的新变化继续使 scope dirty，不把整个 scope 标为 current。

先做可读性和稳定性检查，再基于同一份读取内容生成 hash/文本/符号。元数据前后改变或文件仍被写入时进行有界重试；hash 用于识别内容不变并跳过重复索引，不能以 Git HEAD、仅 mtime 或 File.Exists(false) 判断内容版本或删除。metadata 未变化也可能漏掉保留时间戳的编辑，周期深度内容核验分批补偿；current 的精确读取按 §5 验证，不能把 metadata 相等宣称为绝对新鲜。

代码单文件更新需原子替换文件记录、符号、引用及相关图边；补齐按文件删除/替换事务接口，不能先对外删除旧符号再逐条暴露新符号。类型签名、项目配置、依赖或解析选项变化可能影响其他文件绑定，按语言能力扩大为编译单元/项目更新；当前 indexer 不支持精确增量时复用范围重建并报告成本，不伪装成逐文件语义正确。

Lucene 按路径统一 delete/update 并 commit，再安全刷新 reader；全文和代码存储各自提交，不声称跨 SQLite/Lucene 原子事务。共享控制账本记录各 provider 的已提交水位；一个成功、另一个失败时保留差异、重试失败侧，重复应用必须幂等。重建在新 generation 中准备，通过校验后切换当前指针，失败继续保留旧 generation 的可用快照及 stale 状态。writer 锁按实际物理索引资源组织，共享数据库/索引目录不能因为 scope 不同而并发双写。

### 10.4 失效与无效索引清理

| 原因 | 立即处理 | 后台维护 |
| --- | --- | --- |
| 文件内容改变 | 该路径预览/Outline/Summary/ref 和相关搜索负缓存失效 | 重读、hash 校验、替换索引；扩大必要语义依赖范围 |
| 文件确认删除或重命名 | tombstone 屏蔽旧路径新查询，旧 ref 返回失效 | 移除文件记录、符号、入/出图边和引用、Lucene 文档及缓存；清理未解析符号引用的路径关联 |
| 文件变为不适合索引（空文件/过大/类型或排除规则改变） | 更新该 provider 的覆盖与排除状态 | 从相应内容/代码索引移除，文件名能力仍按自己的合同保留 |
| 过滤/解析器/索引 schema/根身份变化 | 配置指纹失配，旧 generation 标记 stale 或 incompatible | 对受影响 provider 重建；切换完成后退休旧代 |
| watcher/队列溢出、监听失败、读取异常 | needsReconcile、覆盖 incomplete，不能返回可信全范围 no_match | 原生监听重建或有退避的轮询；执行校准，成功后恢复健康 |
| scope 注销、可证实的孤儿索引、损坏 generation | 停止接纳相应工作，取消旧 epoch；损坏索引退出查询 | 注销按合同移除自有索引；损坏重建；安全回收已退休/孤儿产物 |
| 磁盘/网络根离线、权限丢失或扫描中断 | unavailable/partial，保留最后可信 manifest | 等待可访问后重新核对；不能以空枚举推断所有文件删除 |

校准使用持久文件 manifest 的 mark-and-sweep：只有该 scope/subtree 被完整成功遍历并确认根身份不变，才可清理“本轮未见”的条目。扫描期间收到变化的路径暂缓 sweep 后再次核对。单个路径真实不存在需要区分父目录可访问、权限异常和临时 rename；失败或离线扫描禁止批量删除。已有 Lucene stalePaths 逻辑必须接入此完整性门禁。

对索引行的逻辑移除及时提交，对旧 generation 的物理目录回收由后台异步 GC 执行：验证归属 manifest、实际绝对路径位于 PathHelper 管理的索引根、未被当前 scope 引用、reader/writer 已释放，并经过可配置宽限期（建议 24h）。Windows 文件占用则退避重试，不强删；未能证明归属或仍在构建的临时目录记录为待核对，不能直接删。清理范围仅为 Pudding 拥有的索引/缓存，绝不删除源码、工作区文件或 Everything 的数据。记录删除数量、释放字节、原因与失败，不以队列完成代替删除后置验证。

### 10.5 周期校准、关闭和可观测性

原生监听是及时路径；后台必须在启动、溢出、恢复连接、配置变更后校准，并保留周期性补漏。建议正常 metadata 校准每 15min，监听不可用时从 60s 轮询并退避，深度 hash 核验按 24h 分片预算推进；均按上次完成后计时、不并发叠加，未完成/逾期必须可见。即时文件变化以 debounce 后入队处理为目标；实际可见延迟包含排队/解析/提交，不承诺固定毫秒 SLA。

查询线程只读取可用快照、做有界精确复核并提交 dirty 提示；不等待全量重建或物理 GC。current 无法在本次预算内满足时返回 partial/stale 与原因。scope 状态建议 separated 为 `availability`（available/unavailable）、`maintenance`（idle/queued/running/reconciling/failed）和各 provider 水位，避免以单一 Active 掩盖积压。

停止时关闭事件源，取消接纳新任务、持久化 dirty/checkpoint，在有界超时内结束 writer/reader/worker，再释放 watcher；未完成待办重启恢复，不阻塞 Desktop 长期退出。状态中心/workspace_open(status) 显示 watcher 模式与错误、队列深度、最老待办年龄、desired/committed 水位、最近成功校准、溢出/重试/失败数、索引文件数量、退休待清理字节和端到端更新延迟。日志沿用 canonical trace，避免每个 Changed 事件刷日志。

### 10.6 U3 交付包与 U5 验收扩展

U3 在原任务内按四个可验证提交推进：A 监听/Channel/防抖与状态；B 持久待办、增量提交和 in-flight 补跑；C manifest 校准、失效和安全删除/GC；D 查询缓存协同、运维状态与故障恢复。主要入口为 CodeIndexScheduler/ICodeIndexScheduler、ICodeIndexStore/SqliteCodeIndexStore、各语言 indexer、LuceneSearchEngine/IFullTextSearchEngine、Runtime Services/Search、Host DI/HostedService、配置/存储管理投影；仍采用已有索引，不引入新搜索引擎。

U5 必须覆盖：重复保存/事件乱序、目录移动和临时文件替换；持续写入与合并上限；两 scope 公平调度；OS buffer overflow/Channel 满；执行期间再次修改；持久登记前/提交前/提交后未确认时崩溃；删除文件后所有相关图边/引用/全文命中消失；离线盘/权限失败不触发 sweep；过滤/解析器版本变化；损坏索引重建失败保留旧快照；Windows reader 占用时 GC 延迟并可恢复；停止/重启无遗留 worker，新增/删除后真实 Agent 检索结果更新。正向与故障场景均检查数据库/索引查询和目标路径后置条件，而不只检查事件已入队。

平台依据：[.NET FileSystemWatcher](https://learn.microsoft.com/en-us/dotnet/api/system.io.filesystemwatcher?view=net-10.0) 明确存在重复通知、目录移动通知和缓冲溢出边界；[System.Threading.Channels](https://learn.microsoft.com/en-us/dotnet/core/extensions/channels) 提供有界队列与满队列处理方式。回调、恢复和持久化合同由本项目设计补齐，并非 API 自动保证。
