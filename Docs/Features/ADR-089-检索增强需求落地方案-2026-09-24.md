# 检索增强：需求 → 现状核实 → 缺口 → 施工顺序（2026-09-24）

> 用户需求原文（2026-09-24）：
> 「我认为代码检索，非常适合 LSP 和增强向量检索结合起来，同时，我们需要指定搜索指定的目录或子目录，文件类型。完成之后，我们还需评估性能和准确率。另外，我们还需忽略一些文件夹，比如制成品、node_modules、读取 git 忽略文件的规则进行忽略。」
>
> 本文所有「现状」断言均**实测**（带 `文件:行`），非推测。属 ADR-089 的落地细化，不替代 ADR-089 §10 硬约束。

## 1. 需求 → 现状 → 缺口（逐条）

| # | 用户需求 | 现状（实测） | 缺口 |
|---|---|---|---|
R1 | **读 gitignore 规则进行忽略** | ⚠️ **解析器存在但是死代码**：`Source/PuddingCodeIndex/Services/IndexExcludePatterns.cs:40-183`（`CollectGitIgnorePatterns` / `ParseGitIgnoreFile` / `MatchesGitIgnore` / `ShouldExclude`）。全仓 `code_symbol_search "GitIgnore"` 仅 5 个符号、**全在该文件内**；6 个定向目录 grep 均无外部调用点 ⇒ **它从未生效过** | **必须重做**：见 §2.1 |
R2 | **忽略制成品 / node_modules 等目录** | ⚠️ **全仓 7 套互不相同的规则**，见 §2.2 | **必须统一**：同一目录如今可能在一个工具里被忽略、在另一个里不被忽略 |
R3 | **指定目录 / 子目录** | ✅ `search_grep` 支持（`directory`）；❌ **代码智能族只支持项目级作用域**（`code_symbol_search` / `code_explore` 等无法限定子目录） | 代码智能族需补 scope |
R4 | **指定文件类型** | ✅ `search_grep` 有 `file_ext`（如 `cs;ts`）；❌ 代码智能族无按类型限定 | 同上，合同化 |
R5 | **LSP + 向量结合** | ⚠️ LSP 为**仿 LSP**：`PuddingCodeIntelligence/Lsp/IndexBasedLanguageServerService.cs`（仅 Hover/Definition/References，**依赖索引**，无外部 LSP 进程，**零消费者、无单测、未端到端验证**；`Character`/`PayloadJson` 字段无人使用）。❌ **向量侧无代码 chunk**（`IEmbeddingService` 在 `PuddingRuntime`，当前只服务记忆/会话召回；RRF 融合在 `MemoryEngine/Services/MemoryRecallService.cs`，权重 0.6/0.7/0.7/0.8/1.0 与 k=60 **未冻结、无敏感性实验**） | R5 是全新增量，见 §3 |
R6 | **评估性能与准确率** | ⚠️ **几乎为零**：只有 `Tests/PuddingMemoryEngineBenchmarks`（记忆引擎 BDN，且最近运行 2026-05-11，**当前能否编译运行未验证**）；**检索质量/召回率评测为 0** | **必须先建评测，再做调优**，见 §4 |

## 2. 两个缺陷级发现（比"缺功能"更该先修）

### 2.1 「读 gitignore」看起来实现了，其实从未生效 —— 且实现非规范

`IndexExcludePatterns.cs` 的问题不是"没接线"，是**两重**：

1. **零调用方**（该文件外无任何引用）⇒ 索引链路真正只用了 `IsNoisePath`（`:26-36`，按目录名段匹配的 24 项噪声目录表）。唯一真实使用者是 `CodeIndexWatcher.cs:393-410` 的 `IsExcluded`。
2. **即便接上也不正确**（`:84-115` `ParseGitIgnoreFile` 注释自述 `Skip negation rules (simplified handling)`）：
   - **跳过 `!` 取反** ⇒ `!keep.me` 无法恢复被忽略的路径
   - **无 `**` 语义、无字符类 `[]`、无目录专属规则（尾 `/` 仅被 `TrimEnd` 掉）**
   - `MatchesGitIgnore`（`:127-149`）只有三段粗糙匹配：`EndsWith(core)` ∥ `Contains("/{core}/")` ∥ `Contains("/{core}")`
   - 不处理嵌套 `.gitignore` 的**层级作用域**与**父目录否定恢复**（gitignore 的核心语义）

**反证（这条最能说明问题）**：Lucene 把 `.gitignore` 当作**可索引的纯文本扩展名**（`Source/PuddingFullTextIndex/FullTextIndexOptions.cs:60-61` 白名单含 `.gitignore`、`.gitattributes`）⇒ 系统把 `.gitignore` 当成**检索内容**，而不是**排除规则**。

**轮子已存在（不需要自己写）**：
- 首选 `Ignore`（NuGet `Ignore` / `github.com/goelhardik/ignore`）：**按 .gitignore spec 2.29.2 实现**，且**用 LibGit2Sharp 对真实 `git status` 输出做过测试** ⇒ 与 git 行为一致。
- 备选 `GitignoreParserNet`（`Guiorgy/GitignoreParserNet`）：宣称支持 manpage 全部特性。
- 也可用 **LibGit2Sharp 的 ignore API**，但 `LibGit2Sharp` 目前在 `PuddingRuntime`（`:26`），组件边界不允许 `PuddingCodeIndex` 反向引用 ⇒ 不作为首选。

### 2.2 七套排除规则并存（结果不一致的根因）

| 规则集 | 位置 | 规模 |
|---|---|---|
`search_grep` 默认 `exclude_dirs` | `Source/PuddingRuntime/Tools/BuiltIns/Search/SearchGrepTool.cs:34` | 12 项（`$outputWwwroot;dist;node_modules;bin;obj;.git;.pudding;TestResults;artifacts;publish;.venv;.tmp`） |
同上（description 内联**再来一份**） | 同文件 `:20` | 12 项 |
索引噪声目录 | `Source/PuddingCodeIndex/Services/IndexExcludePatterns.cs:12-24` | **24 项** |
索引器 CLI | `Source/PuddingCodeIndexer.Cli` | 自有一套 `excludedDirs` |
`file_search` / `list_dir` / `project_map` | 各自实现 | 各自规则 |

⇒ 两侧并集/交集不一致：`NoiseDirNames` 有 `.vs/.idea/__pycache__/.tox/coverage/Release/x64...` 而 `search_grep` 没有；`search_grep` 有 `publish/artifacts/.pudding` 而索引侧没有。**这正是"同一个目录有的工具忽略、有的不忽略"的原因。**

## 3. 施工顺序（U4 系列；每项独立组件化，按规程 S1→S5）

| 序 | 切片 | 内容 | 为什么在这个位置 |
|---|---|---|---|
**U4-0** | **评测设施（性能 + 准确率）** | 见 §4 | **必须最先做**：用户要"评估性能和准确率"，且后续每次调优都需要它当判据。没有基线就没有"变好了" |
**U4-1** | **统一忽略合同** | 一个 `IIgnoreRules`：① 规范 gitignore 语义（复用轮子）② 内置噪声目录（制成品/node_modules/…的**并集**）③ 显式排除 + 追加 ④ 「禁用默认」语义保留。所有工具共用 | 修 §2.1 + §2.2；这是"准确率"的第一大污染源（命中 node_modules/dist 的噪声结果） |
**U4-2** | **作用域 + 类型过滤合同化** | 统一 `scope`（目录/子目录）+ `fileTypes`，**并补到代码智能族**（R3/R4） | 用户直接需求；代码智能族当前完全缺失 |
**U4-3** | **LSP 增强（B 起步）** | 先把现有仿 LSP 做成**真正被消费**的能力（补 references facade、禁止字符串匹配冒充语义引用），预留真 LSP 进程接口位 | 现有实现零消费者 ⇒ 先让它有用，再谈外部进程 |
**U4-4** | **向量增强（代码 chunk）** | 代码/文件 chunk 表 + 写入 + 检索接入 + 与词法检索 RRF 融合（复用既有 `IEmbeddingService` 与 RRF 实现），**并把融合权重冻结为带敏感性实验的参数** | 量大，且依赖 U4-1/U4-2 的过滤与作用域先就位 |
**U4-5** | **并行检索** | Provider 共享总 deadline；解决当前 `search_grep` 2000 文件/64MB/10s 三重硬上限导致的**假否定** | 性能项；在 U4-1/U4-2 语义稳定后做，避免"并行化了一个错误的过滤" |

## 4. 评测设施设计（U4-0，用户明确要求）

**性能**（每项都要求可复现的原始输出）：
- 延迟：`p50 / p95 / p99`，按 **冷/热**、按 **语言（C# / TS-TSX / md）**、按 **scope 大小**分层
- 规模：不同仓库规模下的退化曲线；索引构建耗时与索引体积
- 并发：并行查询下的 P95
- **门槛**：用户目标是"毫秒级检索到合适目标"⇒ 需把"毫秒级"落成可判定的阈值（建议：本地中型仓库热态 **p95 ≤ 50ms** 为达标线，需用户确认）

**准确率**（当前完全空白）：
- 需要**标注集**：`query → 期望命中的文件/符号`（建议每个语言分层 20~30 条，共 60~90 条；含"符号名精确命中""自然语言意图""跨文件引用"三类）
- 指标：`recall@k`（k=1/5/10）、`MRR`、`precision@k`；噪声率（返回命中落在被忽略目录的比例 —— 直接度量 U4-1 的收益）
- **对照**：词法（grep）基线 vs +向量 vs +outline/LSP，**逐项加测**，证明每一步的增量收益

## 5. 待你裁决（我给出推荐，不反对即按推荐执行）

1. **允许新增 NuGet 包吗？** 推荐引入 **`Ignore`**（spec 2.29.2 + 用真实 git 输出测过）作为 gitignore 语义实现。若不允许新增依赖，则退化为"自己按 spec 实现"，成本高且易出错（现有那份简化实现就是前车之鉴）。
2. **统一忽略合同放哪？** 推荐**放 `PuddingCodeIndex`**（它已是叶子工程，且 `PuddingRuntime` 已引用它 ⇒ **零新增依赖边**，`search_grep` 可直接复用）；若日后超出"检索基础设施"范畴，再按既有模式抽独立叶子组件（与 LSP 的「B 起步、保留 A 接口位」同口径）。备选：现在就新建 `PuddingIgnoreRules` 叶子组件（更纯，但多一个工程与一轮构建成本）。
3. **"毫秒级"的达标线**：建议定为本地中型仓库热态 **p95 ≤ 50ms**；你也可以直接给一个数字。

## 6. 沿用硬约束（不得放宽）

- `PuddingCodeIndex` 不得引用 `PuddingCodeIntelligence` / `PuddingRuntime` / `PuddingHost` / `PuddingAgent`
- 只清理 Pudding 自己拥有的索引/缓存，**绝不删除源码或工作区文件**
- 组件交付顺序：S1 独立工程 → S2 独立测试 → S3 无宿主验证 → S4 边界断言 → **S5 才接入宿主**；S5 前不得改宿主
- 任何 `git mv` / 命名空间变更后，**必须做全解决方案构建**（规程 §4.2）
- 每个切片必须给出**能取红的变异证据**；时序/并发测试单次通过不构成证据

---

## 7. U4-0 首份实测基线（2026-09-24，只报原始数字）

> 仪器：`Source/PuddingRetrievalEval`（叶子组件，`ProjectReference=0`、`PackageReference=0`，只依赖端口 `ISearchProbe`）
> + `Source/PuddingRetrievalEvalTests`（独立测试工程，78 用例）。
> 适配器：`temp/U4-0-probe/PuddingRetrievalEvalProbe`（不进 slnx、不进版本控制）。
> 标注集：`Source/PuddingRetrievalEval/eval/sets/seed-v1.json`（80 条 / 98 个 (query, 期望文件) 对；C# 36、TS-TSX 22、md 22）。
> 报告：`Source/PuddingRetrievalEval/eval/reports/`（Markdown + JSON，含每 case 命中列表与每次调用耗时原始值）。

### 7.1 检索面

本刀接入的**唯一**真实检索面 = **Lucene 全文索引**（`Source/PuddingFullTextIndex/Infrastructure/Search/LuceneSearchEngine.cs`），
它正是 `search_grep` 的快速候选路径：`SearchGrepTool.cs:340` 持有 `IFullTextSearchEngine` 并先向它要候选文件，
再用托管扫描复核（`SearchGrepTool.cs:355-367`）。

### 7.2 准确率（原始值，未判定）

| scope | 用例 | recall@1 | recall@5 | recall@10 | MRR | precision@5 | precision@10 | noiseRate@10 |
|---|---|---|---|---|---|---|---|---|
`Source/`（C#+TS） | 58 | 0.4224 | 0.6638 | 0.6638 | 0.5768 | 0.1621 | 0.0810 | 0.0290
`Source/`（C#，`file_ext=.cs`） | 36 | 0.6389 | 0.6944 | 0.6944 | 0.7292 | — | — | 0.0439
`Source/`（TS-TSX，`file_ext=.ts;.tsx`） | 22 | 0.0682 | 0.6136 | 0.6136 | 0.3273 | — | — | 0.0045
`Docs/`（md） | 20 | 0.2500 | 0.4750 | 0.5250 | 0.4238 | 0.1300 | 0.0700 | 0.0000

重复间一致（`repetitionStable=True`）：准确率数字可复现。

### 7.3 延迟（ms，冷/热分离；`--warmup 1 --measured 3`）

| scope | bucket | 样本数 | p50 | p95 | p99 | max | mean |
|---|---|---|---|---|---|---|---|
`Source/` | 冷（每查询首次调用） | 58 | 7.126 | 27.216 | 625.385 | 625.385 | 21.264
`Source/` | 热（其后全部） | 232 | 7.116 | 26.548 | 31.313 | 33.081 | 10.446
`Docs/` | 冷 | 20 | 4.189 | 9.922 | 567.352 | 567.352 | 33.170
`Docs/` | 热 | 80 | 4.175 | 7.698 | 12.896 | 12.896 | 4.685

**仪器不内置达标线**：§4 建议的"热态 p95 ≤ 50 ms"是否成立，与这里的数字对照即可，但**判定权在用户**，
不写进仪器（以免把产品决策固化成测试常量）。

### 7.4 索引构建（同一引擎）

| scope | 可索引文件 | 可索引字节 | `BuildIndexAsync` 实测 |
|---|---|---|---|
`Source/` | 3,514 | 52,239,306 | 462,192 ms（≈7.7 min）|
`Docs/` | 680 | 11,327,664 | 13,058 ms |

### 7.5 由本刀实测得出、直接决定 U4-1 口径的三条事实

1. **`BuildIndexAsync` 对每一种扩展名各遍历一次目录树**（`FullTextIndexOptions.PlainTextExtensions ∪ ParsedExtensions` = 77 个模式）
   ⇒ 索引成本 ≈ 91 × 单次遍历。实测：`Source/` 单次遍历 6.6 s vs 索引 462 s ÷ 77 ≈ 6.0 s；`Docs/` 单次遍历 19 ms vs 索引 13.1 s（后者由解析/写入主导）。两种规模各一次实测，模型一致。
2. **仓储根作为 scope 的索引构建不可行**：实测根 scope **可索引 28,178 文件 / 743 MB**、单次遍历 **195,536 ms** ⇒ 量级为小时级。原始输出 `temp/U4-0-probe/corpus-inventory.txt`。
3. **根 scope 的头部噪声源不在任何现有排除表内**：`.pudding` **20,926** 个可索引文件（工具结果与索引缓存）、`.tmp-build` **1,335**、`.pnpm-store` **1,075**、`.tmp-test-out` **240**；按扩展名 `.json` 11,101 + `.txt` 10,697 占 28,178 的 77%。⇒ U4-1 的收益可用"噪声率"与"根 scope 可索引规模"两个数字直接判定。

### 7.6 覆盖度与已知不可测项

80 条用例中 **78 条**已有基线（58 条在 `Source/` scope + 20 条在 `Docs/` scope）。余下 **2 条**
（intent/markdown，期望命中为仓储根文件 `Agents.md`、`Agents-Hygiene.md`）**当前不可测**：
它们的 scope 只能是仓储根，而按第 7.5 条第 2 点，根 scope 的索引构建不可行。
一条 `ISearchProbe` 调用只能限定一个根目录，因此适配器用 `--expected-under` 只保留期望命中全部落在 scope 内的用例，
并**打印被丢弃的用例清单**，杜绝"结构上不在 scope 内 ⇒ 记 0 分"的假数字。
