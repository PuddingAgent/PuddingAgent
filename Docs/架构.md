# Pudding Agent Network — 架构总览

> **2026-05-02 架构简化**：从多服务分布式模型简化为单进程 P2P 模型。每个 Agent 是一个独立可执行程序，内嵌 Web UI、Controller、Runtime、SQLite。Agent 之间通过 P2P 发现与直连通信。

## 产品定位

Pudding 不是一个需要 Nginx + PostgreSQL + Redis + RabbitMQ + MinIO 全家桶才能跑起来的"企业平台"。

它是一个**双击即用的 AI 代理程序**。

> **Pudding = 可 P2P 组网的 AI Agent 进程**

- 双击启动，浏览器打开即用
- 一个进程 = 一个 Agent（内嵌 Web UI + LLM 对话 + 记忆 + 工具）
- 多个 Agent 之间通过 P2P 协议自动发现、直连通信、协作完成任务
- 数据库只依赖 SQLite，零外部基础设施
- 仍支持 Docker 部署（单容器暴露端口即可）

## 单 Agent 内部结构

```
┌─────────────────── Pudding Agent（单进程）────────────────┐
│                                                            │
│  浏览器 → localhost:8080                                   │
│  ┌──────────────────────────────────────────────────┐     │
│  │  内嵌 Web UI（管理界面 + 对话界面）                 │     │
│  ├──────────────────────────────────────────────────┤     │
│  │  Controller 逻辑（路由/鉴权/调度/审计，同进程）     │     │
│  ├──────────────────────────────────────────────────┤     │
│  │  Runtime（LLM 对话/工具执行/记忆/Skill 装配）     │     │
│  ├──────────────────────────────────────────────────┤     │
│  │  SQLite（配置/会话/记忆/审计，单文件数据库）        │     │
│  ├──────────────────────────────────────────────────┤     │
│  │  连接器（Connector）                               │     │
│  │  ├─ WebChat / CLI / Email / Webhook / MQTT        │     │
│  │  └─ 双向通道：接收事件 + 发送消息 + 操作系统        │     │
│  ├──────────────────────────────────────────────────┤     │
│  │  P2P 网络层（节点发现/直连通信/事件广播）           │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  ← P2P →  其他 Agent 节点                                  │
└────────────────────────────────────────────────────────────┘
```

## 多 Agent 协作：P2P 网络模型

Agent 不是孤立进程。当多个 Pudding Agent 运行在同一局域网或可互通网络中时：

- **自动发现**：基于 mDNS 或 DHT 发现对等节点
- **直连通信**：Agent 之间直接建立 HTTP/gRPC 连接，无需中心控制器
- **事件广播**：Agent 将任务完成、状态变更等事件广播给感兴趣的节点
- **数字蚁群**：每个 Agent 像蚂蚁一样感知局部状态，协作推进任务图

这就是原来的 Controller/Runtime 两层架构的 P2P 实现——每个 Agent 同时具备控制面和执行面能力，协作去中心化。

## 对最终用户的体验

```
用户视角：只有一个东西

1. 下载 pudding-agent（单个可执行文件，约 50MB）
2. 双击运行
3. 浏览器打开 http://localhost:8080
4. 直接进入对话界面（类 ChatGPT 体验）
5. 顶栏可切换"场景"和"Agent"，点击⚙️进入设置

如果想多 Agent 协作：
6. 在另一台机器上也运行 pudding-agent
7. 它们自动发现对方
8. 在 UI 中可以看到对等节点，分配任务

高级用户：
- 可以用 Docker 单容器部署：docker run -p 8080:8080 pudding-agent
- 可以在 Linux 服务器上 systemd 托管
```

### 默认 UI 布局（V1）

```
┌──────────────────────────────────────────────┐
│ [工作 ▼]  [💬 助手 Agent ▼]      ⚙️ 设置    │  ← 顶栏
├──────────────────────────────────────────────┤
│                                              │
│         💬  Chat 对话区                       │
│                                              │
├──────────────────────────────────────────────┤
│ [输入消息...]                        [发送]  │
└──────────────────────────────────────────────┘
```

- **场景选择器**（顶栏左侧）：切换不同生活/工作场景，每个场景有独立的 Agent 集合和记忆
- **Agent 选择器**（顶栏中部）：切换当前对话的 Agent 人格
- **设置（⚙️）**：进入后台管理（场景管理、Agent 管理、模板库、全局设置）

## 技术选型

| 组件 | 技术 | 原因 |
|---|---|---|
| 后端框架 | ASP.NET Core | C# 生态，高性能 |
| 数据库 | SQLite（通过 EF Core） | 零配置，单文件，足够个人/小团队使用 |
| 前端 | React/TypeScript，打包嵌入后端 | 单进程部署 |
| P2P 发现 | mDNS + UDP 广播（局域网）/ DHT（广域网） | 零配置自动发现 |
| 节点通信 | HTTP/gRPC 直连 | 简单可靠 |
| LLM | 直接调用 API（OpenAI 兼容） | 不经过中间代理 |
| 连接器 | `IPuddingConnector` 接口（DI 注册） | 替代旧 `IPuddingGatewayAdapter`，双向通道管理 |

> **连接器迁移说明**：旧 `IPuddingGatewayAdapter` 保留兼容，新旧接口在 DI 中并存。已有 Adapter 逐步迁移为 Connector。连接器详细设计见 [Tasks/task36-event-trigger-and-subagent.md](Tasks/task36-event-trigger-and-subagent.md) §0。

## 不再需要的基础设施

以下全部废弃，由程序自身替代：

| 废弃 | 替代方案 |
|---|---|
| Nginx | ASP.NET Core 内置 Kestrel 直接对外 |
| PostgreSQL | SQLite |
| Redis | 内存缓存 + SQLite |
| RabbitMQ | P2P 直连事件广播 |
| MinIO | 本地文件系统 + SQLite BLOB |
| Docker Compose 多容器 | 可选：单容器或裸进程 |

## 解决方案结构

```text
Source/
├── PuddingAgent/          ← 主程序入口（单进程宿主）
│   ├── WebUI/             ← 内嵌前端（React 构建产物）
│   ├── Controller/        ← 控制逻辑（路由/鉴权/调度/事件路由）
│   ├── Connectors/        ← 连接器模块（Webhook/MQTT/邮箱等协议通道）
│   ├── Runtime/           ← Agent 执行（LLM/工具/记忆/Skill/子代理）
│   ├── P2P/               ← P2P 网络层（发现/直连/事件广播）
│   └── Data/              ← SQLite 数据访问（EF Core）
├── PuddingCore/           ← 共享抽象与协议（ILlmGateway/IPuddingConnector等）
├── PuddingPlatformAdmin/  ← 前端源码（构建后嵌入 PuddingAgent）
└── PuddingCode/           ← 旧 CLI 原型（保留过渡）
```

## 关键边界

- 一个进程 = 一个 Agent，一个 Agent = 一个身份
- Agent 拥有自己的 SQLite 数据库、记忆、配置
- Agent 之间平等，通过 P2P 协议协作，无中心节点
- 用户在任一 Agent 的 Web UI 上可以管理自己的 Agent，也可以看到对等网络中的其他 Agent
- 工具在 Agent 进程内直接执行
- 高风险操作需用户确认

## 场景（Scene/Workspace）与 Agent 关系模型

> **2026-05-03 设计确定**：Workspace 保留作为"用户场景分组"概念，面向用户称为"场景"。Agent 模板简化为全局模板库，Agent 从模板创建。

### 设计决策

| 维度 | 旧定位（平台思维） | 新定位（个人助手） |
|------|---------------------|---------------------|
| 用户第一眼看到 | Workspace 列表（管理面板） | Chat 对话界面 |
| Workspace 的角色 | 必须创建的顶层组织 | 场景分组（工作/生活/创意），用户可切换 |
| Agent 管理 | Workspace 下两级模板+实例 | 全局模板库 → 场景内创建 Agent 实例 |
| 权限/成员/治理 | 暴露在 UI 中 | 隐藏（V2+ 再考虑） |
| 冻结/解冻 | 暴露在 UI 中 | API 保留，UI 隐藏（V1 单进程无意义） |

### 数据模型（后端不变）

```
用户 (单用户，V1)
  │
  ├── 场景A (Workspace: "工作")
  │   ├── Agent: 助手 (默认，继承自"通用助手"模板)
  │   ├── Agent: 邮件管理 (继承自"邮件处理"模板)
  │   └── Agent: 会议纪要
  │
  ├── 场景B (Workspace: "生活")  
  │   ├── Agent: 生活助手 (默认)
  │   └── Agent: 智能家居 (继承自"Home Assistant"模板，含 MQTT 连接器)
  │
  └── 场景C (Workspace: "开源项目")
      ├── Agent: 代码助手 (默认)
      └── Agent: 代码审查 (继承自"代码审查"模板)
```

### 原则

1. **用户直接面对对话** — 打开就是聊天界面，顶栏切换场景和 Agent
2. **每个进程自带 `default` 场景** — 对用户透明，无需手动配置即可使用
3. **场景是 Agent 的容器** — 不同场景的 Agent 有独立记忆和上下文，互不混杂
4. **全局 Agent 模板库** — 模板跨场景复用，创建 Agent 时从模板选择
5. **设置页是后台** — 场景管理、Agent 管理、模板库均在 ⚙️ 设置中，对话页不暴露管理复杂度

## Agent 能力分层模型

> **2026-05-02 新增**：基于 Anthropic/Hermes/OpenClaw 等业界标杆分析，定义 Pudding Agent 的六层能力模型及 V1 优先级。

```
┌─────────────────────────────────────────────────┐
│  L5  自动化层    Cron调度 · 事件触发 · 工作流管道   │  ← P2
├─────────────────────────────────────────────────┤
│  L4  接入层      多渠道Connector · Web UI · CLI    │  ← P1
├─────────────────────────────────────────────────┤
│  L3  多智能体层  子代理 · Orchestrator · 路由分发  │  ← P1
├─────────────────────────────────────────────────┤
│  L2  工具与技能层  工具注册/执行 · Skill系统 · MCP  │  ← P0
├─────────────────────────────────────────────────┤
│  L1  记忆与上下文  会话记忆 · 长期记忆 · 上下文管理  │  ← P0
├─────────────────────────────────────────────────┤
│  L0  基础层      LLM对话循环 · 多模型 · 流式输出   │  ← P0
└─────────────────────────────────────────────────┘
```

### L0 基础层（P0 — V1 必须）

| 组件 | 说明 | 对应任务 |
|------|------|---------|
| 增强型 LLM 对话循环 | 多轮对话 + 工具调用 + 流式输出（Anthropic 的 Augmented LLM） | task-20260502-008 |
| 多模型/多 Provider | OpenAI 协议优先，可扩展 Anthropic/自定义端点 | task-20260502-012 |
| SQLite 数据持久化 | 会话历史、Agent 配置持久化 | task-20260502-007 |

### L1 记忆与上下文（P0 — V1 必须）

| 组件 | 说明 | 对应任务 |
|------|------|---------|
| 会话记忆 | 当前对话上下文管理，多轮传递 | task-20260502-008（部分） |
| 长期记忆引擎 | 跨会话记忆召回与写回，越用越了解用户偏好 | **待创建** |
| 上下文窗口管理 | Token 预算控制、摘要压缩 | 后续 |

借鉴 Hermes 的持久记忆设计：运行越久，越了解用户，不再需要每次重新解释上下文。

### L2 工具与技能层（P0 — V1 必须）

| 组件 | 说明 | 对应任务 |
|------|------|---------|
| 工具注册与执行 | Function Calling 工具定义、进程内执行 | task-20260502-008（部分） |
| Skill 系统 | 可复用技能（SKILL.md 格式），自动技能创建 | **待创建** |
| MCP 协议支持 | Model Context Protocol 客户端实现 | 后续 |

借鉴 Hermes 的自动技能创建：Agent 解决问题后自动记录可复用技能。借鉴 Anthropic 的 ACI（Agent-Computer Interface）设计原则：工具定义应投入与 prompt 同等的设计精力。

### L3 多智能体层（P1 — V1 重要）

| 组件 | 说明 | 对应任务 |
|------|------|---------|
| 子代理（Sub-Agent） | 进程内逻辑分区，独立对话上下文，并行执行 | **待创建**（设计已完成：task-20260502-010） |
| Orchestrator-Workers | 动态任务拆分、委派、结果合并（Anthropic 模式） | 同上 |
| P2P 任务路由 | Agent 间任务分发与协作 | task-20260502-005 |

设计参考：[Docs/Tasks/task36-event-trigger-and-subagent.md](Docs/Tasks/task36-event-trigger-and-subagent.md)。

### L4 接入层（P1 — V1 重要）

| 组件 | 说明 | 对应任务 |
|------|------|---------|
| 连接器框架（Connector） | 双向通道管理器抽象，类比 MCP 为 Agent 提供外部通道 | **待创建**（设计已完成：task-20260502-010） |
| WebChat 连接器 | 内嵌 Web UI 对话通道 | task-20260502-002 |
| CLI 连接器 | 命令行交互通道 | 待创建 |
| Webhook 连接器 | HTTP POST 事件接收 | 待创建 |

借鉴 OpenClaw 的 Gateway 设计：Connector 是会话、路由和渠道连接的统一抽象。设计参考：[Docs/07架构/04PuddingController与Gateway.md](Docs/07架构/04PuddingController与Gateway.md)。

### L5 自动化层（P2 — V1 后续）

| 组件 | 说明 | 对应任务 |
|------|------|---------|
| Cron 定时任务 | 内置调度器，支持周期性任务（日报、备份、审计） | **待创建** |
| 工作流管道 | Prompt Chaining + Routing + Evaluator-Optimizer 模式 | **待创建**（P2，后续实现） |
| 事件触发自动化 | 外部事件驱动 Agent 自主执行 | **待创建**（设计已完成：task-20260502-010） |

### 远期规划（P3）

| 组件 | 说明 | 状态 |
|------|------|------|
| 浏览器自动化 | Computer Use 场景，Playwright 集成 | P3，暂不实现 |
| 移动端接入 | iOS/Android 节点 | 不考虑 |
| 沙箱执行 | Docker 隔离代码执行 | 当前阶段不考虑 |

## V1 实施优先级总览

| 优先级 | 能力 | 理由 |
|--------|------|------|
| **P0** | L0 基础层 + L1 记忆 + L2 工具/Skill | 这是 Agent 的骨架，没有这些不叫 Agent |
| **P1** | L3 子代理 + L4 连接器 | 差异化竞争力：多 Agent 协作 + 多渠道接入 |
| **P2** | L5 Cron + 工作流 | 锦上添花，实现简单但非 V1 核心 |
| **P3** | 浏览器自动化 | 远期生态扩展 |

## 当前状态

处于从 PuddingCode 原型向单进程 Pudding Agent 的过渡期。

已废弃的概念：Controller/Runtime/Platform/Gateway 分进程部署、Docker Compose 多容器、RabbitMQ/Redis/MinIO 依赖。

保留的概念（经过重新定位）：场景/Workspace（场景分组，详见 [场景与 Agent 关系模型](#场景sceneworkspace与-agent-关系模型)）、Agent Template、Skill/MCP、记忆引擎、连接器（Connector）、子代理、事件驱动、数字蚁群——这些仍然有效，只是实现方式从分布式服务变为单进程内模块 + P2P 协作。

新增概念：Agent 能力分层模型（L0-L5），详见上方 [Agent 能力分层模型](#agent-能力分层模型)。UI 模型：Chat 首页 + 场景选择器 + Agent 选择器 + 设置后台（v2026-05-03）。
