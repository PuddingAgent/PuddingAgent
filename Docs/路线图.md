Pudding Agent — 后期路线图与任务规划
最后更新：2026-05-18 基础架构：ADR-013（记忆）、ADR-015（潜意识LLM）、ADR-016（会话状态层解耦）、ADR-018（上下文缓存可观测性） 关联：架构.md · Tasks.md · context.md

当前状态快照（2026-05-17）
已完成
维度	状态
V1 核心	✅ 单进程 Agent / SQLite / LLM 多轮对话 / 工具调用 / P2P 发现
ADR-016 接口	✅ ISessionStateManager + SessionStateManager（Channel 生命周期独立于 HTTP 连接）
ADR-016 端点	✅ SessionEventsController（会话 SSE stream + REST 历史分页）
ADR-016 表	✅ session_event_log + session_sub_agents SQLite 表（append-only）
执行引擎集成	✅ AgentExecutionService.ExecuteStreamAsync 通过 SSM 推送帧
连接器契约	✅ ConnectorGatewayContracts.BuildEventType + ConnectorInboundPayload 统一映射
多协议连接器	✅ MQTT / HTTP / WebSocket / Webhook 最小实现（Program 路由已统一）
编译	✅ dotnet build 0 error
✅ 已提交 (98aad31 feat: Add Gateway Diagnostics, HTTP Connector, and WebSocket Controllers)
✅ Doc/Temp 临时文件已清理 (2026-05-17)
前端状态
登录鉴权正常（Bootstrap 初始化 + JWT）
Chat 页面走旧 ChatApiController 临时 Channel → SessionEventHub → SSE
切换会话/断连后事件丢失、仅最终 replyText 持久化
source 卡片映射能力存在但未系统化


Phase 1：收敛与基础闭环 🔴（P0，1—3 天）
T-100 提交当前改动 ✅
项目	说明
目标	整理 staging area：connector 改造、MqttConnector、HttpConnector、Program 路由、Webhook 边界修复、GatewayDiagnosticsController
复杂度	中
前置	无
状态	✅ 已完成 (98aad31)
DoD	git status 仅少量预期文件；dotnet build 通过
T-101 清理临时文件 ✅
项目	说明
目标	删除 Doc/Temp/*.ps1 / *.js，确认 TestScripts/PuddingWsTest 正确纳入工程
复杂度	低
前置	T-100
状态	✅ 已完成 (2026-05-17)
DoD	Doc/Temp 为空；无垃圾文件
T-102 前端 SSM SSE 接入 ✅
状态	✅ 已完成 (2026-05-17, QA: PASS_WITH_NOTES)
目标：前端 Chat 页实时流从旧 ChatApiController 临时 Channel 切换到 SessionEventsController 的持久 Channel。

项目	说明
涉及文件	ChatApiController.cs、api.ts、useChatState.ts
关键改动	
(1)	ChatApiController.SendMessage 改为 fire-and-forget + metadata 超时返回 500
(2)	api.ts 新增 sendChatMessage + AdminChatStreamEvent 类型补全
(3)	useChatState.ts sendMessage 重构为走持久 SSE + Ener 中止 + 错误处理
复杂度	中
前置	SessionEventsController 已存在
DoD	发送消息 → 前端实时收到 thinking/delta/tool_call/done 帧；切换会话可回放历史
遗留	流失败缺 error 帧（下个迭代解决）
T-103 前端 source 卡片映射 ✅
状态	✅ 已完成 (2026-05-17, QA: PASS_WITH_NOTES)
目标	metadata 帧解析 source_type/source_id/source_name → ChatSource 枚举 → 消息卡片显示来源标签
涉及文件	MessageContracts.cs（MessageIngressRequest 新增 Metadata）、SessionRouter.cs（metadata SSE 帧注入 source）、PlatformApiClient.cs（SendMessageAsync/StreamAsync 新增 metadata 参数）、api.ts（metadata 类型追加 source）、useChatState.ts（修复命名兼容）
复杂度	低
前置	T-102
DoD	WebSocket/Webhook/MQTT 消息卡片分别显示不同来源 icon 和标签
遗留	3×P2：displayName/avatarEmoji camelCase 兼容、metadata 未写入 event log
T-104 ChatApiController 旧路径收敛 ✅
状态	✅ 已完成 (2026-05-18, QA: PASS)
项目	说明
目标	删除 ChatApiController 内 7 项死代码（SendMessageStream/ConfigureSseResponse/WriteRawSseAsync/PersistMessagesAsync/DualWriteToMemoryDbAsync/TryEnqueueJsonl/TokenUsageDto），删除前端 sendAdminChatMessageStream + import
涉及文件	ChatApiController.cs、api.ts、useChatState.ts
复杂度	中
前置	T-102（前端不再依赖旧路径）
DoD	旧 tempChannel 代码已删除；SSE 只走 SessionEventsController；会话历史查询迁移到 GET /api/sessions/{id}/events；0 error build；无残留引用

Phase 2：ADR-016 剩余实现 🟡（P1，3—5 天）
T-200 工作区通知 SSE 端点
项目	说明
目标	实现 WorkspaceNotificationsController — GET /api/workspaces/{id}/notifications/stream
涉及文件	WorkspaceNotificationsController.cs（新）
复杂度	中
前置	SessionStateManager.SubscribeWorkspace 已实现
DoD	SSE 推送子代理完成/新会话创建/Cron 触发等通知
T-201 前端工作区通知消费
项目	说明
目标	页面级 SSE 连接（独立于会话 SSE）：侧栏未读标记 + Toast 通知
涉及文件	api.ts 新增 workspace SSE hook、ChatLayout.tsx
复杂度	中
前置	T-200
DoD	子代理完成后侧栏出现橙色圆点；Cron 触发的会话出现在侧栏列表（无需手动刷新）
T-202 子代理异步完成前端可见
项目	说明
目标	SubAgentCompleted 帧通过 SSM Channel 到达前端 → 渲染"子代理 X 已完成"卡片
涉及文件	AgentEventHandler.PushSubAgentCompletedAsync（保证推送）、useChatState.ts（帧类型解析）
复杂度	低
前置	T-102（前端已接入 SSM Channel）
DoD	子代理完成 → 当前会话 UI 出现 subagent.completed 卡片
T-203 E2E 回归自动化
项目	说明
目标	PuddingWsTest 增加 --auto 非交互模式，断言 done / 无 `error
涉及文件	TestScripts/PuddingWsTest/AutoTest.cs
复杂度	低
前置	无
DoD	dotnet run --project TestScripts/PuddingWsTest -- --auto 在 30s 内返回 PASS/FAIL
hase 3：平台化增强 🟢（P1—P2，1—2 周）
3.1 前端管理页面
ID	任务	优先级	来源
T-300	仪表盘页面：连接器状态 / 会话统计 / 错误率	P1	GatewayDiagnosticsController 后端已完成
T-301	Webhook 管理页面：创建/启用/签名密钥轮换	P1	Tasks.md task-012
T-302	Cron 管理页面：创建/暂停/触发记录	P1	Tasks.md task-011
T-303	Agent 详情页 + 场景管理	P2	Tasks.md task-ui-04
3.2 执行引擎增强
ID	任务	优先级	来源
T-304	Hook 系统 PreToolUse / PostToolUse	P1	Tasks.md task-003
T-305	MicroCompact 上下文压缩	P1	Tasks.md task-009
T-306	工作流管道前端（FlowGram 风格画布）	P2	Tasks.md task-028
3.3 协议与可观测性
ID	任务	优先级
T-307	MQTT broker 内嵌或文档化外部 broker 配置	P2
T-308	结构化 tracing：traceId 贯通 connector → event → session → 前端	P2
Phase 4：远期演进 🔵（Post V1）
ID	任务	说明
T-400	浏览器自动化连接器（Playwright）	Tasks.md task-029
T-401	P2P 协作增强：任务分配 / 结果汇聚 / 数字蚁群 V2	多 Agent 协作深化
T-402	多客户端 SDK（移动端 / 桌面端 / CLI）	ADR-016 解耦后的自然产物
T-403	Eval-Opt 评测优化循环	Anthropic Building Effective Agents 对照产出
T-404	client_read_position 表实现：多客户端各自维护已读序列号	ADR-016 §4.3

Phase 5：上下文缓存可观测性 🟢（P0—P2，已完成，2026-05-18）
关联 ADR：Docs/07架构/18上下文缓存可观测性ADR.md

5.1 数据管道（P0）✅ 已完成
ID	任务	说明
T-CACHE-001	TokenUsageDto 新增 PromptCacheHitTokens / PromptCacheMissTokens
T-CACHE-002	DirectLlmClient.ChatAsync() 解析 cache tokens
T-CACHE-003	LlmModelEntity 新增 CacheHitPricePer1MTokens + EF 迁移
T-CACHE-004	新增 TokenUsageStatsEntity + EF 迁移
T-CACHE-005	ChatApiController fire-and-forget 增量更新统计表
T-CACHE-006	管理后台模型表单新增缓存价格字段

5.2 前端缓存指示器 + 主会话（P1）✅ 已完成
ID	任务	说明
T-CACHE-007	StatusBarTokenIndicator 双层圆环改造（内环上下文/外环缓存命中率）
T-CACHE-008	useChatState 新增 mainSessionId + 缓存统计累加
T-CACHE-009	TokenStatsIndicator 数据接入
T-CACHE-010	ChatPage 集成主会话逻辑
T-CACHE-011	GET /api/sessions/{id}/token-stats API

5.3 管理后台统计页面（P2）✅ 已完成
ID	任务	说明
T-CACHE-012	StatsApiController 新增 GET /api/stats/tokens/monthly
T-CACHE-013	Admin Token 统计页面（pages/stats/tokens/）
T-CACHE-014	路由配置 /stats/tokens → pages/stats/tokens


依赖关系图
Phase 1                     Phase 2                    Phase 3          Phase 5
──────────────────────────────────────────────────────────────────────────────────
T-100 ──→ T-101
  │
  └──→ T-102 ──→ T-103
          │
          └──→ T-104      T-200 ──→ T-201
                            │
                            └──→ T-202
                            
                          T-203 (独立)           T-300~T-308 (各自独立)

                                                T-CACHE-001~006 (数据管道)
                                                         │
                                                T-CACHE-007~011 (前端)
                                                         │
                                                T-CACHE-012~014 (Admin)


文件变更预估
Phase	新增文件	修改文件	主要模块
1	0—1	5—8	PuddingPlatformAdmin (前端) + ChatApiController
2	1	4—6	PuddingPlatform + PuddingPlatformAdmin
3	3—5	5—10	PuddingPlatformAdmin + PuddingRuntime
4	2—4	3—5	多个模块
5	3—5	8—12	PuddingPlatform + PuddingPlatformAdmin + PuddingRuntime
风险与缓解
风险	等级	缓解
前端切换 SSE 源后 sessionId 不同步	中	发送消息返回的 sessionId 存入 useRef，SSE 订阅复用同一值
旧 ChatApiController 删除后遗漏调用方	低	全局搜索 SendMessageStream 调用方 + 编译兜底
Channel TTL 30min 内用户返回但 Channel 已清理	低	GetOrCreate 在 subscribe 时自动重建；重建后 SSE 从最新 seq 开始
滚轮分页加载 SQLite 性能	低	联合索引 (session_id, sequence_num) 保证 O(log N)；单会话通常 <5000 帧