Phase 2A-3B: Activity Evidence Exporter + External Acceptance Script

BrowserActivityEvidenceExporter:
- New DTOs: BrowserActivityEvidenceDocument, BrowserActivityEvidenceItem
- Exports sanitized activity records (command names only, no payload/text/URL/secrets)
- Atomic write (temp file + rename)
- JSON camelCase, UTF-8, .sanitized.json extension

BrowserWorkspaceController:
- Added CaptureActivityEvidenceAsync to IBrowserWorkspaceController
- UI-dispatcher-consistent snapshot of Activities (max 100 items)
- BridgeState, ControlState, ActiveContextId/PageId, AgentTargetPageId

BrowserWorkspaceView:
- Added Export button in Activity Pane header
- ExportActivity_Click handler: capture + export + open in Explorer
- Disabled until browser initialized
- Destination: DataRoot/diagnostics/browser-activity/

External Acceptance Script:
- start-phase2a3b-external-acceptance.ps1
- -PrepareOnly mode: validates layout, workspace, writes internal-handoff.json
- Full mode: TestSite + Desktop lifecycle + shutdown observation
- No secret in desktop.json, no auto-kill, PID-based child tracking

Tests:
- BrowserActivityEvidenceExporterTests (10 scenarios):
  allowed fields only, sort order, success/errorCode, file name,
  cancellation, atomic write, sensitive value exclusion, state fields

Docs: Updated 76, 77, 78, README, code_map, Agents.md, How-Debuge.md

Known: build/test verification blocked by running PuddingAgent file locks
Status: ready-for-external-deploy